## Implementation

- [x] 1. 新增 `sys.log.retentionDays` OpenSpec 增量规范，记录全局最长保留天数、各日志模块清理语义、数据权限例外、缓存一致性和验证要求。
- [x] 2. 在宿主配置服务中新增 `sys.log.retentionDays` 运行时参数，默认 `90`，完成正整数校验、快照解析、host config 读取和单元测试。
- [x] 3. 新增本迭代宿主 SQL，幂等写入 `sys.log.retentionDays`，同步宿主 `i18n` 配置文案，并为登录日志自动清理补充必要索引。
- [x] 4. 改进任务调度执行日志和在线会话清理：执行日志先按全局最长天数清理，再应用既有任务级策略；在线会话清理使用会话超时与全局最长天数中的更严格边界。
- [x] 5. 为 `linapro-monitor-loginlog` 增加源码插件内建清理定时任务，读取全局最长保留天数并按 `login_time` 范围硬删除。
- [x] 6. 为 `linapro-ai-core` 增加源码插件内建清理定时任务，读取全局最长保留天数并按 `created_at` 范围硬删除。
- [x] 7. 补充或更新宿主与插件后端单元测试，覆盖默认值、非法值、host config 运行时读取、执行日志全局上限、在线会话边界、登录日志清理和调用日志清理。
- [x] 8. 运行变更范围验证与审查：OpenSpec 严格校验、Go 编译/测试门禁、SQL/i18n 静态检查、插件本地规范入口检查和 `lina-review`。

## Feedback

- [x] **FB-1**: 合并日志保留参数 SQL，并移除本次新增的缺行默认值兼容分支
- [x] **FB-2**: 合并登录日志插件保留清理索引 SQL
