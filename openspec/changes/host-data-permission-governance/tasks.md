## 1. 基础数据权限能力

- [ ] 1.1 使用 `goframe-v2` 技能阅读并确认 GoFrame DAO / gdb.Model 接入方式，避免手写 SQL 拼接
- [ ] 1.2 新增宿主内部数据权限服务，解析当前用户、超级管理员、启用角色和有效 `dataScope`
- [ ] 1.3 实现多角色最宽范围合并规则：全部数据 > 本部门数据 > 仅本人数据 > 无权限
- [ ] 1.4 定义数据权限命名类型和常量，禁止在业务分支中直接硬编码数据范围字符串或裸枚举语义
- [ ] 1.5 定义宿主资源策略注册结构，支持按用户列、部门用户集合和自定义可见用户解析接入资源
- [ ] 1.6 实现列表查询约束注入能力，支持空范围快速返回空结果
- [ ] 1.7 实现详情、写操作、执行类操作的目标记录可见性校验能力
- [ ] 1.8 为数据权限拒绝、上下文缺失、组织能力不可用等情况定义 `bizerr.Code`

## 2. 组织能力与缓存一致性

- [ ] 2.1 复核 `orgcap` 当前能力，补齐数据权限需要的部门用户集合解析接口或适配方法
- [ ] 2.2 实现组织能力不可用时的失败关闭策略：全部数据不受影响，本部门返回空结果或拒绝写操作，仅本人继续可用
- [ ] 2.3 将角色 `dataScope` 变更、角色启停、用户角色关系变更纳入访问拓扑或数据权限缓存失效
- [ ] 2.4 如果实现中缓存部门用户集合或数据权限上下文，新增明确作用域的缓存修订号和新鲜度检查
- [ ] 2.5 补充集群模式下缓存失效测试，覆盖修订号变化后旧数据权限快照失效

## 3. 用户管理接入

- [ ] 3.1 用户列表和导出接入数据权限，支持全部、本部门、仅本人范围
- [ ] 3.2 用户详情接入数据权限，范围外用户返回结构化数据不可见错误
- [ ] 3.3 用户更新、状态变更、重置密码和角色关联变更接入目标用户可见性校验
- [ ] 3.4 用户单删和批量删除接入数据权限，批量操作任一目标不可见时整体拒绝
- [ ] 3.5 角色授权用户列表和用户选择范围接入数据权限，避免授权页面暴露范围外用户
- [ ] 3.6 保留内置管理员和当前用户删除保护规则，确保数据权限不会绕过既有保护

## 4. 文件管理接入

- [ ] 4.1 文件列表接入 `sys_file.created_by` 上传人范围过滤
- [ ] 4.2 文件详情、批量信息和文件访问路径接入上传人可见性校验
- [ ] 4.3 文件下载接入数据权限，范围外文件不得返回二进制内容
- [ ] 4.4 文件删除接入数据权限，拒绝删除范围外数据库记录和物理文件
- [ ] 4.5 文件后缀、场景等聚合查询按当前数据权限范围统计，避免泄露范围外数据存在性

## 5. 定时任务与日志接入

- [ ] 5.1 用户创建任务列表接入 `sys_job.created_by` 数据权限过滤
- [ ] 5.2 保持 `sys_job.is_builtin=1` 内置任务投影不按数据权限过滤，继续使用内置任务治理规则
- [ ] 5.3 用户创建任务详情、编辑、删除、启用、禁用、重置接入目标任务可见性校验
- [ ] 5.4 手动触发用户创建任务前校验数据权限，范围外任务不得创建 `sys_job_log`
- [ ] 5.5 任务日志列表、详情、清理、终止 running 日志接入所属任务的数据权限边界
- [ ] 5.6 确认 Shell 任务独立权限点和数据权限同时生效，缺少任一权限都必须拒绝

## 6. 在线用户与用户消息接入

- [ ] 6.1 在线用户列表按 `sys_online_session.user_id` 接入数据权限过滤
- [ ] 6.2 强制下线前校验目标 `tokenId` 所属用户是否在当前数据权限范围内
- [ ] 6.3 用户消息未读数、列表、标记已读和删除路径确认保持当前用户自隔离
- [ ] 6.4 增加用户消息测试，确认拥有全部数据权限的用户仍不能读取、标记或删除他人消息

## 7. i18n 与接口文档治理

- [ ] 7.1 为新增 `bizerr` 错误补充 `zh-CN`、`en-US`、`zh-TW` 运行时翻译
- [ ] 7.2 检查本次是否修改 API DTO 文档元数据；若修改，同步维护非英文 apidoc i18n JSON
- [ ] 7.3 确认角色页面不新增前端可见字段，前端运行时语言包无需新增角色表单翻译
- [ ] 7.4 新增或更新 i18n 完整性测试，确保新增错误翻译不缺失

## 8. 后端测试

- [ ] 8.1 新增数据权限解析单元测试，覆盖超级管理员、多角色最宽范围、禁用角色、无角色、无用户上下文
- [ ] 8.2 新增组织能力降级测试，覆盖本部门失败关闭和仅本人不依赖组织能力
- [ ] 8.3 新增用户管理数据权限测试，覆盖列表、详情、导出、写操作、批量删除
- [ ] 8.4 新增文件管理数据权限测试，覆盖列表、详情、下载、删除、聚合查询
- [ ] 8.5 新增定时任务和任务日志数据权限测试，覆盖用户创建任务、内置任务投影和日志终止
- [ ] 8.6 新增在线用户数据权限测试，覆盖列表和强制下线
- [ ] 8.7 新增用户消息自隔离回归测试，覆盖全部数据权限不放宽消息边界
- [ ] 8.8 运行 `cd apps/lina-core && go test ./...`

## 9. E2E 测试

- [ ] 9.1 创建 `hack/tests/e2e/settings/user/TC0170-user-data-permission.ts`，覆盖 TC-170a 本部门用户列表过滤、TC-170b 仅本人详情限制、TC-170c 范围外用户写操作拒绝
- [ ] 9.2 创建 `hack/tests/e2e/settings/file/TC0171-file-data-permission.ts`，覆盖 TC-171a 文件列表过滤、TC-171b 范围外下载拒绝、TC-171c 范围外删除拒绝
- [ ] 9.3 创建 `hack/tests/e2e/scheduler/job/TC0172-job-data-permission.ts`，覆盖 TC-172a 用户创建任务列表过滤、TC-172b 内置任务投影可见性、TC-172c 范围外触发拒绝
- [ ] 9.4 创建 `hack/tests/e2e/monitor/TC0173-online-user-data-permission.ts`，覆盖 TC-173a 在线用户列表过滤、TC-173b 范围外强制下线拒绝
- [ ] 9.5 创建 `hack/tests/e2e/notification/TC0174-user-message-self-boundary.ts`，覆盖 TC-174a 全部数据权限不读取他人消息、TC-174b 不标记他人消息、TC-174c 不删除他人消息
- [ ] 9.6 运行受影响 E2E 用例并记录结果

## 10. 验证与审查

- [ ] 10.1 运行 `openspec validate host-data-permission-governance --strict`
- [ ] 10.2 运行 `make init`，确认新增 SQL 或索引变更可重建
- [ ] 10.3 运行受影响前端类型检查或构建命令
- [ ] 10.4 执行 `/lina-review`，重点审查数据权限遗漏、缓存一致性和 i18n 完整性
