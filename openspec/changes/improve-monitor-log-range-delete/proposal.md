## 背景

监控日志目录下的操作日志和登录日志页面当前仍以表格勾选为删除入口：页面展示复选框，删除按钮依赖选中记录按 ID 批量删除。用户要求去掉多选框，并让删除按钮支持按照指定日志时间范围清理日志。

已确认两个插件后端均已提供 `DELETE /clean` 清理接口，且接口支持 `beginTime` 和 `endTime` 查询参数。本变更聚焦补齐前端入口、更新页面行为规范和 E2E 覆盖，不修改 `lina-core` 宿主契约，不新增数据库结构。
