# 删除`linactl`历史兼容面

当前项目没有历史兼容负担，`linactl`仍存在若干为了旧写法继续可用而保留的公开入口，包括旧配置变量、旧参数别名、环境变量覆盖、自动回退构建路径和宽松布尔值解析。这些兼容面会扩大工具契约，增加文档和测试成本，也容易让新插件或脚本继续沿用非标准写法。本变更收敛`linactl`的配置与参数契约，只保留当前标准入口，并以单元测试和文档同步证明旧写法已被拒绝。

