# Tasks

## Implementation

- [x] 1. 补充`service-dependency-injection-governance`规范，明确消费方专用窄接口基于复杂度收益定义。
- [x] 2. 合并`config`、`role`、`dict`、`jobmgmt`、`middleware`、`plugin`、`i18n`生产者侧仅用于自组合的接口。
- [x] 3. 将`RawReader`、认证/角色访问快照和确有复杂度收益的`i18n`消费方窄依赖迁移到对应消费者包。
- [x] 4. 运行 OpenSpec 校验、静态检索和 Go 编译门禁，确认旧接口引用清理完成。
- [x] 5. 完成`lina-review`审查并处理阻塞问题。

## Feedback

- [x] **FB-1**: `jobhandler`控制器不应为已有`i18n.Service`可满足的单方法消费重复定义本地窄接口。
- [x] **FB-2**: 普通控制器不应为已有`i18n.Service`可满足的本地化能力重复定义本地窄接口。
- [x] **FB-3**: 普通业务服务及无特殊适配收益的插件生命周期内部服务不应为已有`i18n.Service`可满足的本地化能力重复定义本地窄接口。
- [x] **FB-4**: 非`i18n`依赖中，普通 service、cron、插件内部子组件和 capability adapter 不应为已有目标`Service`可满足的方法重复定义本地窄接口。
- [x] **FB-5**: `user`控制器不应在已有`orgspi.Service`可满足时直接依赖`WorkspaceViewService`子视图。
- [x] **FB-6**: `httpRuntime`不应在已有`config.Service`可满足时额外保存`ServerExtensionsConfig`字段。
- [x] **FB-7**: `auth`服务不应在已有`tenantspi.Service`可满足时重复定义`authTenantAccessService`。
- [x] **FB-8**: `cron`服务不应在已有`config.Service`可满足时额外保存`SessionConfig`快照，也不应为无差异启动预热逻辑保留`startupJob`抽象。
- [x] **FB-9**: `i18n.Service`应合并包装方法并删除无业务入口的管理诊断接口、插件消息搜索能力和对应 API。
- [x] **FB-10**: `menu`服务不应在已有`tenantspi.Service`可满足时重复定义`platformMenuTenantService`。
- [x] **FB-11**: `middleware`服务不应在已有`i18n.Service`和`tenantspi.Service`可满足时重复定义`middlewareI18nService`和`tenantMiddlewareService`。
- [x] **FB-12**: `capabilityhost`不应在已有`auth.Service`、`i18n.Service`、`tenantspi.Service`和`jobmgmt.Service`嵌入的`jobmeta.Owner`可满足时重复定义宿主服务窄接口，也不应通过匿名接口探测插件 scoped 能力。
- [x] **FB-13**: `plugin/internal/integration`不应保留仅用于同包`Service`自组合的导出接口，职责相邻的小文件应合并以减少碎片化。
- [x] **FB-14**: `integration_shared_state_test.go`在源文件合并后失去对应源码文件，应重命名到主要覆盖的`integration_enablement.go`测试文件。
- [x] **FB-15**: `plugin/internal/runtime`不应保留仅用于同包`Service`自组合的导出接口，薄职责文件应合并到相邻实现以减少碎片化。
- [x] **FB-16**: `capabilityhost`不应在已有`tenantspi.Service.Filter()`可满足时额外构造并保存`tenantFilter`，应复用租户目标组件已有过滤契约。
- [x] **FB-17**: `plugin/internal/runtime`不应在已有`bizctx.Service`、`role.Service`和`i18n.Service`可满足时重复定义本地窄接口。
- [x] **FB-18**: `plugin/internal/integration`不应在已有`bizctx.Service`、`cluster.Service`、`capability.Services`、`orgcap.Service`和`runtime.Service`可满足时重复定义或外露本地依赖接口与共享状态构造。
- [x] **FB-19**: `plugin/internal/openapi`不应在已有`catalog.Service`、`store.Service`和`i18n.Service`可满足时重复定义本地投影依赖接口。
- [x] **FB-20**: `role`服务不应在已有`orgcap.Service`和`tenantspi.Service`可满足时重复定义`OrganizationCapabilityState`和`roleTenantGovernanceService`。
- [x] **FB-21**: `plugin/internal/runtime`不应将同一个`integration.Service`消费拆成多个重复本地窄接口和构造参数，`storageCleanupServices`收敛到`capability.Services`后测试替身也必须同步。
- [x] **FB-22**: `plugin/internal/testutil.Services`不应为生命周期组件已有`lifecycle.Service`可满足的消费者重复定义`LifecycleService`字段类型。
- [x] **FB-23**: `role.PermissionMenuFilter`仅服务`role`构造注入时不应作为生产者包导出接口。
- [x] **FB-24**: 当前插件相关组件中仅包内消费的公开接口应改为私有，能由已有目标`Service`满足的窄接口应直接复用目标`Service`。
- [x] **FB-25**: 剩余普通 service 和插件内部组件不应在已有目标`Service`可满足时继续定义重复窄接口。
- [x] **FB-26**: `internal/service`中仅包内命名引用的接口、错误码和测试辅助入口不应继续扩大导出面。
- [x] **FB-27**: `closeutil.Closer`不应重复导出标准库已有的`io.Closer`契约。
- [x] **FB-28**: `dialect.Dialect`不应公开仅启动期内部使用的`OnStartup`和`RuntimeConfig`契约。
- [x] **FB-29**: `pluginhost`生命周期聚合内部使用的可选断言接口不应作为公开插件契约导出。
- [x] **FB-30**: 非文本 AI 子能力尚未实现 provider SPI 时不应导出未被使用的`Provider`和`ProviderRequest`。
- [x] **FB-31**: `aicap`仅包内使用的聚合构造选项不应导出为公开`Option`和`With*`函数。
- [x] **FB-32**: `pluginhost` jobs registrar 不应为已有 integration enablement 和 cluster topology 服务可满足的判断重复定义 `PluginEnabledChecker` 和 `PrimaryNodeChecker` 依赖。
- [x] **FB-33**: `pluginhost`生命周期聚合不应为内部 hook 分发继续保留大量单方法接口，应使用显式回调表收敛分发复杂度。
- [x] **FB-34**: `pluginhost` HTTP route/global middleware registrar 不应继续保留 `PluginEnabledChecker` 和 host-owned 具体实现，应复用 integration enablement 服务。
- [x] **FB-35**: `cachecoord.serviceImpl.topology`不应在已有`cluster.Service`可满足时重复定义本地拓扑窄接口。
- [x] **FB-36**: 跨包使用的`New*`初始化函数不应返回私有类型，应返回公开契约或公开类型。
- [x] **FB-37**: `apps/lina-core/pkg`组件中仅包内使用的 helper、诊断常量、描述符别名和 WASM 内部常量不应继续作为导出标识暴露。
- [x] **FB-38**: `apps/lina-core/internal/service`导出面复审后，测试专用入口、包内 helper 和未使用 facade 转接不应继续作为生产导出。
- [x] **FB-39**: `userCapabilityAdapter`不应在已有`tenantcap.Service.Filter()`可满足时继续保存`tenantcap.FilterService`字段。
- [x] **FB-40**: `user`服务不应在已有`orgspi.Service`可满足时同时保存`orgcap.Service`和`orgspi.ScopeService`字段。
- [x] **FB-41**: `linapro-content-notice`服务不应在已有`tenantcap.Service.Filter()`可满足时继续保存`tenantcap.FilterService`字段。
- [x] **FB-42**: `linapro-ops-demo-guard`中间件不应在已有`i18ncap.Service`和`plugincap.StateService`可满足时重复定义`Translator`和`EnablementReader`窄接口。
- [x] **FB-43**: `linapro-demo-dynamic`服务不应在已有`pluginbridge`、`plugincap.ConfigService`和`recordstore.DB`契约可满足时重复定义 guest host service 窄接口。
- [x] **FB-44**: `linapro-monitor-loginlog`清理任务不应在已有`loginlog.Service`可满足时重复定义`loginLogRetentionCleaner`窄接口。
- [x] **FB-45**: `linapro-ai-core`清理任务不应在已有`ai.Service`可满足时重复定义`invocationRetentionCleaner`窄接口。
- [x] **FB-46**: `linapro-tenant-core`默认租户解析器不应在已有`membership.Service`可满足时重复定义`userTenantLister`窄接口。
- [x] **FB-47**: 插件消费者不应在已有`plugincap.Service`可满足时继续直接保存或接收`plugincap.StateService`、`plugincap.ConfigService`和`plugincap.LifecycleService`子能力。
- [x] **FB-48**: `apps/lina-plugins`后端 Go 代码中仅组件内部使用的接口、变量、常量、类型和方法不应继续保持公开定义，错误码和跨组件初始化返回类型除外。
- [x] **FB-49**: 剩余源码插件普通 service 不应在已有`tenantcap.Service.Filter()`可满足时继续保存或接收`tenantcap.FilterService`字段。
- [x] **FB-50**: `linapro-tenant-core` provider 和 tenant plugin governance 不应在已有`tenantcap.Service.Plugins()`可满足时直接依赖`tenantcap.PluginService`窄接口。

## Impact Record

- `i18n`影响：FB-1 至 FB-8、FB-10 至 FB-18 无运行时用户可见文案、API 文档源文本、插件清单或语言包资源变更；FB-12 仅将`capabilityhost`适配器依赖类型收敛到`i18n.Service`；FB-15 仅移动 runtime 本地化投影 helper，翻译键、回退语义和资源加载路径不变；FB-16 仅调整`capabilityhost`租户过滤依赖来源；FB-17 仅将 runtime 字段类型从本地窄接口收敛到已有`i18n.Service`，翻译键、回退语义和缓存失效调用不变；FB-18 仅收敛 integration/lifecycle/runtime 构造依赖类型。FB-9 删除无业务入口的 i18n 管理诊断 API、对应 apidoc 翻译资源和插件消息搜索方法，运行时语言列表与运行时语言包 API 响应结构保持不变。
- 缓存一致性影响：FB-1 至 FB-8、FB-10 至 FB-17 无缓存权威数据源、失效机制、跨实例同步或共享实例策略变化；`cron`仍通过启动期共享的`config.Service`和`role.Service`执行运行时参数与权限拓扑快照预热，集群周期同步仍由已有内建任务和持久调度器负责。FB-9 将`BundleRevision(ctx, locale)`作为读取修订号前的 freshness 检查入口，不改变运行时 i18n 缓存权威数据源、作用域失效、共享修订号或故障降级策略。FB-12 中`session.Store`由`auth.Service.SessionStore()`提供，继续复用启动期共享 auth/session 状态，不新增本地缓存或独立状态后端。FB-15 仅移动 runtime cache/reconciler change reason 常量和相关 helper 的文件位置，稳定字符串值、失效触发点和共享修订号发布路径不变。FB-16 复用启动期共享的`tenantspi.Service.Filter()`，不新增本地缓存、派生状态或独立后端。FB-17 复用启动期共享的`bizctx.Service`、`role.Service`和`i18n.Service`，不新增缓存实例、权限快照后端或翻译缓存后端。FB-18 仅收回`integration.sharedState`外部构造参数并复用同一`integration.Service`实例内的 enabled snapshot，刷新、Set/Delete 和路由绑定读写语义不变；runtime 存储清理直接持有启动期`capability.Services`，不新增缓存后端或失效路径。
- 数据权限影响：无新增数据读取、写入、过滤、租户/组织边界或插件数据访问路径变化；FB-9 删除既有 i18n 管理诊断读取接口，不新增可见性入口；FB-10 仅收敛内部依赖类型，菜单平台治理判断仍使用同一启动期`tenantspi.Service`实例；FB-11 仅收敛中间件依赖类型，认证、权限和租户解析行为不变；FB-12 仅收敛`capabilityhost`依赖契约，session、file、job 和 notify 既有可见性检查与租户过滤路径不变；FB-13 仅移动 generic plugin resource data-scope 实现位置，查询阶段注入数据权限过滤的语义不变；FB-14 仅重命名单元测试文件并调整文件注释，不改变数据权限逻辑；FB-15 仅合并 runtime 文件和自组合接口，不改变插件列表、动态路由或资源投影查询路径；FB-16 仅将插件表租户过滤上下文来源从`capabilityhost`内部构造改为启动期`tenantspi.Service.Filter()`，同一`bizctx`派生语义和租户过滤行为不变；FB-17 仅替换 runtime 构造依赖类型，动态路由身份注入、权限投影、数据范围快照和租户边界逻辑不变；FB-18 仅将插件资源 data-scope 读取来源从`BizCtxProvider`转为`bizctx.Service.Get(ctx)`并将组织能力从`AssignmentService`提升到`orgcap.Service.Assignment()`，数据库查询阶段的 deny/all/dept/self 过滤语义不变。
- 开发工具跨平台影响：不修改脚本、Makefile、CI 或生成工具。
- DI 来源检查：不新增运行期依赖来源；普通 controller、普通 service、cron、插件内部子组件和 capability adapter 在已有目标`Service`可直接满足时复用`config.Service`、`role.Service`、`jobmgmt.Service`、`plugin.Service`、`auth.Service`、`notify.Service`、`apidoc.Service`、`orgspi.Service`、`tenantspi.Service`，`httpRuntime`复用已有`config.Service`读取 server extensions 配置而不额外保存`ServerExtensionsConfig`字段，`cron`复用已有`config.Service`读取 session 配置而不额外保存`SessionConfig`字段，`menu`复用启动期传入的`tenantspi.Service`执行平台菜单治理检查而不额外定义`platformMenuTenantService`，`middleware`复用启动期传入的`i18n.Service`和`tenantspi.Service`执行本地化响应与租户解析而不额外定义`middlewareI18nService`和`tenantMiddlewareService`；FB-12 中`capabilityhost.New`和`plugin.NewHostServices`移除重复的 auth token issuer、session store、runtime i18n、tenant host service 和 job host service 参数，auth token 能力直接复用`auth.Service`，session store 通过`auth.Service.SessionStore()`复用同一共享后端，i18n 直接复用`i18n.Service`，tenant 直接复用`tenantspi.Service`，job 写入和执行复用`jobmgmt.Service`已嵌入的`jobmeta.Owner`以避免 Go 包循环，file/notify scoped 能力改由`file.CapabilityService`和`notify.CapabilityService`显式声明；FB-13 不新增构造函数参数或运行期依赖，只合并`integration.Service`自组合接口与职责相邻文件；FB-15 不新增构造函数参数或运行期依赖，只把`runtime.Service`自组合接口直接收敛到默认`Service`并合并薄文件；FB-16 不新增构造函数参数，`capabilityhost.New`复用启动期传入的`tenantspi.Service.Filter()`并移除`directory.tenantFilter`和本地`NewPluginTableFilter`构造，租户服务缺失时在初始化阶段显式返回错误；FB-17 不新增运行期依赖，runtime 继续接收启动期已存在的`bizctx.Service`、`role.Service`和`i18n.Service`实例，删除`userCtxAdapter`、`RoleAccessProjector`和`runtimeI18nService`本地重复契约，动态路由权限快照、请求上下文和运行时翻译仍复用同一共享服务图；FB-18 不新增运行期依赖，`integration.New`直接复用启动期共享`bizctx.Service`、`cluster.Service`、`capability.Services`、`orgcap.Service`和`runtime.Service`，`lifecycle.New`和`runtime.New`直接复用启动期共享`cluster.Service`和`capability.Services`，根插件服务删除`runtimeTopologyAdapter`并在缺省时使用`cluster.New(nil)`提供单节点拓扑；`authHookService`因`plugin.RuntimeDelegate`启动构造环保留，`preTokenStore`和`revokeStore`因`auth`内部 token 状态存储策略保留，`session.Store`、`jobhandler.Registry`和`jobmgmt.Scheduler`因共享状态或生产者侧调度子契约保留。
- 测试策略：内部接口治理使用`openspec validate`、静态检索和变更包`go test`编译门禁验证；FB-9 涉及 API/插件能力删除和缓存读取路径调整，补充运行时 i18n 控制器、i18n service、插件 management list、动态 OpenAPI 投影和启动路由绑定相关 Go 测试；FB-10 通过`internal/service/menu`和启动装配包测试覆盖构造签名与平台菜单治理入口；FB-11 通过`internal/service/middleware`和启动装配包测试覆盖构造签名与中间件租户解析入口；FB-12 通过`go test ./internal/service/plugin/internal/capabilityhost -count=1`、`go test ./internal/service/jobmgmt/capabilityadapter -count=1`、`go test ./internal/service/plugin -count=1`、`go test ./internal/cmd -count=1`、`go test ./internal/service/auth ./internal/service/file ./internal/service/notify ./internal/service/jobmgmt -count=1`、旧接口静态检索和`openspec validate move-narrow-interfaces-to-consumers --strict`覆盖；FB-13 通过`internal/service/plugin/internal/integration`和`internal/service/plugin`包测试、OpenSpec 严格校验、静态检索验证自组合接口已移除且行为未变；FB-14 通过`internal/service/plugin/internal/integration`包测试、OpenSpec 严格校验和文件存在性检查验证测试文件与源码文件对应；FB-15 通过`internal/service/plugin/internal/runtime`包测试、OpenSpec 严格校验、静态检索和文件数量检查验证 runtime 自组合接口与薄文件已收敛且行为未变；FB-16 通过`capabilityhost`包测试、`plugin`包测试、`internal/cmd`启动装配包测试、OpenSpec 严格校验、静态检索和 diff 检查验证租户过滤依赖来源已收敛；FB-17 通过 OpenSpec 严格校验、旧接口静态检索、格式检查和 runtime/plugin/cmd Go 编译门禁验证，若当前工作区既有非本任务编译错误阻断则记录阻断原因且不标记完成；FB-18 通过`integration`、`lifecycle`、`plugin`和`internal/cmd`包测试、runtime 构造/协调子测试、OpenSpec 严格校验、静态检索和 diff 检查验证 integration 依赖收敛。
- FB-13 验证结果：`openspec validate move-narrow-interfaces-to-consumers --strict`通过；静态检索确认`BackendConfigService`、`ResourceQueryService`、`SourceRegistrationService`、`HookDispatchService`、`MenuFilterService`、`StartupSnapshotService`、`PluginStateService`、`MenuSyncService`和`ResourceReferenceService`无剩余引用；`go test ./internal/service/plugin/internal/integration -count=1`、`go test ./internal/service/plugin/internal/lifecycle -count=1`和`go test ./internal/service/plugin/internal/upgrade -count=1`通过。FB-12 收敛后`go test ./internal/service/plugin -count=1`通过；`go test ./internal/service/plugin/internal/runtime -count=1`被当前工作区既有`pkg/plugin/pluginbridge/recordstore`的 WASM 构建字段不一致阻断，非 FB-13 引入。
- FB-14 验证结果：文件存在性检查确认`integration_shared_state_test.go`已移除且`integration_enablement_test.go`存在；`go test ./internal/service/plugin/internal/integration -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check`通过。
- FB-15 验证结果：`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check`通过；静态检索确认 runtime 自组合接口`ArtifactService`、`RuntimeStateQueryService`、`DynamicRouteService`、`LifecycleReconcileService`、`RuntimeRegistryService`、`RuntimeProjectionService`、`RuntimeReconcilerService`、`RuntimeUpgradeService`、`DynamicLifecyclePreconditionService`、`DynamicJobService`、`RuntimeLifecycleService`、`ActiveManifestService`和`DynamicPackageService`无剩余引用；文件数量检查确认`plugin/internal/runtime` Go 文件数为 39，原薄文件`runtime_authorization_status.go`、`runtime_upgrade_state.go`、`runtime_i18n.go`、`runtime_route_review.go`、`runtime_artifact_resources.go`、`runtime_change_reason.go`、`runtime_route_response.go`和`runtime_route_dispatch.go`已移除；`go test ./internal/service/plugin -count=1`通过；`go test ./internal/service/plugin/internal/runtime -run 'TestBuildArtifactResourceViews|TestDynamicRouteEntrypointStaysSlim|TestDynamicUpgradeFailurePhaseSurvivesJoinedDiagnostics|TestTagDynamicUpgradeFailureKeepsExistingPhase|TestNextBackgroundReconcileDecisionUsesSharedRevision|TestNotifyReconcilerChangedUsesReconcilerScope' -count=1`通过。`go test ./internal/service/plugin/internal/runtime -count=1`仍被当前工作区既有`pkg/plugin/pluginbridge/recordstore`的 WASM 构建字段不一致阻断，非 FB-15 引入。
- FB-16 验证结果：`go test ./internal/service/plugin/internal/capabilityhost -count=1`通过；`go test ./internal/service/plugin -count=1`通过；`go test ./internal/cmd -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；静态检索确认`capabilityhost`和`plugin_host_services.go`中不存在`tenantFilter capabilitytenantcap.FilterService`字段、`tenantFilter:`赋值或`NewPluginTableFilter()`本地构造；`git diff --check`通过。
- FB-17 验证结果：`openspec validate move-narrow-interfaces-to-consumers --strict`通过；静态检索确认`RoleAccessProjector`、`UserContextSetter`、`userImpersonationSetter`、`runtimeI18nService`、`userCtxAdapter`和`runtimeWiringUserContext`无剩余生产引用；`git diff --check -- apps/lina-core/internal/service/plugin/internal/runtime/runtime.go apps/lina-core/internal/service/plugin/internal/runtime/runtime_route_auth.go apps/lina-core/internal/service/plugin/plugin.go apps/lina-core/internal/service/plugin/plugin_adapters.go apps/lina-core/internal/service/plugin/internal/testutil/testutil_services.go apps/lina-core/internal/service/plugin/internal/runtime/runtime_wiring_test.go openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`通过；`go test ./internal/service/plugin/internal/runtime -run 'TestNewRuntimeWiresRequiredDependencies|TestDynamicRouteIdentitySnapshotFiltersRolesByTokenTenant|TestNextBackgroundReconcileDecisionUsesSharedRevision|TestNotifyReconcilerChangedUsesReconcilerScope' -count=1`通过；`go test ./internal/service/plugin -count=1`通过；`go test ./internal/cmd -count=1`通过。`go test ./internal/service/plugin/internal/runtime -count=1`仍被当前工作区既有`pkg/plugin/pluginbridge/recordstore`的 WASM 构建字段不一致阻断，非 FB-17 引入。
- FB-18 验证结果：`go test ./internal/service/plugin/internal/integration ./internal/service/plugin/internal/lifecycle ./internal/service/plugin -count=1`通过；`go test ./internal/service/plugin/internal/runtime -run 'TestNewRuntimeWiresRequiredDependencies|TestRuntimeReconcilerLockSerializesSamePlugin|TestReconcileDynamicPluginRequestBumpsRevision|TestStorageCleanup' -count=1`通过；`go test ./internal/service/plugin/internal/store ./internal/service/plugin/internal/upgrade -count=1`通过；`go test ./internal/service/plugin/internal/testutil -count=1`通过；`go test ./internal/cmd -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check`通过；静态检索确认`BizCtxProvider`、`SourceServicesProvider`、`DynamicJobExecutor`、`StorageCleanupServicesProvider`、`TopologyProvider`、`runtimeTopologyAdapter`、`NewSharedState`和`SourceServicesForPlugin`无剩余定义或调用。`go test ./internal/service/plugin/internal/runtime -count=1`仍被当前工作区既有`pkg/plugin/pluginbridge/recordstore`的 WASM 构建字段不一致阻断，非 FB-18 引入。
- FB-19 影响分析：`plugin/internal/openapi`删除重复的`projectionCatalog`、`projectionStore`和`LocaleBundleReader`本地接口，直接复用启动期已有`catalog.Service`、`store.Service`和`i18n.Service`；`RevisionReader`因根插件服务构造期循环打断继续保留。`i18n`影响仅为依赖类型收敛到已有共享`i18n.Service`，不修改运行时用户可见文案、OpenAPI 源文本、插件清单或语言包资源；缓存一致性不改变 OpenAPI 投影缓存权威数据源、失效触发点、共享运行时修订号或 bundle revision 分区策略；数据权限不新增数据读取、写入、过滤或租户/组织边界；开发工具跨平台无影响；DI 来源不新增运行期依赖，生产装配继续由`plugin.New`创建并传递同一启动期 catalog/store/i18n 实例，测试装配复用同一测试 i18n 实例。
- FB-19 验证结果：`go test ./internal/service/plugin/internal/openapi -count=1`通过；`go test ./internal/service/plugin/internal/testutil -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check -- apps/lina-core/internal/service/plugin/internal/openapi/openapi.go apps/lina-core/internal/service/plugin/internal/openapi/openapi_projection_cache.go apps/lina-core/internal/service/plugin/internal/openapi/openapi_test.go apps/lina-core/internal/service/plugin/internal/testutil/testutil_services.go openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`通过；静态检索确认`projectionCatalog`、`projectionStore`、`LocaleBundleReader`、`DefaultLocaleBundleReader`和`localeReader`无剩余引用；`rg -n "openapi.New\\(" apps/lina-core -g '*.go'`确认调用点仅剩生产装配和测试装配。`go test ./internal/service/plugin -count=1`被当前工作区既有`plugin_integration.go`中`scoping`未导入问题阻断，非 FB-19 引入，归属 FB-18 方向。
- FB-20 影响分析：`role`删除重复的`OrganizationCapabilityState`和`roleTenantGovernanceService`本地接口，直接复用启动期已有`orgcap.Service`和`tenantspi.Service`。`i18n`影响仅为依赖类型收敛，不修改运行时用户可见文案、API 文档源文本、插件清单或语言包资源；缓存一致性不改变角色访问快照、权限拓扑修订号、组织能力状态或租户 provider 状态的权威数据源和共享实例策略；数据权限不新增读取、写入、过滤或租户/组织边界，部门数据范围可用性判断与角色租户 membership 校验仍调用同一目标组件方法；开发工具跨平台无影响；DI 来源不新增运行期依赖，生产装配继续由`httpstartup`创建并传递同一启动期`orgcap.Service`和`tenantspi.Service`实例；测试策略为`internal/service/role`包测试、`internal/cmd`启动装配包测试、旧接口静态检索、格式检查和 OpenSpec 严格校验。
- FB-20 验证结果：`go test ./internal/service/role -count=1`通过；`go test ./internal/cmd -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check -- apps/lina-core/internal/service/role/role.go apps/lina-core/internal/service/role/role_impl.go apps/lina-core/internal/service/role/role_test_dependencies_test.go apps/lina-core/internal/service/role/role_data_scope_test.go apps/lina-core/internal/service/role/role_tenant_boundary_test.go openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`通过；静态检索确认`OrganizationCapabilityState`、`roleTenantGovernanceService`、`orgCapabilityState`和`roleI18nTranslator`无剩余引用。
- FB-21 影响分析：`plugin/internal/runtime`将`MenuManager`、`ResourceReferenceManager`、`HookDispatcher`和`PermissionMenuFilter`四个同源 integration 依赖收敛为单一`IntegrationService`，由于`integration`包仍依赖`runtime.Service`，暂由 runtime 消费方保留一个聚合接口以避免 Go 导入环；`plugin.New`和测试装配只传入一次 integration delegate。`storageCleanupServices`继续直接依赖启动期共享`capability.Services`，测试装配同步使用 capability directory 或 nil。`i18n`无运行时用户可见文案、API 文档源文本、插件清单或语言包资源影响；缓存一致性不改变插件 enabled snapshot、runtime cache、reconciler revision 或跨实例发布路径；数据权限不新增数据读取、写入、过滤或租户/组织边界；开发工具跨平台无影响；DI 来源不新增运行期依赖，生产装配继续由`plugin.New`创建同一`integration.Service`、`capability.Services`和 runtime 共享服务图，仅减少重复构造参数。
- FB-21 验证结果：静态检索确认`MenuManager`、`ResourceReferenceManager`、`HookDispatcher`、`PermissionMenuFilter`和`StorageCleanupServicesProvider`在 runtime、integration delegate 和 integration 相关文件中无剩余引用；`go test ./internal/service/plugin/internal/runtime -run 'TestNewRuntimeWiresRequiredDependencies|TestReconcileDynamicPluginRequestBumpsRevision|TestNextBackgroundReconcileDecisionUsesSharedRevision|TestNotifyReconcilerChangedUsesReconcilerScope|TestRunReconcilerTickSafelyRecoversPanic' -count=1`通过；`go test ./internal/service/plugin/internal/integration -count=1`通过；`go test ./internal/service/plugin -count=1`通过；`go test ./internal/cmd -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check -- apps/lina-core/internal/service/plugin/internal/runtime/runtime.go apps/lina-core/internal/service/plugin/internal/runtime/runtime_wiring.go apps/lina-core/internal/service/plugin/internal/runtime/runtime_route.go apps/lina-core/internal/service/plugin/plugin.go apps/lina-core/internal/service/plugin/plugin_runtime_delegates.go apps/lina-core/internal/service/plugin/internal/testutil/testutil_services.go apps/lina-core/internal/service/plugin/internal/runtime/runtime_wiring_test.go apps/lina-core/internal/service/plugin/internal/runtime/runtime_reconciler_test.go apps/lina-core/internal/service/plugin/internal/integration/integration.go apps/lina-core/internal/service/plugin/internal/integration/integration_extensions.go apps/lina-core/internal/service/plugin/internal/integration/integration_menu.go openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`通过。`go test ./internal/service/plugin/internal/runtime -count=1`已消除本次构造签名编译错误，但完整包测试仍被当前工作区既有`pkg/plugin/pluginbridge/recordstore` WASM 构建字段不一致阻断，失败点为`recordstore_exec_wasip1.go`中`Tx.invoker`字段缺失，非 FB-21 引入。
- FB-22 影响分析：`plugin/internal/testutil.Services.Lifecycle`删除重复的本地`LifecycleService`字段类型并直接复用生命周期组件已有`lifecycle.Service`；runtime 和 integration 测试改用`Install`/`Uninstall`公共生命周期入口，`lifecycle.serviceImpl`删除无调用的`InstallDynamic`/`UninstallDynamic`包装方法，内部`installDynamic`仍服务正式安装流程。`singleNodeTopology`同步实现已有`cluster.Service`，`upgrade.New`测试装配直接复用同一拓扑实例，不新增运行期依赖或独立服务图。`i18n`无运行时用户可见文案、API 文档源文本、插件清单或语言包资源影响；缓存一致性不改变 runtime cache、enabled snapshot、reconciler revision 或跨实例发布路径；数据权限不新增读取、写入、过滤或租户/组织边界；开发工具跨平台无影响。
- FB-22 验证结果：静态检索确认`type LifecycleService`、`Lifecycle LifecycleService`、`InstallDynamic`和`UninstallDynamic`无剩余引用；`go test ./internal/service/plugin/internal/testutil -count=1`通过；`go test ./internal/service/plugin/internal/lifecycle -count=1`通过；`go test ./internal/service/plugin/internal/integration -count=1`通过；`go test ./internal/service/plugin/internal/runtime -run 'TestListRuntimeStatesProjectsInstalledDynamicPluginsWithoutReleasePointNPlusOne|TestInstallRepairsEmptyReleaseArchive|TestInstallRejectsLowerVersionBeforeRegistrySync|TestDispatchDynamicRoutePreserves' -count=1`通过；`go test ./internal/service/plugin -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check`通过。`go test ./internal/service/plugin/internal/runtime -count=1`仍被当前工作区既有`pkg/plugin/pluginbridge/recordstore` WASM 构建字段不一致阻断，失败点为`recordstore_exec_wasip1.go`中`Tx.invoker`字段缺失，非 FB-22 引入。
- FB-23 影响分析：`role.PermissionMenuFilter`改为包内`permissionMenuFilter`，`role.New`仍通过显式构造函数接收插件权限菜单过滤依赖，方法契约`FilterPermissionMenus`保持不变，跨包实现无需引用接口类型名即可继续满足构造参数。`i18n`无运行时用户可见文案、API 文档源文本、插件清单或语言包资源影响；缓存一致性不改变角色访问快照、权限拓扑修订号或插件 enabled snapshot 的权威数据源、失效机制和共享实例策略；数据权限不新增读取、写入、过滤或租户/组织边界；开发工具跨平台无影响；DI 来源不新增运行期依赖，生产装配继续由`httpstartup`传入同一启动期`pluginRuntime`实例。
- FB-23 验证结果：静态检索确认`PermissionMenuFilter`在`apps/lina-core`Go 源码中无剩余引用；`go test ./internal/service/role -count=1`通过；`go test ./internal/cmd -count=1`通过；`go test ./internal/controller/i18n -count=1`通过；`go test ./internal/service/user -count=1`通过；`go test ./internal/service/jobmgmt -count=1`通过；`go test ./internal/service/plugin -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check`通过。
- FB-24 影响分析：`upgrade`中仅包内消费的`RuntimeCacheFreshener`、`CachePublisher`和`I18nService`改为私有接口；`openapi.RevisionReader`改为私有`revisionReader`，保留`DeferredRevisionReader`作为根插件服务构造期循环绑定入口；`store.ManifestCatalog`和`NodeIDProvider`删除并直接复用启动期已有`catalog.Service`和`cluster.Service`；`dependency.ReleaseSnapshotReader`删除并直接复用`store.Service`；`lifecycle.TenantProvisioningService`和`capabilityowner.RuntimeStateStore`改为私有；根插件 host config helper 删除`HostRawConfigReader`并直接复用`config.Service`；`pluginhost.Services`删除，source-plugin registrar、hook payload 和 lifecycle 输入直接复用已有`capability.Services`契约，并同步更新`apps/lina-core/pkg/plugin`中英文 README 的公开契约说明。`runtime.Service`字段复扫中，`TopologyProvider`已在 FB-18 清理，`IntegrationService`、`CacheChangeNotifier`和`DependencyValidator`因`runtime`与`integration`、根插件服务构造存在导入环和延迟绑定需求继续作为跨包构造契约保留，`*revisionctrl.ObservedRevision`和`*revisionctrl.Controller`为具体共享状态组件指针，不属于重复窄接口。剩余插件相关公开非`Service`接口复扫确认承担跨包组件契约、协议入口或公开组件契约用途，不属于仅包内消费。`i18n`无运行时用户可见文案、API 文档源文本、插件清单或语言包资源影响；缓存一致性不改变 runtime cache、enabled snapshot、OpenAPI 投影缓存、bundle revision、共享修订号或跨实例发布路径；数据权限不新增读取、写入、过滤或租户/组织边界；开发工具跨平台无影响。DI 来源不新增运行期依赖，生产装配继续复用启动期共享`catalog.Service`、`cluster.Service`、`config.Service`、`store.Service`、`capability.Services`、runtime revision 和 cache 发布实例，测试替身同步补齐既有`catalog.Service`契约。
- FB-24 验证结果：静态检索确认`RuntimeCacheFreshener`、`CachePublisher`、`I18nService`、`RevisionReader`、`TenantProvisioningService`、`RuntimeStateStore`、`ReleaseSnapshotReader`、`ManifestCatalog`、`NodeIDProvider`、`HostRawConfigReader`、`pluginhost.Services`和旧签名 provider env fake 无剩余生产或文档引用；公开接口复扫确认剩余导出接口具有跨包契约、协议入口或公开组件契约用途；`go test ./internal/service/plugin/internal/upgrade ./internal/service/plugin/internal/openapi ./internal/service/plugin/internal/store ./internal/service/plugin/internal/dependency ./internal/service/plugin/internal/lifecycle -count=1`通过；`go test ./internal/service/plugin/internal/capabilityowner -count=1`通过；`go test ./internal/service/plugin/internal/integration ./internal/service/plugin/internal/lifecycle ./internal/service/plugin/internal/capabilityhost ./internal/service/user -count=1`通过；`go test ./pkg/plugin/pluginhost ./pkg/plugin/capability/tenantcap/tenantspi ./pkg/plugin/capability/orgcap/orgspi ./pkg/plugin/capability/aicap/aitext -count=1`通过；`go test ./internal/service/auth ./internal/service/menu ./internal/service/role ./internal/service/user -count=1`通过；`go test ./internal/service/plugin -count=1`通过；`go test ./internal/service/plugin/internal/wasm -count=1`通过；`go test ./internal/cmd -count=1`通过；`go test ./internal/service/plugin/internal/runtime -run 'TestNewRuntimeWiresRequiredDependencies|TestRuntimeReconcilerLockSerializesSamePlugin|TestReconcileDynamicPluginRequestBumpsRevision|TestStorageCleanup|TestBuildArtifactResourceViews|TestDynamicRouteEntrypointStaysSlim|TestNextBackgroundReconcileDecisionUsesSharedRevision|TestNotifyReconcilerChangedUsesReconcilerScope' -count=1`通过。`go test ./internal/service/plugin/internal/runtime -count=1`仍被当前工作区既有`pkg/plugin/pluginbridge/recordstore` WASM 构建字段不一致阻断，失败点为`recordstore_exec_wasip1.go`中`Tx.invoker`字段缺失，非 FB-24 引入；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check`通过。
- FB-25 影响分析：`user`服务将租户作用域、成员关系和平台访问判断字段合并为启动期共享的`tenantspi.Service`；根`plugin`服务将 startup consistency、tenant provisioning 和平台治理三段租户能力合并为同一个`tenantspi.Service`构造参数，并把生命周期子组件字段同步命名为`tenantSvc`；`jobhandler`插件生命周期同步直接依赖已有`plugin.Service`；`plugin/internal/migration`直接依赖`store.Service`；`jobmgmt/internal/shellexec`直接依赖`config.Service`；`plugin`和`upgrade`的 i18n 字段直接复用`i18n.Service`。为恢复当前工作区测试门禁，`plugin/internal/wasm`补齐`intUserIDRequest`请求 DTO，协议字段保持`userId`不变。`i18n`影响：仅依赖类型收敛为已有`i18n.Service`，不修改运行时用户可见文案、API 文档源文本、插件清单、语言包资源、翻译键或回退语义。缓存一致性影响：不新增缓存、失效路径、共享修订号或跨实例同步机制；插件 runtime cache、i18n runtime bundle 和 enabled snapshot 继续复用启动期共享实例。数据权限影响：不新增读取、写入、过滤、租户/组织边界或可见性入口；WASM tenant host call 仅补齐已使用的请求结构，原有`ensureHostCallUsersVisible`校验路径不变。开发工具跨平台影响：不修改脚本、Makefile、CI 或生成工具。DI 来源检查：不新增运行期依赖，只减少构造参数和本地接口；生产装配继续由`httpstartup`创建并传递同一`tenantspi.Service`、`i18n.Service`、`config.Service`、`store.Service`和`plugin.Service`共享实例。测试策略：内部依赖治理使用变更包 Go 编译门禁、旧接口静态检索、OpenSpec 严格校验和 diff 检查验证；无用户可观察行为或 HTTP API 契约变化，不新增 E2E。
- FB-25 验证结果：旧接口静态检索确认`userTenantAccessService`、`pluginTenantStartupCapability`、`pluginTenantProvisioningCapability`、`platformGovernanceTenantCapability`、`tenantProvisioningService`、`pluginI18nService`、`type i18nService interface`、`type storeReader interface`、`type shellGate interface`、`PluginLifecycleBridge`和`PluginLifecycleObserverRegistrar`在`apps/lina-core/internal/service`与`apps/lina-core/internal/cmd`Go 源码中无剩余引用；`go test ./internal/service/user -count=1`通过；`go test ./internal/service/plugin ./internal/service/plugin/internal/lifecycle ./internal/service/plugin/internal/migration ./internal/service/plugin/internal/upgrade -count=1`通过；`go test ./internal/service/plugin/internal/wasm -count=1`通过；`go test ./internal/service/jobhandler ./internal/service/jobmgmt/internal/shellexec ./internal/service/jobmgmt -count=1`通过；`go test ./internal/cmd -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check`通过。
- FB-26 影响分析：`apidoc.PluginRouteProvider`、`cachecoord.Topology`、`datascope.AccessProvider`、`jobhandler.LogCleaner`、`jobmgmt/internal/scheduler.Scheduler`、`menu.MenuFilter`、`session.SessionConfigurableStore`、`catalog`内部分类接口、`frontend.BundleReader`和`governance`局部依赖接口改为包内私有命名；根`jobmgmt.Scheduler`、`session.Store`、`jobhandler.Registry`和其他稳定 service/capability 契约继续保留导出。无跨`apps/lina-core`引用的`Code*`错误码改为包内私有，controller 层仍引用的上传、权限和导入错误码保持导出；同目录外部测试不再引用生产错误码变量，改为断言稳定 runtime code 字符串。`datascope.WithTenantForTest`删除，测试直接使用已有生产语义`WithTenantScope`；`frontend.ResetBundleCache`删除，测试通过`frontend.Service.InvalidateAllBundles`走正式缓存失效契约。`config`测试覆盖函数因仍被跨包测试装配使用、`kvcache`provider/option 构造函数因启动装配生产路径使用，本次保留导出。`i18n`影响：不修改运行时用户可见文案、API 文档源文本、插件清单、语言包资源、翻译键或回退语义。缓存一致性影响：仅将 frontend 测试清理改走既有`InvalidateAllBundles`，不改变缓存权威数据源、失效触发点、共享修订号、跨实例同步或生产失效策略。数据权限影响：不新增读取、写入、过滤、租户/组织边界或可见性入口；`WithTenantScope`继续保持原有租户上下文语义。开发工具跨平台影响：不修改脚本、Makefile、CI 或生成工具。DI 来源检查：不新增运行期依赖、不新增构造参数、不创建独立服务图；仅收敛局部类型名和测试清理入口。测试策略：内部治理类反馈使用静态检索、Go 编译门禁、受影响包完整测试、OpenSpec 严格校验和 diff 检查验证；无用户可观察行为变化，不新增 E2E。
- FB-26 验证结果：静态检索确认`PluginRouteProvider`、`AccessProvider`、`LogCleaner`、`SessionConfigurableStore`、`ManifestReader`、`SQLAssetCatalog`、`FrontendAssetCatalog`、`ManifestMetadata`、`BundleReader`、`TenantCapability`、`ManifestResolver`、`ResetBundleCache`和`WithTenantForTest`无剩余 Go 引用；`type [A-Z].* interface`复扫确认剩余导出接口为`Service`、稳定共享状态、capability、工厂、协议或跨包构造契约；错误码扫描确认剩余导出`Code*`均存在跨包生产或测试引用，包内闭环错误码已私有化。`go test ./internal/service/... -run '^$' -count=1`通过；`go test ./internal/cmd -count=1`通过；`go test ./internal/service/plugin/internal/frontend ./internal/service/plugin ./internal/service/datascope -count=1`通过；`go test ./internal/service/plugin/internal/runtime -run 'TestDispatchDynamicRouteBlocksPendingUpgrade|TestDispatchDynamicRouteBlocksFailedActiveRelease|TestInstallRejectsLowerVersionBeforeRegistrySync' -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check -- apps/lina-core/internal/service openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`通过。`go test ./internal/service/plugin/internal/runtime -count=1`完整包测试仍被当前工作区既有`pkg/plugin/pluginbridge/recordstore` WASM 构建字段不一致阻断，失败点为`recordstore_exec_wasip1.go`中`Tx.invoker`字段缺失，非 FB-26 引入。
- FB-27 至 FB-31 影响分析：`closeutil.Close`直接接收标准库`io.Closer`，删除重复公开接口`closeutil.Closer`；`dialect.Dialect`删除启动期`OnStartup`方法和`RuntimeConfig`接口，宿主启动装配改用已有`SupportsCluster()`决定是否执行`cluster.enabled`内存兼容覆盖，当前唯一支持的 PostgreSQL 方言保持集群能力不变；`pluginhost`生命周期聚合内部断言接口改为包内私有命名，保留公开`LifecycleHook`、输入 DTO、`LifecycleParticipant`、`LifecycleRequest`、`LifecycleDecision`、`LifecycleResult`和 handler 注册 facade；非文本 AI 子能力删除尚无注册入口和调用方的`Provider`/`ProviderRequest`，保留各子能力`Service`、请求/响应 DTO、状态和 fallback 错误语义；`aicap.New`收敛为文本能力构造入口，`ForPlugin`改用包内构造保留非文本子能力的 plugin-scoped 包装。`pkg/listorder`静态检索确认仍被`api/job`、`api/jobgroup`、`api/joblog`、`api/file`和`api/user`请求 DTO 作为生产契约引用，本轮不删除，避免把治理任务扩大为 API DTO 契约调整。`i18n`无运行时用户可见文案、API 文档源文本、插件清单或语言包资源影响；缓存一致性不新增缓存、失效路径、共享修订号或跨实例同步机制；数据权限不新增读取、写入、过滤、租户/组织边界或可见性入口；开发工具跨平台无影响；DI 来源不新增运行期依赖、不新增构造参数、不创建独立服务图。`apps/lina-core/pkg/plugin`中英文 README 静态检索确认未引用本轮删除的生命周期断言接口、非文本 provider SPI 或`aicap.With*`入口，因此无需修改文档镜像。
- FB-27 至 FB-31 验证结果：静态检索确认`closeutil.Closer`、`type RuntimeConfig interface`、`Dialect.OnStartup`、导出的`BeforeInstaller`/`AfterInstaller`/`Upgrader`等 lifecycle 断言接口、非文本 AI 子能力`Provider`/`ProviderRequest`、`aicap.WithImage`至`WithVideo`和`aicap.Option`无剩余引用；`aitext.Provider`和`aitext.ProviderRequest`保留并继续被`linapro-ai-core`使用；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`go test ./pkg/closeutil ./pkg/dialect ./pkg/plugin/pluginhost ./pkg/plugin/capability/aicap/... -count=1`通过；`go test ./internal/service/config ./internal/cmd -count=1`通过；`go test ./pkg/plugin/pluginbridge/... ./internal/service/plugin/internal/capabilityhost ./internal/service/plugin/internal/wasm -count=1`通过；`go test ./internal/service/plugin ./internal/service/plugin/internal/lifecycle ./internal/service/plugin/internal/upgrade -count=1`通过；`go test ./pkg/plugin/capability -count=1`通过；`go test ./api/job/v1 ./api/jobgroup/v1 ./api/joblog/v1 ./api/file/v1 ./api/user/v1 ./pkg/listorder -count=1`通过；`GOWORK=off go test ./backend/... -count=1`在`apps/lina-plugins/linapro-ai-core`和`apps/lina-plugins/linapro-tenant-core`均通过；`git diff --check`通过。
- FB-32 影响分析：`pluginhost.JobsRegistrar`继续作为源码插件可见契约保留，host-owned jobs registrar 具体实现移动到`plugin/internal/integration`，直接复用同一`serviceImpl`上的 plugin business-entry enablement 快照和`cluster.Service`拓扑判断，删除 jobs 场景的`PluginEnabledChecker`和`PrimaryNodeChecker`函数型转接依赖。`i18n`无运行时用户可见文案、API 文档源文本、插件清单或语言包资源影响；缓存一致性不改变 plugin enabled snapshot 的权威数据源、刷新、Set/Delete 或跨实例策略，jobs guard 继续复用启动期共享 integration 实例；数据权限不新增读取、写入、过滤、租户/组织边界或可见性入口；开发工具跨平台无影响；DI 来源不新增运行期依赖或独立服务图，只减少`NewJobsRegistrar`构造参数和中间 checker 转接函数。
- FB-32 验证结果：静态检索确认`NewJobsRegistrar`、`PrimaryNodeChecker`、`primaryNodeChecker`和`buildPrimaryNodeChecker`在`apps/lina-core`中无剩余引用；`go test ./pkg/plugin/pluginhost ./internal/service/plugin/internal/integration ./internal/service/plugin -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check -- apps/lina-core/pkg/plugin/pluginhost/pluginhost_jobs_registrar.go apps/lina-core/internal/service/plugin/internal/integration/integration_extensions_jobs_registrar.go apps/lina-core/internal/service/plugin/internal/integration/integration_extensions.go apps/lina-core/internal/service/plugin/internal/integration/integration_enablement.go apps/lina-core/internal/service/plugin/internal/integration/integration_enablement_test.go apps/lina-core/pkg/plugin/pluginhost/pluginhost_source_plugin_test.go`通过。
- FB-33 影响分析：`pluginhost`生命周期聚合删除内部 hook 分发使用的 16 个单方法接口，改为`LifecycleCallbacks`显式回调表；源码插件 lifecycle facade 注册 API、`LifecycleHook`、输入 DTO、`LifecycleParticipant`、`LifecycleRequest`、`LifecycleDecision`和`LifecycleResult`保留，source plugin install、upgrade、tenant lifecycle 调用语义、并发执行、超时、panic recover 和 veto 聚合行为不变。`i18n`无运行时用户可见文案、API 文档源文本、插件清单或语言包资源影响；缓存一致性不新增缓存、失效路径、共享修订号或跨实例同步机制；数据权限不新增读取、写入、过滤、租户/组织边界或插件数据访问路径；开发工具跨平台无影响；DI 来源不新增运行期依赖、不新增构造参数、不创建独立服务图；E2E 质量审查未触发，原因是本次为后端内部 hook 分发治理，无用户可观察页面、端到端工作流或 E2E 资产变化。
- FB-33 验证结果：静态检索确认`beforeInstaller`、`afterInstaller`、`beforeUpgrader`、`afterUpgrader`、`upgrader`、`beforeDisabler`、`afterDisabler`、`beforeUninstaller`、`afterUninstaller`、`uninstaller`、`beforeTenantDisabler`、`afterTenantDisabler`、`beforeTenantDeleter`、`afterTenantDeleter`、`beforeInstallModeChanger`、`afterInstallModeChanger`接口定义和旧`Callback:`字段无剩余引用；`go test ./pkg/plugin/pluginhost -count=1`通过；`go test ./internal/service/plugin/internal/lifecycle ./internal/service/plugin/internal/upgrade ./internal/service/plugin -count=1`通过；`go test ./internal/cmd -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check -- apps/lina-core/pkg/plugin/pluginhost/pluginhost_lifecycle_precondition.go apps/lina-core/pkg/plugin/pluginhost/pluginhost_lifecycle_adapter.go apps/lina-core/pkg/plugin/pluginhost/pluginhost_lifecycle_precondition_test.go apps/lina-core/pkg/plugin/pluginhost/pluginhost_source_plugin_test.go apps/lina-core/internal/service/plugin/internal/lifecycle/lifecycle_source_install.go apps/lina-core/internal/service/plugin/internal/upgrade/upgrade_source_lifecycle.go openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`通过。
- FB-34 影响分析：`pluginhost.HTTPRegistrar`、`RouteRegistrar`、`RouteGroup`和`GlobalMiddlewareRegistrar`继续作为源码插件可见契约保留，host-owned HTTP route/global middleware 具体实现移动到`plugin/internal/integration`，直接复用同一`serviceImpl`上的 plugin business-entry enablement 快照和请求上下文判断，删除`PluginEnabledChecker`、`NewHTTPRegistrar`、`NewRouteRegistrar`和`NewGlobalMiddlewareRegistrar`函数型转接入口；`httpRegistrar`内部字段不再以公开 registrar 接口自持有，integration 私有实现使用具体结构体字段。`i18n`无运行时用户可见文案、API 文档源文本、插件清单或语言包资源影响；缓存一致性不改变 plugin enabled snapshot 的权威数据源、刷新、Set/Delete 或跨实例策略，HTTP route/global middleware guard 继续复用启动期共享 integration 实例；数据权限不新增读取、写入、过滤、租户/组织边界或插件数据访问路径；开发工具跨平台无影响；DI 来源不新增运行期依赖、不新增构造参数、不创建独立服务图，只减少`pluginhost`边界的 checker 转接函数；E2E 质量审查未触发，原因是本次为后端内部插件宿主注册治理，无前端页面、端到端工作流或 E2E 资产变化。`apps/lina-core/pkg/plugin`中英文 README 静态检索确认未引用删除的 HTTP registrar 构造器或`PluginEnabledChecker`，无需更新文档镜像。
- FB-34 验证结果：静态检索确认`PluginEnabledChecker`、`NewHTTPRegistrar`、`NewRouteRegistrar`、`NewGlobalMiddlewareRegistrar`和`buildBackgroundEnabledChecker`在`apps/lina-core`中无剩余引用；`go test ./pkg/plugin/pluginhost ./internal/service/plugin/internal/integration ./internal/service/plugin -count=1`通过；`go test ./internal/cmd -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check -- apps/lina-core/pkg/plugin/pluginhost/pluginhost_http_registrar.go apps/lina-core/pkg/plugin/pluginhost/pluginhost_route_registrar.go apps/lina-core/pkg/plugin/pluginhost/pluginhost_source_route_binding.go apps/lina-core/pkg/plugin/pluginhost/internal/routebinding/routebinding.go apps/lina-core/pkg/plugin/pluginhost/pluginhost_http_registrar_test.go apps/lina-core/pkg/plugin/pluginhost/pluginhost_route_registrar_test.go apps/lina-core/internal/service/plugin/internal/integration/integration_extensions.go apps/lina-core/internal/service/plugin/internal/integration/integration_enablement.go apps/lina-core/internal/service/plugin/internal/integration/integration_enablement_test.go apps/lina-core/internal/service/plugin/internal/integration/integration_extensions_http_registrar.go apps/lina-core/internal/service/plugin/internal/integration/integration_extensions_http_routebinding.go apps/lina-core/internal/service/plugin/internal/integration/integration_extensions_http_registrar_test.go openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`通过。
- FB-35 影响分析：`cachecoord.serviceImpl.topology`从本地重复窄接口收敛为已有`cluster.Service`，`New`、`Default`、`NewWithCoordination`和`DefaultWithCoordination`直接接收`cluster.Service`；为解除`cachecoord -> cluster -> config -> cachecoord`包循环，`ClusterConfig`、`ElectionConfig`和`ClusterRedisConfig`归属到`cluster`包，`config`包保留类型别名作为配置加载入口；`cachecoord.NewStaticTopology`返回实现`cluster.Service`的静态占位服务，插件 runtime 删除`runtimeCacheCoordTopology`并直接复用启动期共享`cluster.Service`。`i18n`无运行时用户可见文案、API 文档源文本、插件清单、语言包资源或翻译缓存影响；缓存一致性不改变 cachecoord 的权威数据源、修订号存储、失效触发点、事件发布、跨实例同步或最大陈旧窗口，只复用同一 cluster 拓扑契约读取 cluster.enabled 和 node ID；数据权限不新增读取、写入、过滤、租户/组织边界或插件数据访问路径；开发工具跨平台无影响；DI 来源不新增运行期依赖、不新增构造参数、不创建独立服务图，cachecoord 和插件 runtime 均复用启动期共享`cluster.Service`，静态拓扑仅保留早期构造与测试占位语义；E2E 质量审查未触发，原因是本次为后端内部接口治理和包图收敛，无用户可观察页面、端到端工作流或 E2E 资产变化。
- FB-35 验证结果：静态检索确认`cachecoord`本地`topology`接口和`runtimeCacheCoordTopology`无剩余定义；静态检索确认`internal/service/cluster`无`internal/service/config`导入；`go test ./internal/service/cluster ./internal/service/config ./internal/service/cachecoord -count=1`通过；`go test ./internal/service/plugin/internal/runtime -run 'TestNewRuntimeWiresRequiredDependencies|TestNextBackgroundReconcileDecisionUsesSharedRevision|TestNotifyReconcilerChangedUsesReconcilerScope' -count=1`通过；`go test ./internal/cmd -count=1`通过；`go test ./internal/service/... -run '^$' -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check -- apps/lina-core/internal/service/cluster apps/lina-core/internal/service/config/config_cluster.go apps/lina-core/internal/service/cachecoord apps/lina-core/internal/service/plugin/internal/runtime/runtime_reconciler_revision.go openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`通过。
- FB-36 影响分析：`jobmgmt/internal/scheduler.New`返回公开`Scheduler`契约；`capabilityowner.NewRuntimeStateStore`返回公开`RuntimeStateStore`类型；`hostconfig.NewSysConfigCapabilityAdapter`返回已有公开`hostconfigcap.SysConfigService`契约。实现结构体仍保持私有，未新增构造参数、运行期依赖、数据库查询、缓存后端、插件 host service 协议、HTTP API 或用户可见行为。`i18n`无运行时用户可见文案、API 文档源文本、插件清单或语言包资源影响；缓存一致性不改变 runtime state、sys_config 缓存修订号或调度器状态权威来源；数据权限不新增读取、写入、过滤、租户/组织边界或插件数据访问路径；开发工具跨平台无影响；DI 来源检查：不新增运行期依赖、不创建独立服务图，仅修正跨包初始化返回类型的公开契约。
- FB-36 验证结果：静态扫描确认`apps/lina-core/internal/service`生产 Go 源码中`New*`返回未导出本包类型的问题已清除；`go test ./internal/service/jobmgmt/internal/scheduler ./internal/service/jobmgmt -count=1`通过；`go test ./internal/service/plugin/internal/capabilityowner ./internal/service/plugin/internal/hostconfig ./internal/service/plugin/internal/capabilityhost -count=1`通过；`go test ./internal/service/plugin/internal/wasm ./internal/service/plugin -count=1`通过；`go test ./internal/cmd -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check -- apps/lina-core/internal/service/jobmgmt/internal/scheduler/scheduler.go apps/lina-core/internal/service/plugin/internal/capabilityowner/capabilityowner_runtime_state.go apps/lina-core/internal/service/plugin/internal/hostconfig/hostconfig.go openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`通过。
- FB-37 影响分析：`pkg/dialect/internal/sqlscan`、`pkg/dialect/internal/postgres`、`pkg/i18nresource`、`pkg/plugin/capability/internal/capabilityregistry`、`pkg/plugin/pluginbridge/internal/hostservice`和`pkg/plugin/pluginbridge/internal/artifact/internal/wasmsection`中仅包内消费的 helper、SQLSTATE 诊断常量、capability 状态原因常量、host service 内部描述符别名和 WASM section 常量改为私有命名；保留跨包包装仍需要的`postgres.IsRetryableWriteConflict`和`postgres.IsUniqueConstraintViolation`，保留`SplitStatements`、`Manager`、`ProviderFactory`、`EnablementReader`、`ProviderEnvFactory`、`HostServiceSpec`和公开 host service 协议目录等稳定契约。未新增构造参数、运行期依赖、数据库查询、HTTP API、插件公开协议或用户可见行为。`i18n`无运行时用户可见文案、API 文档源文本、插件清单或语言包资源影响；缓存一致性不新增缓存、失效路径、共享修订号或跨实例同步机制；数据权限不新增读取、写入、过滤、租户/组织边界或插件数据访问路径；开发工具跨平台无影响；DI 来源检查：不新增运行期依赖、不创建独立服务图；E2E 质量审查未触发，原因是本次为后端内部导出面治理，无前端页面、端到端工作流或 E2E 资产变化。`apps/lina-core/pkg/plugin`中英文 README 静态检索确认未引用本次私有化标识，无需更新文档镜像。
- FB-37 验证结果：旧导出标识静态检索确认`SplitTopLevelComma`、`FindMatchingParen`、`ConsumeQuotedEscape`、`ConsumeSQLString`、`IsSQLWhitespace`、`IsKeywordAt`、`KeywordLengthAt`、`LineNumberForStatement`、`ErrorUniqueViolation`、`ErrorSerializationFailure`、`ErrorDeadlockDetected`、`ErrorLockNotAvailable`、`ErrorCheckViolation`、`ErrorForeignKeyViolation`、`ErrorNotNullViolation`、`IsRetryableSQLState`、`IsConstraintViolation`、`IsConstraintSQLState`、`ConfigNodeFromLink`、`QuoteIdentifier`、`SanitizeKeyPart`、`ReasonNoProvider`、`ReasonPluginDisabled`、`ReasonConflict`、`ReasonProviderError`、`HostServiceDescriptors`、`HostServiceMethodDescriptors`、`HostServiceResourceKind`、`HostServiceResourceNone`、`CustomSectionID`、`HeaderLength`、`Magic`和`Version1`无剩余 Go 引用；`SQLState`仅保留为 PostgreSQL driver 约定方法，不再存在`func SQLState`或`SQLState(err)` helper 调用；`go test ./pkg/dialect/... -count=1`、`go test ./pkg/i18nresource -count=1`、`go test ./pkg/plugin/capability/internal/capabilityregistry -count=1`、`go test ./pkg/plugin/pluginbridge/internal/hostservice ./pkg/plugin/pluginbridge/internal/artifact/... -count=1`、`go test ./pkg/plugin/pluginbridge/... -count=1`和`go test ./pkg/... -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check`通过。
- FB-38 影响分析：`config`测试专用 override setter 改为包内私有，并将跨包测试改为通过测试私有配置包装器或`plugin/internal/testutil`注入动态存储、autoEnable和forceUninstall场景；`apidoc`、`plugin/internal/openapi`、`plugin/internal/upgrade`、`plugin/internal/catalog`和`plugin/internal/runtime`中仅包内或测试使用的 helper、常量和薄转发改为私有或迁移到测试工具；根`plugin`包删除无生产调用的`GetMetadata`和`ScanRegisteredSourceManifests`facade。保留 runtime `Service`中的 artifact 解析/校验方法、`config`跨组件保护配置契约、`catalog.CloneManifest`和`openapi.BuildRoutePublicPath`等仍被跨包生产或稳定测试装配使用的契约。`i18n`影响：不修改运行时用户可见文案、API 文档源文本、插件清单、语言包资源、翻译键或回退语义。缓存一致性影响：不新增缓存、失效路径、共享修订号或跨实例同步机制；测试配置包装器只替代测试覆盖入口，生产配置缓存和 runtime bundle 失效策略不变。数据权限影响：不新增读取、写入、过滤、租户/组织边界或插件数据访问路径。开发工具跨平台影响：不修改脚本、Makefile、CI 或生成工具。DI 来源检查：不新增运行期依赖、不新增构造参数、不创建独立服务图；生产装配保持原有共享`config.Service`、`catalog.Service`、runtime service 和插件服务图，测试装配仅使用测试私有 wrapper。E2E 质量审查未触发，原因是本次为后端内部导出面治理，无前端页面、端到端工作流或 E2E 资产变化。
- FB-38 验证结果：旧导出标识静态检索确认`SetPluginDynamicStoragePathOverride`、`SetPluginAutoEnableOverride`、`SetPluginAutoEnableEntriesOverride`、`SetPluginAllowForceUninstallOverride`、`BuildRouteOperationKeyFrom*`、`GetMetadata =`、`ScanRegisteredSourceManifests`、`DistributedLock*`、`CanExecute`、`BuildSQLSummary`、`BuildHostServicesDiff`、`BuildRiskHints`、`BuildRouteOpenAPIOperation`、`NormalizeDynamicRoutePath`、`DynamicKindWasm`、`IsMissingArtifactError`、`BuildArtifactFileName`、`BuildArtifactRelativePath`、`CloneManifests`、`PublicFrontendSettingSpecs`、`ValidatePublicFrontendSettingValue`、`RuntimeParamSpecs`、`LookupRuntimeParamSpec`、`IsManagedRuntimeParamKey`、`ValidateRuntimeParamValue`和`RuntimeParamSnapshotSyncInterval`在`apps/lina-core`Go 源码中无剩余引用；`go test ./internal/service/apidoc ./internal/service/config ./internal/service/i18n ./internal/service/plugin ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/frontend ./internal/service/plugin/internal/openapi ./internal/service/plugin/internal/testutil ./internal/service/plugin/internal/upgrade -count=1`通过；`go test ./internal/service/plugin/internal/runtime -run 'TestScanPluginManifestsDiscoversRuntimePluginFromStorage|TestValidateRuntimeArtifact|TestValidateRuntimeArtifactTreatsOmittedPublicAssetIndexAsDefault|TestListRuntimeStatesProjectsMissingRuntimeArtifactWithoutMutatingRegistry|TestDispatchDynamicRouteBlocksPendingUpgrade|TestDispatchDynamicRouteBlocksFailedActiveRelease|TestNewRuntimeWiresRequiredDependencies|TestNextBackgroundReconcileDecisionUsesSharedRevision|TestNotifyReconcilerChangedUsesReconcilerScope' -count=1`通过；`go test ./internal/cmd -count=1`通过；`go test ./internal/service/... -run '^$' -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check -- apps/lina-core/internal/cmd/cmd_test.go apps/lina-core/internal/service/apidoc apps/lina-core/internal/service/config apps/lina-core/internal/service/i18n/i18n_plugin_dynamic_test.go apps/lina-core/internal/service/plugin openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`通过。`go test ./internal/service/plugin/internal/runtime -count=1`完整包测试仍被当前工作区既有`pkg/plugin/pluginbridge/recordstore` WASM 构建字段不一致阻断，失败点为`recordstore_exec_wasip1.go`中`Tx.invoker`字段缺失，非 FB-38 引入。
- FB-39 影响分析：`userCapabilityAdapter`字段和构造函数从`tenantcap.FilterService`收敛为已有`tenantcap.Service`，插件宿主装配直接传入启动期共享`tenantSvc`；适配器内部仍通过`tenantcap.Service.Filter()`取得同一个过滤上下文并调用`tenantspi.ApplyPluginTableFilter(...)`，用户批量读取、解析和列表查询的租户过滤语义不变。`i18n`影响：不修改运行时用户可见文案、API 文档源文本、插件清单、语言包资源、翻译键或回退语义。缓存一致性影响：不新增缓存、派生状态、失效路径、共享修订号或跨实例同步机制。数据权限影响：不新增数据读取、写入、过滤、租户/组织边界或可见性入口；既有用户 capability 查询仍在数据库查询阶段注入租户过滤和数据权限过滤。开发工具跨平台影响：不修改脚本、Makefile、CI 或生成工具。DI 来源检查：不新增运行期依赖、不创建独立服务图，`userCapabilityAdapter`复用`capabilityhost.New`启动期已经接收的同一个`tenantcap.Service`实例。E2E 质量审查未触发，原因是本次为后端内部依赖类型治理，无前端页面、端到端工作流或用户可观察行为变化。
- FB-39 验证结果：`go test ./internal/service/user/capabilityadapter ./internal/service/plugin/internal/capabilityhost -count=1`通过；`go test ./internal/service/plugin ./internal/cmd -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；静态检索确认`userCapabilityAdapter`不再保存`tenantcap.FilterService`字段，也不再直接把`a.tenantFilter`传给`tenantspi.ApplyPluginTableFilter(...)`；`git diff --check -- apps/lina-core/internal/service/user/capabilityadapter/user_capability.go apps/lina-core/internal/service/plugin/internal/capabilityhost/capabilityhost.go openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`通过。
- FB-40 影响分析：`user.serviceImpl`将`orgcap.Service`和单独`orgspi.ScopeService`字段收敛为启动期共享的`orgspi.Service`，用户列表部门过滤继续通过`orgspi.Service.Scope()`取得同一组织数据范围 seam，用户组织分配读写继续通过`orgspi.Service.Assignment()`执行。`i18n`影响：不修改运行时用户可见文案、API 文档源文本、插件清单、语言包资源、翻译键或回退语义。缓存一致性影响：不新增缓存、失效路径、共享修订号或跨实例同步机制。数据权限影响：不改变用户列表、部门筛选、租户过滤、角色数据权限或组织数据范围过滤语义，查询阶段仍注入原有数据库过滤。开发工具跨平台影响：不修改脚本、Makefile、CI 或生成工具。DI 来源检查：不新增运行期依赖、不创建独立服务图，`httpstartup`继续创建同一个`orgspi.New(...)`实例并传入`role`、`datascope`、`auth`和`user`服务，测试 legacy fake 通过本地 adapter 实现`orgspi.Service`。E2E 质量审查未触发，原因是本次为后端内部依赖类型治理，无前端页面、端到端工作流或用户可观察行为变化。
- FB-40 验证结果：`go test ./internal/service/user -count=1`通过；`go test ./internal/cmd/internal/httpstartup -count=1`通过；`go test ./internal/cmd -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；静态检索确认`user.serviceImpl`不再保存`orgScope`字段，`user.New`不再接收单独`orgCapSvc.Scope()`参数；`git diff --check -- apps/lina-core/internal/service/user/user.go apps/lina-core/internal/service/user/user_impl.go apps/lina-core/internal/service/user/user_test_dependencies_test.go apps/lina-core/internal/cmd/internal/httpstartup/httpstartup_runtime.go apps/lina-core/internal/cmd/internal/httpstartup/httpstartup_routes_test.go openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`通过。
- FB-41 影响分析：`linapro-content-notice.serviceImpl`字段和构造函数从租户过滤子契约收敛为已有`tenantcap.Service`，源码插件路由装配直接传入启动期共享的`services.Tenant()`，服务内部通过`tenantcap.Service.Filter()`取得同一个租户过滤上下文并调用`tenantspi.ApplyPluginTableFilter(...)`。`i18n`影响：不修改运行时用户可见文案、API 文档源文本、插件清单、语言包资源、翻译键或回退语义。缓存一致性影响：不新增缓存、派生状态、失效路径、共享修订号或跨实例同步机制。数据权限影响：不新增数据读取、写入、过滤、租户/组织边界或可见性入口；公告列表、详情、更新和删除仍在数据库查询阶段注入原租户过滤。开发工具跨平台影响：不修改脚本、Makefile、CI 或生成工具。DI 来源检查：不新增运行期依赖、不创建独立服务图，源码插件复用 registrar 暴露的同一个`tenantcap.Service`实例；测试替身仅补齐`tenantcap.Service`方法集以保留原租户过滤上下文。E2E 质量审查未触发，原因是本次为后端内部依赖类型治理，无前端页面、端到端工作流或用户可观察行为变化。
- FB-41 验证结果：`GOWORK=off go test ./backend/internal/service/notice -count=1`通过；`GOWORK=off go test ./backend -count=1`通过；静态检索确认`linapro-content-notice`后端不再保存`tenantcap.FilterService`字段或直接使用`s.tenantFilter`字段；`openspec validate move-narrow-interfaces-to-consumers --strict`通过。
- FB-42 至 FB-46 影响分析：`linapro-ops-demo-guard`删除`Translator`和`EnablementReader`并直接复用`i18ncap.Service`和`plugincap.StateService`；`linapro-demo-dynamic`删除 guest runtime、network、plugin config 和 record store 本地窄接口并直接复用`pluginbridge.RuntimeHostService`、`pluginbridge.NetworkHostService`、`plugincap.ConfigService`和`*recordstore.DB`；`linapro-monitor-loginlog`清理任务删除`loginLogRetentionCleaner`并直接复用`loginlog.Service`，同时把旧的`pluginhost.Services`类型名修正为当前`capability.Services`；`linapro-ai-core`清理任务删除`invocationRetentionCleaner`并直接复用`ai.Service`；`linapro-tenant-core`默认租户解析器删除`userTenantLister`并直接复用`membership.Service`。`i18n`影响：仅调整依赖类型，不修改运行时用户可见文案、API 文档源文本、插件清单、语言包资源、翻译键或回退语义。缓存一致性影响：不新增缓存、派生状态、失效路径、共享修订号或跨实例同步机制；AI tier cache、demo guard authoritative state、动态插件 runtime state 和 loginlog 共享服务实例语义保持不变。数据权限影响：不新增数据读取、写入、过滤、租户/组织边界或可见性入口；tenant resolver、loginlog 查询和 dynamic record store 调用语义保持不变。开发工具跨平台影响：不修改脚本、Makefile、CI 或生成工具。DI 来源检查：不新增运行期依赖、不创建独立服务图，所有消费者复用启动期或 guest SDK 已有目标组件契约；测试替身通过嵌入目标`Service`保持只覆盖实际调用方法。E2E 质量审查未触发，原因是本次为后端内部依赖类型治理，无前端页面、端到端工作流或用户可观察行为变化。
- FB-42 至 FB-46 验证结果：静态检索确认`EnablementReader`、`Translator`、`runtimeHostService`、`networkHostService`、`pluginConfigService`、`recordStoreService`、`loginLogRetentionCleaner`、`invocationRetentionCleaner`和`userTenantLister`无剩余 Go 引用；静态检索确认`pluginhost.Services`无剩余 Go 引用；使用临时 Go workspace 包含`apps/lina-core`和五个受影响插件后运行`go test ./apps/lina-plugins/linapro-ops-demo-guard/backend/internal/service/middleware ./apps/lina-plugins/linapro-ops-demo-guard/backend ./apps/lina-plugins/linapro-demo-dynamic/backend/internal/service/dynamic ./apps/lina-plugins/linapro-demo-dynamic/backend ./apps/lina-plugins/linapro-monitor-loginlog/backend ./apps/lina-plugins/linapro-monitor-loginlog/backend/internal/service/loginlog ./apps/lina-plugins/linapro-ai-core/backend ./apps/lina-plugins/linapro-ai-core/backend/internal/service/ai ./apps/lina-plugins/linapro-tenant-core/backend/internal/service/resolver ./apps/lina-plugins/linapro-tenant-core/backend -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check`通过。
- FB-47 影响分析：`linapro-ops-demo-guard`中间件字段和构造函数从`plugincap.StateService`提升为`plugincap.Service`并在启用判断处通过`State()`读取同一插件状态子能力；`linapro-monitor-server`配置加载和清理任务从`plugincap.ConfigService`提升为`plugincap.Service`并在读取配置时通过`Config()`取得同一配置子能力；`linapro-demo-dynamic`guest 服务字段和工厂从`plugincap.ConfigService`提升为`plugincap.Service`并在 host-call 配置示例中通过`Config()`读取插件配置；`linapro-tenant-core`provider、tenant plugin governance 和 tenant CRUD 服务从`plugincap.LifecycleService`提升为`plugincap.Service`并在生命周期前置检查与通知处通过`Lifecycle()`读取同一生命周期子能力。`i18n`影响：不修改运行时用户可见文案、API 文档源文本、插件清单、语言包资源、翻译键或回退语义。缓存一致性影响：不新增缓存、派生状态、失效路径、共享修订号或跨实例同步机制，demo guard authoritative state、monitor 配置读取与 tenant lifecycle hook 仍复用原插件域后端。数据权限影响：不新增数据读取、写入、过滤、租户/组织边界或可见性入口。开发工具跨平台影响：不修改脚本、Makefile、CI 或生成工具。DI 来源检查：不新增运行期依赖、不创建独立服务图，源码插件复用 registrar 暴露的同一个`plugincap.Service`，动态插件复用 guest SDK 暴露的同一个 plugin-domain client；测试替身只通过`plugincap.Service`契约方法返回必要子能力。E2E 质量审查未触发，原因是本次为后端内部依赖类型治理，无前端页面、端到端工作流或用户可观察行为变化。
- FB-47 验证结果：静态检索确认`apps/lina-plugins`生产 Go 源码中无`plugincap.StateService`、`plugincap.ConfigService`或`plugincap.LifecycleService`直接消费；静态检索确认`pluginConfigSvc`、`enablementReader`、`newPluginConfigService`、`startDemoControlTestServerWithReader`和`pluginLifecycleSvc`无剩余引用；使用临时 Go workspace 包含`apps/lina-core`和四个受影响插件后运行`go test ./apps/lina-plugins/linapro-ops-demo-guard/backend/internal/service/middleware ./apps/lina-plugins/linapro-ops-demo-guard/backend ./apps/lina-plugins/linapro-monitor-server/backend/internal/service/config ./apps/lina-plugins/linapro-monitor-server/backend ./apps/lina-plugins/linapro-demo-dynamic/backend/internal/service/dynamic ./apps/lina-plugins/linapro-demo-dynamic/backend ./apps/lina-plugins/linapro-tenant-core/backend/internal/provider/tenantadapter ./apps/lina-plugins/linapro-tenant-core/backend/internal/service/tenantplugin ./apps/lina-plugins/linapro-tenant-core/backend/internal/service/tenant ./apps/lina-plugins/linapro-tenant-core/backend -count=1`通过。
- FB-48 影响分析：`linapro-ai-core`删除仅用于同包`Service`自组合的导出子接口，并将同包能力、协议、层级、调用状态和缓存失效 helper 收敛为私有命名；`linapro-content-notice`、`linapro-monitor-loginlog`、`linapro-monitor-operlog`、`linapro-demo-dynamic`和`linapro-tenant-core`中仅包内使用的字典、状态、默认值、生命周期超时演示常量、动态 demo record 内部错误 helper、生命周期计数依赖接口和测试专用 membership disabled 状态收敛为私有命名。保留错误码公共常量、`ModelSourceManual`等跨包创建输入常量、`RegisterPlugin`等构建器/运行时按符号名消费入口、provider SPI 实现方法、controller 路由方法以及跨组件`New`返回类型。`i18n`影响：不修改运行时用户可见文案、API 文档源文本、插件清单、语言包资源、翻译键或回退语义；仅调整 Go 标识符可见性和注释。缓存一致性影响：不新增缓存、派生状态、失效路径、共享修订号或跨实例同步机制；AI tier cache 仍复用原`cachecap.Service`和本地缓存清理路径。数据权限影响：不新增数据读取、写入、过滤、租户/组织边界或可见性入口；公告、登录日志、操作日志、tenant membership 和 AI 查询路径语义保持不变。开发工具跨平台影响：不修改脚本、Makefile、CI、构建器或代码生成配置；`linapro-demo-dynamic.RegisterPlugin`公开入口保留。DI 来源检查：不新增运行期依赖、不新增构造函数参数、不创建独立服务图；`lifecycleprecondition.New`只将参数接口改为包内私有契约，返回的`Checker`公开类型保持不变。E2E 质量审查未触发，原因是本次为后端内部导出面治理，无前端页面、端到端工作流或用户可观察行为变化。
- FB-48 验证结果：静态检索确认`ProviderService`、`ProviderEndpointService`、`ModelService`、`ModelCapabilityService`、`TierService`、`InvocationService`、`TextGenerationService`、`TierCacheService`、`InvalidateTierCache`、`ModelSourceAPI`、`TierCodeBasic`、`TierCodeStandard`、`TierCodeAdvanced`、`InvocationStatusSuccess`、`InvocationStatusFailed`、`InvocationPurposeTierTest`、`DictTypeNoticeType`、`DictTypeNoticeStatus`、`NoticeTypeNotice`、`NoticeTypeAnnouncement`、`NoticeStatusDraft`、`NoticeStatusPublished`、`DictTypeLoginStatus`、`LoginStatusSuccess`、`LoginStatusFail`、`DictTypeOperType`、`DictTypeOperStatus`、`MaxExportRows`、`BeforeInstallTimeoutMs`、`NewDemoRecordInvalidInputError`、`TenantCounter`、`MembershipStatusDisabled`、`DefaultIsolationMode`和`OnAmbiguousFirstOwned`无剩余 Go 标识符引用；仅同包使用的非错误码公开常量扫描无结果。`GOWORK=off go test ./backend/internal/service/ai ./backend -count=1`、`GOWORK=off go test ./backend/internal/service/notice ./backend -count=1`、`GOWORK=off go test ./backend/internal/service/loginlog ./backend -count=1`、`GOWORK=off go test ./backend/internal/service/operlog ./backend/internal/service/middleware ./backend -count=1`、`GOWORK=off go test ./backend/internal/controller/dynamic ./backend/internal/service/dynamic ./backend -count=1`和`GOWORK=off go test ./backend/internal/service/lifecycleprecondition ./backend/internal/service/membership ./backend -count=1`均通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check -- apps/lina-plugins openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`通过。
- FB-49 影响分析：`linapro-demo-source`、`linapro-monitor-loginlog`、`linapro-monitor-operlog`、`linapro-org-core`普通 service 字段和构造函数从直接保存或接收`tenantcap.FilterService`收敛为已有`tenantcap.Service`，在需要 GoFrame 表过滤时通过`tenantcap.Service.Filter()`派生并继续调用`tenantspi.ApplyPluginTableFilter(...)`；`linapro-monitor-online`原租户过滤依赖未被使用，直接删除而不是提升为宽接口；`orgspi.ProviderEnv`同步从`TenantFilter tenantcap.FilterService`收敛为`Tenant tenantcap.Service`，使组织 provider adapter 与普通 service 使用同一租户能力入口。插件公开 README 已同步调整，不再建议普通源码插件直接注入`tenantcap.FilterService`。`i18n`影响：不修改运行时用户可见文案、API 文档源文本、插件清单、语言包资源、翻译键或回退语义，README 属技术文档同步。缓存一致性影响：不新增缓存、派生状态、失效路径、共享修订号或跨实例同步机制。数据权限影响：不新增数据读取、写入、过滤、租户/组织边界或可见性入口，数据库查询阶段仍使用同一租户过滤上下文注入插件表过滤。开发工具跨平台影响：不修改脚本、Makefile、CI、生成工具或跨平台执行入口。DI 来源检查：不新增运行期依赖、不创建独立服务图，源码插件复用 registrar 暴露的同一个`tenantcap.Service`实例，组织 provider 复用宿主 plugin integration 传入的同一 tenant capability；`linapro-monitor-online`删除未使用依赖后依赖图更小。E2E 质量审查未触发，原因是本次为后端内部依赖类型治理和技术文档同步，无前端页面、端到端工作流、用户可观察行为或 E2E 资产变化。
- FB-49 验证结果：静态检索确认目标源码插件普通 service/provider 中不存在继续保存或构造注入的`s.tenantFilter`、`p.tenantFilter`或`tenantFilter:`字段初始化，剩余`tenantcap.FilterService`仅用于`pluginTableFilter()`派生 helper、纯函数参数或测试替身；`go test ./pkg/plugin/capability/orgcap/orgspi ./internal/service/plugin -count=1`、`go test ./internal/cmd -count=1`、`GOWORK=off go test ./backend ./backend/internal/service/demo -count=1`、`GOWORK=off go test ./backend ./backend/internal/service/monitor -count=1`、`GOWORK=off go test ./backend ./backend/internal/service/loginlog -count=1`、`GOWORK=off go test ./backend ./backend/internal/service/operlog -count=1`和`GOWORK=off go test ./backend ./backend/internal/provider/orgcapadapter ./backend/internal/service/dept ./backend/internal/service/post -count=1`均通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check -- apps/lina-core/internal/service/plugin/plugin_integration.go apps/lina-core/pkg/plugin/capability/orgcap/orgspi/orgspi.go apps/lina-core/pkg/plugin/README.md apps/lina-core/pkg/plugin/README.zh-CN.md openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`和`git -C apps/lina-plugins diff --check -- linapro-demo-source/backend/plugin.go linapro-demo-source/backend/internal/service/demo/demo.go linapro-demo-source/backend/internal/service/demo/demo_record.go linapro-monitor-online/backend/plugin.go linapro-monitor-online/backend/internal/service/monitor/monitor.go linapro-monitor-online/backend/internal/service/monitor/monitor_session_test.go linapro-monitor-loginlog/backend/plugin.go linapro-monitor-loginlog/backend/internal/service/loginlog/loginlog.go linapro-monitor-loginlog/backend/internal/service/loginlog/loginlog_impl.go linapro-monitor-operlog/backend/plugin.go linapro-monitor-operlog/backend/internal/service/operlog/operlog.go linapro-monitor-operlog/backend/internal/service/operlog/operlog_impl.go linapro-org-core/backend/plugin.go linapro-org-core/backend/plugin_provider_test.go linapro-org-core/backend/internal/provider/orgcapadapter/orgcapadapter.go linapro-org-core/backend/internal/service/dept/dept.go linapro-org-core/backend/internal/service/dept/dept_impl.go linapro-org-core/backend/internal/service/post/post.go linapro-org-core/backend/internal/service/post/post_impl.go`通过。
- FB-50 影响分析：`tenantspi.ProviderEnv`从直接发布`PluginGovernance tenantcap.PluginService`收敛为发布`Tenant tenantcap.Service`，`plugin.TenantProviderEnv`传入同一个 plugin-scoped `services.Tenant()`；`linapro-tenant-core`的`tenantadapter.New`、`tenantplugin.New`和路由装配均接收`tenantcap.Service`，写操作仅在调用点通过`Tenant.Plugins()`取得治理子能力。`i18n`影响：不修改运行时用户可见文案、API 文档源文本、插件清单、语言包资源、翻译键或回退语义。缓存一致性影响：不新增缓存、失效路径、共享修订号或跨实例同步机制，租户插件启停仍复用原宿主插件运行时缓存刷新路径。数据权限影响：不新增数据读取、写入、过滤、租户/组织边界或可见性入口，租户插件列表、启停和默认供给语义不变。开发工具跨平台影响：不修改脚本、Makefile、CI、生成工具或跨平台执行入口。DI 来源检查：不新增运行期依赖、不创建独立服务图，provider env、tenantadapter 和 tenantplugin 均复用启动期 capability host directory 暴露的同一个`tenantcap.Service`实例。E2E 质量审查未触发，原因是本次为后端内部 Go 契约治理，无前端页面、端到端工作流或用户可观察行为变化。`apps/lina-core/pkg/plugin` README 已检查，现有文档已经说明通过`Services.Tenant().Plugins()`访问租户插件治理，无需同步。
- FB-50 验证结果：精确静态检索`PluginGovernance\s+tenantcap\.PluginService|env\.PluginGovernance|pluginGovernanceSvc\s+tenantcap\.PluginService|tenantplugin\.New\([^\n]*tenantDomain\.Plugins\(`无结果；`cd apps/lina-core && go test ./pkg/plugin/capability/tenantcap/tenantspi ./internal/service/plugin -count=1`通过；`cd apps/lina-core && go test ./internal/cmd -count=1`通过；`cd apps/lina-plugins/linapro-tenant-core && GOWORK=off go test ./backend/internal/provider/tenantadapter ./backend/internal/service/tenantplugin ./backend -count=1`通过；`openspec validate move-narrow-interfaces-to-consumers --strict`通过；`git diff --check -- apps/lina-core/pkg/plugin/capability/tenantcap/tenantspi/tenantspi.go apps/lina-core/internal/service/plugin/plugin_integration.go openspec/changes/move-narrow-interfaces-to-consumers/tasks.md`和`git -C apps/lina-plugins diff --check -- linapro-tenant-core/backend/plugin.go linapro-tenant-core/backend/plugin_provider_test.go linapro-tenant-core/backend/internal/provider/tenantadapter/tenantadapter.go linapro-tenant-core/backend/internal/service/tenantplugin/tenantplugin.go linapro-tenant-core/backend/internal/service/tenantplugin/tenantplugin_impl.go linapro-tenant-core/backend/internal/service/tenantplugin/tenantplugin_test.go`通过。
