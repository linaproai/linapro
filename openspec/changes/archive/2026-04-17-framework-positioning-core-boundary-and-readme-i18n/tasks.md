## 1. 规范基线与词汇表收敛

- [x] 1.1 更新 `CLAUDE.md`、仓库入口文档与活跃规范，将项目定位统一为“`AI驱动的全栈开发框架`”并新增 `lina-core` 核心边界顶级要求
- [x] 1.2 在不修改用户指定保留文件的前提下，将 `README.md` / `README.zh_CN.md` 的英文主文档 + 中文镜像规则写入项目规范、OpenSpec 上下文与仓库入口文档
- [x] 1.3 更新 `openspec/config.yaml` 与本次涉及的活跃主规范，使后续 OpenSpec 产物继续沿用新的项目定位与 README 国际化约束
- [x] 1.4 形成并落地统一术语：区分“核心宿主服务”“默认管理工作台”“工作台适配接口”，清理项目级规范中的旧表述

## 2. 项目级元数据与工作台边界文案调整

- [x] 2.1 更新后端 `OpenAPI` 标题、说明、组件描述和相关配置模板，移除“`LinaPro Admin API`”“后台管理系统接口文档”等旧表述
- [x] 2.2 按现有打包流程同步校验或刷新 `apps/lina-core/internal/packed/manifest/` 中受影响的嵌入配置副本
- [x] 2.3 更新前端登录页、系统信息页、入口 HTML meta、插件 Slot 描述、脚本 banner 等高曝光入口文案，使其统一反映新的项目定位
- [x] 2.4 审核并调整关键工作台导向接口、DTO 注释和服务注释的表述，将菜单路由投影、当前用户工作台启动数据、树选择器和下拉选项输出明确归类为工作台适配语义

## 3. 仓库根入口 README 建设

- [x] 3.1 创建仓库根英文主文档 `README.md`，完整介绍项目定位、目录结构、开发流程、插件机制、关键命令和规范入口
- [x] 3.2 创建仓库根中文镜像文档 `README.zh_CN.md`，与 `README.md` 保持一致的结构和技术信息
- [x] 3.3 校验根 README 中链接到的规范、OpenSpec 流程和子目录入口均可正常使用，并确保中英文内容无漂移

## 4. 子目录 README 双语镜像渐进治理

- [x] 4.1 盘点现有 24 个子目录 `README.md`，形成治理清单并确认每个目录对应的 `README.zh_CN.md` 目标路径
- [x] 4.2 完成 `apps/lina-plugins/` 及其示例插件目录下 README 的英文主文档改写与中文镜像补齐
- [x] 4.3 完成 `apps/lina-vben/` 下 packages、scripts、app 内局部目录 README 的英文主文档改写与中文镜像补齐
- [x] 4.4 复核仓库内本次允许修改范围中所有存在 `README.md` 的目录，确认均已存在同步维护的 `README.zh_CN.md`，并记录后续新增 README 必须遵循同一规则

## 5. 验证与回归

- [x] 5.1 更新受项目文案影响的前端断言和 E2E 用例预期，至少覆盖登录页、系统信息页和系统接口页的关键文本变化
- [x] 5.2 运行本次变更相关的校验或测试，确认新的项目定位、`OpenAPI` 文案、系统信息页展示和 README 双语镜像规则没有引入回归
- [x] 5.3 进行一次人工 review，确认项目入口、活跃规范、核心边界要求与 README 体系对外传递的是同一套定位和术语

## 6. 合并自 `guard-db-init-and-mock-commands`

- [x] 6.1 更新仓库根 `Makefile` 中的 `init`、`mock` 目标，要求显式传入与命令匹配的 `confirm` 变量，并在缺失或错误时输出正确示例后失败退出
- [x] 6.2 更新 `apps/lina-core/Makefile` 中的 `init`、`mock` 目标，复用同一套 `confirm` 校验与透传逻辑，确保后端目录直接执行时也默认安全
- [x] 6.3 为 `apps/lina-core/internal/cmd/` 下的 `init`、`mock` 命令增加 `confirm` 入参和统一校验逻辑，未确认时拒绝执行任何 `SQL`
- [x] 6.4 调整公共 `SQL` 执行流程为首错即停并返回 `error`，让 `init`、`mock` 在任一 `SQL` 失败时返回失败状态
- [x] 6.5 更新命令帮助文案、日志或错误提示，明确展示正确调用方式，例如 `make init confirm=init`、`go run main.go mock --confirm=mock`
- [x] 6.6 补充或更新命令相关单元测试，覆盖未确认拒绝执行、正确确认后允许执行、错误确认值拒绝执行等场景
- [x] 6.7 补充或更新 `SQL` 执行失败路径测试，验证任一文件失败时会停止后续执行并向调用方返回失败状态
- [x] 6.8 运行本次变更相关测试与必要的命令校验，确认双层确认护栏和失败语义生效且未引入回归

## 7. 合并自 `plugin-install-authorization-dialog-optimization`

- [x] 7.1 用单一安装审查弹窗替代插件安装 `Popconfirm` 与动态插件重复授权确认，并让源码插件与动态插件都先展示插件详情再确认安装
- [x] 7.2 优化动态插件安装授权弹窗的信息结构，补齐插件详情区并按“数据服务 → 存储服务 → 网络服务 → 运行时服务”顺序展示授权项，收敛重复方法说明与提示文案
- [x] 7.3 安装阶段确认过授权的动态插件 release 在后续启用时直接复用授权快照，不再重复弹出授权窗，并更新相关 E2E 覆盖验证源码/动态插件安装与启用流程
- [x] 7.4 数据服务的数据表展示改为 `table_name (table comment)` 格式，并保持授权勾选交互与 E2E 定位稳定
- [x] 7.5 动态插件授权弹窗改为只读服务清单展示，确认时默认授权插件声明的全部服务，并去掉各服务勾选提示、末尾未勾选提示及“当前授权状态”行
- [x] 7.6 动态插件授权弹窗在只读模式下继续展示各服务资源列表，并去掉运行时服务上的“无需额外确认”等补充提示文案
- [x] 7.7 动态插件授权弹窗中的资源项改为真正的无序列表展示，避免只读资源块视觉上仍像表格或分栏列表
- [x] 7.8 收敛默认工作台首页中的模板化演示内容和失效快捷入口，改为 LinaPro 管理后台真实可达的快捷导航与运维信息
- [x] 7.9 优化默认分析页的指标分组、时间范围切换和图表标题语义，避免重复标题与模板化统计叙事
- [x] 7.10 补齐默认仪表盘页面的 E2E 覆盖，新增 `TC0076` 与 `TC0077` 并完成相关回归验证
- [x] 7.11 修复插件自有资源查询接口误绑 `plugin:query` 导致已授权业务用户访问动态插件资源返回 `403` 的权限治理缺陷，并完成相关插件回归验证
- [x] 7.12 修复 `TC0041` 在完整回归中创建通知被拒绝导致消息预览用例失败的问题，确保通知详情链路在全量 E2E 中稳定通过
- [x] 7.13 修复 `TC0062` 在完整回归中创建用户后成功提示与角色展示断言不稳定的问题，确保用户角色关联场景稳定通过
- [x] 7.14 修复 `TC0062` 删除用户时操作按钮定位脆弱导致超时的问题，确保用户角色关联清理用例稳定通过
- [x] 7.15 修复 `TC0005` 共享用户名与跨子用例状态依赖导致 worker 重启后用户 CRUD 回归不稳定的问题，确保该用例文件可独立重跑且全量回归稳定通过

## 8. 合并自 `redirect-dev-logs-to-project-temp`

- [x] 8.1 将 `make dev` 生成的前后端开发日志从 `/tmp` 调整为仓库根 `temp/`，并同步更新开发状态提示与相关运行时产物目录

## 9. 变更治理收口

- [x] 9.1 将 `guard-db-init-and-mock-commands`、`plugin-install-authorization-dialog-optimization` 与 `redirect-dev-logs-to-project-temp` 的 proposal、design、tasks 与 delta specs 并入当前迭代，仅保留 `framework-positioning-core-boundary-and-readme-i18n` 作为唯一活跃变更

## Feedback

- [x] **FB-4**: 将当前生效的项目品牌展示文案统一调整为 `LinaPro`，同步仓库说明、系统信息、登录页、OpenAPI 文案、插件说明与相关测试预期
- [x] **FB-1**: 将宿主数据库初始化 SQL 目录收敛为固定约定 `manifest/sql`，移除 `config.template.yaml` 中的 `init.sqlDir` 显式配置
- [x] **FB-2**: 修复宿主 `make init` / `make mock` 在重复执行场景下暴露的 SQL 语法与幂等性问题，确保初始化与 Mock 数据加载可稳定回归
- [x] **FB-3**: 删除宿主 `manifest/sql/009-cleanup-dict-test-data.sql` 这类历史测试污染清理脚本，并同步清理嵌入副本中的陈旧 SQL 文件
- [x] **FB-5**: 调整源码插件治理语义为默认未安装，并通过插件管理页显式执行安装、启用、卸载与对应生命周期校验
- [x] **FB-6**: 为源码插件示例补齐安装/卸载 SQL、示例 CRUD 页面与卸载时可选的数据/文件清理弹窗，并覆盖禁用保留数据、卸载清理或保留数据的生命周期回归
- [x] **FB-7**: 修复源码插件示例列表页持续滚动问题，并移除顶部提示板块，仅保留示例记录页面主体
- [x] **FB-8**: 收窄源码插件示例记录新增/编辑弹窗宽度，并拉开附件提示与上传区域的垂直间距
- [x] **FB-9**: 拉开编辑态“提交时移除当前附件”选项与上下板块的垂直间距，避免附件区域视觉过挤
- [x] **FB-10**: 为动态插件示例补齐安装/卸载 SQL、示例 CRUD 页面与卸载时可选的数据/文件清理弹窗，并覆盖禁用保留数据、卸载保留或清理数据的生命周期回归
- [x] **FB-11**: 收窄 `plugin-demo-dynamic` 后端 `dynamic` 服务接口签名，按业务场景定义专用入参，避免所有方法都直接依赖同一个桥接请求信封
- [x] **FB-12**: 继续拆分 `plugin-demo-dynamic` 控制器层的桥接请求解析 helper，使其与各路由控制文件一一对应，仅保留少量底层取值辅助函数
