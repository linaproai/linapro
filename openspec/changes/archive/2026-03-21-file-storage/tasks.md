## Feedback

- [x] **FB-1**：新增 `sys_file` 数据表及版本 SQL 文件 `v0.6.0.sql`，记录所有上传文件的元信息（文件名、原始名、大小、后缀、存储路径、上传者等）
- [x] **FB-2**：设计并实现后端文件存储抽象层（Storage 接口 + 本地存储实现），预留 OSS 扩展能力
- [x] **FB-3**：实现文件管理后端 API（`POST /file/upload` 上传、`GET /file` 列表、`GET /file/download/{id}` 下载、`DELETE /file/{ids}` 删除），生成 DAO/Controller 骨架
- [x] **FB-4**：创建前端通用文件上传 API 和 FileUpload / ImageUpload 组件，参考 ruoyi-plus-vben5 的上传组件设计
- [x] **FB-5**：新增文件管理页面（系统管理 > 文件管理），包含文件列表、搜索、上传弹窗、下载、批量删除、图片预览等功能，参考 ruoyi-plus-vben5 的 OSS 文件管理页面
- [x] **FB-6**：改造 TiptapEditor 富文本编辑器的图片上传，从 Base64 内嵌改为调用通用文件上传接口
- [x] **FB-7**：改造用户头像上传，使用通用文件上传接口替代原有独立实现（移除旧的 avatar 上传端点和静态文件服务路由）
- [x] **FB-8**：编写文件管理模块及改造功能的 E2E 测试用例
- [x] **FB-9**：下载按钮点击报错，前端 handleDownload 使用 requestClient.get 触发 JSON 响应拦截器解析二进制数据失败，应改用 requestClient.download 方法
- [x] **FB-10**：文件预览列展示的 URL 为相对路径，后端应在返回文件列表时拼接完整的 HTTP 地址
- [x] **FB-11**：上传者列展示账号名称（username）而非昵称（nickname）
- [x] **FB-12**：文件大小列和上传时间列支持排序，后端支持排序参数
- [x] **FB-13**：sys_file 表增加 hash 字段（SHA-256），上传时计算散列值，同名文件通过散列值判断是否重复，重复文件直接复用已有文件地址
- [x] **FB-14**：新增 sys_file_usage 表记录文件使用场景（用户头像、通知公告图片、附件等），与 sys_file 为一对多关系
- [x] **FB-15**：文件管理页面增加文件使用场景筛选条件，按使用场景筛选文件列表
- [x] **FB-16**：文件列表增加详情按钮，点击弹出对话框展示文件完整信息（文件名、原始文件名、上传者、上传时间、文件大小、文件路径、使用场景等）
- [x] **FB-17**：访问文件管理页面时请求 /file/usage/ 返回 301 重定向循环导致"网络异常"错误，将接口路径从 /file/usage/scenes 改为 /file/scenes 避免路径冲突
- [x] **FB-18**：文件详情弹窗宽度不足导致部分列被折叠换行，增加弹窗宽度至 900px 并设置 Descriptions 组件的最小标签宽度和内容宽度
- [x] **FB-19**：后端文件上传接口增加 scene 可选参数，上传成功后自动记录文件使用场景
- [x] **FB-20**：前端 uploadApi 增加 scene 可选参数，调用时传递给后端
- [x] **FB-21**：用户头像上传时传入 scene=avatar 参数记录使用场景
- [x] **FB-22**：通知公告图片上传时传入 scene=notice_image 参数记录使用场景
- [x] **FB-23**：文件管理列表默认开启预览模式
- [x] **FB-24**：修改后端文件上传接口，新增可选参数 `bizId`（业务关联ID，可选，默认为0），上传成功后记录到文件使用场景表
- [x] **FB-25**：修改前端文件上传 API，新增可选参数 `bizId`，调用时传递给后端
- [x] **FB-26**：修改后端获取使用场景列表接口，返回系统预定义的所有使用场景（从 SceneLabelMap 获取），而非仅返回数据库中已存在的场景
- [x] **FB-27**：头像上传时bizId硬编码为0，应传入当前用户ID作为业务关联ID
- [x] **FB-28**：详情弹窗中业务关联ID为0时被JavaScript falsy判断过滤显示为'-'，应正确展示数值0
- [x] **FB-29**：文件上传接口的BizId字段去掉v:"required"校验，0为合法值表示不关联具体业务
- [x] **FB-30**：文件管理页面使用场景筛选下拉框选项为空，根因是后端未重启导致运行旧代码返回错误格式数据
- [x] **FB-31**：头像上传时 `userStore.userInfo?.id` 取不到用户ID（后端返回字段为 `userId` 而非 `id`），导致 bizId 始终为 0
- [x] **FB-32**：使用场景筛选框无值（验证后发现已正常，不可复现）
- [x] **FB-33**：移除文件使用场景中的业务关联ID（bizId）字段，简化文件管理——包括数据库表字段、后端API/Service、前端上传参数和详情展示
- [x] **FB-34**：详情弹窗中使用场景区域的列宽与上方文件信息表格不一致，应保持 label/content 的 minWidth 统一
- [x] **FB-35**：去掉文件列表中的"文件名"列，将"文件后缀"改为"文件类型"，详情弹窗中也对应修改标签名
- [x] **FB-36**：文件类型搜索框从 Input 改为 Select 下拉选择，选项从数据库中查询已存在的文件后缀列表（实现后端 Suffixes 方法并对接前端）
- [x] **FB-37**：通知公告模块增加附件上传功能（FileUpload 组件 + notice_attachment 场景），移除 sys_file_usage 关联表，将 scene 字段直接放到 sys_file 表中简化架构
- [x] **FB-38**：文件类型筛选下拉框显示 JSON 格式值（如 `{"suffix":"docx"}`），后端 Suffixes 方法 Scan 到 []string 时整行序列化，应改用 Array() 提取单列值
- [x] **FB-39**：文件预览列中不支持预览的文件（非图片非PDF）仅展示原始URL文本，应改为展示可点击的"下载"链接
- [x] **FB-40**：文件类型筛选下拉框的选项 Label 不应包含点号（如 `.docx` 应显示为 `docx`）
- [x] **FB-41**：文件预览列中不支持预览的文件应显示 URL 地址（过长省略，鼠标悬停显示完整链接），替代当前的"下载"链接
