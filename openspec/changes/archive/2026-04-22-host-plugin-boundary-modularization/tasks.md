## 1. Menu skeleton and host directory management

- [x] 1.1 Design and confirm the new default backend first-level directory structure and stable parent `menu_key`
- [x] 1.2 Update the host menu initialization SQL and create stable directories such as `dashboard`, `iam`, `org`, `setting`, `content`, `monitor`, `scheduler`, `extension`, `developer`, etc.
- [x] 1.3 Adjust the host menu projection logic to only consume host stable directory records and support the new first-level directory structure
- [x] 1.4 Implement automatic hiding rules for empty parent directories
- [x] 1.5 Supplementary backend testing and frontend routing assembly verification after menu reconstruction

## 2. Plug-in list and mounting rule management

- [x] 2.1 Solidified official source plugin ID: `org-center`, `content-notice`, `monitor-online`, `monitor-server`, `monitor-operlog`, `monitor-loginlog`
- [x] 2.2 Add verification to the source plugin menu mounting rules, restricting them to the host stable directory or plugin internal nodes
- [x] 2.3 Remove the `plugin-` prefix examples in plugin documents and samples, and change them to domain-capability naming.
- [x] 2.4 Supplement unit testing for plugin management menu synchronization and parent mounting

## 3. Host core boundary extraction

- [x] 3.1 Inventory and mark the code boundaries between host retained capabilities and capabilities to be plugged in
- [x] 3.2 Define and implement a unified login event contract and publisher for the host
- [x] 3.3 Integrate login success, login failure, and logout success into unified login events, and remove the direct dependence of the authentication link on the specific login log implementation.
- [x] 3.4 Define and implement a unified audit event contract and publisher for the host
- [x] 3.5 Integrate write operations and queries with the `operLog` tag into unified audit events, and remove the middleware's direct dependence on the implementation of specific operation logs.
- [x] 3.6 Separate the organizational capability interface to avoid user management directly relying on departments/positions for implementation
- [x] 3.7 Split the boundary between "authentication session kernel" and "online user governance capabilities" and clarify that the host is responsible for the session truth source, active time maintenance, timeout determination and cleanup

## 4. Monitoring capability source plugin

- [x] 4.1 Create source plugin `monitor-operlog`
- [x] 4.2 Migrate operation log query, details, export, cleaning and pages to `monitor-operlog`
- [x] 4.3 Create source plugin `monitor-loginlog`
- [x] 4.4 Migrate login log query, details, export, cleaning and pages to `monitor-loginlog`
- [x] 4.5 Create source plugin `monitor-server`
- [x] 4.6 Migrate service monitoring collection, cleaning, storage, query and page to `monitor-server`
- [x] 4.7 Create source plugin `monitor-online`
- [x] 4.8 On the premise of retaining the host session kernel, migrate online user query and forced offline management to `monitor-online`
- [x] 4.9 adds installation, activation, deactivation, uninstallation and menu mounting verification for the 4 monitoring plugins.

## 5. Organization and content capability source plugin

- [x] 5.1 Create source plugin `org-center`
- [x] 5.2 Migrate department management to `org-center`
- [x] 5.3 Migrate position management to `org-center`
- [x] 5.4 Implement user management UI and interface degradation when the organization plugin is missing
- [x] 5.5 Create source plugin `content-notice`
- [x] 5.6 Migrate notification announcement capabilities to `content-notice`

## 6. Plug-in explicit wiring and delivery documentation

- [x] 6.1 updated `apps/lina-plugins/lina-plugins.go` to provide explicit wiring entry for official source plugins
- [x] 6.2 Supplement `README.md` and `README.zh_CN.md` for each official source plugin
- [x] 6.3 Updated `apps/lina-plugins/README.md` and `README.zh_CN.md` to explain the host/plugin boundary and official plugin list
- [x] 6.4 Supplementary operation and maintenance instructions for plugin installation, start and stop, menu mounting and uninstall cleaning

## 7. Front-end routing and menu visibility linkage

- [x] 7.1 Adjust frontend static routing and dynamic menu adaptation to match the new first-level directory structure
- [x] 7.2 Implement menu and dynamic routing refresh convergence logistic regression verification after plugin startup and shutdown
- [x] 7.3 Implement the hiding of fields on the user management page when the organization plugin is missing
- [x] 7.4 Implement system monitoring empty directory hiding when the monitoring plugin is missing
- [x] 7.5 Implement the ability to detect degradation in the department/position fields of user lists, details and edit drawers when the organization plugin is missing

## 8. E2E Testing

- [x] 8.1 Use `lina-e2e` to plan E2E use cases after menu reconstruction and pluginization
- [x] 8.2 Added menu skeleton and empty parent directory hiding test
- [x] 8.3 Added `monitor-operlog` life cycle and functional verification test (`TC0098a` + existing monitoring function use cases)
- [x] 8.4 Added `monitor-loginlog` life cycle and functional verification test (`TC0098b` + existing monitoring function use cases)
- [x] 8.5 adds `monitor-server` life cycle and functional verification test (`TC0098c` + existing monitoring function use cases)
- [x] 8.6 Added `monitor-online` life cycle and functional verification test (`TC0098d` + existing monitoring function use cases)
- [x] 8.7 Added `org-center` life cycle and user management downgrade tests (`TC0098e`, `TC0081`)
- [x] 8.8 added `content-notice` life cycle tests (`TC0098f`, `TC0037`)
- [x] 8.9 Added regression test that login, authentication and session timeout still work when `monitor-online` is missing or disabled (`TC0099a`)
- [x] 8.10 Added regression test that the login process and normal business requests are still normal when the log plugin is missing or disabled (`TC0099b`)
- [x] 8.11 Run the relevant E2E regression and log the results

## 9. Verification and review

- [x] 9.1 Run host and plugin related backend unit tests
- [x] 9.2 Running frontend type checking and build verification
- [x] 9.3 E2E suite related to running plugin start, stop and menu refresh
- [x] 9.4 Check whether the host still only retains core capabilities against the specifications
- [x] 9.5 Call `lina-review` for change review

## Feedback

- [x] **FB-1**: Change the collaboration between host and plugin to stabilize the capability seam, and remove scattered plugin occupancy judgment and high-coupling branches.
- [x] **FB-2**: The 6 official source plugins must be completely implemented in `apps/lina-plugins/<plugin-id>/`, and aligned with the `plugin-demo-source` directory structure and explicit wiring method
- [x] **FB-3**: Remove the `pkg` bridge module that is not used by any plugin or host code to avoid exposing invalid public interfaces
- [x] **FB-4**: Recycle host private menu mount keys and official plugin management constants from `apps/lina-core/pkg/` to `internal/` to avoid `pkg` from carrying host management rules
- [x] **FB-5**: `orgcap` capability seams are not sealed - `internal/service/orgcap/orgcap.go` directly `import deptsvc` and exposes `[]*deptsvc.TreeNode`, `controller/user/user_v1_dept_tree.go` and is therefore forced to rely on `deptsvc`; need to let `orgcap` own `DeptTreeNode` and let the host business layer completely no longer need `import deptsvc`
- [x] **FB-6**: Clean up residual artifacts - remove empty directory `apps/lina-core/pkg/officialplugin/` and commented-only SQL stubs `apps/lina-core/manifest/sql/003-oper-login-log.sql`, `004-notice-message.sql`
- [x] **FB-7**: Plug-in frontend realization - 6 plugins `frontend/pages/*.vue` are currently all thin wrappers of `import HostPage from '#/views/...'`; need to combine `apps/lina-vben/apps/web-antd/src/views/{monitor,system}/...` and `src/api/{monitor,system}/...` The corresponding code is physically migrated to each plugin `frontend/`, and the `/user/post-options` capability endpoint is added to the host (reusing the existing `/user/dept-tree`) so that `user-drawer` no longer relies on the plugin frontend API.
  - 2026-04-21: The host has been supplemented with `/user/post-options`, and `user-drawer` has been switched to depend on `#/api/system/user`; the pages, pop-up windows and client APIs of `org-center`, `content-notice`, `monitor-online`, `monitor-loginlog`, `monitor-operlog` and `monitor-server` have been physically moved to their respective locations. `frontend/pages/` no longer relies on the host view and host frontend API module through the `HostPage` thin wrapper
- [x] **FB-8**: The `api/`, `internal/controller/` and `internal/service/` of `dept/post/notice/loginlog/operlog/servermon` are still in `apps/lina-core/` and are exposed through `pkg/plugincontroller/` and `pkg/pluginservice/` bridges; refer to `apps/lina-plugins/plugin-demo-source/backend/` Import each plugin `backend/api` and `backend/internal/{controller,service}`, and delete the corresponding host `internal/` directory, bridge package and `dao/sys_{dept,post,user_dept,user_post,notice,login_log,oper_log,server_monitor}.go`
- 2026-04-21: `dept/post` of `org-center`, `notice`, `monitor-online`, `monitor-loginlog`, `monitor-operlog` and `monitor-server` of `org-center` have all been migrated into each plugin `backend/api` and `backend/internal/{controller,service}`; the host is old `api/`, `internal/controller/`, `internal/service/`, `pkg/plugincontroller/`, `pkg/pluginservice/{loginlog,operlog,servermon}` and the corresponding `dao/model` artifacts have been deleted, and the database access mode continues to converge to the `dao/do/entity` generated by the plugin local `gf gen dao` in subsequent feedback.
- [x] **FB-9**: Explicit host boundary exception for `orgcap` - the org capability seam continues to be installed by the host consumer plugin into the `sys_dept/sys_post/sys_user_dept/sys_user_post` table in the host library as a read-only/associative source of truth, but the exception must be explicitly declared in the OpenSpec documentation and code comments to avoid inconsistencies between `FB-8` goals and actual implementation semantics
- [x] **FB-10**: Stable `pkg/pluginservice/session` external contract - remove the direct alias publishing of the `internal/service/session` type, replace it with the host's own independent `Session/ListFilter/ListResult` DTO, and let `monitor-online` only rely on this stable contract
- [x] **FB-11**: Block the default fallback of the user page to the organization plugin frontend API - `user/dept-tree` and `user/post-options` are already host capability endpoints, `system/user/dept-tree.vue` needs to rely on the host user API/type by default instead of `#/api/system/dept`, to avoid reintroduction of plugin frontend coupling during subsequent reuse
- [x] **FB-12**: The system information page E2E needs to follow the backend dynamic metadata to verify the current component description to avoid continuing to assert the changed static GoFrame copy.
- [x] **FB-13**: `TC0021c` depends on the fixed `testuser`, causing the complete regression to be skipped. It needs to be changed to a self-created user in the test and cleaned up to ensure that the use case can be run independently and repeatedly.
  - 2026-04-21: `hack/tests/e2e/system/TC0021-user-dept-tree-count.ts` has been changed to create a unique username user within the test, complete department changes through the API and clean up in `finally`, removing the pre-dependency on the fixed `testuser`
- [x] **FB-14**: The backend database access of the official source plugin and `plugin-demo-source` needs to be uniformly switched to the `dao/do/entity` generated by the plugin's local `gf gen dao`, and `hack/config.yaml` must be completed for each source plugin `backend/`
  - 2026-04-21: `hack/config.yaml` has been completed for `content-notice`, `monitor-loginlog`, `monitor-online`, `monitor-operlog`, `monitor-server`, `org-center`, `plugin-demo-source`’s `backend/`; and local generation of source plugins that currently have database access `internal/dao`, `internal/model/do`, `internal/model/entity` artifacts, related services have been transformed to access the database through the plugin local `dao/do/entity`
- [x] **FB-15**: `middleware_request_body_limit` still uses naked `g.Map` when handwriting unified error response, which needs to be changed to `ghttp.DefaultHandlerResponse` or the host's own typed DTO, keeping it consistent with the unified return structure
  - 2026-04-21: `apps/lina-core/internal/service/middleware/middleware_request_body_limit.go` has been uniformly changed to output `ghttp.DefaultHandlerResponse`, keeping the over-limit upload error response consistent with the host's default JSON structure
- [x] **FB-16**: The host's default database loading boundary is still leaked to the plugin - remove the DML for the organization, announcement and other source plugin business tables in `apps/lina-core/manifest/sql/mock-data/`, and complete the plugin's own demo data loading scheme
  - 2026-04-21: The host has deleted `001-mock-depts.sql`, `002-mock-posts.sql`, `004-mock-associations.sql`, `005-mock-notices.sql`, and converged the organization/announcement demonstration data to each plugin `manifest/sql/` and loaded it with the plugin installation in the life cycle resources
- [x] **FB-17**: The organization capability still retains the storage exception of the plugin table held by the host - delete the host's `dao/do/entity` and direct table lookup logic (including `orgcap`, plugin resource range and other paths) for the organization plugin business table, and instead provide implementation by `org-center` through the stable capability provider, and the host only retains the interface, DTO and empty implementation
  - 2026-04-21: The host has deleted the `dao/do/entity` and direct lookup table implementation of `sys_dept`, `sys_post`, `sys_user_dept`, `sys_user_post`; `orgcap`, plugin resource scope and user management path have been unified and changed to obtain organizational capabilities through the `pkg/orgcap` provider. The host direct check organization table exception recorded in `FB-9` has been removed
- [x] **FB-18**: The official source plugin business table still uses the `sys_*` naming - the plugin own tables of `org-center`, `content-notice`, `monitor-loginlog`, `monitor-operlog`, and `monitor-server` will be uniformly migrated to the `plugin_<plugin_id_snake_case>_` scope prefix, and the installation/uninstallation SQL and plugin local will be updated simultaneously. `gf gen dao` configuration, backend implementation, testing and documentation
  - 2026-04-21: The business tables of `org-center`, `content-notice`, `monitor-loginlog`, `monitor-operlog`, and `monitor-server` have been uniformly migrated to the `plugin_*` prefix, and the plugin installation/uninstallation SQL, `backend/hack/config.yaml`, local `gf gen dao` artifacts, backend implementation and change documents have been updated simultaneously.
- [x] **FB-19**: The official organization source plugin naming is adjusted from `org-management` to `org-center`, and the directory name, Go module name, plugin management constants, permission keys, test and document references are simultaneously updated.
- [x] **FB-20**: The business table name of the single-table official source plugin should not repeat the resource suffix - the physical tables of `content-notice`, `monitor-loginlog`, `monitor-operlog` and `monitor-server` will be converged to `plugin_content_notice`, `plugin_monitor_loginlog`, `plugin_monitor_operlog`, `plugin_monitor_server`, and synchronously adjust `removePrefix` of `backend/hack/config.yaml` to avoid codegen leaving empty table names or unnecessary repeated suffixes
- 2026-04-21: The installation/uninstallation SQL, local database table name, plugin `backend/hack/config.yaml` and `gf gen dao` artifacts of `content-notice`, `monitor-loginlog`, `monitor-operlog`, `monitor-server` have been unified into a single table naming; `content-notice` uses `removePrefix: "plugin_content_"` Generate `Notice`, 3 monitoring plugins use `removePrefix: "plugin_monitor_"`, correspondingly generate `Loginlog`, `Operlog`, `Server`
- [x] **FB-21**: `openspec/specs/` main specification still retains the old `sys_*` plugin table name and old host boundary description - synchronously update the user, organization, announcement, log, service monitoring and notification domain master specifications to the final table name and capability boundary description after plugin
  - 2026-04-21: `openspec/specs/{user-management,dept-management,post-management,notice-management,login-log,oper-log,server-monitor,plugin-notify-service}/spec.md` has been updated, and the old `sys_*` plugin table name, host direct holding plugin table description and old menu boundary expression are unified and converged into the final specification after current pluginization
- [x] **FB-22**: Merge the mistakenly created active iteration `host-plugin-boundary-followup` back to the current `host-plugin-boundary-modularization` and restore the single active change governance state
  - 2026-04-21: The `openspec/changes/host-plugin-boundary-followup/` directory that was created by mistake and does not carry valid documents has been deleted, and subsequent feedback will continue to be appended to the currently unarchived `host-plugin-boundary-modularization`
- [x] **FB-23**: Change the variadic constructor used for "optional dependencies" in the host business component to an explicit parameter signature; uniformly pass `nil` when not explicitly injected, and explain the default behavior in the constructor comment
  - 2026-04-21: `auth.New`, `user.New`, `menu.New`, `orgcap.New`, `plugin.New` and `controller/plugin.NewV1` have been uniformly changed to explicit parameter signatures; the host call point and related tests have been uniformly changed to pass in dependencies on demand or explicitly pass `nil`, and the default behavior annotation has been added
- [x] **FB-24**: Change the permission menu filtering dependency of `role` on `plugin` to narrow interface injection, and recycle the menu management metadata from `menu/metadata` to `menu` root package cohesion maintenance after the circular dependency is removed.
  - 2026-04-21: `role` has been changed to rely on the narrow interface `PermissionMenuFilter`, and the direct dependence on `plugin.Service` has been removed; the `menu` stable directory and official plugin mounting metadata have been recycled to `internal/service/menu/menu_metadata.go`, `plugin/internal/{catalog,integration}`, `orgcap` and related tests have been switched synchronously
- [x] **FB-25**: Host controller constructor does not synchronize explicit dependency injection signatures, causing `go test ./...` to fail to compile directly with backend builds at `auth`, `role`, `joblog` controllers
  - 2026-04-22: `apps/lina-core/internal/controller/{auth,role,joblog}/*_new.go` has been synchronously changed to explicit construction of `pluginSvc` / `orgCapSvc` and passed in `auth.New(...)`, `role.New(...)`; then `apps/lina-core/go test ./...`, plugin/building module `go test ./...` and frontend `pnpm test:unit` all passed
- [x] **FB-26**: `TC0069` still uses the old organization association table `sys_user_dept/sys_user_post` for cleaning, causing all E2E to fail in the final stage of the dynamic plugin permission management use case
  - 2026-04-22: `hack/tests/e2e/plugin/TC0069-plugin-permission-governance.ts` has been changed to clean up `plugin_org_center_user_dept`, `plugin_org_center_user_post`; single test `TC0069` passed a separate regression, and then `npx playwright test` passed all 341 items.
- [x] **FB-27**: The operation log needs to be changed to a source plugin that self-registers the audit link through the global HTTP middleware registrar encapsulated by the host. When the plugin is deactivated, the collection logic is completely bypassed and the host-specific `OperLog` business middleware is removed.
  - 2026-04-22: The host has released a unified `HTTPRegistrar` (routing registrar + global middleware registrar), `monitor-operlog` has been changed to self-register `/api/v1/*` audit middleware and emit audit events through the stable `pkg/pluginservice/audit` seam; the host static/dynamic routing link has been removed and the dedicated `OperLog` Middleware, the integration layer memory snapshot is used to instantly bypass the global middleware / routing guard / cron guard when the plugin starts and stops running; `apps/lina-core` and the affected source plugin backend `go test ./...` have been returned, and passed Playwright use cases `TC0026`, `TC0098`, `TC0099`
- [x] **FB-28**: `pkg/pluginbridge` / `pkg/pluginhost` The behavioral objects exposed to plugins need to be unified interfaces, `NewSourcePlugin` will no longer return specific structs, and the source plugin capability registration will be changed to group interface objects
  - 2026-04-22: `pluginhost.NewSourcePlugin` has been changed to return the `SourcePlugin` interface, and the `Assets/Lifecycle/Hooks/HTTP/Cron/Jobs/Governance` group registration facade has been added; the host registry and manifest have been switched to the `SourcePluginDefinition` reading view, `pluginbridge.NewGuestRuntime` and `NewGuestControllerRouteDispatcher` has also been changed to the return interface; the relevant source plugin wiring and unit tests have been updated simultaneously, and the regression of `apps/lina-core` related packages, `plugin-demo-dynamic` and the affected source plugin `go test ./...` has been completed
- [x] **FB-29**: Delete the `plugin.jobs` plugin general task processor capability, retain only the `plugin.cron` built-in scheduled task projection link, and remove the sample registration code in `plugin-demo-source`
- 2026-04-22: The public contract and implementation of `pkg/pluginhost` for `plugin.jobs` / `JobHandlerRegistration` has been removed, the registration logic for the universal plugin task processor in `jobhandler` has been removed, the `pluginbridge.BuildPluginHandlerRef` and `plugin-demo-source` sample registration codes have been deleted; and `cd apps/lina-core has been returned && go test ./pkg/pluginhost ./pkg/pluginbridge ./internal/service/jobhandler ./internal/service/plugin/... ./internal/service/jobmgmt/...`, `cd apps/lina-plugins/plugin-demo-source/backend && go test ./...`, `cd hack/tests && npx playwright test e2e/system/job/TC0090-job-plugin-cascade.ts`
- [x] **FB-30**: Delete the `http.request.after-auth` / `RegisterAfterAuthHandler` plugin extension points, remove the host distribution link and source plugin residual calls
  - 2026-04-22: Removed `http.request.after-auth` from `openspec/specs/plugin-hook-slot-extension/spec.md` with the current change delta spec; host `pkg/pluginhost` Removed `RegisterAfterAuthHandler`, `AfterAuthInput`, `ExtensionPointHTTPRequestAfterAuth` and corresponding registration storage; the after-auth distribution/call chain in `middleware`, `plugin/internal/{integration,runtime}`, `plugin` facade and `monitor-operlog` source plugin has been cleaned up synchronously. This feedback does not involve user observable behavior changes, and no E2E has been added; `cd apps/lina-core && go test ./pkg/pluginhost ./internal/service/middleware ./internal/service/plugin/...` has been returned, as well as `go test ./...` of each source plugin backend.
- [x] **FB-31**: `monitor-operlog` audit middleware needs to refer to `lina-core/internal/service/middleware` to converge to the plugin `service` layer to avoid `backend/audit_middleware.go` carrying thick orchestration logic in the root directory
  - 2026-04-22: The `apps/lina-plugins/monitor-operlog/backend/internal/service/middleware/` component has been added, which carries audit middleware orchestration, metadata normalization, sensitive field desensitization and asynchronous audit event distribution according to the host `middleware.Service` mode; `backend/plugin.go` is changed to registration `middlewaresvc.New().Audit`, and delete the root directory `audit_middleware.go` and the corresponding test file. This feedback belongs to the internal architecture convergence and does not introduce user-observable behavior changes. Therefore, the plugin backend `go test ./...` and the new middleware unit test are supplemented and run, and no E2E is added.
- [x] **FB-32**: The source plugin `service` directory hierarchy is not standardized - it is uniformly migrated to `backend/internal/service/`, and it is prohibited to continue to place it directly in `backend/service/`
- 2026-04-22: The `service` components of `content-notice`, `monitor-loginlog`, `monitor-online`, `monitor-operlog`, `monitor-server`, `org-center` and `plugin-demo-source` have been uniformly migrated to `backend/internal/service/`, and simultaneously repair the controller, provider, plugin registration entrance and all import paths in the test; then complete the `go test ./...` regression of the above plugin module and `apps/lina-plugins` aggregation module
- [x] **FB-33**: Supplement the source plugin directory structure to the project specification, explicitly constrain `backend/internal/service/`, `backend/internal/controller/`, `backend/hack/config.yaml` and the plugin resource directory
  - 2026-04-22: Updated `CLAUDE.md` (i.e. root `AGENTS.md`), `apps/lina-plugins/README.md`, `apps/lina-plugins/README.zh_CN.md`, `apps/lina-plugins/plugin-demo-source/README.md`, `apps/lina-plugins/plugin-demo-source/README.zh_CN.md`, and currently actively changed `plugin-manifest-lifecycle` adds standard directory constraints for source plugins in the incremental specification
- [x] **FB-34**: Log TraceID should be turned off by default, and support both `config.yaml` static switch and system parameter dynamic override
  - 2026-04-22: The host has added `logger.extensions.traceIDEnabled` static configuration item and protected system parameter `sys.logger.traceID.enabled` (supports `inherit|true|false`). The log will no longer output TraceID by default; the custom logger handler is driven by `config.Service.IsLoggerTraceIDEnabled` during runtime to dynamically clip TraceID and has returned. `go test ./pkg/logger -run 'Test(ConfigureInstallsCustomHandler|StructuredTraceIDAwareHandlerDropsTraceID|TextTraceIDAwareHandlerKeepsTraceID|BindServerAlignsSharedOutput|BindServerUsesDefaultPatternWhenFileMissing)'`, `go test ./internal/service/config -run 'Test(GetLoggerUsesStructuredSwitch|GetLoggerUsesDefaultWhenSwitchMissing|ValidateRuntimeParamValue|IsLoggerTraceIDEnabledUsesStaticConfigFallba ck|IsLoggerTraceIDEnabledUsesRuntimeOverride|IsLoggerTraceIDEnabledSupportsRuntimeEnableOverride|IsLoggerTraceIDEnabledSupportsInheritMode)'',`go test -run '^$' ./internal/service/role` and `pnpm exec playwright test e2e/system/TC0079-config-runtime-params.ts --config=/tmp/pw-chrome.config.ts --project=chrome`
- [x] **FB-35**: System menu types still use `"D"` / `"M"` / `"B"` strings scatteredly in the host code, which need to be converged into strongly typed constants and reused uniformly
- 2026-04-22: `apps/lina-core/pkg/menutype` has been added to define strongly typed constants for menu types shared by the host/plugin, and uniformly switches host menu verification, menu/user controller and related plugin single testing to constant reuse; `go build ./pkg/menutype ./internal/service/menu ./internal/controller/menu ./internal/controller/user ./internal/service/plugin/...` compiled and passed
- [x] **FB-36**: `monitor-operlog` still uses `1~6` integers to express operation types, which need to be changed to `create/update/delete/export/import/other` semantic strings and synchronize host events, plugin interfaces, dictionaries and frontend contracts
  - 2026-04-22: Added `apps/lina-core/pkg/audittype` unified host audit events, dynamic routing `operLog` verification and plugin drop-in type; `monitor-operlog` SQL, DAO/DO/Entity, API DTO, controller, service, frontend client and dictionary values have all been switched to semantic string types, and the local MySQL `plugin_monitor_operlog` table structure has been updated before execution `gf gen dao`. Verification passed: `cd apps/lina-core && go test ./pkg/menutype ./pkg/audittype ./pkg/pluginbridge ./pkg/pluginhost`, `cd apps/lina-core && go build ./pkg/menutype ./pkg/audittype ./pkg/pluginbridge ./pkg/pluginhost ./pkg/pluginservice/audit ./internal/service/menu ./internal/controller/menu ./internal/controller/user ./internal/service/plugin/...`、`cd apps/lina-plugins/monitor-operlog/backend && go test ./api/operlog/... ./internal/controller/operlog ./internal/service/operlog`、`cd apps/lina-plugins/monitor-operlog/backend && go build ./...`
- [x] **FB-37**: The host workbench still has unreferenced old versions of `monitor-operlog` frontend pages and API copies. It is necessary to delete duplicate implementations and return to operation log-related E2E to avoid subsequent mistaken reconnection of the old contract.
- 2026-04-22: The old host page copy of `apps/lina-vben/apps/web-antd/src/{api/monitor/operlog,views/monitor/operlog}/` has been removed, and currently only `apps/lina-plugins/monitor-operlog/frontend/pages/` is retained as the frontend source of truth; `rg` has confirmed that there are no residual references in the host source. Initially, the official regression was not completed due to the lack of local available Playwright browser products; now it has been re-run and passed the affected use cases `TC0026`, `TC0027`, `TC0028`, `TC0029`, `TC0035` and the plugin life cycle regression `TC0098` (command: `cd hack/tests && npx playwright test e2e/monitor/TC0026-operlog-list.ts e2e/monitor/TC0027-operlog-detail.ts e2e/monitor/TC0028-operlog-clean.ts e2e/monitor/TC0029-operlog-export.ts e2e/monitor/TC0035-operlog-delete.ts e2e/plugin/TC0098-official-source-plugin-lifecycle.ts --project=chromium`, passed 20/20)

- [x] **FB-38**: `pluginbridge` exposes unified guest controller scaffolding - new generics `BindJSON[T](request)`, `WriteJSON(statusCode, payload)`, envelope path and query accessors (`PathParam` / `QueryValue` / `QueryInt` / `QueryFlag`), and `ErrorClassifier` + `NewErrorClassifier(ErrorCase...)` combinator and complete unit tests covering normal / empty body / illegal JSON / marshal failure 4 paths
  - 2026-04-22: Added `apps/lina-core/pkg/pluginbridge/pluginbridge_guest_helpers.go` and `pluginbridge_guest_helpers_test.go`; purely incremental changes do not modify the existing API. Regression: `go test ./pkg/pluginbridge/...` fully green (including 13 new use cases), `lina-core` full module `go build ./...` passed, `apps/lina-plugins` aggregation module `go build ./...` passed, `plugin-demo-dynamic` `GOOS=wasip1 GOARCH=wasm go build ./...` passed
- [x] **FB-39**: `plugin-demo-dynamic` controller template slimming down - delete `dynamic_request.go` and all `buildXxxRouteInput` / `readDemoRecordIDFromXxxRoute` duplicate packaging, all routes use `pluginbridge` new helpers, `buildDynamicErrorResponse` is changed to `ErrorClassifier` assembly; verify `go test ./pkg/pluginbridge/...`, `GOOS=wasip1 GOARCH=wasm go build`, `TC0067-runtime-wasm-lifecycle` and `.wasm` product size before and after reconstruction
  - 2026-04-22: Deleted `dynamic_request.go`, `dynamic_error_response.go` rewritten to `pluginbridge.NewErrorClassifier(...)` assembly, 8 controllers (CRUD / attachment download / backend summary / host call demonstration / cron heartbeat) all use `BindJSON[T]` + `WriteJSON` + `PathParam` / `QueryValue` / `QueryInt` / `QueryFlag`. Effect: Controller directory 459 → 262 lines (-43%), wasm artifact 9,017,751 → 9,016,169 bytes (-1,582 bytes / -0.02%, not bloated). Regression: `lina-core` full version `go test ./...` fully green, `GOOS=wasip1 GOARCH=wasm go build ./...` passed, `TC0067-runtime-wasm-lifecycle` 15/15 passed (including CRUD / paging / multipart upload / attachment download / backend-summary / host-call / cron-heartbeat)
- [x] **FB-40**: The dynamic plugin guest controller supports the typed req/res signature of the API DTO driver - allowing the `plugin-demo-dynamic` routing controller to directly use the `context.Context + *v1.Req -> *v1.Res` form, and complete the `backend-summary` / `host-call-demo` API DTO to align with the actual input and output
- 2026-04-22: `pluginbridge` has added typed guest controller scheduling, context and binding capabilities. The 8 dynamic routing controllers of `plugin-demo-dynamic` are now uniformly switched to the `context.Context + *v1.Req -> *v1.Res` form; at the same time, a new `apps/lina-core/pkg/pluginbridge/apimeta` zero-dependency metadata carrier is added to allow dynamic plugin API DTO Route tags are retained instead of bringing GoFrame metadata packets into the Wasm guest initialization link, and `backend-summary` / `host-call-demo` DTOs are aligned with real input and output. Regression: `cd apps/lina-core && go test ./pkg/pluginbridge/... ./internal/service/plugin/internal/runtime/...`, `cd apps/lina-plugins/plugin-demo-dynamic && go test ./...`, `cd apps/lina-plugins/plugin-demo-dynamic && GOOS=wasip1 GOARCH=wasm go build ./...` All passed
- [x] **FB-41**: Now that the upstream `github.com/gogf/gf/v2` has fixed the panic of `gdebug.init.0` for empty `os.Args` under Wasm, remove the `apimeta.Meta` compatibility layer of the dynamic plugin API DTO, and re-verify the typed guest controller and the dynamic plugin Wasm running regression
  - 2026-04-22: The `apps/lina-core/pkg/pluginbridge/apimeta/` compatibility layer has been deleted, and the `plugin-demo-dynamic` API DTO is changed to directly reference the GoFrame official `gmeta.Meta`; during the verification process, the `frame/g` path was additionally confirmed. Although the `gdebug` initialization panic is no longer triggered, the new Wasm will still be introduced. Running regression (`wasm error: unreachable` on the `internal_strconv.dboxFtoa` stack appears when `host-call-demo` and `cron-heartbeat` are executed), so the final convergence is to use `gmeta.Meta` directly without custom aliases. Regression: `cd apps/lina-core && go test ./pkg/pluginbridge/... ./internal/service/plugin/...`, `cd apps/lina-plugins/plugin-demo-dynamic && go test ./...`, `cd apps/lina-plugins/plugin-demo-dynamic && GOOS=wasip1 GOARCH=wasm go build ./...` All passed; `TC0067-runtime-wasm-lifecycle` tried to execute but was pulled up by the local `make dev`. The Vite/esbuild `write EPIPE` process crashed and blocked, and no valid functional conclusion was formed.
- [x] **FB-42**: Remove the `sys.logger.traceID.enabled` parameter configuration, the log TraceID switch is only allowed to be controlled by `logger.extensions.traceIDEnabled` in `config.yaml`
- 2026-04-22: Removed host `config.Service.IsLoggerTraceIDEnabled` runtime override link with `sys.logger.traceID.enabled` protected parameter definition, `main.go` and `pkg/logger` instead consume only `logger.extensions.traceIDEnabled` static boolean switch; `007-config-management.sql` Changed to clearing old parameters during initialization and no longer inserting new records, `TC0079` synchronization changed to assert that the parameter is not retrievable. In order to restore the local verification link, the `gopsutil` dependency of `apps/lina-plugins/monitor-server/go.mod` / `go.sum` has been completed and `go.work.sum` has been updated. Verification passed: `cd apps/lina-core && go test ./pkg/logger ./internal/service/config ./internal/service/sysconfig ./internal/service/role`, `cd apps/lina-core && go build ./...`, `cd apps/lina-plugins/monitor-server && go test ./...`, `make init confirm=init`, followed by MySQL and `/api/v1/config` query confirms that `sys.logger.traceID.enabled` no longer exists, and at the same time, the headless Playwright manual script is used to verify that the `#/system/config` page can still retrieve `sys.jwt.expire` and cannot retrieve `sys.logger.traceID.enabled`

- [x] **FB-43**: The unified task scheduling interface document group name is changed to `Task Scheduling/Timed Tasks`, `Task Scheduling/Task Grouping`, `Task Scheduling/Execution Log`, `Task Scheduling/Plug-in Processor`
  - 2026-04-22: The relevant `g.Meta tags` in `apps/lina-core/api/job*/v1/*.go` have been updated, and the full-text search has confirmed that the old group tags have been cleaned up; this feedback only affects the group display of interface documents, and does not involve runtime behavior, and no E2E is added.
