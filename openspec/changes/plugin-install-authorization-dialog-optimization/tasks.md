## Feedback

- [x] **FB-1**: 用单一安装审查弹窗替代插件安装 `Popconfirm` 与动态插件重复授权确认，并让源码插件与动态插件都先展示插件详情再确认安装
- [x] **FB-2**: 优化动态插件安装授权弹窗的信息结构，补齐插件详情区并按“数据服务 → 存储服务 → 网络服务 → 运行时服务”顺序展示授权项，收敛重复方法说明与提示文案
- [x] **FB-3**: 安装阶段确认过授权的动态插件 release 在后续启用时直接复用授权快照，不再重复弹出授权窗，并更新相关 E2E 覆盖验证源码/动态插件安装与启用流程
- [x] **FB-4**: 数据服务的数据表展示改为 `table_name (table comment)` 格式，并保持授权勾选交互与 E2E 定位稳定
- [x] **FB-5**: 动态插件授权弹窗改为只读服务清单展示，确认时默认授权插件声明的全部服务，并去掉各服务勾选提示、末尾未勾选提示及“当前授权状态”行
- [x] **FB-6**: 动态插件授权弹窗在只读模式下继续展示各服务资源列表，并去掉运行时服务上的“无需额外确认”等补充提示文案
- [x] **FB-7**: 动态插件授权弹窗中的资源项改为真正的无序列表展示，避免只读资源块视觉上仍像表格或分栏列表
- [x] **FB-8**: 收敛默认工作台首页中的模板化演示内容和失效快捷入口，改为 LinaPro 管理后台真实可达的快捷导航与运维信息
- [x] **FB-9**: 优化默认分析页的指标分组、时间范围切换和图表标题语义，避免重复标题与模板化统计叙事
- [x] **FB-10**: 补齐默认仪表盘页面的 E2E 覆盖，新增 `TC0076` 与 `TC0077` 并完成相关回归验证
- [x] **FB-11**: 修复插件自有资源查询接口误绑 `plugin:query` 导致已授权业务用户访问动态插件资源返回 `403` 的权限治理缺陷，并完成相关插件回归验证
- [x] **FB-12**: 修复 `TC0041` 在完整回归中创建通知被拒绝导致消息预览用例失败的问题，确保通知详情链路在全量 E2E 中稳定通过
- [x] **FB-13**: 修复 `TC0062` 在完整回归中创建用户后成功提示与角色展示断言不稳定的问题，确保用户角色关联场景稳定通过
- [x] **FB-14**: 修复 `TC0062` 删除用户时操作按钮定位脆弱导致超时的问题，确保用户角色关联清理用例稳定通过
- [x] **FB-15**: 修复 `TC0005` 共享用户名与跨子用例状态依赖导致 worker 重启后用户 CRUD 回归不稳定的问题，确保该用例文件可独立重跑且全量回归稳定通过
