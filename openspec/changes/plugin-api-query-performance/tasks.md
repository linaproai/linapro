## 1. 插件列表只读化

- [x] 1.1 新增插件只读列表服务方法，避免 `GET /api/v1/plugins` 调用同步写入流程
- [x] 1.2 保持 `POST /api/v1/plugins/sync` 使用显式同步流程，并确保同步后返回总数
- [x] 1.3 补充插件列表查询不会写入治理表的单元测试

## 2. 元数据与会话写入优化

- [x] 2.1 将插件 hostServices 表注释查询改为不会触发 schema 探测错误的只读查询
- [x] 2.2 为在线会话认证链路增加 `last_active_time` 短窗口写入节流
- [x] 2.3 补充表注释查询和会话活跃时间节流的单元测试

## 3. 验证与治理

- [x] 3.1 运行受影响的后端测试
- [x] 3.2 确认本次改动不需要新增或修改 i18n 资源（本次仅调整后端只读查询、元数据读取和会话写入节流，无新增用户可见文案或翻译键）
- [x] 3.3 执行 Lina 变更审查
