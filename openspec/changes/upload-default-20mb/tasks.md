## 1. 默认值来源对齐

- [x] 1.1 将 `sys.upload.maxSize` 的宿主初始化默认值调整为 20MB，并同步更新相关 manifest 或派生产物。
- [x] 1.2 将上传配置模板和后端静态回退默认值统一调整为 20MB，消除当前 10MB/16MB 的默认值分裂。

## 2. 上传链路校验同步

- [x] 2.1 更新文件上传校验、请求体大小保护和友好错误提示相关实现或断言，使默认基线统一为 20MB。
- [x] 2.2 更新受影响的后端自动化测试，覆盖默认 20MB 与运行时覆写上传上限两类场景。

## 3. 验证

- [x] 3.1 执行受影响的后端测试与必要的初始化验证，确认 `sys.upload.maxSize` 初始值、运行时限制和错误提示均为 20MB 基线。
- [x] 3.2 如构建流程包含嵌入或打包的 manifest 产物，补充校验这些产物中的默认上传上限也已更新为 20MB。
