## 1. 入口护栏改造

- [x] 1.1 更新仓库根`Makefile`中的`init`、`mock`目标，要求显式传入与命令匹配的`confirm`变量，并在缺失或错误时输出正确示例后失败退出
- [x] 1.2 更新`apps/lina-core/Makefile`中的`init`、`mock`目标，复用同一套`confirm`校验与透传逻辑，确保后端目录直接执行时也默认安全

## 2. 后端命令安全收口

- [x] 2.1 为`apps/lina-core/internal/cmd/`下的`init`、`mock`命令增加`confirm`入参和统一校验逻辑，未确认时拒绝执行任何`SQL`
- [x] 2.2 调整公共`SQL`执行流程为首错即停并返回`error`，让`init`、`mock`在任一`SQL`失败时返回失败状态
- [x] 2.3 更新命令帮助文案、日志或错误提示，明确展示正确调用方式，例如`make init confirm=init`、`go run main.go mock --confirm=mock`

## 3. 验证与回归

- [x] 3.1 补充或更新命令相关单元测试，覆盖未确认拒绝执行、正确确认后允许执行、错误确认值拒绝执行等场景
- [x] 3.2 补充或更新`SQL`执行失败路径测试，验证任一文件失败时会停止后续执行并向调用方返回失败状态
- [x] 3.3 运行本次变更相关测试与必要的命令校验，确认双层确认护栏和失败语义生效且未引入回归
