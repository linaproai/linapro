## 1. 宿主登录页配置参数接入

- [x] 1.1 在 `apps/lina-core/internal/service/config/` 中新增 `sys.auth.loginPanelLayout` 的受保护 public frontend 参数定义、默认值、值域校验与白名单投影字段。
- [x] 1.2 更新宿主初始化 SQL 中的内置系统参数清单，确保登录框位置参数默认值为 `panel-right` 且可在系统参数页维护。
- [x] 1.3 补充后端配置单测，覆盖登录框位置参数的合法值校验、非法值拒绝与 `GET /config/public/frontend` 输出。

## 2. 登录页入口与布局默认值调整

- [x] 2.1 调整前端默认偏好与公共前端运行时同步逻辑，使登录页默认使用 `panel-right`，并支持从 `auth.panelLayout` 应用宿主配置。
- [x] 2.2 调整登录页装配，显式隐藏忘记密码、注册账号、手机号登录、扫码登录和第三方登录入口，仅保留账号密码登录。
- [x] 2.3 调整认证路由注册与回退逻辑，使未实现的认证子页面统一回退到 `/auth/login`，避免继续暴露无效页面入口。

## 3. 前端交互测试与回归验证

- [x] 3.1 更新前端运行时相关测试，覆盖公共前端配置对登录框位置默认值的同步行为。
- [x] 3.2 创建 `hack/tests/e2e/auth/TC0102-login-page-presentation.ts`，实现以下断言：`TC-102a` 登录页隐藏未实现入口；`TC-102b` 默认使用居右登录框布局；`TC-102c` 修改系统参数后登录页按配置切换布局。
- [x] 3.3 执行本轮相关验证（至少包含后端配置测试、前端相关测试与 `TC0102`），确认登录页展示与系统参数行为一致。

## Feedback

- [x] **FB-1**: 将 `sys.auth.pageDesc` 的长度限制放宽到 500 个字符，并补齐系统参数校验与公开前端配置回归测试。
- [ ] **FB-2**: 将登录页默认说明文案调整为“面向业务演进，提供开箱即用的管理入口与灵活可插拔的扩展机制”，并将默认登录框布局调整为居右。
