## 1. P0 框架元数据统一

- [x] 1.1 在 `metadata.yaml` 中新增框架元数据段，统一维护名称、版本号、介绍、项目官网、仓库地址、开源协议。
- [x] 1.2 调整系统信息接口返回框架元数据，并改造系统信息页顶部项目卡片为后端数据驱动。

## 2. P0 正式源码升级命令

- [x] 2.1 新增 `upgrade` CLI 命令和 `make upgrade` 入口，并沿用显式确认机制。
- [x] 2.2 在升级前完成备份提醒、Git 工作区脏检查、当前项目版本读取和目标版本比较。
- [x] 2.3 实现目标标签代码拉取与覆盖本地框架代码。
- [x] 2.4 实现从第一条宿主 SQL 开始按顺序执行全部宿主 SQL。
- [x] 2.5 当目标版本不高于当前项目版本时，明确提示无需升级并安全退出。

## 3. 验证

- [x] 3.1 增加版本比较、目标标签解析和 Git 工作区检查的自动化测试。
- [x] 3.2 增加升级 SQL 全量执行路径的自动化测试。

## 4. P1 预留

- [x] 4.1 在当前变更中保留业务系统运行时升级的后续方向说明，但不在本轮实现。

## Feedback

- [x] **FB-5**: 框架元数据中的 `homepage` 应表示官网地址，并新增独立的仓库地址字段供系统信息展示和升级命令使用。
- [x] **FB-4**: `internal/cmd` 的单元测试文件应按命令组件职责重新内聚，避免保留已删除 helper 文件对应的测试文件名。
- [x] **FB-3**: `internal/cmd` 中与 `init`、`mock`、`upgrade` 相关的非测试逻辑应尽量内聚到对应命令文件或 `cmd.go`，减少零散辅助文件。
- [x] **FB-1**: 源码升级不再引入数据库升级状态表、升级记录表和 SQL 游标表。
- [x] **FB-2**: `make upgrade` 执行宿主 SQL 时从第一条 SQL 开始全量执行，不再依赖数据库中的执行位置。
