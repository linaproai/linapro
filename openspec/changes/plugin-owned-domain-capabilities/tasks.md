## 1. 规范与规则冻结

- [x] 1.1 更新本变更 specs，确认 core-owned/plugin-owned 分类、`backend/cap`目录、owner-aware host service、依赖治理、缓存一致性、数据权限、`i18n`和测试策略已覆盖。
- [x] 1.2 更新`.agents/rules/architecture.md`，将“插件领域能力维护”调整为 core-owned 与 plugin-owned 双路径，并记录源码插件和动态插件同步暴露要求。
- [x] 1.3 更新`.agents/rules/plugin.md`，增加`backend/cap`公开契约、跨插件依赖、动态 owner 能力声明和禁止 import 内部实现的规则。
- [x] 1.4 更新`apps/lina-core/pkg/plugin/README.md`和`README.zh-CN.md`，说明 core 只拥有插件内核、通用注册、动态路由、授权和治理机制。
- [x] 1.5 更新`apps/lina-plugins/linapro-ai-core/README.md`和`README.zh-CN.md`，说明`AI`能力 owner、源码消费、动态 SDK、provider SPI、依赖声明和版本策略。

## 2. Core 通用能力注册与动态协议

- [x] 2.1 在`lina-core`新增通用 capability descriptor、owner/method 索引、重复注册校验和 descriptor 测试。
- [x] 2.2 改造源码插件声明路径，使`pluginhost`接收通用 descriptor，并删除或替换`ProvideAIText`、`GetAITextProviderFactory`和 AI provider 专属字段。
- [x] 2.3 扩展`HostServiceSpec`、manifest YAML/JSON 解析、artifact section、水合、归一化和校验，支持结构化`owner`与`version`字段。
- [x] 2.4 改造 host service catalog，使 core-owned 静态 catalog 与 owner descriptor 投影合并，并在管理端/API 中保留 owner 来源。
- [x] 2.5 改造安装、启用、升级 diff 和授权快照，记录 owner、service、version、method、resources 和重新授权判断。
- [x] 2.6 实现 owner-aware WASM dispatcher，按授权快照和 descriptor 通用转发 owner 调用，并删除或替换 AI 专属 dispatcher 分支。
- [x] 2.7 完成 DI 来源检查记录：说明 descriptor registry、catalog、dispatcher、依赖检查和缓存相关新增运行期依赖的 owner、创建位置、传递路径和共享实例策略。

## 3. 依赖治理与 import 边界

- [x] 3.1 增加源码插件 import `backend/cap/...` 与`plugin.yaml dependencies.plugins`一致性扫描。
- [x] 3.2 增加动态插件`hostServices.owner`与`dependencies.plugins`一致性校验，覆盖 manifest、artifact、安装和启用路径。
- [x] 3.3 扩展反向依赖检查，阻断会破坏下游 owner 能力消费方的 owner 插件禁用、卸载和不兼容升级。
- [x] 3.4 增加跨插件 import 边界扫描，阻断生产代码 import 其他插件`backend/internal`、`dao`、`do`、`entity`、controller、service 和`backend/pkg`领域入口。
- [x] 3.5 记录开发工具影响：若新增或修改治理扫描入口，按跨平台规则实现为 Go 工具或现有 linactl 子组件，并说明 Windows/Linux/macOS 验证方式。

## 4. linapro-ai-core Owner 契约迁移

- [x] 4.1 在`apps/lina-plugins/linapro-ai-core/backend/cap/aicap`建立普通消费契约、DTO、命名类型、错误语义和方法状态投影。
- [x] 4.2 在`backend/cap/aicap/spi`建立 provider SPI、typed factory env 和 descriptor helper。
- [x] 4.3 在`backend/cap/aicap/bridge`建立动态 guest SDK、owner-aware 声明 helper、codec 和 host call client。
- [x] 4.4 将`AI`文本和多模态已发布 methods 迁移到`linapro-ai-core` owner descriptor，覆盖或记录排除所有现有 core catalog AI methods。
- [x] 4.5 更新`linapro-ai-core/backend/plugin.go`，使用 owner helper 注册`AI`descriptor，并移除对 core `aicap`契约 owner 的依赖。
- [x] 4.6 按插件`i18n.enabled: true`维护新增错误码、授权展示名称、API 文档源文本和`manifest/i18n/<locale>`/`apidoc`资源。

## 5. 调用方、动态 Demo 与文档迁移

- [x] 5.1 替换所有生产代码和测试替身中的 core `lina-core/pkg/plugin/capability/aicap` import，改为 owner 插件`backend/cap/aicap`或通用 descriptor 接口。
- [x] 5.2 更新动态 demo 和 guest 构建代码，使用 owner-aware`hostServices`声明、`dependencies.plugins`和`linapro-ai-core`bridge SDK。
- [x] 5.3 更新动态 artifact 打包、catalog 解析、授权确认和 upgrade preview 测试 fixture，使 owner 字段可序列化、可比较、可展示。
- [x] 5.4 静态检索并删除 core AI 专属 codec、method 常量、dispatcher 和 public re-export 残留；若必须短暂过渡，必须在同一变更内删除。
- [x] 5.5 记录数据库影响：确认本变更不新增业务表；若实施中产生 SQL 变更，按单迭代单 SQL 文件、幂等 DDL/Seed 和索引规则补充。

## 6. 测试与验证

- [x] 6.1 增加 unit tests：descriptor 注册、重复注册、未知 owner、owner 禁用、版本不满足、方法未注册、provider 冲突和缓存失效。
- [x] 6.2 增加 dynamic host service tests：owner-aware manifest 校验、artifact 解析、授权快照、未声明依赖拒绝、未授权 method 拒绝和 owner handler 成功转发。
- [x] 6.3 增加 dependency tests：缺失 owner 依赖、版本不满足、反向禁用阻断、反向升级阻断和首屏列表不触发完整依赖检查。
- [x] 6.4 增加 import boundary tests：跨插件`backend/internal`失败、`backend/cap`成功、`backend/pkg`领域入口失败和测试例外边界。
- [x] 6.5 增加`linapro-ai-core`包测试：owner 契约、SPI helper、bridge SDK codec、provider 注册、状态降级和敏感错误脱敏。
- [x] 6.6 新增宿主插件框架 E2E `hack/tests/e2e/extension/plugin/TC017-owner-capability-authorization.ts`，覆盖 TC-17a owner 授权来源展示、TC-17b 缺失依赖阻断、TC-17c 版本不满足提示和截图/i18n key 检查。
- [x] 6.7 新增 AI 插件 E2E `apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC007-ai-owner-capability-contract.ts`，覆盖 TC-7a 插件启用后能力状态、TC-7b 禁用后降级、TC-7c 动态 demo owner AI 调用成功和失败路径。
- [x] 6.8 运行 Go 编译门禁：`cd apps/lina-core && go test ./internal/cmd -count=1`以及覆盖修改包的`go test <changed-package> -count=1`。
- [x] 6.9 运行插件测试和治理验证：`linapro-ai-core`相关 Go 测试、动态插件构建/host call 测试、i18n 检查、import 边界扫描和文档文件检查。
- [x] 6.10 运行`openspec validate plugin-owned-domain-capabilities --strict`并记录结果。

## 7. 审查与完成记录

- [x] 7.1 在任务记录中明确`i18n`、缓存一致性、数据权限、数据库、API、前端、开发工具跨平台和测试策略的影响判断。
- [x] 7.2 调用`lina-review`完成规范合规、代码边界、DI 来源、E2E 质量、缓存一致性和文档同步审查。
- [x] 7.3 根据`lina-review`反馈修复问题，并在所有验证通过后再逐项标记任务完成。

## 执行记录

- `2.2`：`pluginhost.ProviderDeclarations`新增`ProvideCapability(descriptor)`，`SourcePluginDefinition`新增`GetCapabilityDescriptors()`；已删除`ProvideAIText`、`GetAITextProviderFactory`、`aiTextProvider`和源码插件同步时的`AI`专属 factory 注册路径。DI 影响：本任务没有新增运行期服务依赖或缓存后端，仅保存启动期源码插件声明的 descriptor 值快照；已有 core `AI`旧 service 构造暂未在本任务迁移，后续`5.4`统一删除残留。
- `2.3`：`HostServiceSpec`、manifest `JSON/YAML`和`WASM`artifact host service section 已支持结构化`owner`与`version`字段；校验会拒绝`service: plugin:...`拼接键，要求`owner/version`成对出现，并按`owner/service/version`识别重复声明、授权输入和 upgrade diff。owner-aware host service 暂不再派生旧 core `host:ai:*`能力，静态 core-owned catalog 校验保持仅作用于无`owner`声明。管理端授权请求、host service 授权展示项和 upgrade preview diff 已保留`owner/version`，并同步更新宿主`zh-CN`apidoc 翻译。DI 影响：本任务没有新增运行期服务、构造函数依赖、共享实例或缓存后端；已有 release manifest snapshot 缓存和事务后失效路径保持不变。数据权限影响：未新增数据读取或写入路径，`data`host service 表命名空间校验保持原有 plugin-aware 规则。缓存一致性影响：未新增缓存类型或失效触发点。`i18n`影响：仅新增宿主 API 文档源文本和`zh-CN`apidoc 翻译，无运行时 UI 文案变更。数据库、前端和开发工具影响：无 SQL、前端页面和工具入口变更；本任务不触发新增 E2E，使用协议/API/controller/unit 测试覆盖。
- `2.4`：`protocol/hostservices`新增 owner-aware catalog 投影，`CatalogWithDescriptors`会将 core-owned 静态 catalog 与 plugin-owned `capregistry.Descriptor`合并；owner 投影保留`owner`、`service`、`version`、`sourceContract`、`dynamicContract`、method risk、resource kind、payload 名称和发布状态，`protocol`层同步暴露`HostServiceCatalogFromDescriptors`与`HostServiceMethodsFromDescriptors`。管理端/API 侧 owner 来源沿用`2.3`已补充的`HostServicePermissionItem`和 upgrade diff 投影，本任务通过 controller 测试复验。DI 影响：未新增运行期服务、构造函数参数、共享实例或缓存后端；新增逻辑是纯值投影 helper。缓存一致性影响：未新增缓存类型、失效触发点或跨实例状态。数据权限影响：未新增数据读取、写入或宿主数据可见性判断；owner 能力实际依赖、启用状态和运行时授权阻断仍由后续`2.5`、`2.6`、`3.2`、`3.3`落实。`i18n`影响：无运行时 UI 文案、API 文档源文本或语言包变更。数据库、前端和开发工具影响：无 SQL、前端页面和工具入口变更；旧 core `AI`codec/dispatcher/static residual 不在本任务删除，继续由`5.4`统一清理。本任务不触发新增 E2E，使用 catalog/protocol/hostservice/controller/service/unit 测试覆盖。
- `2.5`：安装、启用和升级授权快照继续通过`HostServiceSpecIdentity`按`owner/service/version`归一化，`store`授权输入与持久化快照保留`owner`、`service`、`version`、`methods`、`resources/resourceRefs`、`tables`、`paths`和`keys`；安装生命周期在无显式授权时现在同时过滤 path/resource/table/key scoped host service，避免 key-scoped 配置服务进入生命周期预检。升级 diff 的`HostServiceChange`和公开`PluginUpgradeHostServiceChange`新增`fromResourceRefs`与`toResourceRefs`，`AuthorizationChanged`继续由 method、table、path、key 和 resource refs 变化共同触发，管理端/API 可展示 owner 来源、资源引用变化和重新授权判断。DI 影响：未新增运行期服务、构造函数参数、共享实例或缓存后端；本任务只扩展既有授权快照和 diff 纯值投影。缓存一致性影响：未新增缓存类型或失效触发点，release manifest snapshot 和授权快照仍沿用既有事务后同步与运行时刷新路径。数据权限影响：未新增数据库读取、写入或宿主数据访问路径；`data`host service 的表命名空间校验和授权收窄逻辑保持不变。`i18n/API`影响：新增 API DTO 字段和宿主`zh-CN`apidoc 翻译，无运行时 UI 文案变更。数据库、前端和开发工具影响：无 SQL、前端页面和工具入口变更；本任务不触发新增 E2E，使用后端单元、controller 投影和 API 编译门禁覆盖。
- `2.6`：`HostServiceRequestEnvelope`新增`owner`和`version`传输字段，`capregistry.Descriptor`新增运行期`Invoker`，并通过`Invocation`与`InvocationResult`表达通用 owner 调用结果，避免 core 为`AI`继续维护领域专属分发入口。`WASM`运行时新增显式`ownerCapabilities *capregistry.Registry`依赖，`serviceImpl.New`启动期调用`buildSourceCapabilityRegistry()`从`pluginhost.ListSourcePlugins()`读取源码插件 descriptor 声明并注入`newWasmHostServiceRuntime`/`wasm.NewRuntime`；测试 fixture 使用空 registry 显式传入，不在请求路径临时构造全局服务图。`hostServiceAccessSnapshot`改为按`HostServiceIdentity(owner, service, version)`索引授权快照；`handleHostServiceInvoke`在检测到`owner`后进入`dispatchOwnerHostService`，按 descriptor 注册、授权快照和 resource ref 校验后调用 owner invoker，`version`不带`owner`会被拒绝。生产 dispatcher registry 已移除`AI`专属注册分支，旧`wasm_host_service_ai.go`中的函数未再挂载到生产 registry，物理删除和 core `AI`codec/常量残留继续由`5.4`统一清理。DI 影响：新增依赖的 owner 为源码插件 descriptor registry，创建位置是`buildSourceCapabilityRegistry`，传递路径是`serviceImpl.New`到`newWasmHostServiceRuntime`再到`wasm.NewRuntime`，运行期复用启动期 registry 快照且没有 per-request 全局查找。缓存一致性影响：本任务未新增缓存后端；registry 是源码插件启动声明快照，动态 artifact、依赖失效和跨实例生命周期刷新由后续依赖治理与缓存任务继续落地。数据权限影响：未新增数据库读取、写入或宿主数据访问路径；owner 动态调用先经授权快照阻断，owner handler 内部访问宿主或插件数据时仍由 owner 契约负责接入数据权限。`i18n`影响：无运行时 UI 文案、API 文档源文本、语言包或`apidoc`资源变更；新增 host-call fallback 字符串仅用于内部错误响应测试覆盖。API、数据库、前端和开发工具影响：无 HTTP API DTO、SQL、前端页面和工具入口变更；本任务不触发新增 E2E，使用后端单元与启动绑定编译门禁覆盖。
- `2.7`：已完成本阶段 DI 来源审查记录，范围覆盖 descriptor registry、catalog、dispatcher、依赖检查和缓存相关运行期依赖。descriptor registry 的 owner 是源码插件注册声明，权威输入为`pluginhost.ListSourcePlugins()`返回的`SourcePluginDefinition.GetCapabilityDescriptors()`；创建位置是`buildSourceCapabilityRegistry()`，传递路径是`serviceImpl.New`到`newWasmHostServiceRuntime`再到`wasm.NewRuntime`，运行期由`hostServiceRuntime.ownerCapabilities`复用同一个启动期 registry 快照，测试 fixture 只能显式传入空 registry 或测试 registry。catalog 投影没有新增运行期服务依赖，`CatalogWithDescriptors`和`HostServiceCatalogFromCapabilityDescriptors`只把调用方传入的 descriptor slice 复制进临时`capregistry.Registry`做重复校验和纯值投影；owner 来源继续来自源码插件 descriptor 或后续动态 artifact 解析结果，不保存共享后台实例。dispatcher 运行期依赖只新增`ownerCapabilities`，其余`capability.Services`、`PluginConfigFactory`、`HostConfigService`和`ManifestFactory`继续由启动装配显式传入；owner 调用上下文只携带调用插件 ID、owner、service、version、method、resource ref、table 和 payload，不在请求路径调用`New()`创建服务图。依赖检查当前仍使用无状态`plugindep.New()`resolver 和请求级`WithDependencySnapshotCache`，快照来源为 manifest 扫描、registry/release 持久化快照和候选 manifest；本阶段没有为 owner host service 新增单独 resolver 或共享缓存，`hostServices.owner`与`dependencies.plugins`强一致校验、反向阻断和首屏性能门禁继续由`3.2`、`3.3`和`6.3`落地。缓存一致性审查结论：本阶段没有新增缓存后端；能力注册表是启动期源码插件声明快照，catalog 是按需纯值投影，授权快照仍以 release manifest snapshot 为权威并沿用安装、启用、升级事务后刷新路径，运行时缓存发布继续复用既有`runtimeCacheChangeNotifierProvider`、`upgradeCachePublisher`、`upgradeCacheFreshener`、`cachecoord`和`topology`实例；动态 owner artifact、owner 启停和跨实例失效的完整策略仍由后续依赖治理、生命周期和缓存验证任务补齐。数据权限影响：本任务只记录 DI 来源，不新增读取、写入、导出、下载、聚合或执行动作；owner 调用保持先经过授权快照，owner handler 访问宿主或插件数据时继续由 owner 契约接入数据权限。`i18n`影响：未修改运行时 UI、API 文档源文本、错误码或语言包。数据库、API、前端和开发工具影响：无 SQL、HTTP API DTO、前端页面和工具入口变更；不触发新增 E2E。本任务属于治理记录，验证方式为`openspec validate`和`git diff --check`。
- `3.1`：已在`hack/tools/linactl/internal/plugingovernance`扩展`plugins.check`治理扫描，扫描器会发现本地插件 Go module owner，使用`go/parser`读取生产 Go import，并在跨插件 import owner 插件`backend/cap/...`时要求调用方`plugin.yaml dependencies.plugins`声明对应 owner 插件且提供非空版本范围；同插件自引用、`*_test.go`、`hack/tests`、`manifest/sql`和 migration 等既有受控非生产路径继续豁免。开发工具影响：复用现有`linactl plugins.check`Go 工具入口，未新增 shell/平台专属脚本，路径处理使用`filepath`和 slash 归一化，适用于 Windows/Linux/macOS；本轮在 macOS 执行单元测试和真实仓库 smoke，Linux/Windows 预期由同一 Go 标准库实现覆盖。DI 影响：无运行期服务、构造函数依赖、共享实例或缓存后端新增。缓存一致性影响：无缓存或快照状态变更。数据权限影响：无读取、写入、导出、下载、聚合或执行类接口变更。`i18n`影响：无运行时文案、API 文档源文本、语言包或`apidoc`资源变更。数据库、API 和前端影响：无 SQL、HTTP API DTO 或前端页面变更；本任务属于开发工具治理扫描，不触发新增 E2E。
- `3.2`：已在`catalog`新增 owner-aware host service 依赖一致性校验，`ValidateManifest`会先归一化`hostServices`，再要求每个`hostServices.owner`在`dependencies.plugins`中声明同一 owner 插件且提供非空版本范围；`ValidateUploadedRuntimeManifest`复用同一校验覆盖动态 artifact manifest；`store.BuildManifestSnapshot`在安装、启用和 release snapshot 准备阶段再次基于归一化 host services 兜底校验，防止内部直接构造 manifest 绕过文件校验。DI 影响：未新增运行期服务、构造函数依赖、共享实例或缓存后端；新增逻辑为 manifest 纯值校验。缓存一致性影响：不新增缓存类型、失效触发点或跨实例状态。数据权限影响：未新增读取、写入、导出、下载、聚合或执行类接口；校验发生在依赖治理和授权快照生成前，不暴露业务数据存在性。`i18n`影响：无运行时 UI 文案、API 文档源文本、错误码或语言包变更；新增错误仅为内部治理校验文本。数据库、API、前端和开发工具影响：无 SQL、HTTP API DTO、前端页面或工具入口变更；本任务不触发 E2E，使用后端单元、安装/启用 snapshot 兜底和启动绑定编译门禁覆盖。
- `3.3`：已在插件禁用路径进入 source/dynamic 生命周期副作用前复用反向依赖检查，阻断会破坏已安装下游硬依赖的 owner 插件禁用；卸载继续复用`ensureNoReverseDependencies`，不兼容升级继续通过`validateUpgradeCandidateDependencies`携带候选版本执行反向版本检查。反向依赖 snapshot 现在从当前 manifest 和有效 release snapshot 保留 owner-aware`hostServices`声明，并在`CheckPluginDependencies`、API DTO 和`CodePluginReverseDependencyBlocked`参数中暴露`ownerHostServices`摘要，包含下游插件、owner、service、version 和 methods，避免管理端为每个下游插件逐项补查详情。新增测试覆盖动态插件声明`hostServices.owner`和`dependencies.plugins`后，禁用 owner 插件被`CodePluginReverseDependencyBlocked`阻断且 owner 保持启用。DI 影响：未新增运行期服务、构造函数依赖、共享实例或缓存后端；新增逻辑只复用已有 dependency resolver、release snapshot 和 lifecycle service。缓存一致性影响：未新增缓存类型或失效触发点；阻断发生在状态变更和运行时缓存发布前，成功禁用路径仍沿用既有事务后刷新。数据权限影响：新增状态变更前置校验只读取插件治理元数据和 release snapshot，不读取业务数据，不暴露租户或业务记录存在性。`i18n/API`影响：新增只读 API 投影字段`ownerHostServices`和宿主`zh-CN`apidoc 翻译，无运行时 UI 文案变更。数据库、前端和开发工具影响：无 SQL、前端页面或工具入口变更；本任务不触发新增 E2E，使用后端单元、controller 投影和启动绑定编译门禁覆盖。
- `3.4`、`3.5`：已扩展`hack/tools/linactl/internal/plugingovernance`中的生产 Go import 扫描，统一解析本地插件 Go module owner；跨插件 import 只有目标插件`backend/cap/...`公开契约可进入依赖声明校验，生产代码 import 其他插件`backend/internal`、`backend/internal/dao`、`backend/internal/model/do`、`backend/internal/model/entity`、controller、service、provider adapter、`backend/pkg`或其他非`backend/cap`后端入口会产生`plugin-cross-plugin-private-import`治理 finding。同插件自引用和既有受控非生产路径继续豁免，避免阻断插件内部 controller/service/DAO 分层实现。开发工具影响：复用现有`linactl plugins.check`Go 子组件和命令入口，未新增 shell、PowerShell、平台专属脚本或新命令；路径处理继续使用`filepath`和 slash 归一化，导入解析使用`go/parser`，预期在 Windows/Linux/macOS 下由 Go 标准库保持一致。本轮在 macOS 执行`go test`和真实仓库`go run . plugins.check` smoke；Linux/Windows 由同一 Go 实现和 CI 复用命令覆盖。DI、缓存一致性、数据权限、`i18n`、数据库、API 和前端影响：无运行期服务、缓存、业务数据访问、文案、SQL、HTTP API DTO 或前端页面变更；本任务属于开发工具治理扫描，不触发 E2E。
- `4.1`、`4.2`、`4.4`、`4.5`：`linapro-ai-core/backend/cap/aicap`已清除对 core internal provider registry 的依赖，`backend/cap/aicap/spi`发布`ai.v1`owner descriptor helper，覆盖旧 core catalog 中`text`、`image`、`embedding`、`audio`、`vision`、`document`、`safety`和`video`全部已发布方法；`backend/plugin.go`改为通过 owner helper 注册 descriptor，插件 ID 来源改为 owner SPI。
- `4.3`：已新增`linapro-ai-core/backend/cap/aicap/bridge`动态 guest SDK，提供`HostServiceSpec()`owner-aware 声明 helper、完整`ai.v1`方法集 helper、owner DTO 直接 JSON codec、状态类 JSON value envelope codec、默认`pluginbridge.InvokeOwnerHostService`host-call client 和可注入测试 client；typed client 实现`aicap.Service`及文本、图片、向量、音频、视觉、文档、安全审核和视频子能力接口，所有请求/响应复用 owner 契约 DTO，不 import `backend/internal/**`、DAO、DO、Entity 或 provider adapter。`pluginbridge`仅新增通用`InvokeOwnerHostService`transport 入口，用现有`HostServiceRequestEnvelope.Owner/Version`传输 owner 字段，没有新增 AI 专属 core codec 或 dispatcher 分支；`spi`中`text.method_status.get`请求投影同步修正为实际使用的`aicap.MethodStatusQuery`。依赖影响：`linapro-ai-core`因直接编译`pluginbridge/protocol`补充`google.golang.org/protobuf v1.36.10`间接依赖和`go.sum`，版本与`lina-core`一致。DI 影响：未新增宿主运行期 service、构造函数依赖、共享实例或缓存后端；SDK 默认使用现有 guest host-call transport，测试通过显式注入`HostServiceInvoker`替身。缓存一致性影响：未新增缓存、快照、失效触发点或跨实例状态。数据权限影响：SDK 不读取或写入宿主/插件业务数据，运行期仍必须经 owner-aware host service 授权快照、依赖校验和 owner handler 数据权限边界。`i18n`影响：无运行时 UI 文案、API 文档源文本、错误码、插件清单文案或语言包变更；插件`i18n.enabled: true`资源无需在本任务新增。数据库、API、前端和开发工具影响：无 SQL、HTTP API DTO、前端页面或治理工具入口变更；已审查`apps/lina-core/pkg/plugin`和`linapro-ai-core`双语 README，现有 owner-aware bridge 说明已覆盖本任务，无需同步修改。本任务不触发 E2E，使用后端包测试、transport stub 测试和 OpenSpec 校验覆盖。
- `4.6`：已按`linapro-ai-core/plugin.yaml`中`i18n.enabled: true`和`en-US`、`zh-CN`语言范围补齐插件自身`manifest/i18n/<locale>/error.json`与`plugin.json`资源，覆盖 owner 契约新增`AI_PROVIDER_UNAVAILABLE`、`AI_PURPOSE_REQUIRED`、`AI_TIER_INVALID`、`AI_ASSET_REF_REQUIRED`、`AI_OPERATION_REF_REQUIRED`、`AI_METHOD_UNSUPPORTED`以及全部`AI_TEXT_*`错误码 message key，并为`plugin.linapro-ai-core.capability.services.ai`、`plugin.linapro-ai-core.capability.versions.v1`和`spi.MethodDescriptors()`发布的全部`ai.v1`动态授权方法补齐展示名称。API 文档源文本影响：仅将`ProviderOperationItem.ErrorCode`示例从不存在的`AI_CORE_PROVIDER_UNAVAILABLE`修正为真实 owner 错误码`AI_PROVIDER_UNAVAILABLE`，未改变 HTTP 路由、DTO 字段结构、时间字段或调用契约；`eg`示例按规则不写入、不翻译`apidoc`资源。`apidoc`影响：`en-US/apidoc`保持空对象占位，`zh-CN/apidoc`无新增 DTO 文档源文本翻译需求。新增`backend/cap/aicap/aicap_i18n_test.go`从插件本地运行时语言包读取`error.json`和`plugin.json`，锁定 owner 错误 message key 与动态授权展示 key 在`en-US`、`zh-CN`下完整覆盖。DI 影响：未新增运行期服务、构造函数依赖、共享实例或缓存后端。缓存一致性影响：未新增缓存、快照、失效触发点或跨实例状态。数据权限影响：未新增读取、写入、导出、下载、聚合、授权关系变更或执行动作。数据库影响：无 SQL、迁移、seed 或 mock 数据变更。前端影响：无页面、路由、组件、API 调用或用户可观察交互变更。开发工具跨平台影响：无脚本、命令、构建或治理工具入口变更。E2E 策略：本任务为插件 owner 契约 i18n/API 示例治理和后端包测试，不涉及用户可观察页面或端到端工作流，不触发新增 E2E。
- `5.1`：已清理生产代码和测试替身对 core `lina-core/pkg/plugin/capability/aicap`的消费 import，`capability.Services`与动态 guest `pluginbridge.Services`不再暴露`AI()`入口；`NewHostServices`、插件运行时 delegate 和 HTTP startup 装配移除 core `AITextProviderEnv`/`aitext.NewManager()`传递链，源码插件与动态插件后续通过 owner 插件`backend/cap/aicap`契约或通用 descriptor/owner-aware dispatcher 进入`AI`能力。已删除动态 guest `domainhostcall_ai.go`、core `protocol_hostservice_ai_codec*.go`和生产`wasm_host_service_ai.go`，`WASM`owner-dispatch 测试改用本地 JSON DTO 验证通用 owner dispatcher；旧`apps/lina-core/pkg/plugin/capability/aicap`包本体和 core catalog 中少量`AI`payload 名称残留继续由`5.4`统一删除，不作为生产消费入口。静态检索结果显示旧 core `aicap`路径仅剩双语 README 中的禁止说明，生产代码和测试替身已无消费 import。DI 影响：本任务减少 core `AI`provider manager/env 运行期依赖，未新增构造函数依赖、共享实例或缓存后端；owner 调用继续复用`2.6`建立的启动期 descriptor registry 与授权快照。缓存一致性影响：未新增缓存、快照、失效触发点或跨实例状态。数据权限影响：未新增读取、写入、导出、下载、聚合、授权关系变更或执行动作；owner handler 访问宿主或插件数据时仍由 owner 契约负责数据权限。数据库影响：无 SQL、迁移、seed 或 mock 数据变更。API 影响：未修改 HTTP 路由、请求 DTO、响应 DTO 或`g.Meta`源文本。`i18n`影响：无运行时 UI 文案、错误码、API 文档源文本、插件清单文案或语言包变更。前端影响：无页面、路由、组件、API 调用或用户可观察交互变更。开发工具跨平台影响：无脚本、命令、构建或治理工具入口变更。E2E 策略：本任务为内部契约 import 迁移和后端启动/bridge 测试，不涉及用户可观察页面或端到端工作流，不触发新增 E2E。
- `5.2`：`linapro-demo-dynamic/plugin.yaml`已在`dependencies.plugins`中声明`linapro-ai-core >=0.1.0`，并以结构化`owner: linapro-ai-core`、`service: ai`、`version: v1`声明 owner-aware`hostServices`方法`text.generate`与`text.method_status.get`；运行期 demo 只读取`text.generate`的方法状态，不执行 provider 生成，避免测试环境依赖渠道配置或产生调用日志。动态 guest `go.mod`新增`lina-plugin-linapro-ai-core`本地`replace`依赖，服务层通过`linapro-ai-core/backend/cap/aicap/bridge`创建 owner bridge client，并在`HostCallDemo`响应中返回 owner、service、version、capability type、method、available、capability ID、active provider 和 reason 投影。API 影响：仅扩展既有`POST /host-call-demo`动作响应 DTO 与`g.Meta dc`说明，不新增路由或改变 HTTP 方法；该接口原本就是副作用演示动作，本次新增只读 owner 方法状态读取，不改变请求频次模型。DI 影响：新增的`aiSvc`由动态 guest 服务构造时调用 owner bridge SDK 创建，owner 为`linapro-ai-core`，创建位置是`linapro-demo-dynamic/backend/internal/service/dynamic.New()`经`newAIHostService()`，传递路径为`serviceImpl.aiSvc`到`runHostCallDemoAI()`；它复用现有`pluginbridge.InvokeOwnerHostService`host-call transport，不在宿主运行时新增服务依赖、共享实例或缓存后端。数据权限影响：本次只读取 owner capability 方法状态，不读取业务记录、租户数据、调用日志或 provider 私有配置；实际运行仍先经过 owner-aware host service 授权快照和依赖治理，owner handler 若访问业务数据继续由 owner 契约接入数据权限。缓存一致性影响：未新增缓存、快照、失效触发点或跨实例状态。数据库影响：无 SQL、迁移、seed、mock 数据或 DAO 生成变更。`i18n`影响：`linapro-demo-dynamic`已启用`i18n`，本次更新 API 文档源文本并同步维护`zh-CN/apidoc`翻译，`en-US/apidoc`继续保持空对象占位；无运行时 UI 文案变更。前端影响：无页面、路由、组件、表格、表单、权限交互或前端 API 调用变更。开发工具跨平台影响：未新增脚本、命令或平台专属构建入口；动态构建继续复用仓库根`make wasm dir=...`与现有 Go 工具链。E2E 策略：本任务为动态 guest owner bridge 接入和 API 投影扩展，未修改用户可观察页面；完整 owner 授权和动态 demo 成功/失败端到端路径由后续`6.6`、`6.7`统一覆盖。
- `5.3`：已补齐动态 artifact 打包、runtime artifact 解析、授权确认投影和 upgrade preview fixture 的 owner-aware 覆盖。`wasmbuilder`测试中的动态插件清单同时声明`linapro-ai-core`硬依赖和`owner: linapro-ai-core`、`service: ai`、`version: v1`的`hostServices`，断言嵌入 manifest dependency section 与后端 host service custom section 均保留 owner、version 和 method；runtime artifact 解析测试断言 artifact section 反序列化后恢复 owner-aware `AI`host service；controller upgrade preview 测试断言目标 manifest 的`RequestedHostServices`响应展示 owner、service、version 和 method，并继续保留 data 表注释；授权确认路径复验既有`buildAuthorizationInput`和 store 授权 identity 测试，确认 owner、service、version 参与比较和投影。DI 影响：本任务只更新测试 fixture 和断言，未新增运行期服务、构造函数依赖、共享实例或缓存后端。缓存一致性影响：未新增缓存、快照、失效触发点或跨实例状态。数据权限影响：未新增读取、写入、导出、下载、聚合、授权关系变更或执行动作；data host service 仅在测试中验证表注释投影。数据库影响：无 SQL、迁移、seed、mock 数据或 DAO 生成变更。API 影响：未修改 HTTP 路由、请求 DTO、响应 DTO、`g.Meta`或`apidoc`源文本，只复验已有 DTO 投影字段。`i18n`影响：无运行时 UI 文案、错误码、API 文档源文本、插件清单文案或语言包变更。前端影响：无页面、路由、组件、API 调用或用户可观察交互变更。开发工具跨平台影响：涉及`hack/tools/linactl/internal/wasmbuilder`测试 fixture，生产工具入口未变；继续使用 Go 标准库打包和解析逻辑，未新增 shell、PowerShell 或平台专属脚本，本轮在 macOS 执行 Go 测试和`plugins.check`smoke，Windows/Linux 由同一 Go 实现和 CI 复用命令覆盖。E2E 策略：本任务为内部 artifact、parser、controller 投影和授权快照单元覆盖，不涉及用户可观察页面或端到端工作流；完整授权展示 E2E 仍由后续`6.6`、`6.7`执行。
- 验证：`cd apps/lina-core && go test ./pkg/plugin/pluginhost -count=1`、`cd apps/lina-core && go test ./pkg/plugin/capability/capregistry -count=1`、`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/protocol/hostservices -count=1`、`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/internal/hostservice -count=1`、`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/protocol -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/store -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/runtime -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/lifecycle -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/upgrade -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/wasm -count=1`、`cd apps/lina-core && go test ./internal/controller/plugin -count=1`、`cd apps/lina-core && go test ./api/plugin/v1 -count=1`、`cd apps/lina-core && go test ./internal/service/plugin -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`cd apps/lina-core && go test ./internal/service/i18n -count=1`、`make i18n.check`、`python3 -m json.tool apps/lina-core/manifest/i18n/zh-CN/apidoc/core-api-plugin.json`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/cap/aicap/... -count=1`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/internal/service/ai -count=1`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend -count=1`均通过；本次`2.6`续跑补充验证`cd apps/lina-core && go test ./pkg/plugin/capability/capregistry -count=1`、`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/protocol -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/wasm -count=1`、`cd apps/lina-core && go test ./internal/service/plugin -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`cd apps/lina-core && go test ./pkg/plugin/pluginhost -count=1`、`openspec validate plugin-owned-domain-capabilities --strict`和`git diff --check`均通过；本次`2.7`补充验证`openspec validate plugin-owned-domain-capabilities --strict`和`git diff --check`均通过；本次`3.1`补充验证`cd hack/tools/linactl && go test ./internal/plugingovernance -count=1`、`cd hack/tools/linactl && go run . plugins.check`均通过；本次`3.2`补充验证`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/store -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/lifecycle -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/upgrade -count=1`、`cd apps/lina-core && go test ./internal/service/plugin -count=1`和`cd apps/lina-core && go test ./internal/cmd -count=1`均通过；本次`3.3`补充验证`cd apps/lina-core && go test ./internal/service/plugin/internal/dependency -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/lifecycle -count=1`、`cd apps/lina-core && go test ./internal/controller/plugin -count=1`、`cd apps/lina-core && go test ./internal/service/plugin -count=1`、`cd apps/lina-core && go test ./api/plugin/v1 -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/upgrade -count=1`、`python3 -m json.tool apps/lina-core/manifest/i18n/zh-CN/apidoc/core-api-plugin.json`、`make i18n.check`、`openspec validate plugin-owned-domain-capabilities --strict`和`git diff --check`均通过；本次`3.4`、`3.5`补充验证`cd hack/tools/linactl && go test ./internal/plugingovernance -count=1`、`cd hack/tools/linactl && go run . plugins.check`、`openspec validate plugin-owned-domain-capabilities --strict`和`git diff --check`均通过；本次`4.3`补充验证`cd apps/lina-core && go test ./pkg/plugin/pluginbridge -count=1`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/cap/aicap/bridge -count=1`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/cap/aicap/... -count=1`和`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend -count=1`均通过；`make i18n.check`仍输出既有模块级`$t()`warning，但退出码为`0`且非本任务新增。
- 本次`4.6`补充验证：`python3 -m json.tool apps/lina-plugins/linapro-ai-core/manifest/i18n/en-US/error.json`、`python3 -m json.tool apps/lina-plugins/linapro-ai-core/manifest/i18n/zh-CN/error.json`、`python3 -m json.tool apps/lina-plugins/linapro-ai-core/manifest/i18n/en-US/plugin.json`、`python3 -m json.tool apps/lina-plugins/linapro-ai-core/manifest/i18n/zh-CN/plugin.json`、`python3 -m json.tool apps/lina-plugins/linapro-ai-core/manifest/i18n/en-US/apidoc/plugin-api-main.json`、`python3 -m json.tool apps/lina-plugins/linapro-ai-core/manifest/i18n/zh-CN/apidoc/plugin-api-main.json`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/cap/aicap -count=1`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/cap/aicap/... -count=1`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/api/... -count=1`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend -count=1`、`make i18n.check`和`openspec validate plugin-owned-domain-capabilities --strict`均通过；`make i18n.check`仍仅输出既有 48 个模块级`$t()`warning，退出码为`0`，非本任务新增。
- 本次`5.1`补充验证：`rg -n "lina-core/pkg/plugin/capability/aicap|capability/aicap" apps/lina-core/internal apps/lina-core/pkg/plugin apps/lina-plugins --glob "!apps/lina-core/pkg/plugin/capability/aicap/**" -S`仅命中`linapro-ai-core`双语 README 的禁止旧路径说明；`cd apps/lina-core && go test ./internal/service/plugin ./internal/service/plugin/internal/wasm ./internal/service/plugin/internal/capabilityhost ./internal/service/plugin/internal/integration ./internal/service/plugin/internal/testutil ./pkg/plugin/capability ./pkg/plugin/pluginbridge ./pkg/plugin/pluginbridge/protocol -count=1`、`cd apps/lina-core && go test ./internal/cmd/internal/httpstartup -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`和`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/... -count=1`均通过。
- 本次`5.2`补充验证：`rg -n "owner:\\s*linapro-ai-core|lina-plugin-linapro-ai-core|text\\.method_status\\.get|text\\.generate" linapro-demo-dynamic -S`确认动态 demo 清单和 guest 代码已指向 owner 插件契约；`rg -n "HostServiceAI|CapabilityAI|AIText|TextGenerate|\\.AI\\(|lina-core/pkg/plugin/capability/aicap" linapro-demo-dynamic -S`未发现旧 core `aicap`消费路径，命中项仅为 owner 契约常量；`cd apps/lina-plugins/linapro-demo-dynamic && GOWORK=off go test ./backend/... -count=1`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/cap/aicap/... -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog -count=1`、`make wasm dir=apps/lina-plugins/linapro-demo-dynamic`、`python3 -m json.tool apps/lina-plugins/linapro-demo-dynamic/manifest/i18n/en-US/apidoc/plugin-api-main.json`、`python3 -m json.tool apps/lina-plugins/linapro-demo-dynamic/manifest/i18n/zh-CN/apidoc/plugin-api-main.json`、`cd hack/tools/linactl && go run . plugins.check`、`make i18n.check`、`openspec validate plugin-owned-domain-capabilities --strict`、`git diff --check`和`git -C apps/lina-plugins diff --check`均通过；`make i18n.check`仍仅输出既有 48 个模块级`$t()`warning，退出码为`0`，非本任务新增。额外预跑`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/internal/hostservice ./pkg/plugin/pluginbridge/protocol -count=1`时，`protocol`包通过，但`internal/hostservice`因仍要求 core `HostServiceAITextGenerateRequest`payload 类型失败，该残留已归属后续`5.4`“删除 core AI 专属 codec、method 常量和 public re-export 残留”，不作为`5.2`完成门禁。
- 本次`5.3`补充验证：`cd hack/tools/linactl && go test ./internal/wasmbuilder -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/runtime -count=1`、`cd apps/lina-core && go test ./internal/controller/plugin -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/upgrade -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/store -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog -count=1`、`cd hack/tools/linactl && go run . plugins.check`、`openspec validate plugin-owned-domain-capabilities --strict`、`git diff --check`和`git -C apps/lina-plugins diff --check`均通过。
- `5.4`：已删除 core `apps/lina-core/pkg/plugin/capability/aicap`目录、core `AI`静态 host service catalog block、`CapabilityAI*`、`HostServiceAI`、`HostServiceMethodAI*`常量和`pluginbridge/protocol`public re-export 残留；`protocol_hostservice_ai_codec*.go`、`domainhostcall_ai.go`和生产`wasm_host_service_ai.go`已不再存在。测试 fixture 改为使用 owner-aware `owner: linapro-ai-core`、`service: ai`、`version: v1`与方法字符串或 owner 插件契约，不再依赖 core 公共`AI`常量；descriptor 治理测试仅校验 core-owned 静态 dispatcher，通用`dispatchOwnerHostService`不再被误判为静态 service dispatcher。`apps/lina-core/pkg/plugin`双语 README 的生成 catalog 已调整为只列出 core-owned services，`AI`说明仅保留 owner 插件入口和 owner-aware 声明示例。DI 影响：本任务删除旧 core `AI`公开契约、codec、常量和测试残留，未新增运行期服务、构造函数依赖、共享实例或缓存后端；owner 调用仍复用`2.6`已建立的启动期 descriptor registry 与授权快照。缓存一致性影响：未新增缓存、快照、失效触发点或跨实例状态。数据权限影响：未新增读取、写入、导出、下载、聚合、授权关系变更或执行动作；owner handler 访问宿主或插件数据时仍由 owner 契约接入数据权限。数据库影响：无 SQL、迁移、seed、mock 数据或 DAO 生成变更。API 影响：未修改 HTTP 路由、请求 DTO、响应 DTO、`g.Meta`或`apidoc`源文本；仅移除 Go 公共协议层的旧 core `AI`专属别名和常量。`i18n`影响：无运行时 UI 文案、错误码、API 文档源文本、插件清单文案或语言包变更。前端影响：无页面、路由、组件、API 调用或用户可观察交互变更。开发工具跨平台影响：仅更新`hack/tools/linactl/internal/wasmbuilder`测试 fixture，生产工具入口未变，继续使用 Go 标准库路径和打包逻辑；本轮在 macOS 执行 Go 测试和`plugins.check`smoke，Windows/Linux 由同一 Go 实现和 CI 复用命令覆盖。E2E 策略：本任务为内部契约和治理清理，不涉及用户可观察页面或端到端工作流，不触发新增 E2E。
- 本次`5.4`补充验证：`rg -n 'HostServiceAI|HostServiceMethodAI|CapabilityAI|HostServiceAI[A-Za-z]+Request|protocol_hostservice_ai_codec|dispatchAI|wasm_host_service_ai|domainhostcall_ai|pkg/plugin/capability/aicap|capability/aicap' apps/lina-core hack/tools/linactl -S`无命中；`test ! -d apps/lina-core/pkg/plugin/capability/aicap`通过；`rg -n 'HostServiceAI|HostServiceMethodAI|HostServiceAI[A-Za-z]+Request|protocol_hostservice_ai_codec|dispatchAI|wasm_host_service_ai|domainhostcall_ai|pkg/plugin/capability/aicap|capability/aicap' apps/lina-plugins -S`仅命中`linapro-ai-core`双语 README 的禁止旧 core 路径说明；`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/internal/hostservice -count=1`、`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/protocol/hostservices ./pkg/plugin/pluginbridge/protocol -count=1`、`cd apps/lina-core && go test ./pkg/plugin/pluginbridge -count=1`、`cd apps/lina-core && go test ./pkg/plugin/capability/... -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/wasm -count=1`、`cd apps/lina-core && go test ./internal/controller/plugin -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/runtime -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/store -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/upgrade -count=1`、`cd apps/lina-core && go test ./internal/service/plugin -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`cd hack/tools/linactl && go test ./internal/wasmbuilder -count=1`、`cd hack/tools/linactl && go run . plugins.check`、`openspec validate plugin-owned-domain-capabilities --strict`、`git diff --check`和`git -C apps/lina-plugins diff --check`均通过。
- `5.5`：已确认本变更不新增业务表、字段、索引、Seed DML、Mock 数据、插件安装 SQL 或卸载 SQL；未生成或修改 DAO、DO、Entity，未触发`make db.init`或`make dao`流程。SQL 文件管理影响：无宿主或插件`manifest/sql/`变更，因此不需要新增本迭代 SQL 文件，单迭代单 SQL 文件规则无适用对象。SQL 幂等性、数据分类、自增主键写入和软删除语义影响：均无 SQL 交付物，未引入只能执行一次的 DDL/DML、未混入 Mock 数据、未写入自增`id`、未修改`deleted_at`语义。查询性能和索引影响：本任务只记录数据库影响，不新增高频查询路径、表结构或查询辅助字段；本变更中 owner-aware host service 与依赖治理的性能验证继续由相关 Go 单元测试和后续`6.3`覆盖。DI、缓存一致性、数据权限、API、`i18n`、前端和开发工具跨平台影响：本任务为数据库治理记录，不新增运行期依赖、缓存后端、数据访问路径、HTTP 契约、文案、页面或工具入口；不触发新增 E2E。
- 本次`5.5`补充验证：`git diff --name-only -- '*.sql' '*/manifest/sql/*'`、`git status --short -- '*.sql' '*/manifest/sql/*'`、`git -C apps/lina-plugins diff --name-only -- '*.sql' '*/manifest/sql/*'`、`git diff --name-only -- '*/internal/dao/*' '*/internal/model/do/*' '*/internal/model/entity/*'`、`git status --short -- '*/internal/dao/*' '*/internal/model/do/*' '*/internal/model/entity/*'`和`git -C apps/lina-plugins diff --name-only -- '*/internal/dao/*' '*/internal/model/do/*' '*/internal/model/entity/*'`均无输出；`openspec validate plugin-owned-domain-capabilities --strict`、`git diff --check`和`git -C apps/lina-plugins diff --check`均通过。
- `6.1`：已补齐 owner capability 单元测试闭环。`capregistry`既有测试覆盖 descriptor 注册、owner/method 索引、重复 descriptor、重复 method、缺失 owner/service/version/method 和 nil registry；`pluginhost`新增声明入口 owner 校验，拒绝 owner 与 source plugin ID 不一致的 descriptor；`plugin_host_services_test`新增 source descriptor 启动 registry 索引测试和启动汇总 owner 校验测试，防止自定义`SourcePluginDefinition`绕过声明 facade；`wasm_host_service_test`新增未知 owner、版本不满足、已授权但未注册 method 三类 owner-aware dispatcher 拒绝用例，并继续覆盖缺 handler、未授权 method、成功转发和错误脱敏。owner 禁用/可用性由`TestSourceProviderAvailabilityFollowsEnabledSnapshot`和`TestDisableBlocksInstalledReverseOwnerHostServiceDependency`覆盖；provider 冲突由`pkg/plugin/capability/internal/capabilityregistry`既有 manager 测试覆盖；缓存失效由`TestRuntimeCacheChangeInvalidatesManagementList`和 runtime cache wiring 测试覆盖。DI 影响：本任务只新增 descriptor 校验和单元测试，未新增运行期服务、构造函数依赖、共享实例或缓存后端；启动期 registry 仍由`buildSourceCapabilityRegistry()`从`pluginhost.ListSourcePlugins()`构建并注入既有`ownerCapabilities`。缓存一致性影响：未新增缓存类型、失效触发点或跨实例状态；本任务只复验已有 runtime cache invalidation 测试。数据权限影响：未新增读取、写入、导出、下载、聚合、授权关系变更或执行动作；owner 调用仍在 dispatcher 侧先经 descriptor 与授权快照拒绝，owner handler 数据访问边界不变。数据库、API、`i18n`、前端和开发工具跨平台影响：无 SQL、DAO/DO/Entity、HTTP 路由/DTO、用户可见文案、语言包、前端页面或工具入口变更；不触发新增 E2E。
- 本次`6.1`补充验证：`cd apps/lina-core && go test ./pkg/plugin/pluginhost -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/wasm -count=1`、`cd apps/lina-core && go test ./pkg/plugin/capability/capregistry -count=1`、`cd apps/lina-core && go test ./pkg/plugin/capability/internal/capabilityregistry -count=1`、`cd apps/lina-core && go test ./internal/service/plugin -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/integration -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend -count=1`、`openspec validate plugin-owned-domain-capabilities --strict`、`git diff --check`和`git -C apps/lina-plugins diff --check`均通过。
- `6.2`：已补齐 dynamic host service 测试闭环。`catalog`测试覆盖 owner-aware`plugin.yaml hostServices.owner`必须匹配`dependencies.plugins`并提供版本范围、uploaded runtime manifest 不能省略 owner 依赖、install/enable snapshot 构建会再次拒绝未声明 owner 依赖；`runtime_artifact_test`既有用例覆盖 artifact 解析恢复 owner、service、version 和 method，本任务新增`TestParseRuntimeArtifactRejectsOwnerHostServiceWithoutDependency`，验证嵌入 artifact 的 owner host service section 不能绕过 owner 依赖校验；`store_authorization_test`覆盖授权快照按`owner/service/version`识别并只保留确认的 resource refs；`wasm_host_service_test`覆盖未授权 method 拒绝、未知 owner、版本不满足、已授权但未注册 method、缺 handler 和 owner handler 成功转发；`controller`测试覆盖授权展示、授权输入和 upgrade preview 投影保留 owner 来源。DI、缓存一致性、数据权限、数据库、API、`i18n`、前端影响：本任务只新增动态 artifact 单元测试和复验既有动态 host service 测试，未新增运行期依赖、缓存后端、数据访问路径、SQL、HTTP 契约、用户可见文案、语言包或前端页面；不触发新增 E2E。开发工具跨平台影响：仅复验`linactl`的 Go wasmbuilder 测试，未新增脚本、命令或平台专属逻辑。
- 本次`6.2`补充验证：`cd apps/lina-core && go test ./internal/service/plugin/internal/runtime -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/store -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/wasm -count=1`、`cd apps/lina-core && go test ./internal/controller/plugin -count=1`、`cd hack/tools/linactl && go test ./internal/wasmbuilder -count=1`、`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/internal/hostservice ./pkg/plugin/pluginbridge/protocol -count=1`、`openspec validate plugin-owned-domain-capabilities --strict`、`git diff --check`和`git -C apps/lina-plugins diff --check`均通过。
- `6.3`：已补齐 dependency 测试闭环。`dependency_resolver_test`新增 owner-aware 反向升级诊断测试，验证候选版本不满足下游范围时返回`reverse_dependency_version` blocker，并只保留目标 owner 的`ownerHostServices`方法摘要；既有 resolver 测试继续覆盖缺失依赖、版本不满足、反向禁用阻断和通用反向升级阻断。`plugin_dependency_test`新增 source owner 升级集成测试，验证已安装动态 consumer 声明`hostServices.owner`和`dependencies.plugins`后，owner 从`v0.1.0`升级到`v0.2.0`会被`CodePluginReverseDependencyBlocked`阻断，且有效版本和启用状态保持不变；既有 owner 禁用测试继续覆盖反向禁用阻断和 owner host service 摘要投影。`plugin_list_test`新增首屏 summary 测试，动态插件声明 owner host service 依赖时，`List`仍不附带`DependencyCheck`、不返回 detail-only host service 投影，并通过`startupstats`断言只发生一次 manifest scan、一次 catalog snapshot build、零 integration snapshot build，避免首屏列表触发完整依赖检查。DI 影响：本任务只新增单元和集成测试，未新增运行期服务、构造函数依赖、共享实例或缓存后端。缓存一致性影响：未新增缓存类型、失效触发点或跨实例状态；仅验证 summary list 复用既有 management list/read snapshot 路径。数据权限影响：测试只读取插件治理元数据、registry/release snapshot 和 owner host service 声明，不新增业务数据读取、写入、导出、下载、聚合、授权关系变更或执行动作。数据库、API、`i18n`、前端和开发工具跨平台影响：无 SQL、DAO/DO/Entity、HTTP 路由/DTO、用户可见文案、语言包、前端页面、脚本或工具入口变更；不触发新增 E2E。
- 本次`6.3`补充验证：`cd apps/lina-core && go test ./internal/service/plugin/internal/dependency -count=1`、`cd apps/lina-core && go test ./internal/service/plugin -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/lifecycle -count=1`和`cd apps/lina-core && go test ./internal/service/plugin/internal/upgrade -count=1`均通过。
- `6.4`：已补齐 import boundary 测试闭环。`plugingovernance`既有 fixture 覆盖跨插件生产代码 import owner `backend/internal/controller`、`backend/internal/dao`、`backend/internal/model/do`、`backend/internal/model/entity`、provider adapter、`backend/internal/service`和`backend/pkg`均触发`plugin-cross-plugin-private-import`；`TestScanAllowsDeclaredCrossPluginCapImport`覆盖声明 owner 依赖和版本范围后跨插件`backend/cap/...`成功、自插件`backend/cap/...`成功。本任务新增`TestScanAllowsCrossPluginPrivateImportsOnlyInTests`，锁定`*_test.go`中的跨插件 internal/pkg import 作为受控测试例外不会产生 finding，同时同 fixture 中生产`backend/pkg`入口仍被阻断，防止测试例外扩大到生产路径。开发工具跨平台影响：仅补充`hack/tools/linactl/internal/plugingovernance`Go 测试，生产扫描器仍使用 Go 标准库`filepath`、`os`和`go/parser`，未新增 shell、PowerShell、平台专属脚本或命令入口；本轮在 macOS 执行单元测试和真实仓库 smoke，Windows/Linux 由同一 Go 实现和 CI 复用命令覆盖。DI、缓存一致性、数据权限、数据库、API、`i18n`、前端影响：无运行期服务、缓存、业务数据访问、SQL、HTTP 契约、用户可见文案、语言包或页面变更；不触发新增 E2E。
- 本次`6.4`补充验证：`cd hack/tools/linactl && go test ./internal/plugingovernance -count=1`、`cd hack/tools/linactl && go run . plugins.check`、`openspec validate plugin-owned-domain-capabilities --strict`、`git diff --check`和`git -C apps/lina-plugins diff --check`均通过。
- `6.5`：已补齐`linapro-ai-core`包测试闭环。`backend/cap/aicap/aicap_test.go`新增方法状态批量读取测试，覆盖文本、图片和未知能力在无 provider 时稳定降级为 unavailable，并验证`MaxMethodStatusBatchSize`超限返回`CodeCapabilityLimitExceeded`；`backend/cap/aicap/spi/spi_test.go`新增 owner descriptor 元数据测试，锁定`SourceContract`、`DynamicContract`、method risk、resource kind、payload 名称，并确认`computer.act`和`ui.operate`不进入本轮`AI`边界；`backend/cap/aicap/bridge/bridge_test.go`新增空请求、 malformed response、空 response、nil invoker 和 transport 错误降级测试，确认 bridge SDK 使用 owner-aware envelope 且状态读取不会泄露 transport 错误中的`sk-*`敏感片段；`backend/plugin_test.go`新增源码插件实际注册断言，确认`pluginhost`中`linapro-ai-core`声明了`AI`owner descriptor 与关键方法；`backend/internal/service/ai/ai_adapter_test.go`将 provider HTTP 错误脱敏覆盖扩展到 OpenAI-compatible 与 Anthropic-compatible 双协议。DI 影响：本任务只新增单元测试和测试断言，未新增运行期服务、构造函数依赖、共享实例或缓存后端；provider 注册测试只读取启动期已注册的 source plugin descriptor。缓存一致性影响：未新增缓存、快照、失效触发点或跨实例状态；状态降级测试只验证 fallback 和 guest transport 错误降级。数据权限影响：未新增读取、写入、导出、下载、聚合、授权关系变更或执行动作；测试不访问业务数据边界。数据库影响：无 SQL、迁移、seed、mock 数据、DAO、DO 或 Entity 变更。API 影响：未修改 HTTP 路由、请求 DTO、响应 DTO、`g.Meta`、`dc`或`eg`标签。`i18n`影响：未修改运行时用户可见文案、API 文档源文本、错误码、插件清单或语言包；敏感错误测试仅验证现有结构化错误不泄露 provider 响应体。前端影响：无页面、路由、组件、API 调用、权限交互或用户可观察行为变更。开发工具跨平台影响：未新增或修改脚本、`linactl`命令、构建入口或治理工具。E2E 策略：本任务为插件后端契约与 SDK 单元覆盖，不涉及用户可观察页面或端到端工作流，后续`6.6`和`6.7`继续覆盖 owner 授权与`AI`插件端到端路径。
- 本次`6.5`补充验证：`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/cap/aicap/... -count=1`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend -count=1`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/internal/service/ai -count=1`、`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/... -count=1`、`openspec validate plugin-owned-domain-capabilities --strict`、`git diff --check`和`git -C apps/lina-plugins diff --check`均通过。
- `6.6`：已新增宿主插件框架 E2E `hack/tests/e2e/extension/plugin/TC017-owner-capability-authorization.ts`，覆盖动态插件 owner 授权来源展示、安装授权 payload 保留`owner/service/version/methods/resourceRefs`、缺失 owner 依赖阻断和 owner 版本不满足阻断。宿主前端 owner-aware 展示同步补齐：`HostServicePermissionItem`、授权输入和升级 diff 前端类型保留`owner`与`version`；host service 卡片按`owner/service/version`归一化 identity，展示来源插件与能力版本标签，避免同名`service`跨 owner 合并；安装、启用和升级重新授权提交 payload 保留 owner 与 version；升级 diff 文案保留 owner 来源。根 E2E 使用合规的合成 owner 插件 ID`plugin-dev-owner-ai-core-e2e`，不在根套件直接引用官方源码插件 ID，真实`linapro-ai-core`端到端覆盖留给`6.7`插件自有测试。API 影响：未修改后端 HTTP 路由、DTO、`g.Meta`、`dc`或`eg`，仅让前端类型与既有后端 owner-aware DTO 字段对齐。`i18n`影响：新增宿主前端运行时语言键`pages.system.plugin.hostServices.identity.owner`和`pages.system.plugin.hostServices.identity.version`，已同步`zh-CN`和`en-US`；E2E 对关键文案和原始`pages.system.plugin`key 缺失进行断言。数据权限影响：未新增业务数据读取、写入、导出、下载、聚合或执行动作；授权关系 payload 仅保留 owner 身份和资源引用，仍由既有安装/启用授权边界处理。DI、缓存一致性、数据库、后端和开发工具跨平台影响：未新增运行期依赖、缓存后端、SQL、后端服务装配或脚本工具入口。前端影响：插件管理授权弹窗和升级预览增加来源插件、能力版本展示及 owner-aware identity 分组，属于用户可观察行为，已通过 E2E 与截图验证覆盖。E2E 质量审查：触发原因是新增宿主插件框架 E2E 和用户可观察授权展示；覆盖成功授权 payload、依赖缺失失败路径、版本不满足失败路径、关键翻译文案和截图人工审查，测试文件可单独运行且不依赖其他 E2E 残留状态。
- 本次`6.6`补充验证：`pnpm -C apps/lina-vben/apps/web-antd typecheck`、`pnpm -C apps/lina-vben/apps/web-antd i18n:check`、`pnpm -C hack/tests test:validate`和`pnpm -C hack/tests exec playwright test e2e/extension/plugin/TC017-owner-capability-authorization.ts --project=chromium`均通过；截图证据为`temp/20260710/105853-tc017-owner-authorization.png`、`temp/20260710/105859-tc017-missing-owner-dependency.png`和`temp/20260710/105904-tc017-owner-version-unsatisfied.png`，已确认无明显原始`i18n`key、布局重叠或错误 toast。补充治理验证：`openspec validate plugin-owned-domain-capabilities --strict`、`git diff --check`和`git -C apps/lina-plugins diff --check`将在进入`6.7`前复跑记录。
- `6.7`：已新增`linapro-ai-core`插件本地 E2E `apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC007-ai-owner-capability-contract.ts`。TC-7a 安装并启用真实`linapro-ai-core`后读取`AI`档位 API 与智能中心档位页，断言文本/图像/向量/音频/视觉理解/文档理解/安全审核/视频能力页签和`basic`、`standard`、`advanced`档位稳定展示；TC-7b 禁用`linapro-ai-core`后断言插件 API 返回失败、`/ai/tiers`路由降级到 404 页面，并在截图前等待菜单加载提示消失；TC-7c 构建当前`linapro-demo-dynamic.wasm`，安装并启用`linapro-demo-source`和动态 demo，先验证动态 demo 未启用时`host-call-demo`路由失败，再启用后验证插件详情`authorizedHostServices`保留`owner=linapro-ai-core`、`service=ai`、`version=v1`和`text.method_status.get`，并通过`/x/linapro-demo-dynamic/api/v1/host-call-demo`读取 owner-aware`AI`文本方法状态。测试实现中对动态 demo 增加安装前后硬清理，只删除该测试插件在`sys_plugin_state`、`sys_plugin_node_state`、`sys_plugin_resource_ref`、`sys_plugin_migration`、`sys_plugin_release`和`sys_plugin`中的治理行，避免同版本旧 release snapshot 干扰 owner 字段验证；同时修正本测试本地`unwrapApiData`，只在响应存在数值`code`时解包`data`，避免动态直连路由业务字段`data`被误判为 API envelope。
- 本次`6.7`影响判断：`i18n`无新增运行时语言键、菜单文案、API 文档源文本或插件语言包变更；E2E 对智能中心中文文案和原始`plugin.linapro-ai-core`key 缺失进行断言。API 影响为测试调用既有插件管理 API、`linapro-ai-core`档位 API 和动态 demo 路由，未修改后端 HTTP 路由、DTO、`g.Meta`、`dc`或`eg`。数据权限影响为测试只操作插件治理元数据和动态 demo 自有测试数据清理，不新增生产数据读取、写入、导出、下载、聚合或授权关系变更；owner AI host-call 仍经已授权`owner/service/version/method`快照进入 owner handler。数据库影响为无生产 SQL、迁移、Seed、Mock 数据、DAO、DO 或 Entity 变更；测试内 SQL 仅用于 E2E 隔离清理。缓存一致性和 DI 影响为无新增运行期服务、构造函数依赖、共享实例、缓存后端或失效路径；测试通过`make dev`重启本地服务以使用当前代码，并复验旧快照清理后 owner-aware artifact 可正常同步。前端影响为无页面、组件、路由或语言资源生产变更；用户可观察行为仅通过 E2E 覆盖。开发工具跨平台影响为未新增脚本、`linactl`命令或构建入口；测试沿用现有`make wasm`、Playwright 和 Postgres E2E helper。
- 本次`6.7`补充验证：`make dev`通过并启动`Lina Core`与`Lina Vben`；`pnpm -C hack/tests test:validate`通过；`pnpm -C hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC007-ai-owner-capability-contract.ts --project=chromium`通过，结果为`3 passed`。截图证据为`temp/20260710/20260710033427-tc007-ai-owner-enabled-status.png`和`temp/20260710/113439-TC007-ai-owner-disabled-degraded.png`，已确认无明显原始`i18n`key、布局重叠、错误 toast 或加载残留。
- `6.8`：已按当前工作区变更范围运行 core Go 编译门禁，覆盖插件 API DTO、启动绑定、controller、plugin service、capabilityhost、catalog、dependency、integration、lifecycle、runtime、store、upgrade、WASM owner dispatcher、capability、capregistry、pluginbridge、hostservice、protocol、hostservices catalog 和 pluginhost 等变更包。DI 影响：本任务为编译与单元测试门禁，不新增运行期服务、构造函数依赖、共享实例或缓存后端；`./internal/cmd`启动绑定测试已复验 owner descriptor registry、WASM runtime 和插件服务装配可编译。缓存一致性、数据权限、数据库、API、`i18n`、前端和开发工具跨平台影响：本任务只执行验证，不修改生产逻辑、SQL、DTO、语言包、页面或工具入口；不触发新增 E2E。
- 本次`6.8`补充验证：`cd apps/lina-core && go test ./api/plugin/v1 ./internal/cmd ./internal/cmd/internal/httpstartup ./internal/controller/plugin ./internal/service/plugin ./internal/service/plugin/internal/capabilityhost ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/dependency ./internal/service/plugin/internal/integration ./internal/service/plugin/internal/lifecycle ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/store ./internal/service/plugin/internal/testutil ./internal/service/plugin/internal/upgrade ./internal/service/plugin/internal/wasm ./pkg/plugin/capability ./pkg/plugin/capability/... ./pkg/plugin/pluginbridge ./pkg/plugin/pluginbridge/internal/hostservice ./pkg/plugin/pluginbridge/protocol ./pkg/plugin/pluginbridge/protocol/hostservices ./pkg/plugin/pluginhost -count=1`通过。
- `6.9`：已运行插件测试和治理验证。`linapro-ai-core`后端全包测试覆盖 owner 契约、SPI、bridge SDK、provider 注册、状态降级和错误脱敏；`linapro-demo-dynamic`后端全包测试与`make wasm dir=apps/lina-plugins/linapro-demo-dynamic`覆盖动态插件 owner-aware host call demo 编译和 WASM artifact 构建；`linactl`的`wasmbuilder`与`plugingovernance`测试、真实仓库`plugins.check`覆盖动态 artifact host service section、跨插件 import 边界和 owner 依赖声明。`i18n`影响：`pnpm -C apps/lina-vben/apps/web-antd i18n:check`和`make i18n.check`通过；`make i18n.check`仍有既有 48 个模块级`$t()`warning，退出码为`0`，且非本次变更新增。文档和资源检查：已验证`apps/lina-core/pkg/plugin/README.md`、`README.zh-CN.md`、`apps/lina-plugins/linapro-ai-core/README.md`和`README.zh-CN.md`存在；已用`json.tool`校验宿主和插件变更涉及的`manifest/i18n`JSON。数据库和生成代码影响：宿主与插件 SQL、`DAO`、`DO`、`Entity`diff 检查均无输出，确认本任务未引入 SQL 或生成代码变更。DI、缓存一致性、数据权限、API、前端和开发工具跨平台影响：本任务只执行验证；未新增运行期依赖、缓存后端、数据访问路径、HTTP 契约、页面或平台专属脚本；开发工具入口继续为 Go 实现并通过 macOS 本地 smoke，Windows/Linux 复用同一 Go 工具链和 CI 命令。
- 本次`6.9`补充验证：`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/... -count=1`、`cd apps/lina-plugins/linapro-demo-dynamic && GOWORK=off go test ./backend/... -count=1`、`cd hack/tools/linactl && go test ./internal/wasmbuilder ./internal/plugingovernance -count=1`、`make wasm dir=apps/lina-plugins/linapro-demo-dynamic`、`cd hack/tools/linactl && go run . plugins.check`、`pnpm -C apps/lina-vben/apps/web-antd i18n:check`、`make i18n.check`、`pnpm -C hack/tests test:validate`、`python3 -m json.tool`校验相关`manifest/i18n`JSON、双语 README 存在性检查、宿主与插件 SQL diff 检查、宿主与插件`DAO`/`DO`/`Entity`diff 检查均通过或无输出。
- `6.10`：已运行`openspec validate plugin-owned-domain-capabilities --strict`，严格校验通过。影响判断：本任务只执行 OpenSpec 治理校验并更新任务记录，不新增运行期依赖、缓存、数据权限路径、数据库脚本、HTTP 契约、前端行为、开发工具入口或`i18n`资源；文档治理影响仅为`tasks.md`记录更新，遵循本变更中文文档语言和 Markdown inline code 规则。
- 本次`6.10`补充验证：`openspec validate plugin-owned-domain-capabilities --strict`通过，输出`Change 'plugin-owned-domain-capabilities' is valid`。
- `7.1`：最终影响总结已覆盖要求的规则域。`i18n`：`linapro-ai-core`启用`i18n.enabled: true`，本变更同步维护插件错误码、动态授权展示名、宿主前端 owner/version 展示键和相关`apidoc`资源；全仓`make i18n.check`与前端`i18n:check`通过，既有 48 个模块级`$t()`warning 非本次新增。缓存一致性：能力注册表、授权快照、owner 可用性和 runtime catalog 按关键运行时数据治理；实现复用启动期 descriptor registry、release manifest snapshot、既有 runtime cache notifier/publisher/freshener 和事务后刷新路径，未新增缓存后端；owner 禁用、升级、授权变化由单元测试、集成测试和 E2E 验证不会在过期授权下继续成功调用。数据权限：本变更不新增业务数据列表、详情、导出、下载、聚合或批量接口；owner-aware host call 保留调用插件、owner、service、version、method、resource refs 和授权快照，owner handler 后续访问宿主或插件业务数据时仍必须在 owner 查询阶段接入数据权限。数据库：未新增业务表、字段、索引、Seed、Mock、安装 SQL、卸载 SQL、`DAO`、`DO`或`Entity`生成物；SQL 和生成代码 diff 检查均无输出。API：后端 HTTP 路由和方法语义保持不变，变更集中在 owner-aware DTO 投影、授权快照字段、upgrade diff、动态 demo 响应和 API 文档翻译；时间字段契约无新增影响，高频列表不做完整依赖展开并由`6.3`测试验证。前端：插件管理授权弹窗、host service 卡片和升级预览展示 owner/version 来源，用户可观察行为由`TC017`覆盖；`linapro-ai-core`真实启用、禁用和动态 demo host-call 路径由`TC007`覆盖，截图已审查无明显原始`i18n`key、布局重叠、错误 toast 或加载残留。开发工具跨平台：治理扫描和 WASM 打包继续使用`linactl`Go 子组件，未新增 shell、PowerShell 或平台专属脚本；本地 macOS 测试通过，Windows/Linux 复用 Go 标准库路径处理和 CI 命令。测试策略：采用 Go 单元/集成测试覆盖 descriptor、catalog、dependency、store、WASM dispatcher、bridge SDK、插件后端和动态 demo；采用`plugins.check`、`i18n`、JSON、README、SQL/生成代码、OpenSpec 和 diff 检查覆盖治理；采用`TC017`与`TC007`覆盖核心端到端成功和失败路径。DI 来源：新增运行期依赖集中为启动期 owner descriptor registry，从`buildSourceCapabilityRegistry()`创建，经`serviceImpl.New`传递到 WASM runtime 并复用同一启动期快照；本轮`6.8`、`6.9`、`6.10`和`7.1`只执行验证与任务记录，不新增 DI、缓存、数据权限、数据库、API、前端或工具入口。
- 本次`7.1`补充验证：`openspec validate plugin-owned-domain-capabilities --strict`通过，输出`Change 'plugin-owned-domain-capabilities' is valid`。
- `7.2`：已按`lina-review`完成全变更规范合规审查，范围来源为`git status --short`、`git ls-files --others --exclude-standard`、`git -C apps/lina-plugins status --short`、`git -C apps/lina-plugins diff --name-only`、`git -C apps/lina-plugins ls-files --others --exclude-standard`和本变更 OpenSpec 上下文。已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/testing.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/architecture.md`、`.agents/rules/api-contract.md`、`.agents/rules/data-permission.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/dev-tooling.md`、`.agents/rules/database.md`、`.agents/rules/frontend-ui.md`、`.agents/rules/i18n.md`和`.agents/instructions/markdown-format.instructions.md`；已使用`goframe-v2`、`lina-e2e`和`lina-review`技能完成对应审查。插件本地规范检查结果：`apps/lina-plugins/linapro-ai-core/AGENTS.md`和`apps/lina-plugins/linapro-demo-dynamic/AGENTS.md`均不存在，按仓库顶层规范执行。发现的问题：严重问题已修复，`linapro-ai-core/backend/cap/aicap/spi.TextProviderDescriptor(factory)`原先只返回纯`Descriptor()`，会导致源码插件实际注册的 owner descriptor 缺少运行期`Invoker`，动态 owner 调用在`dispatchOwnerHostService`命中`registered.Invoker == nil`后返回`owner host service handler not registered`。修复后`capregistry.Invocation`携带 owner-scoped`capability.Services`，`dispatchOwnerHostService`通过`capabilityowner.ServicesForPlugin(hcc.runtime.domainServices, owner)`注入 owner 作用域服务，`TextProviderDescriptor`挂载`newTextProviderInvoker(factory)`，`linapro-ai-core`新增生产 invoker 负责 typed DTO 解码、状态查询、业务错误 envelope 和敏感错误脱敏，测试覆盖 descriptor invoker 非空、注册后 method invoker 保留、真实`text.generate`和`text.method_status.get`分发以及 owner-scoped services。审查结论：未发现剩余阻塞问题；后端 Go、插件边界、架构、API、数据权限、缓存一致性、数据库、前端/UI、`i18n`、E2E、开发工具跨平台和文档同步均通过当前变更门禁。工作区中另有`hack/tools/linactl/internal/agents`相关已暂存改动与本 OpenSpec 主题不同，未作为本变更完成依据，未回退；本变更命中的开发工具审查集中在`plugingovernance`、`wasmbuilder`和`plugins.check`路径。
- `7.3`：已根据`lina-review`反馈完成修复并复验。修复影响判断：`i18n`无新增运行时文案、API 文档源文本、错误码或语言包；缓存一致性无新增缓存后端，owner-scoped services 从启动期`domainServices`目录按 owner 派生；数据权限无新增业务数据列表、详情、导出、下载、聚合或写动作，owner 调用仍经授权快照和 owner handler 数据权限边界；数据库无 SQL、Seed、Mock、`DAO`、`DO`或`Entity`变更；API 无 HTTP 路由、DTO、`g.Meta`、`dc`或`eg`变更；前端无页面、路由、组件或用户可观察行为变更；开发工具无新增脚本、`linactl`命令或平台专属入口；DI 来源已记录为启动期`domainServices`共享目录到 owner dispatcher invocation 的显式传递，不在请求路径临时`New()`关键服务。补充验证：`cd apps/lina-core && go test ./pkg/plugin/capability/capregistry ./internal/service/plugin/internal/wasm -count=1`通过，`cd apps/lina-plugins/linapro-ai-core && GOWORK=off go test ./backend/cap/aicap/spi ./backend -count=1`通过；此前已完成更大范围`apps/lina-core`插件相关包测试、`linapro-ai-core`后端全包测试、`linapro-demo-dynamic`后端全包测试、`make wasm`、`plugins.check`、`make i18n.check`、前端`i18n:check`、E2E`TC017`和`TC007`。最终治理校验已复跑，`openspec validate plugin-owned-domain-capabilities --strict`、`git diff --check`和`git -C apps/lina-plugins diff --check`均通过，`openspec instructions apply --change plugin-owned-domain-capabilities --json`显示`41/41`完成并进入`all_done`状态。

- 审查后简化（2026-07-10）：已收敛 owner invoker 到`aicap.Service`（`aicap.NewOwnerInvoker`/`aicap.ProviderDescriptor`），删除`spi/spi_invoker.go`镜像 DTO 与巨型 switch；动态 descriptor 仅发布可运行方法`text.generate`、`text.method_status.get`、`ai.methods.status.batch_get`，多模态 DTO/常量仍保留在 owner 契约；`wasm_host_service_owner`去掉同包双 alias；`CatalogFromDescriptors`改为纯值投影；bridge 非文本子客户端共用`subClient`状态样板；同步更新双语 README 与相关 OpenSpec specs/design。验证：`GOWORK=off go test ./backend/cap/aicap/... ./backend -count=1`、`go test ./pkg/plugin/pluginbridge/... ./pkg/plugin/pluginhost ./internal/service/plugin/internal/wasm ./pkg/plugin/pluginbridge/protocol/hostservices -count=1`、`openspec validate plugin-owned-domain-capabilities --strict`通过。

- CapabilityID 命名改进（2026-07-10）：将 AI 能力族 ID 从`framework.ai.*`改为 plugin-owned 形式`plugin.linapro-ai-core.ai.<family>.v1`；Go 常量同步为`CapabilityTextV1`等更短名称；根命名空间为`plugin.linapro-ai-core.ai.v1`。验证：`GOWORK=off go test ./backend/cap/aicap/... ./backend -count=1`、core hostservices/plugin/wasm 相关测试、`openspec validate --strict`通过。

- 文件命名规范改进（2026-07-10）：将`aicap_i18n_test.go`合并入`aicap_test.go`以满足单元测试与源码文件成对命名；将`backend/cap/aicap/aicommon`重命名为职责更清晰的`aitypes`（含`aitypes.go`/`aitypes_code.go`）并更新全部 import/引用。验证：`GOWORK=off go test ./backend/cap/aicap/... ./backend -count=1`。
