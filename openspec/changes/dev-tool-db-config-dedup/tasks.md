## 1. 开发态配置收敛

- [x] 1.1 调整 `apps/lina-core/hack/config.yaml`，使用 YAML anchor 收敛宿主开发态数据库连接配置并移除 `multiStatements=true`
- [x] 1.2 检查并更新依赖 `hack/config.yaml` 的开发态读取逻辑，确保统一连接参数后升级工具与本地命令仍能正常读取配置

## 2. 本地 SQL 执行链路改造

- [x] 2.1 在 `apps/lina-core/internal/cmd/` 中实现 SQL 文件拆分与逐语句执行逻辑，保持顺序执行和失败即停语义
- [x] 2.2 调整错误与日志上下文，确保逐语句执行失败时仍能定位到对应 SQL 文件

## 3. 测试与验证

- [x] 3.1 补充命令层单元测试，覆盖多语句拆分、注释/空白跳过、字符串中分号处理与失败中断场景
- [x] 3.2 运行受影响的 Go 单元测试并记录验证结果，确认开发态工具脚本调整后行为稳定
