## 1. 跨平台安装脚本入口

- [x] 1.1 在仓库根 `hack/scripts/install/` 下新增 `install.sh` 与 `install.ps1`，统一定义仓库、`ref`、当前目录/指定目录、覆盖保护等核心参数语义与帮助信息
- [x] 1.2 在两个脚本中实现源码归档下载、临时目录解压、动态顶层目录识别与目标目录落地逻辑，确保主流程不依赖 `git clone`
- [x] 1.3 实现安全目录策略：默认落地到安全目录，显式支持当前目录模式，并在目标目录非空且未允许覆盖时拒绝继续执行

## 2. 安装后引导与文档说明

- [x] 2.1 在安装脚本末尾增加 `Go`、`Node.js`、`pnpm`、`MySQL`、`make` 等关键依赖的环境体检输出与项目路径提示
- [x] 2.2 输出统一的安装后下一步引导，明确 `make init`、`make mock`、`make dev` 等推荐操作及注意事项
- [x] 2.3 更新仓库根 `README.md` 与 `README.zh_CN.md`，补充 `macOS/Linux` 和 `Windows` 的快速安装示例、参数说明及官方入口映射关系

## 3. 验证与回归

- [x] 3.1 为安装脚本补充自动化验证或最小可执行 smoke test，覆盖参数解析、归档下载地址生成、目录保护与错误提示等核心行为
- [x] 3.2 运行脚本相关验证，确认 `macOS/Linux` 与 `Windows` 两条入口在核心参数契约上保持一致

## Feedback

- [x] **FB-1**: 将安装脚本目录约定从 `hack/scripts/` 调整为 `hack/scripts/install/`，并同步更新 proposal、design、spec 与 tasks 中的路径说明
- [x] **FB-2**: 为安装脚本 smoke test 增加可复用的本地/CI 执行入口，避免验证方式只停留在手工命令
- [x] **FB-3**: 在 `hack/scripts/install/` 目录下新增中英文说明文档，介绍安装脚本用途、参数、使用方式与验证方法
- [x] **FB-4**: 当用户不显式传入 `ref` 时，默认解析并安装仓库最新稳定标签版本；若不存在稳定标签，则回退到默认主分支，并在输出中展示最终解析的引用值
- [x] **FB-5**: 将仓库级独立 Go 工具统一迁移到 `hack/tools/`，并同步更新构建、升级、测试与规范中的目录引用
- [x] **FB-6**: 移除 `sys_job` 中内置清理任务的 SQL 种子数据，改为仅通过源码注册与启动投影同步生成
- [x] **FB-7**: 拆分核心 HTTP 启动函数并补充关键逻辑注释，降低 `cmd_http.go` 的单函数复杂度
- [x] **FB-8**: 统一开发构建与后端运行时相对路径到仓库根 `temp/`，避免在 `apps/lina-core` 下生成重复 `temp` 目录
- [x] **FB-9**: 修复定时任务列表中定时表达式列在深色主题下对比度不足、内容难以辨认的问题
