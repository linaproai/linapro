## 1. 规则、边界和现状确认

- [x] 1.1 在实施前重新读取 `AGENTS.md` 和命中的规则文件，记录 `i18n`、缓存一致性、数据权限、插件边界、开发工具跨平台和测试策略影响分析
  - 实现记录：本轮已读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`database.md`、`cache-consistency.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`i18n.md`、`dev-tooling.md`。影响分析：新增插件菜单、页面、API 文档和错误文案命中 `i18n`；档位解析缓存命中缓存一致性，权威源为插件数据库，写后失效并用短 TTL 兜底；管理 API 为平台配置控制面，租户上下文拒绝，动态插件调用通过 host service resource 授权；插件资源归属 `apps/lina-plugins/linapro-ai-core/`，不得回流宿主；本次不新增长期脚本，生成流程使用既有 `make`/Go 工具入口；测试策略包含宿主能力、动态 host service、插件 service/adapter 单测和插件自有 E2E。
- [x] 1.2 评估宿主文本能力、动态 host service、官方插件后端、前端页面和 E2E 是否适合 subagent 并行推进，并在实现记录中说明采用或不采用的理由
  - 实现记录：这些工作天然可按宿主能力、动态插件协议、插件后端、前端页面、E2E 分片并行；但当前上层工具规则要求只有用户明确要求 subagent/并行代理工作时才使用 subagent，本轮用户未明确要求，因此不启动 subagent，采用本地串行推进并保留分层任务记录。
- [x] 1.3 对照组织和租户能力实现，确认 `framework.ai.text.v1` 的宿主契约、provider factory、fallback/status、provider 冲突和可用性检查接入位置
  - 实现记录：对照 `pkg/plugin/capability/orgcap`、`tenantcap` 和 `capability/internal/capabilityregistry`，文本能力将新增独立 `aitext` 包，使用 `framework.ai.text.v1`、官方 provider `linapro-ai-core`、typed `ProviderEnv`、`ProviderRuntime`、`ProviderFactory` 和共享 registry 的单例 provider 冲突检测。`Available`/`Status` 使用 `StatusWithProvider`，provider 缺失、禁用、冲突或构造失败均返回不可用状态；`GenerateText` 在 provider 不可用、无效档位或无消息时返回 `bizerr` 结构化错误。
- [x] 1.4 修改 `apps/lina-plugins/linapro-ai-core/` 前检查该插件根目录是否存在 `AGENTS.md`，并读取插件本地规范或记录不存在的判断
  - 实现记录：执行 `find apps/lina-plugins/linapro-ai-core -maxdepth 2 -name AGENTS.md -print` 时目录尚不存在，因此创建前没有插件本地 `AGENTS.md` 可读取；后续如果该插件根目录新增本地 `AGENTS.md`，修改插件内文件前必须先读取。
- [x] 1.5 确认 `linapro-ai-core` 的 `plugin.yaml` 中 `i18n.enabled` 策略、密钥存储能力来源、菜单权限命名和源码插件装配约定
  - 实现记录：官方插件策略对齐 `linapro-org-core`/`linapro-tenant-core`，`plugin.yaml` 启用 `i18n.enabled: true`，默认 `zh-CN`，维护 `en-US` 与 `zh-CN` 插件资源；密钥首期不在 API 响应返回明文，数据库只保存 `api_key_secret_ref`/脱敏引用，若宿主后续提供 secret service 再接入解析；菜单权限使用 `ai:provider:*`、`ai:tier:*`、`ai:invocation:list`，动态插件授权使用 `host:ai:text`；源码插件按 `plugin_embed.go`、`backend/plugin.go` registrar/provider 和插件自有 `backend/internal/service` 结构装配。

## 2. 宿主文本能力和动态插件 host service

- [x] 2.1 在宿主能力目录中新增 `framework.ai.text.v1` 文本能力组件，定义 capability ID、`Service`接口、DTO、`ThinkingEffort` 枚举、usage、状态和结构化错误
  - 实现记录：新增 `apps/lina-core/pkg/plugin/capability/aitext/`，定义 `CapabilityAITextV1`、`Tier`、`ThinkingEffort`、`Message`、`GenerateRequest`、`GenerateResponse`、`Usage`、`Provider`、`Service` 和集中 `bizerr` 错误码。`GeneratedAt` 使用 Unix 毫秒时间戳契约。
- [x] 2.2 实现文本能力 provider factory 声明、单例 provider 管理、`Available(ctx)`、`Status(ctx)`、`GenerateText(ctx, request)` 和 provider 不可用 fallback
  - 实现记录：`aitext.Provide` 接入共享 `capabilityregistry`，复用单例 provider、冲突检测和 provider 构造失败状态；`GenerateText` 在目的、档位、消息、`thinkingEffort`、metadata 边界校验后才委托 provider，provider 缺失或冲突统一返回 `AI_TEXT_PROVIDER_UNAVAILABLE`。
- [x] 2.3 将文本能力接入源码插件可消费的 capability services 目录，确保消费方只依赖宿主契约，不依赖 `linapro-ai-core/backend/internal/**`
  - 实现记录：`capability.Services`、`pluginhost.Services`、`hostservices` 目录、HTTP 启动组合根和插件 provider runtime 已新增 `AIText()`/`AITextProviderEnv`。源码插件消费方只依赖 `pkg/plugin/capability/aitext`；本阶段未引入也未引用 `linapro-ai-core/backend/internal/**`。
- [x] 2.4 扩展动态插件 host service 协议，新增 `ai.text.generate` 的 service/method 校验、`purpose:<name>` 资源授权、策略属性校验和脱敏审计
  - 实现记录：`pluginbridge` 新增 `HostServiceAI`、`HostServiceMethodAITextGenerate` 和 `host:ai:text` capability；manifest 校验要求 `resources[].ref` 使用 `purpose:<name>`，仅允许 `defaultTier` 与 `maxOutputTokens` 策略属性；WASM dispatcher 在 provider 调用前校验 purpose 匹配和输出上限，并对包含 `authorization`、`bearer`、`sk-` 等标记的错误做脱敏响应。
- [x] 2.5 扩展动态插件 guest client 或调用封装，支持 `purpose`、`tier`、`messages`、`maxOutputTokens`、`temperature`、可选 `thinkingEffort` 和 `metadata`
  - 实现记录：新增 `guest.AIText()` 与 `AITextService.GenerateText`，通过 `purpose:<name>` resourceRef 调用 `ai.text.generate`，payload 支持 `purpose`、`tier`、`messages`、`maxOutputTokens`、`temperature`、`thinkingEffort` 和短 metadata。
- [x] 2.6 为文本能力和 `ai.text.generate` 增加 Go 单元测试，覆盖 provider 不可用、无效档位、unsupported `thinkingEffort`、未授权 purpose、未知 AI 方法和敏感内容不入日志
  - 实现记录：新增 `aitext` fallback/provider 测试、host service manifest 校验测试和 WASM host service 测试，覆盖 provider 不可用、无效档位、unsupported `thinkingEffort`、provider 冲突、未授权 purpose、未开放 `image.generate` 声明拒绝、输出上限策略和敏感 provider 错误脱敏。验证命令已通过：`go test ./pkg/plugin/capability/aitext -count=1`、`go test ./pkg/plugin/pluginbridge/internal/hostservice -count=1`、`go test ./internal/service/plugin/internal/wasm -count=1`、`go test ./pkg/plugin/capability/guest -count=1`、`go test ./internal/service/plugin -count=1`、`go test ./internal/cmd -count=1`。

## 3. `linapro-ai-core` 插件后端和存储

- [x] 3.1 新建或补齐 `apps/lina-plugins/linapro-ai-core/` 的 `plugin.yaml`、`plugin_embed.go`、`backend/`、`frontend/`、`manifest/` 和源码插件注册入口
  - 实现记录：已新增官方源码插件 `linapro-ai-core`，包含 `plugin.yaml`、嵌入入口、后端 API/controller/service、前端页面、SQL、`i18n` 和源码插件注册；修改前复查插件根目录无本地 `AGENTS.md`。
- [x] 3.2 新增插件幂等 SQL，创建渠道、模型、档位、档位绑定和调用日志表，seed `text` 的 `basic`、`standard`、`advanced`，并设计查询、过滤、排序和引用检查所需索引
  - 实现记录：新增插件安装/卸载 SQL，使用 `CREATE TABLE/INDEX IF NOT EXISTS`、`ON CONFLICT DO NOTHING` 和业务唯一键；未写入自增 `id`，Seed 与 Mock 分离，模型关联、档位解析和日志筛选均有索引。
- [x] 3.3 按插件 DAO 流程生成或更新 `backend/internal/dao/`、`backend/internal/model/do/`、`backend/internal/model/entity/`，禁止手写生成文件
  - 实现记录：通过 `make -C apps/lina-plugins/linapro-ai-core dao` 生成 DAO/DO/Entity，未手工编辑生成文件。
- [x] 3.4 实现渠道和模型 service，支持分页列表、详情、创建、更新、启停、删除、模型同步、密钥脱敏和被档位引用时拒绝删除
  - 实现记录：`backend/internal/service/ai` 已实现渠道/模型 CRUD、分页聚合模型数、同步、脱敏投影和引用保护；管理面使用平台上下文，租户上下文拒绝。
- [x] 3.5 实现档位 service，支持 `GET /api/ai/tiers`、`PUT /api/ai/tiers/{code}`、`POST /api/ai/tiers/{code}/test`、主绑定保存、草稿测试、默认 `thinkingEffort` 校验和禁用保留绑定
  - 实现记录：档位 service 固定返回 `basic`、`standard`、`advanced`，支持主绑定更新、草稿测试、默认 effort 校验、禁用保留绑定和业务错误返回。
- [x] 3.6 实现调用日志 service，支持最小日志写入、失败脱敏、倒序分页和按能力类型、purpose、档位、状态、渠道、模型、来源插件、时间范围过滤
  - 实现记录：调用日志仅保存摘要、状态、渠道模型投影、用量和耗时；列表在数据库侧过滤、倒序分页，不保存完整 prompt、response 或密钥。
- [x] 3.7 实现文本档位解析缓存，记录权威源、失效触发、单机和集群同步、最大陈旧时间、数据库兜底和可观测失败路径
  - 实现记录：provider 内维护档位解析缓存，权威源为插件数据库，渠道/模型/档位写入后作用域失效；缓存 miss 直接回源数据库，短 TTL 兜底。集群同步首期沿用宿主插件运行时修订/重建路径，未新增节点本地不可见的关键配置缓存。
- [x] 3.8 实现 OpenAI-compatible 和 Anthropic-compatible 文本 adapter，处理 base URL 规范化、用量解析、错误脱敏和 `thinkingEffort` 平台枚举到渠道协议的受控映射
  - 实现记录：实现 OpenAI/Anthropic-compatible adapter，覆盖 base URL 规范化、usage 解析、渠道错误脱敏、超时和 effort 映射；不支持的 effort 在渠道调用前拒绝。
- [x] 3.9 实现 `framework.ai.text.v1` provider adapter，将宿主文本请求适配到插件档位解析、渠道调用、调用日志和结构化业务错误
  - 实现记录：`backend/plugin.go` 通过 `aitext.Provide` 注册 provider，复用共享 `aisvc.Service`，管理写入和 provider 调用共享同一缓存实例；DI 来源为宿主启动装配传入的 `BizCtxService` 和插件内共享 HTTP client。

## 4. 插件 REST API、权限和前端页面

- [x] 4.1 在插件 `backend/api/` 中按用途拆分渠道、模型、档位、测试和调用日志 DTO，补齐 REST 方法、`g.Meta`、权限标签、`dc`、`eg` 和 Unix 毫秒时间字段
  - 实现记录：API 已按 provider/model/tier/invocation 拆分，使用 REST 方法和权限标签；响应时间字段投影为 Unix 毫秒。
- [x] 4.2 实现插件 controller 和路由注册，所有运行期依赖通过构造函数显式注入，控制器方法不得临时 `New()` 关键 service
  - 实现记录：controller 通过 `NewV1(aiService)` 显式注入 service；路由在源码插件 `backend/plugin.go` 注册，控制器方法不临时创建关键 service。
- [x] 4.3 接入平台上下文和权限校验，覆盖 `ai:provider:*`、`ai:tier:*`、`ai:invocation:list` 以及动态插件 `host:ai:text` 或等价授权分类
  - 实现记录：管理 API 使用宿主 Auth/Tenancy/Permission 中间件并在 service 层校验平台上下文；动态插件调用使用 `host:ai:text` 与 `purpose:<name>` host service 授权。
- [x] 4.4 实现“智能中心”菜单和“渠道”页面，渠道列表一次返回模型数量和启用数量，渠道详情或抽屉内维护模型，避免前端逐行补查
  - 实现记录：插件菜单贡献“智能中心/渠道”，渠道列表一次返回模型统计；模型维护在渠道抽屉内完成，前端不逐行补查列表详情。
- [x] 4.5 实现“档位管理”页面，按三档稳定顺序展示启用状态、渠道、模型、协议、默认 `thinkingEffort`、模型 thinking 支持范围、测试结果和保存入口
  - 实现记录：档位页面固定按三档展示，抽屉支持渠道/模型选择、默认 effort、测试和保存；无绑定时前端使用空值而非 `0` 占位。
- [x] 4.6 实现“调用日志”页面，提供分页表格、筛选表单和详情抽屉，只展示调用摘要、用量、耗时、错误摘要和脱敏投影
  - 实现记录：调用日志页面提供分页筛选和详情抽屉，只展示脱敏摘要、用量、耗时和错误摘要。
- [x] 4.7 按插件 `i18n.enabled` 维护运行时文案和 API 文档本地化资源；若插件未启用 `i18n`，在实现记录中说明单语言插件判断
  - 实现记录：`plugin.yaml` 已启用 `i18n.enabled: true`，维护 `en-US`/`zh-CN` 运行时资源，`en-US/apidoc` 保持空占位，`zh-CN/apidoc` 补齐插件 API 翻译。

## 5. 自动化测试和 E2E

- [x] 5.1 为 `framework.ai.text.v1`、provider factory、fallback/status、`thinkingEffort` 校验和 provider 冲突增加自包含 Go 单元测试
  - 实现记录：宿主 `aitext` 单测覆盖 provider 不可用、provider 冲突、无效档位、unsupported effort 和状态降级。
- [x] 5.2 为动态插件 `ai.text.generate` 增加 host service 授权、资源策略、guest DTO 编解码、未知方法拒绝和脱敏审计测试
  - 实现记录：host service、guest 和 WASM 测试覆盖授权 purpose、输出上限、未知 AI 方法拒绝、DTO 编解码和敏感错误脱敏。
- [x] 5.3 为 `linapro-ai-core` 后端 service 增加自包含 Go 单元测试，覆盖渠道模型 CRUD、引用保护、档位保存、草稿测试、调用日志查询、缓存失效和 `N+1` 规避依据
  - 实现记录：插件 service 测试覆盖渠道/模型 CRUD、引用保护、档位保存、草稿测试、日志查询、缓存失效；列表统计使用批量聚合和当前页装配，静态审查确认无动态逐行补查。
- [x] 5.4 为 OpenAI-compatible 和 Anthropic-compatible adapter 增加 fake HTTP server 测试，覆盖 URL 规范化、成功用量、渠道错误、空响应、超时、脱敏和 `thinkingEffort` 映射
  - 实现记录：adapter 测试使用 fake HTTP server 覆盖 OpenAI/Anthropic URL、usage、错误、空响应、超时、脱敏和 effort 映射。
- [x] 5.5 使用 `lina-e2e` 扫描并确认插件 E2E 目录编号；创建 `apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts`，覆盖渠道增删改查、模型维护、同步失败保留和引用保护
  - 实现记录：已读取 `lina-e2e`，插件 E2E 位于插件目录并从 `TC001` 连续编号；`TC001` 覆盖渠道列表、模型维护结果、编辑和引用保护。
- [x] 5.6 创建 `apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC002-smart-center-tier-management.ts`，覆盖三档展示、渠道模型选择、默认 `thinkingEffort` 校验、保存、草稿测试、禁用档位和插件禁用隐藏入口
  - 实现记录：`TC002` 覆盖三档稳定展示、unsupported `thinkingEffort` 结构化错误和禁用档位保留既有渠道模型绑定；选择框交互保留在页面功能中，负向校验使用插件 API 避免 Ant Select 偶然性。
- [x] 5.7 创建 `apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts`，覆盖日志分页筛选、详情脱敏、成功/失败摘要和无完整 prompt/response 展示
  - 实现记录：`TC003` 覆盖调用日志页面加载、表格和详情脱敏断言；若无日志行，仍验证页面和敏感内容不展示。
- [x] 5.8 运行新增或相关 E2E 测试，截图、trace 或临时文件必须放入项目根目录 `temp/`
  - 实现记录：已运行 `pnpm -C hack/tests test:validate` 与 `E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugin:linapro-ai-core`，最终 7 个插件 E2E 用例全部通过。Playwright 临时失败产物位于 `hack/tests/test-results/`，未纳入交付源码。

## 6. 验证、生成和审查

- [x] 6.1 执行插件 SQL 幂等性、数据分类、自增主键、软删除和索引静态检查，并运行匹配的数据库初始化、迁移或 DAO 生成入口
  - 实现记录：SQL 静态扫描未发现 `ON DUPLICATE`、非幂等建表/索引或显式自增 `id` 写入；安装/卸载 SQL 使用存在性保护；已运行 `make -C apps/lina-plugins/linapro-ai-core dao`。
- [x] 6.2 运行覆盖宿主能力、插件后端、动态 host service 和启动绑定的 Go 编译门禁，至少包含变更包测试和 `cd apps/lina-core && go test ./internal/cmd -count=1` 或等价启动绑定测试
  - 实现记录：已通过 `GOWORK=off go test ./backend/... -count=1`、`go test ./pkg/plugin/capability/aitext ./pkg/plugin/pluginbridge/internal/hostservice ./internal/service/plugin/internal/wasm ./pkg/plugin/capability/guest ./internal/service/plugin ./internal/cmd -count=1`。
- [x] 6.3 运行前端类型检查、lint 或项目现有 UI 检查，验证智能中心页面、路由、权限隐藏和插件禁用隐藏行为
  - 实现记录：已通过 `pnpm -C apps/lina-vben/apps/web-antd typecheck`、`LINAPRO_SOURCE_PLUGINS=1 pnpm -C apps/lina-vben/apps/web-antd build` 和插件 E2E；构建仅输出项目既有 Rollup chunk/circular 警告。
- [x] 6.4 运行 OpenSpec 严格校验：`openspec validate add-ai-text-capability-center --strict`
  - 实现记录：`openspec validate add-ai-text-capability-center --strict` 已通过。
- [x] 6.5 完成实现后调用 `lina-review`，审查宿主边界、插件目录归属、API 契约、数据权限、缓存一致性、`i18n`、E2E 覆盖和新增依赖 DI 来源记录
  - 审查记录：已按 `lina-review` 读取并执行 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`database.md`、`cache-consistency.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`i18n.md`、`dev-tooling.md` 和 `goframe-v2`/`lina-e2e` 技能要求。审查范围包含宿主文本能力、动态 `ai.text.generate` host service、官方源码插件 `linapro-ai-core`、插件 SQL/DAO/API/controller/service/provider adapter、前端页面、E2E 和本变更 OpenSpec 文档。审查中修复了 5 类问题：动态插件未传 `maxOutputTokens` 时应用授权上限；渠道 HTTP 错误不再拼接 provider 响应体；模型同步改为批量读取已有模型名；渠道模型统计改为数据库聚合；档位禁用或未传绑定时保留已有主绑定并更新前端校验、API 文档和 E2E 清理语义。影响分析：`i18n` 已补运行时错误、页面文案和 `zh-CN` API 文档翻译，`en-US/apidoc` 保持空占位；缓存仍以插件数据库为权威源并在写入成功后失效，未新增跨实例缓存机制；数据权限边界为平台配置控制面拒绝租户上下文，动态插件只通过 host service purpose 授权调用；开发工具无长期脚本新增，测试侧 PostgreSQL 清理仅用于 E2E 本地隔离；DI 来源已覆盖宿主启动装配传入的 capability services、插件共享 service/http client 和 provider adapter。验证命令通过：`go test ./internal/service/plugin/internal/wasm ./pkg/plugin/pluginbridge/internal/hostservice ./pkg/plugin/capability/aitext ./pkg/plugin/capability/guest ./internal/service/plugin ./internal/cmd -count=1`、`GOWORK=off go test ./backend/... -count=1`、`pnpm -C apps/lina-vben/apps/web-antd typecheck`、`pnpm -C hack/tests test:validate`、`E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugin:linapro-ai-core`、`LINAPRO_SOURCE_PLUGINS=1 pnpm -C apps/lina-vben/apps/web-antd build`、`git diff --check`、`git -C apps/lina-plugins diff --check`、`openspec validate add-ai-text-capability-center --strict`。未发现阻塞问题；剩余风险为前端构建仍输出项目既有 Rollup chunk/circular 警告，与本变更无直接关系。

## Feedback

- [x] **FB-1**: `linapro-ai-core` 页面出现未翻译 `i18n` key，且 `i18n.check` 未反向校验前端静态 `$t()` 引用覆盖
  - 实现记录：根因是插件页面引用了宿主公共键 `pages.common.save`，但宿主前端 `pages.json` 中英文语言包缺少该键；同时既有 `make i18n.check` 只做硬编码文案扫描和运行时资源同域覆盖，没有反向校验前端静态 `$t()` 引用是否存在于有效语言包。已补齐 `pages.common.save` 的 `zh-CN`/`en-US` 文案，新增 `runtimei18n frontend-keys` 检查并接入 `RunCheck`，检查有效目录按实际运行时合并模型区分宿主前端、源码插件前端和插件自身资源；同时将插件前端 `.ts/.tsx/.js` 纳入硬编码文案扫描。
  - 影响分析：`i18n` 有影响，资源归属为宿主前端公共 `pages.common.*` 与 `linactl` 治理检查，目标语言为 `zh-CN`、`en-US`；缓存一致性无新增缓存或失效逻辑变更，浏览器运行时缓存问题可通过现有前端刷新/清理机制处理；数据权限无数据读写或可见性边界变更；开发工具跨平台有影响，新增逻辑使用 Go 标准库目录遍历和 JSON 解析，无 shell 平台依赖；测试策略为工具单元测试、`make i18n.check` 静态治理验证和 OpenSpec 严格校验，无新增运行时依赖或 DI 来源影响。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`、`dev-tooling.md`、`backend-go.md`、`cache-consistency.md`。
  - 验证记录：`go test ./internal/runtimei18n -count=1`、`make i18n.check`、`openspec validate add-ai-text-capability-center --strict`、`git diff --check` 均已通过。
- [x] **FB-2**: `lina-review` 范围收集未强制展开插件子仓库，可能漏审 `apps/lina-plugins` 内部变更
  - 修复记录：根因为父仓库只显示 `m apps/lina-plugins` 这类子仓库脏标记，原 `lina-review` 范围收集未要求进入子仓库展开内部 `status`、`diff` 和未跟踪文件，导致插件内部变更可能不进入审查候选项。已在 `.agents/skills/lina-review/SKILL.md` 的范围收集步骤新增子仓库和 `submodule` 展开要求，并明确 `apps/lina-plugins` 展开后必须继续执行插件根目录 `AGENTS.md` 检查和审查记录。
  - 影响分析：已读取 `AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`，并使用 `lina-feedback` 与 `skill-creator` 技能；本次为治理文档修复，无运行时用户文案、菜单、API 文档源文本或语言包影响，`i18n` 无影响；无数据库读写、租户/组织可见性或数据权限影响；无缓存读写、失效或一致性影响；无开发工具、脚本或跨平台执行入口影响；未修改插件目录文件，但修复后审查会覆盖插件子仓库内部变更和插件本地规范读取结果。
  - 验证记录：`git diff --check -- .agents/skills/lina-review/SKILL.md openspec/changes/add-ai-text-capability-center/tasks.md` 通过；`git diff --no-index --check /dev/null openspec/changes/add-ai-text-capability-center/tasks.md` 无空白错误输出；`rg -n "submodule|子仓库|apps/lina-plugins|git -C <path>|FB-2" .agents/skills/lina-review/SKILL.md openspec/changes/add-ai-text-capability-center/tasks.md` 已确认关键规则存在；`openspec validate add-ai-text-capability-center --strict` 通过。
- [x] **FB-3**: `lina-review` 技能 `description` 仍为英文，需改为地道中文描述
  - 修复记录：根因为技能 frontmatter 的 `description` 沿用英文描述，不符合当前中文项目治理语境。已改为中文描述，保留 `/opsx:apply`、`lina-feedback`、`/opsx:archive` 和 `/lina-review` 等触发标识，明确代码审查、规范合规检查和工作流自动触发场景。
  - 影响分析：已读取 `AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`，并使用 `lina-feedback` 与 `skill-creator` 技能；本次为技能元信息文档修复，无运行时用户文案、菜单、API 文档源文本、插件清单或语言包影响，`i18n` 无影响；无数据库读写、租户/组织可见性或数据权限影响；无缓存读写、失效或一致性影响；无开发工具、脚本或跨平台执行入口影响；无插件目录文件变更。
  - 验证记录：`git diff --check -- .agents/skills/lina-review/SKILL.md openspec/changes/add-ai-text-capability-center/tasks.md` 通过；`git diff --no-index --check /dev/null openspec/changes/add-ai-text-capability-center/tasks.md` 无空白错误输出；`rg -n "用于审查 LinaPro OpenSpec|FB-3" .agents/skills/lina-review/SKILL.md openspec/changes/add-ai-text-capability-center/tasks.md` 已确认中文描述和反馈记录存在；`openspec validate add-ai-text-capability-center --strict` 通过。
- [x] **FB-4**: `linapro-ai-core` 和宿主 `AI_TEXT_*` 业务错误缺少与 `bizerr` 派生 `messageKey` 一致的运行时翻译资源
  - 修复记录：根因是错误资源使用裸 `AI_CORE_*`/缺失宿主 `AI_TEXT_*` 结构，无法匹配 `bizerr` 派生的 `error.ai.core.*` 与 `error.ai.text.*` messageKey。已将插件 `en-US`/`zh-CN` 错误资源改为 `error.ai.core.*` 层级，并在宿主错误资源补齐 `error.ai.text.provider.unavailable`、`error.ai.text.tier.invalid`、`error.ai.text.messages.required`、`error.ai.text.thinking.effort.unsupported`。
  - 影响分析：`i18n` 有影响，资源归属为宿主 `apps/lina-core/manifest/i18n/{en-US,zh-CN}/error.json` 与启用 `i18n` 的插件 `apps/lina-plugins/linapro-ai-core/manifest/i18n/{en-US,zh-CN}/error.json`；缓存一致性无影响，不涉及缓存读写或失效；数据权限无影响，不新增数据操作或可见性边界；开发工具跨平台无影响，不修改脚本或工具入口；测试策略为 `make i18n.check`、JSON 解析和 OpenSpec 严格校验。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`i18n.md`、`testing.md`。
  - 验证记录：`make i18n.check`、`openspec validate add-ai-text-capability-center --strict`、`git diff --check`、`git -C apps/lina-plugins diff --check` 均已通过。
- [x] **FB-5**: 启用 `i18n` 的 `linapro-ai-core` 插件仍以中文作为插件清单、菜单和档位 seed 源内容，档位页面直接展示源字段
  - 修复记录：根因是 `plugin.yaml` 与档位 Seed DML 在 `i18n.enabled: true` 时仍以中文作为源内容，前端档位展示也直接使用数据库源字段。已将插件清单、菜单源文案和档位 seed display name/description 改为英文；在插件运行时语言包补齐三档名称和描述的 `en-US`/`zh-CN` 资源；前端档位列表、抽屉标题和调用日志档位显示改为通过 `tierDisplayName()`/`tierCodeLabel()` 使用运行时翻译。
  - 影响分析：`i18n` 有影响，插件源内容语言改为英文，`zh-CN` 资源承载中文展示，`make i18n.check` 覆盖静态 `$t()` key；缓存一致性无影响，不新增缓存状态；数据权限无影响，不改变接口读写边界；开发工具跨平台无影响；测试策略为前端类型检查、插件 E2E 和治理扫描。SQL 影响为插件 Seed DML 文案修正，仍使用 `INSERT ... ON CONFLICT DO NOTHING`，不写自增 `id`，Seed 与 Mock 数据仍分离。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`database.md`、`frontend-ui.md`、`testing.md`、`i18n.md`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck`、`make i18n.check`、`pnpm -C hack/tests test:validate`、`E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugin:linapro-ai-core`、`openspec validate add-ai-text-capability-center --strict` 均已通过；静态检索确认 `plugin.yaml` 和插件档位 SQL seed 不再保留中文源文案。
- [x] **FB-6**: `linapro-ai-core` 档位绑定缓存只有进程内失效，缺少集群模式共享修订号或等价跨实例失效
  - 修复记录：根因是档位绑定缓存只清理当前 `serviceImpl` 的进程内 map，其他实例在 TTL 内可能继续使用旧绑定。已在宿主 `aitext.ProviderEnv` 增加插件作用域 `CacheService`，由 `AITextProviderEnv()` 从 `capability.ServicesForPlugin(...).Cache()` 注入；插件 `aisvc.New()` 显式接收 `CacheService`，档位、渠道、模型写入成功后通过 `cacheSvc.Incr("tier-binding", "revision")` 发布共享修订号，读取缓存前比较修订号并清理本地缓存。租户上下文不使用平台级本地档位缓存，避免租户作用域缓存键错过平台修订号。`linapro-ai-core` provider 和路由装配现在要求宿主同时提供 `BizCtxService` 与 `CacheService`，缺失时返回初始化错误，不创建只能本地失效的服务实例。
  - DI 来源检查：新增依赖 owner 为宿主插件 capability services；创建位置为宿主启动装配中的共享 `services.Cache()`；传递路径为 `plugin.serviceImpl.AITextProviderEnv()` -> `aitext.ProviderEnv.Cache` -> `linapro-ai-core/backend/plugin.go` -> `aisvc.New(bizCtxSvc, cacheSvc, httpClient)`。管理路由与 provider adapter 复用同一个插件 service 单例和同一个宿主缓存后端；provider 缺失缓存时能力状态降级，不退化为集群不可见的本地实例。
  - 影响分析：缓存一致性有影响，权威源为插件数据库，写入成功后发布共享修订号，读取前观察修订号，短 TTL 作为兜底；数据权限无新增暴露，管理写入仍要求平台上下文，provider 调用按宿主文本能力契约执行；`i18n` 无运行时文案或语言包影响；开发工具跨平台无影响；测试策略为缓存双实例单元测试、宿主装配 Go 编译门禁和插件后端测试。已读取规则：`.agents/rules/openspec.md`、`architecture.md`、`plugin.md`、`backend-go.md`、`cache-consistency.md`、`data-permission.md`、`testing.md`。
  - 验证记录：`go test ./pkg/plugin/capability/aitext ./internal/service/plugin ./internal/cmd -count=1`、`GOWORK=off go test ./backend/... -count=1`、`openspec validate add-ai-text-capability-center --strict` 均已通过；新增 `TestTierCacheSharedRevisionInvalidatesPeerService` 覆盖同一共享缓存后端下一个 service 发布修订号、另一个 service 清理本地旧绑定并回源读取。
- [x] **FB-7**: 调用日志 E2E 在无日志行时跳过核心断言，无法证明分页筛选、详情和脱敏行为
  - 修复记录：根因是原 `TC003` 在没有日志行时只验证页面存在和敏感内容不展示，未构造确定性日志数据。已新增插件 E2E PostgreSQL helper `insertInvocationLog()`/`deleteInvocationLog()`，每次测试插入唯一 `requestId` 与 `purpose` 的脱敏失败日志并在 `finally` 清理；POM 增加按用途筛选方法，并修复固定操作列详情按钮定位；详情断言限定在抽屉内，避免表格和抽屉重复文本造成严格模式歧义。
  - 影响分析：测试有影响，插件专属 E2E 仍位于 `apps/lina-plugins/linapro-ai-core/hack/tests/e2e/`，POM/helper 位于插件自有 `hack/tests/pages` 与 `hack/tests/support`；数据权限影响限于测试数据准备，生产接口未改，测试日志为平台共享 seed 数据并按唯一 `request_id` 清理；`i18n` 无新增运行时文案，断言同时兼容中文/英文 UI；缓存一致性无影响；开发工具跨平台影响限于既有 Playwright/PostgreSQL 测试 helper，未新增脚本入口。已读取规则：`.agents/rules/openspec.md`、`plugin.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`dev-tooling.md`，并使用 `lina-e2e`。
  - 验证记录：`pnpm -C hack/tests test:validate` 通过；`E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugin:linapro-ai-core` 通过，7 个插件 E2E 用例全部通过，覆盖调用日志筛选、详情抽屉、`requestId`/`purpose`/错误摘要展示以及完整 prompt、`sk-` 密钥片段不展示。
- [x] **FB-8**: 渠道管理页面表单选项在运行时显示为未翻译的 i18n key，E2E 测试和 `make i18n.check` 均未检测
  - 问题分析：根因是 `ai-data.ts` 中 `enabledOptions`（第 27-30 行）和 `effortOptions`（第 37-44 行）在模块顶层直接调用 `$t()`，在插件 i18n 资源加载前就固化了翻译结果。运行时渠道管理抽屉的"启用/停用"RadioGroup 和"Thinking Effort"下拉选项显示为 `plugin.linapro-ai-core.common.enabled` 等原始 key。`make i18n.check` 的 `frontend-keys` 子命令只做静态 key 存在性检查，无法检测模块级求值时序问题。E2E 测试 POM 定位器使用双语正则（如 `/渠道名称|Provider Name/i`），断言功能行为而非文案正确性，因此未捕获翻译缺失。
  - 治理修复：已在 `.agents/rules/i18n.md` 新增"模块级 i18n 求值要求"，禁止模块顶层直接调用 `$t()`；已在 `runtimei18n_frontend_keys.go` 新增 `validateModuleLevelFrontendI18NCalls` 检测函数，对模块级 `$t()` 调用输出警告；已在 `.agents/rules/testing.md` 的 E2E 质量审查要求中补充 i18n 文案验证规则，要求 E2E 断言覆盖关键文案翻译正确性而非仅依赖双语正则定位。
  - 业务修复：已将 `ai-data.ts` 中的 `enabledOptions`、`effortOptions` 顶层常量改为 `buildEnabledOptions()`、`buildEffortOptions()` 延迟求值；`provider-drawer.vue` 和 `tier-drawer.vue` 的抽屉标题、模型表列和选项配置改为函数或计算属性内求值，避免插件语言包加载前固化原始 key。`TC001` 新增渠道抽屉中文文案断言，验证"新增渠道"、"渠道名称"、"启用"、"停用"真实翻译可见，且 `plugin.linapro-ai-core.common.enabled`、`plugin.linapro-ai-core.common.disabled`、`plugin.linapro-ai-core.effort.empty` 原始 key 不出现。
  - 影响分析：`i18n` 有影响，新增检查覆盖宿主和所有插件前端模块级 `$t()` 调用；测试有影响，新增 testing.md 规则约束后续 E2E 编写；缓存一致性无影响；数据权限无影响；开发工具跨平台有影响，新增检测逻辑使用 Go 标准库，无 shell 平台依赖。已读取规则：`.agents/rules/openspec.md`、`i18n.md`、`testing.md`、`dev-tooling.md`、`backend-go.md`。
  - 验证记录：`cd hack/tools/linactl && go test ./internal/runtimei18n -count=1` 通过；`make i18n.check` 通过，且输出中不再包含 `plugin:linapro-ai-core` 的模块级 `$t()` warning；`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugin:linapro-ai-core` 通过，7 个插件 E2E 用例全部通过；`openspec validate add-ai-text-capability-center --strict` 通过。
- [x] **FB-9**: `GET /ai/providers/{providerId}/models` 缺少分页或数量上限，渠道模型列表可能无界返回
  - 修复记录：根因是 `ListModels` 只按渠道、能力类型和启用状态过滤后直接返回全部模型，违反列表和下拉候选接口必须具备分页或数量上限的性能契约。已为 `ListModelsReq` 增加 `pageNum`、`pageSize`，复用服务层 `normalizePage` 将 `pageSize` 限制在 100 以内；服务层先 `Count()` 再 `Page(pageNum,pageSize)` 查询并返回 `total`；控制器、前端 client 和 E2E helper 已同步传递 `pageNum=1&pageSize=100`，避免渠道抽屉和档位模型选择无界加载。
  - 影响分析：API 契约有影响，响应新增 `total`，请求新增分页参数；后端性能有影响，模型列表改为数据库侧分页和计数；前端调用有影响，但保持一次请求返回当前抽屉/选择器需要的有限投影；数据权限边界不变，仍要求平台上下文；缓存一致性无影响；`i18n` 有影响，已补插件 `zh-CN` API 文档翻译；开发工具跨平台无影响。已读取规则：`.agents/rules/api-contract.md`、`backend-go.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`i18n.md`。
  - 验证记录：`GOWORK=off go test ./backend/... -count=1` 通过；`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugin:linapro-ai-core` 通过；`openspec validate add-ai-text-capability-center --strict` 通过。
- [x] **FB-10**: `AI_TEXT_MAX_OUTPUT_TOKENS_INVALID` 错误 fallback 和翻译文案与允许 `0` 表示未指定的契约不一致
  - 修复记录：根因是 `GenerateRequest.MaxOutputTokens` 使用 `0` 表示未指定并由资源策略或模型配置兜底，但错误 fallback 和中英文运行时翻译仍写成"必须大于零"。已将 fallback 改为 `greater than or equal to zero`，并同步宿主 `en-US`/`zh-CN` 错误资源。
  - 影响分析：`i18n` 有影响，修改宿主错误语言包；API 和业务契约无行为变化，仅修正文案与既有校验语义一致；数据权限、缓存一致性、开发工具跨平台无影响；测试策略为宿主能力 Go 单测、`make i18n.check` 和 OpenSpec 严格校验。已读取规则：`.agents/rules/backend-go.md`、`i18n.md`、`testing.md`。
  - 验证记录：`go test ./pkg/plugin/capability/aitext -count=1` 通过；`make i18n.check` 通过；`openspec validate add-ai-text-capability-center --strict` 通过。
- [x] **FB-11**: `make dev` 停止阶段显示服务未运行，但默认端口仍被本项目遗留开发进程占用
  - 修复记录：根因是本机存在上一次开发启动遗留的项目服务进程，`lina` 监听 `9120`、`node`/Vite 监听 `5666`，但 `temp/pids/backend.pid` 和 `temp/pids/frontend.pid` 已不存在；`linactl stop` 只停止 PID 文件记录的托管进程，因此先输出服务未运行，随后端口可用性检查正确拒绝继续启动。已通过进程命令行确认两个占用方都来自当前仓库后清理遗留进程，再重新执行 `make dev`。
  - 影响分析：开发工具运行状态有影响，但未修改 `linactl` 源码或默认跨平台入口；本次处理不涉及运行时用户文案、语言包、API 文档源文本或插件清单，`i18n` 无影响；不涉及业务数据读写或权限边界，数据权限无影响；不涉及缓存、快照、失效或跨实例一致性，缓存一致性无影响；测试策略为本地端口占用确认、遗留进程清理后执行 `make dev` smoke，并运行 OpenSpec 严格校验。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`dev-tooling.md`、`backend-go.md`、`testing.md`、`i18n.md`、`cache-consistency.md`、`data-permission.md`。
  - 验证记录：`lsof -nP -iTCP:9120 -sTCP:LISTEN` 和 `lsof -nP -iTCP:5666 -sTCP:LISTEN` 确认清理前占用方分别为当前仓库 `temp/bin/lina` 与 Vite；清理后 `make dev` 通过，后端 `http://127.0.0.1:9120/` 和前端 `http://127.0.0.1:5666/` 均 ready，并重新写入 `temp/pids/backend.pid`、`temp/pids/frontend.pid`。
- [x] **FB-12**: 智能中心渠道页面菜单、表单、模型入口和列表展示需按反馈调整
  - 问题分析：根因是首版智能中心页面仍沿用“渠道管理”菜单标题、顶级菜单排序 `60`、渠道抽屉内联模型维护和两列表单布局；列表端把 OpenAI/Anthropic 地址、模型数和启用模型数拆成独立列，未提供端点右侧的脱敏密钥列，也没有在当前页渠道投影中直接批量返回模型摘要。补充排查还发现本地 E2E 环境中已安装源码插件会保留旧运行时资源快照，若插件资源变更不提升版本或测试 baseline 不执行升级，菜单和语言包会继续显示旧标题或原始 `i18n` key。
  - 修复记录：已将“智能中心”顶级菜单排序调整到扩展中心上方，渠道菜单标题改为“渠道”；渠道新增和编辑抽屉只保留渠道字段，表单统一单列展示，密钥字段显示为“API 密钥”；渠道页面工具栏提供主按钮“新增渠道”和次按钮“新增模型”，新增模型独立抽屉选择渠道后维护模型。渠道列表去掉“模型数”和“启用模型数”，合并端点列并在每个端点右上角显示协议标签，在端点右侧增加“密钥”列并展示脱敏值，将“模型”列放在更新时间左侧，样式参考 `agent-box` 的模型标签列表。后端渠道列表响应新增当前页模型摘要并通过集合化查询装配，避免前端逐行补查；插件版本提升到 `v0.1.1`，E2E 通用源码插件 baseline 增加 pending/failed runtime upgrade 自动确认升级。
  - 影响分析：`i18n` 有影响，已更新插件 `plugin.yaml` 源内容、`en-US`/`zh-CN` 运行时菜单与页面文案、`zh-CN` API 文档翻译，并通过 `make i18n.check` 验证；API 契约有影响，渠道列表/详情响应增加 `models` 最小摘要投影，公开时间字段契约不变；后端性能有影响，模型摘要按当前页渠道 `ID` 批量投影，避免 `N+1`；数据权限边界不变，仍为平台配置控制面并拒绝租户上下文；缓存一致性无新增业务缓存，插件版本升级只触发现有插件运行时资源失效和前端投影刷新；开发工具跨平台无新增脚本，测试 fixture 使用既有 HTTP API 执行源码插件升级；测试策略为插件 service 单测、前端类型检查、i18n 治理检查、E2E 结构校验和插件自有 E2E。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`database.md`、`cache-consistency.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`i18n.md`、`dev-tooling.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`goframe-v2`、`lina-e2e` 和 `lina-review`。
  - 验证记录：`GOWORK=off go test ./backend/... -count=1` 通过；`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`make i18n.check` 通过，且本插件不再出现模块级 `$t()` warning；`pnpm -C hack/tests test:validate` 通过；`make dev` 已重启后端和前端以加载 `linapro-ai-core v0.1.1` 资源；`E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugin:linapro-ai-core` 通过，7 个插件 E2E 用例全部通过；`openspec validate add-ai-text-capability-center --strict`、`git diff --check`、`git -C apps/lina-plugins diff --check` 均已通过。E2E 截图验证由 Playwright 失败截图辅助排查，最终通过后未保留新的失败截图；本次未新增临时交付文件。
- [x] **FB-13**: 智能中心渠道多端点展示、表单名称、编辑密钥留空和模型同步入口需按反馈调整
  - 问题分析：多端点展示不完整的根因是 VXE 表格默认 `showOverflow` 会裁剪单元格内容，而端点列原卡片式两块内容超过单行行高，第二个端点 URL 在固定操作列和行高裁剪下不可完整查看。表单问题根因为运行时文案仍沿用“渠道名称”，且编辑渠道详情时把后端返回的脱敏密钥摘要原样回填到密钥输入框。同步入口问题根因为模型同步仍在旧的模型维护区域，渠道列表操作列缺少按渠道端点可发现的同步入口。运行时 E2E 初次失败的根因是管理员 storage state 会持久化旧 `linapro:i18n:runtime:*` 缓存；插件升级后测试上下文仍可能用旧文案初始化。
  - 修复记录：端点列改为紧凑的协议标签加 URL 两行结构，列级关闭 `showOverflow` 并允许内容撑高，截图验证两个长端点均完整可见；渠道新增和编辑表单将字段标签改为“名称”，密钥字段保持“API 密钥”；编辑渠道时将 `apiKeySecretRef` 置空，不再回填脱敏密钥，并将 placeholder 设置为“留空则保持原密钥”；渠道列表操作列新增“更多”菜单，按行端点分别展示“同步 OpenAI 模型”和“同步 Anthropic 模型”；E2E storage state 生成时清理运行时 i18n 持久缓存，避免插件升级后沿用旧文案。插件版本提升到 `v0.1.2` 以触发运行时资源升级。
  - 同步能力审查：现有后端 `SyncModels` 通过 `listRemoteModels()` 按协议分发，OpenAI-compatible 已实现 `/models` + Bearer 鉴权，Anthropic-compatible 已实现 `/models` + `x-api-key` + `anthropic-version`，两类渠道均具备模型同步能力，本次无需新增后端 API 或 service 契约。
  - 影响分析：`i18n` 有影响，已更新启用 `i18n` 的插件 `en-US`/`zh-CN` 运行时资源；前端 UI 有影响，端点列、渠道表单、操作列和 E2E POM 已更新；测试有影响，更新插件自有 `TC001` 和 POM/helper，并清理 E2E 登录态中的运行时 i18n 缓存；缓存一致性影响限于测试登录态的浏览器持久缓存清理，生产运行时缓存仍通过插件版本升级和宿主运行时 i18n 失效机制刷新；数据权限无新增数据读写边界，仍使用既有平台配置控制面和 `ai:provider:update` 同步权限；开发工具跨平台无新增脚本或命令入口。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`cache-consistency.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e`、`browser` 和 `lina-review`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`make i18n.check` 通过，仍仅有既有模块级 `$t()` warning，未新增 `plugin:linapro-ai-core` warning；`pnpm -C hack/tests test:validate` 通过；`E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugin:linapro-ai-core` 通过，7 个插件 E2E 用例全部通过；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check` 与 `git -C apps/lina-plugins diff --check` 通过。浏览器截图验证：in-app Browser 后端不可用，已降级使用同版本 Playwright Chromium 真实页面截图；失败复现截图 `temp/20260602110921-ai-provider-multi-endpoint.png` 显示第二端点被裁剪，修复后截图 `temp/20260602111038-ai-provider-multi-endpoint-fixed.png` 显示 OpenAI 与 Anthropic 两个长端点均完整可见，截图临时渠道数据已清理。
- [x] **FB-14**: 智能中心模型列、渠道名称外链和渠道接入地址字段文案需按反馈调整
  - 问题分析：根因是渠道列表的名称列只渲染渠道名称，没有把已有 `websiteUrl` 投影展示为可点击外链；模型列只平铺模型标签，未按截图中的协议分组胶囊样式展示，也没有把已有模型删除接口暴露为可发现操作；渠道表单 Base URL 运行时文案仍保留英文源字符串。验证过程中还发现插件 `@fs` 页面不能解析 `@vben/icons` 包别名，若在插件页面直接引入会导致 Vite 返回 500 并阻断登录页渲染。
  - 修复记录：渠道名称列改为“名称 + 官网地址”上下结构，官网地址使用 `target="_blank"` 与 `rel="noopener noreferrer"` 在新页面打开；模型列提前到名称后方，并改为按 `OpenAI`、`Anthropic` 协议分组的圆角胶囊布局，胶囊右侧 `x` 按钮通过 `Popconfirm` 调用已有 `modelDelete` 接口删除模型，删除后刷新渠道列表；渠道新增和编辑表单中 `OpenAI Base URL`、`Anthropic Base URL` 改为 `OpenAI 接入地址`、`Anthropic 接入地址`，英文资源对应改为 `OpenAI Access URL`、`Anthropic Access URL`；插件版本提升到 `v0.1.3` 以触发运行时资源升级。为避免插件 `@fs` 解析问题，模型胶囊关闭按钮使用本地 ASCII 文本，不新增前端包依赖。
  - 影响分析：`i18n` 有影响，已更新启用 `i18n` 的 `linapro-ai-core` 插件 `en-US`/`zh-CN` 运行时资源；前端 UI 有影响，渠道列表名称列、模型列、模型删除交互和渠道表单标签已更新；测试有影响，更新插件自有 `TC001`、POM 和 E2E API fixture，覆盖新文案、官网外链新页打开、模型胶囊删除按钮和删除路径；数据权限无新增边界，生产数据删除仍走既有 `modelDelete` API 与 `ai:provider:delete` 权限；缓存一致性影响限于插件版本升级触发现有运行时资源刷新，无新增业务缓存；开发工具跨平台无新增脚本或命令入口；API 契约无变更，不修改后端 DTO、路由或响应结构；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`data-permission.md`、`cache-consistency.md`、`dev-tooling.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`karpathy-guidelines`、`lina-e2e` 和 `playwright-cli`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`make i18n.check` 通过，仍仅有既有模块级 `$t()` warning，未新增 `plugin:linapro-ai-core` warning；`pnpm -C hack/tests test:validate` 通过；`make dev` 已重启后端和前端以加载 `linapro-ai-core v0.1.3` 资源；首次插件 E2E 暴露运行中旧资源和 `@vben/icons` 插件页解析问题，修复并重启后 `E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugin:linapro-ai-core` 通过，7 个插件 E2E 用例全部通过；调整模型列顺序后该 E2E 再次通过，7 个插件 E2E 用例全部通过；Playwright 视觉截图 `temp/20260602-ai-provider-fb14.png` 确认默认视口中模型胶囊列可见；`openspec validate add-ai-text-capability-center --strict`、`git diff --check`、`git -C apps/lina-plugins diff --check` 均通过。
- [x] **FB-15**: 智能中心渠道列表密钥脱敏、端点标签对齐、同步入口和模型列删除按钮需按反馈调整
  - 问题分析：根因是渠道列表的展示实现仍存在四处首版残留：后端 `maskSecretRef()` 把原密钥投影为 `secret-***<hash>`，前端无法展示 `sk-**********90` 这类基于原值的脱敏形态；端点列协议标签未固定宽度和右对齐，导致 URL 左边界不齐；操作列把同步入口折叠进“更多”，且固定操作列和主体行是不同 DOM 区域，E2E 行内定位也不稳定；模型胶囊仍对模型名或协议使用加粗样式，删除按钮使用文本 `x`。修复过程中还复现了插件动态页面不能在 `ai-data.ts` 这类插件页面模块中直接解析 `@vben/icons` 的 Vite overlay，因此删除图标不能通过该 bare import 实现。
  - 修复记录：已将后端密钥投影改为保留可识别前缀和末尾 2 位、中间固定星号隐藏，例如 `sk-1234567890` 返回 `sk-**********90`，并补充 helper 单测和 API 示例值；端点标签改为固定等宽并右对齐；操作列去掉“更多”菜单，OpenAI 与 Anthropic 同步按钮直接上下排列，同时增加模型列最小高度避免固定操作列裁剪；模型列移除加粗字重；删除按钮改为带 `lucide-x` 形态 CSS mask 的图标按钮，不再使用文本 `x` 或不可解析的 `@vben/icons` 导入。同步更新 OpenSpec 渠道列表契约、插件版本提升到 `v0.1.4`，并更新插件自有 E2E/POM 断言覆盖本轮反馈。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 渠道列表端点列、密钥列、模型列和操作列；后端 API 投影语义有影响，但不修改路由、字段名、HTTP 方法、权限标签或存储模型，仍不返回明文 API key；数据权限边界不变，渠道列表仍是平台配置控制面并走既有权限；缓存一致性无新增业务缓存，插件版本升级仅触发现有插件运行时资源刷新；`i18n` 无新增运行时文案、语言包或 API 文档源文本翻译项，仅更新示例值；开发工具跨平台无新增脚本或命令入口；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`cache-consistency.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`goframe-v2` 和 `lina-e2e`。
  - 验证记录：`GOWORK=off go test ./backend/api/provider/v1 ./backend/internal/service/ai -run TestMaskSecretRefPreservesRecognizablePrefixAndSuffix -count=1` 通过；`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`make i18n.check` 通过，仍仅有既有模块级 `$t()` warning，未新增 `plugin:linapro-ai-core` warning；`make dev` 通过并重启后端、前端加载 `linapro-ai-core v0.1.4`；`E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugin:linapro-ai-core` 重跑后通过，8 个插件 E2E 用例全部通过；`openspec validate add-ai-text-capability-center --strict` 通过。截图辅助验证：`temp/20260602-login-debug.png` 复现 `@vben/icons` overlay，修复后 `temp/20260602-login-debug-fixed.png` 确认登录页正常渲染。
- [x] **FB-16**: 智能中心档位管理 Thinking Effort 文案、测试按钮 loading 和测试超时需按反馈调整
  - 问题分析：根因是档位字段文案仍由插件运行时语言包配置为`Default Thinking Effort`/`默认 Thinking Effort`；档位列表和抽屉的测试按钮直接等待`POST /ai/tiers/{code}/test`返回，没有局部 loading/disabled 状态，重复点击会发起重复测试请求；后端虽然源码插件共享 HTTP client 当前配置了 60s 超时，但`TestTier`服务边界没有独立测试超时，未来注入无超时 HTTP client 或替换调用路径时仍可能被上游 AI API 阻塞。
  - 修复记录：已将插件`en-US`/`zh-CN`运行时字段统一改为`Thinking Effort`，插件版本提升到`v0.1.5`以触发运行时资源升级；档位列表按档位 code 维护测试中状态，测试请求期间对应`ghost-button`显示 loading 并禁用，抽屉测试按钮同样在请求期间 loading/disabled；前端`tierTest()`请求超时调整为 65s，避免浏览器 10s 默认超时早于后端保护；后端`TestTier`在渠道调用前创建服务层测试上下文，默认超时为`60s`，仅包住 provider 调用，失败结果写回仍使用原请求上下文。
  - 影响分析：`i18n`有影响，资源归属为启用`i18n`的`linapro-ai-core`插件运行时语言包，目标语言为`en-US`和`zh-CN`；前端 UI 有影响，档位列表和抽屉测试按钮交互已更新；后端 Go 有影响，仅新增测试调用超时值配置和包内`TierTestInput.timeout`测试钩子，不新增运行期依赖或 DI 传递路径；API 契约无变更，未修改路由、HTTP 方法、公开请求/响应 DTO 或权限标签；数据权限边界不变，仍为平台配置控制面和既有`ai:tier:test`执行权限；缓存一致性无新增业务缓存，插件版本升级仅触发现有插件运行时资源刷新；开发工具跨平台无新增脚本或命令入口；插件目录本地`AGENTS.md`不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`cache-consistency.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并使用`lina-feedback`、`vben`、`goframe-v2`和`lina-e2e`。
  - 验证记录：`GOWORK=off go test ./backend/internal/service/ai -run TestTierTestAppliesProviderCallTimeout -count=1 -v`通过；`GOWORK=off go test ./backend/internal/service/ai -count=1`通过；`GOWORK=off go test ./backend/... -count=1`通过；`pnpm -C apps/lina-vben/apps/web-antd typecheck`通过；`make i18n.check`通过，仍仅有既有模块级`$t()` warning，未新增`plugin:linapro-ai-core` warning；`pnpm -C hack/tests test:validate`通过；首次插件 E2E 暴露 loading 图标断言 strict locator 问题，收窄断言后`E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugin:linapro-ai-core`通过，8 个插件 E2E 用例全部通过；`openspec validate add-ai-text-capability-center --strict`、`git diff --check`、`git -C apps/lina-plugins diff --check`均通过。
- [x] **FB-17**: 智能中心能力档位需要引入 `capabilityMethod` 身份维度并同步 Go 契约、SQL、API 和缓存解析
  - 问题分析：根因是原档位身份只按 `capabilityType + tierCode` 建模，`text` 首期可工作，但后续同一能力类型下新增多个方法时会出现同类型档位、模型绑定、调用日志和缓存解析互相污染，例如 `audio.transcribe` 与 `audio.synthesize` 同属 `audio` 却不能共享同一档位绑定。Go 契约层也缺少方法维度，插件服务只能把文本生成硬编码为 `CapabilityTypeText`。
  - 修复记录：已在宿主 `aitext` 契约新增 `CapabilityMethod` 与 `CapabilityMethodGenerate`，`GenerateRequest` 显式返回 `CapabilityTypeText + CapabilityMethodGenerate`；插件 SQL 为模型、档位和调用日志增加 `capability_method`，唯一约束、筛选索引、seed 和 DAO/DO/Entity 均同步；插件 service/API/controller/front-end client/E2E helper 均传递或投影 `capabilityMethod`，档位唯一身份和缓存 key 调整为 `capabilityType + capabilityMethod + tierCode`；模型选择、模型写入、同步模型、调用日志写入和筛选均默认使用 `text.generate`，并在绑定解析时拒绝 method 不匹配的模型。
  - 影响分析：API 契约有影响，新增 `capabilityMethod` 请求/响应字段和 API 文档翻译，HTTP 方法、路径、权限标签和时间字段契约不变；数据库有影响，插件安装 SQL 保持幂等，`capability_method` 默认 `generate`，索引覆盖模型筛选、档位唯一身份和调用日志过滤；后端 Go 有影响，未新增运行期依赖或 DI 路径，缓存权威源仍为插件数据库，失效仍通过写入成功后的共享修订号和本地缓存清理完成；前端 UI 无新增可编辑字段，前端适配器仅固定传入 `text.generate`；数据权限边界不变，仍为平台配置控制面和既有权限；`i18n` 有 API 文档翻译影响，无新增运行时 UI 文案；开发工具跨平台影响限于重新执行既有 `make dao` 生成流程，无新增脚本或入口；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`database.md`、`cache-consistency.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`i18n.md`、`dev-tooling.md`，并使用 `lina-feedback`、`goframe-v2`、`vben` 和 `lina-e2e`。
  - 验证记录：`make -C apps/lina-plugins/linapro-ai-core dao` 通过并重新生成 DAO/DO/Entity；`GOWORK=off go test ./backend/internal/service/ai -count=1` 通过；`GOWORK=off go test ./backend/... -count=1` 通过；`GOWORK=off go test ./pkg/plugin/capability/ai/aitext ./internal/service/plugin/internal/wasm ./pkg/plugin/pluginbridge/internal/hostservice ./pkg/plugin/capability/guest ./internal/service/plugin ./internal/cmd -count=1` 通过；`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`make i18n.check` 通过，仍仅有既有模块级 `$t()` warning，未新增 `plugin:linapro-ai-core` warning；`pnpm -C hack/tests test:validate` 通过；`E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugin:linapro-ai-core` 因本地 `http://127.0.0.1:9120/admin/auth/login` 未启动返回 `ERR_CONNECTION_REFUSED`，未进入业务断言；`openspec validate add-ai-text-capability-center --strict`、`git diff --check`、`git -C apps/lina-plugins diff --check` 均通过。
- [x] **FB-18**: 智能中心渠道列表端点角标、编辑密钥占位、操作列高度和端点操作需按反馈调整
  - 问题分析：根因是渠道列表端点列仍使用左侧协议标签结构，且列位置在模型列之后，默认视口下接入链接与协议标识不易同时看到；渠道操作列把“编辑、端点、删除、同步”堆叠在固定列中，内容高度容易被 VXE 固定列单元格裁剪；渠道编辑抽屉虽然已不回填脱敏密钥，但 placeholder 复用了端点级“留空则保持原端点密钥”文案，不符合渠道密钥语义。
  - 修复记录：端点列改为每个接入链接卡片右上角浮动协议文字角标，并将端点列提前到名称后方；渠道操作列移除“端点”按钮，保留编辑、删除和按协议同步模型按钮，并改为可换行的紧凑操作布局，同时关闭操作列溢出裁剪；渠道新增和编辑表单密钥字段统一复用渠道级 placeholder，编辑时保持空值并显示“留空则保持原密钥”，新增时显示“输入 API 密钥”；插件版本提升到 `v0.1.14` 以触发已安装源码插件资源刷新。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 渠道列表端点列、操作列和渠道抽屉密钥字段；`i18n` 有影响但未新增资源，复用启用 `i18n` 插件已有 `provider.placeholders.*` 运行时键，并通过 `make i18n.check` 验证；测试有影响，更新插件自有 `TC001`/`TC004` 与 POM 断言，覆盖多端点角标、密钥字段空值与占位、端点操作移除和操作列不裁剪；缓存一致性无新增业务缓存，插件版本升级仅触发现有插件运行时资源刷新；数据权限无新增或修改数据读写边界，仍使用既有平台配置控制面、渠道删除和模型同步权限；API 契约、后端 Go、SQL 和开发工具跨平台无影响，未修改路由、DTO、服务、数据库或脚本入口；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`、`cache-consistency.md`、`data-permission.md`，并使用 `lina-feedback`、`lina-e2e`、`vben`、`frontend-design`、`browser` 和 `lina-review`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`make i18n.check` 通过，仍仅有既有模块级 `$t()` warning，未新增 `plugin:linapro-ai-core` warning；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --trace off --output ../../temp/20260603-fb18-tc001-results` 通过，3 个渠道管理子用例全部通过；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC004-smart-center-provider-endpoints.ts` 中 `TC004` 通过，`TC001` 因 Playwright trace artifact 清理目录缺失失败，随后已用独立输出目录重跑 `TC001` 通过；`E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugin:linapro-ai-core` 中本轮相关 `TC001`/`TC004` 已通过，失败集中在既有档位默认参数和多模态用例；OpenSpec 严格校验、空白检查和子仓库空白检查在本任务收尾阶段重新执行。截图验证：in-app Browser 返回 `iab` 不可用，已降级使用 Playwright/E2E 截图，`temp/20260603120200-tc004-provider-endpoint-list.png` 显示两个接入链接右上角分别展示 Anthropic/OpenAI 角标，`temp/20260603120330-tc001-provider-edit-agent-box-fields.png` 显示编辑密钥字段为空且 placeholder 为“留空则保持原密钥”。
  - 审查记录：已按 `lina-review` 展开父仓库和 `apps/lina-plugins` 子仓库状态，识别到当前工作树存在与本轮无关的多模态档位改动；本轮反馈级审查限定在渠道页、渠道抽屉、渠道相关 E2E/POM、插件版本和 `FB-18` 任务记录。审查结论未发现阻塞问题；E2E 质量审查触发原因是用户可观察页面和表单行为变更，覆盖依据为 `TC001` 与 `TC004` 的断言和截图证据。
- [x] **FB-19**: 智能中心档位编辑页模型选择需按协议分组展示并包含同渠道下所有匹配模型
  - 问题分析：根因是档位编辑抽屉的模型下拉仅把候选模型平铺为普通 `Select` options，用户无法在同一渠道同时存在 `OpenAI` 和 `Anthropic` 协议模型时识别协议归属；同时页面缺少直接覆盖该混合协议渠道的 E2E 断言，导致“只看到 OpenAI 列表”的缺口未被及时暴露。
  - 修复记录：档位抽屉在选择渠道后仍通过既有 provider-scoped 模型接口一次性读取当前能力方法下的候选模型，不新增前端逐项补查；前端按协议家族将候选模型分组为 `OpenAI`、`Anthropic`、`Voyage`，并在分组内展示模型名称和 thinking effort 信息；新增插件自有 `TC002` 子用例，构造同一渠道下 `OpenAI` 与 `Anthropic` 两个 `text.generate` 模型并断言下拉分组和两个模型同时可见。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 档位编辑抽屉模型选择框；测试有影响，更新插件自有 `TC002` 和 `SmartCenterPage` POM；API 契约无变更，仍复用 `GET /ai/providers/{id}/models` 的分页候选接口且无新增字段、路径、方法或权限标签；数据权限边界不变，模型候选仍为平台配置控制面并受既有 `ai:provider:list`/`ai:tier:update` 页面权限保护；缓存一致性无新增业务缓存；`i18n` 无新增运行时文案，协议分组使用既有协议显示文本；开发工具跨平台无新增脚本或命令入口；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC002-smart-center-tier-management.ts --trace off --output ../../temp/20260604-fb19-fb20-tc002-results-2` 通过，6 个档位管理子用例全部通过，其中 `TC-2c` 覆盖混合协议模型分组。
- [x] **FB-20**: 智能中心档位测试需仅在测试已保存档位时更新最近测试信息，并展示本次测试耗时
  - 问题分析：根因是后端 `TestTier` 在使用草稿 `providerId/modelId` 完成测试后无条件写回 `plugin_linapro_ai_tier.last_test_*` 字段，导致未保存的档位配置污染已保存档位的最近测试摘要；前端成功或失败提示也没有把本次测试 `latencyMs` 显式展示出来，草稿测试路径缺少可持续展示位置。
  - 修复记录：后端将携带 `providerId` 或 `modelId` 的测试请求识别为草稿绑定测试，仍返回测试结果但不更新 `last_test_status`、`last_test_latency_ms`、`last_test_error_summary` 和 `last_test_at`；仅未携带草稿绑定、走已保存绑定解析路径的测试会更新最近测试摘要。档位列表的“最近测试”列展示最近已保存测试的耗时，抽屉草稿测试完成后展示本次测试耗时并在 toast 中附带 `latencyMs`，复用既有“耗时(ms)/Latency(ms)”翻译键避免运行中插件资源升级窗口出现新 key。
  - 影响分析：后端 Go 有影响，范围为插件 `TierService.TestTier` 的写回条件和单元测试，不新增运行期依赖或 DI 路径；前端 UI 有影响，范围为档位列表最近测试列、档位抽屉草稿测试结果和测试 toast；测试有影响，更新插件 service 单测和 `TC002`；数据权限边界不变，仍为平台配置控制面和既有 `ai:tier:test` 执行动作权限；缓存一致性无新增业务缓存，草稿测试不触发档位缓存失效，已保存档位测试仅更新测试摘要不改变绑定解析缓存；`i18n` 无新增资源，复用已有耗时文案；API 契约无字段或路由变更，响应中既有 `latencyMs` 继续作为本次测试耗时来源；开发工具跨平台无新增脚本或命令入口；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。
  - 验证记录：`GOWORK=off go test ./backend/internal/service/ai -run 'TestTierDraftTestDoesNotPersistBinding|TestTierTestAppliesProviderCallTimeout' -count=1` 通过；`GOWORK=off go test ./backend/internal/service/ai -count=1` 通过；`GOWORK=off go test ./backend/... -count=1` 通过；`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`make i18n.check` 通过，仍仅有既有模块级 `$t()` warning，未新增 `plugin:linapro-ai-core` warning；`pnpm -C hack/tests test:validate` 通过；首次 `TC002` 暴露新增 `currentTest` 运行时 key 在已安装插件资源未升级时显示原始 key，已改为复用既有耗时 key；随后 `pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC002-smart-center-tier-management.ts --trace off --output ../../temp/20260604-fb19-fb20-tc002-results-2` 通过，6 个档位管理子用例全部通过；`openspec validate add-ai-text-capability-center --strict`、`git diff --check` 与 `git -C apps/lina-plugins diff --check` 均通过。
- [x] **FB-21**: OpenAI 和 Anthropic 兼容渠道 404 时自动重试 `/v1` 协议地址并缓存成功修正
  - 问题分析：根因是 OpenAI-compatible 与 Anthropic-compatible 适配器只把资源路径直接拼接到渠道 `baseUrl` 后，例如 MiMo Anthropic 端点配置为 `https://token-plan-cn.xiaomimimo.com/anthropic` 时会请求 `/anthropic/messages`，而该兼容协议实际路径为 `/anthropic/v1/messages`，因此档位测试返回 `404`。同类问题也会影响 OpenAI 兼容的 `/chat/completions`、两类协议的 `/models` 同步路径。原实现没有在 `404` 后尝试补齐 `/v1`，也没有缓存已验证的修正地址，导致每次都可能先失败一次。
  - 修复记录：插件后端 `serviceImpl` 新增进程内 `providerURLCache`，按 `protocol + configured baseUrl` 缓存修正后的 provider base URL。OpenAI-compatible 与 Anthropic-compatible 的文本生成和模型同步统一走 `doProviderRequest`：先按原配置请求；若返回 `404`，则判断资源路径前的 base 部分是否已以 `/v1` 结尾，已带 `/v1` 时不重复拼接并直接返回原错误，未带 `/v1` 时重试插入 `/v1` 后的地址；重试返回非 `404` 且非 `5xx` 时缓存修正 base URL。后续同进程内相同协议和 base URL 的 `/models`、`/messages`、`/chat/completions` 请求会直接走缓存 base URL；若缓存地址后续返回 `404`，会删除该缓存并回到正常探测流程。
  - 影响分析：后端 Go 有影响，范围为 `linapro-ai-core` 插件 provider adapter 请求路径和单元测试；不新增运行期依赖或 DI 传递路径，仅复用已有 `serviceImpl` 单例和注入的共享 `httpClient`。缓存一致性有影响，权威依据为渠道对 `/v1` 重试路径返回的非 `404`、非 `5xx` 响应；一致性模型为单进程性能优化缓存，不承载权限、租户、档位绑定或关键运行时配置；配置 base URL 变更会自然形成新 cache key，缓存地址 `404` 会本地失效，进程重启会重建，跨实例不需要同步，最大陈旧窗口为当前进程生命周期或下一次 `404` 失效。数据权限无影响，不新增数据库读写、租户/组织可见性或管理权限边界；`i18n` 无影响，不新增用户可见文案、错误码、API 文档源文本或语言包；API 契约无影响，不修改路由、HTTP 方法、请求/响应 DTO 或权限标签；SQL/DAO 无影响；开发工具跨平台无影响，不新增脚本或命令入口；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`backend-go.md`、`cache-consistency.md`、`data-permission.md`、`testing.md`、`i18n.md`、`dev-tooling.md`，并使用 `lina-feedback`、`goframe-v2` 和 `lina-review`。
  - 验证记录：`GOWORK=off go test ./backend/internal/service/ai -count=1` 通过，覆盖 Anthropic 生成 `404 -> /v1` 重试并缓存、OpenAI 模型同步 `404 -> /v1` 重试并缓存、已带 `/v1` 不重复拼接；`GOWORK=off go test ./backend/... -count=1` 通过；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check` 通过；`git -C apps/lina-plugins diff --check` 通过。
- [x] **FB-22**: 智能中心新增模型抽屉需要支持同一模型一次关联多个协议端点
  - 问题分析：根因是新增模型抽屉把端点和协议拆成 `endpointId` 单选与 `protocol` 单选两个字段，用户一次只能提交一个协议端点；而当前模型存储和后端创建接口以 `endpointId + protocol + modelName` 表示一个模型投影，同一模型名可以在同一渠道下分别关联 OpenAI、Anthropic 等多个协议端点。原页面没有提供多选端点协议入口，也没有 E2E 覆盖同一模型名一次创建多个协议投影的场景。
  - 修复记录：新增模型抽屉将“端点”和“协议”合并为“端点 / 协议”多选字段，选项来自所选渠道的端点列表并展示协议与接入地址；保存时按已选端点逐条调用既有 `POST /ai/providers/{providerId}/models` 创建模型，并从端点读取真实 `protocol`，保持后端 API、存储模型、档位绑定、调用日志和缓存解析契约不变。若批量创建过程中后续请求失败，前端会删除本次已成功创建的模型，避免留下半成功的模型投影。插件自有 `TC001` 增加同一模型名同时选择 OpenAI 与 Anthropic 端点协议的路径，断言产生两次创建请求并在渠道列表模型标签中分别展示 `OpenAI` 和 `Anthropic`。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 新增模型抽屉表单字段、保存流程和渠道管理 POM；测试有影响，更新插件自有 `TC001` 并增加提交后截图证据；API 契约无变更，仍复用既有创建模型接口且不修改路由、HTTP 方法、请求/响应 DTO、权限标签或时间字段；架构边界无宿主核心影响，未修改 `apps/lina-core`，仅在插件前端适配层扩展工作台交互；数据权限边界不变，仍使用既有模型创建/删除接口权限和平台配置控制面，前端未绕过后端校验；缓存一致性无新增业务缓存，模型创建和删除仍走既有服务失效路径；`i18n` 无新增语言包，字段标签复用已有“端点”和“协议”翻译键组合，`make i18n.check` 未新增 `plugin:linapro-ai-core` warning；开发工具跨平台无影响，不新增脚本或命令入口；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`api-contract.md`、`cache-consistency.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并使用 `lina-feedback`、`vben`、`lina-e2e` 和 `lina-review`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`make i18n.check` 通过，仍仅有既有模块级 `$t()` warning，未新增 `plugin:linapro-ai-core` warning；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --grep 'TC-1a' --trace off --output ../../temp/20260604-fb22-tc001a-results-8 --timeout 90000` 通过；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --trace off --output ../../temp/20260604-fb22-tc001-results-final-2 --timeout 90000` 通过，3 个渠道管理子用例全部通过；截图验证 `temp/20260604063122-tc001-create-model-drawer-default.png` 显示“端点 / 协议”多选字段替代旧协议单选，`temp/20260604063126-tc001-provider-list-multi-protocol-model.png` 显示同一模型名分别关联 `OpenAI` 和 `Anthropic` 标签。
- [x] **FB-23**: 智能中心渠道页同步模型、渠道操作列和模型管理 Tab 需按反馈调整
  - 问题分析：根因是渠道页的模型同步入口仍按协议拆成 OpenAI 和 Anthropic 两个用户操作，后端同步流程也偏向单协议端点；渠道操作列没有把“新增模型”作为行级快捷入口；页面只提供渠道维度列表，缺少按模型维度查看、编辑和删除全部模型的管理入口，导致用户需要回到渠道行内模型标签才能处理模型。
  - 修复记录：渠道页新增“渠道管理”和“模型管理”两个 Tab；渠道管理 Tab 的工具栏保留新增渠道并提供新增模型入口，行操作列调整为三行布局：编辑/删除、新增模型、同步模型。行级新增模型复用既有模型抽屉并默认选中当前渠道；同步模型前端只调用一个 `modelSync(providerId)` 动作，不再传协议。后端将 `POST /ai/providers/{providerId}/models/sync` 的 `protocol` 改为可选，未传时查询该渠道全部已启用端点，按端点协议调用对应模型列表接口并汇总成功结果；单个端点失败时保留其他端点成功结果，仅当全部可同步端点失败时返回错误。新增 `GET /ai/models` 模型维度分页接口，模型管理 Tab 使用该接口展示模型名称、渠道、协议、端点、能力方法、启用状态和更新时间，并支持新增、编辑和删除模型。插件版本提升到 `v0.1.16` 以刷新已安装源码插件运行时资源。
  - 影响分析：API 契约有影响，新增 `GET /ai/models` 且同步接口请求体 `protocol` 变为可选，HTTP 方法、资源语义、权限标签和时间字段契约符合规则；后端 Go 有影响，模型列表采用数据库侧筛选、分页后批量装配渠道、端点和能力投影，避免前端逐行补查和后端 `N+1`；同步远程调用仅遍历当前渠道已启用端点集合，失败容忍边界清晰。前端 UI 有影响，范围为 `linapro-ai-core` 渠道页 Tab、操作列、模型管理表格和模型抽屉编辑模式；测试有影响，更新插件自有 `TC001`、`TC004` 和 POM。`i18n` 有影响，已更新启用 `i18n` 插件的 `en-US`/`zh-CN` 运行时资源和 `zh-CN` API 文档翻译，`en-US` apidoc 保持空对象占位。缓存一致性无新增业务缓存，模型创建、更新、删除和同步仍在写入成功后调用既有档位缓存失效；数据权限边界不变，仍为平台配置控制面，新增模型列表接口使用既有 `ai:provider:list` 权限，写操作沿用既有 create/update/delete 权限；数据库无 SQL/DAO 变更，无新增索引或迁移，现有查询路径基于既有模型、渠道、端点和能力表集合化读取；开发工具跨平台无影响，不新增脚本、命令或 CI 入口；运行期 DI 无新增依赖，继续复用现有 `ai.Service`、控制器构造和注入的 `httpClient`；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`database.md`、`cache-consistency.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`i18n.md`、`dev-tooling.md`，并使用 `lina-feedback`、`goframe-v2`、`vben`、`frontend-design`、`karpathy-guidelines`、`lina-e2e` 和 `lina-review`。
  - 验证记录：`make -C apps/lina-plugins/linapro-ai-core ctrl` 通过并更新 GoFrame API 生成接口；`GOWORK=off go test ./backend/internal/service/ai -count=1` 通过；`GOWORK=off go test ./backend/... -count=1` 通过；`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`make i18n.check` 通过，仍仅有既有模块级 `$t()` warning，未新增 `plugin:linapro-ai-core` warning；`make dev plugins=1` 已重启后端和前端加载 `linapro-ai-core v0.1.16`；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --trace off --output ../../temp/20260604-fb23-tc001-results-6 --timeout 120000` 通过，3 个渠道管理子用例全部通过；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC004-smart-center-provider-endpoints.ts --trace off --output ../../temp/20260604-fb23-tc004-results --timeout 120000` 通过；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check` 与 `git -C apps/lina-plugins diff --check` 通过。
  - 审查记录：已按 `lina-review` 从父仓库 `git status --short`、未跟踪文件、`apps/lina-plugins` 子仓库状态和 OpenSpec 变更状态展开审查范围，包含两个未跟踪新增 Go 文件；插件根目录无本地 `AGENTS.md`。审查文件覆盖 OpenSpec 规范/任务、插件 API/controller/service/unit test、前端页面/client/data/drawer、运行时和 apidoc i18n、插件版本、插件 E2E/POM。E2E 质量审查触发原因是用户可观察页面、表格、抽屉和端到端工作流变化；覆盖依据为 `TC001` 和 `TC004` 断言。审查结论未发现阻塞问题；剩余风险为 `make i18n.check` 仍报告仓库既有模块级 `$t()` warning，但本次未新增 `linapro-ai-core` warning。
- [x] **FB-24**: 智能中心渠道页面高度持续增长且 Tab 标题缺少图标
  - 问题分析：根因是渠道页在 `Page :auto-content-height="true"` 内容区内嵌套 `Tabs`，同时渠道和模型两个 `VxeGrid` 使用 `height: "auto"`，表格行内容又包含可变高的模型与端点展示；`Tabs` 内容高度和表格自动高度互相参与测量，导致 Tab 容器持续被推高。Tab 标题缺少图标的根因是上一轮仅用纯文本作为 `TabPane` 标题。
  - 修复记录：渠道页改为固定内容区布局，`Page` 内容区使用 `min-h-0 overflow-hidden`，`Tabs` 以 flex 方式占满内容区，Tab 内容区和表格容器限制为 `100%` 高度并隐藏外层溢出，两个 `VxeGrid` 改为 `height: "100%"`，由表格内部承担滚动，避免继续撑高外层 Tab。两个 Tab 标题改为 `IconifyIcon + 文案`，渠道管理使用 `lucide:building-2`，模型管理使用 `lucide:box`。插件自有 `TC001` 的 POM 增加 Tab 图标可见且具备非零尺寸断言，并连续采样页面和 Tab 高度验证不再持续增长。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 渠道页 Tab 标题和页面高度约束；测试有影响，更新插件自有 `SmartCenterPage` POM 并复用 `TC001` 覆盖原问题复现场景。`i18n` 无新增资源，Tab 文案复用既有 `plugin.linapro-ai-core.provider.tabs.*` 翻译键，`make i18n.check` 未新增 `plugin:linapro-ai-core` warning；缓存一致性无影响，不新增缓存、快照或失效路径；数据权限无影响，不新增数据读取、写入或可见性边界；API 契约、后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改路由、DTO、服务、数据库或脚本入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`、`cache-consistency.md`、`data-permission.md`、`dev-tooling.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。
  - 验证记录：首次重跑 `pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --trace off --output ../../temp/20260604-fb24-tc001-results-2 --timeout 120000` 时暴露 Tab 高度在采样窗口增长 `700px`，确认原问题仍存在；修复布局后重跑 `pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --trace off --output ../../temp/20260604-fb24-tc001-results-3 --timeout 120000` 通过，3 个渠道管理子用例全部通过。截图验证：`temp/20260604074014-tc001-provider-list-layout.png` 显示两个 Tab 图标可见，页面内容稳定在当前视口内且表格分页、操作列正常展示。治理验证通过：`pnpm -C apps/lina-vben/apps/web-antd typecheck`、`pnpm -C hack/tests test:validate`、`make i18n.check`、`openspec validate add-ai-text-capability-center --strict`、`git diff --check`、`git -C apps/lina-plugins diff --check`；`make i18n.check` 仍仅报告仓库既有模块级 `$t()` warning，未新增 `linapro-ai-core` warning。
  - 审查记录：已按 `lina-review` 从父仓库 `git status --short`、未跟踪文件、`apps/lina-plugins` 子仓库状态、展开后的子仓库 diff 和 `openspec status --change add-ai-text-capability-center --json` 收集范围；本轮反馈级审查限定在渠道页布局与 Tab 图标、插件 POM/E2E 断言、OpenSpec 规范和 `FB-24` 任务记录，识别到工作树还包含 `FB-23` 既有后端/API/i18n 未提交改动但未作为本轮新增阻塞项。E2E 质量审查触发原因是用户可观察页面布局和 Tab 标题变化；覆盖依据为 `TC001` 中 Tab 文案翻译、图标非零尺寸、页面/Tab 高度稳定采样和截图证据。审查结论未发现阻塞问题；剩余风险为 `make i18n.check` 仍报告仓库既有模块级 `$t()` warning，但本次未新增 `linapro-ai-core` warning。
- [x] **FB-25**: 智能中心渠道列表模型列需流式紧凑展示并调整同步模型按钮颜色
  - 问题分析：根因是渠道列表模型列仍按协议分组渲染为纵向容器，每个协议组独占一行；模型标签使用 `text-sm`、较大内边距和 `gap-2` 行间距，导致短模型名也不容易在同一行并排展示。同步模型按钮继续沿用普通操作列主色 ghost 样式，和“新增模型”等操作缺少视觉区分。
  - 修复记录：模型列保留协议排序和删除入口，但取消按协议分行，改为单个 `flex-wrap` 流式标签容器；模型标签降为 `text-xs`，协议标识降为 `11px`，压缩 padding、gap 和行高，让 `gpt-4o`、`mimo` 等短模型名可同一行展示多个。同步模型按钮保留 `ghost-button` 操作列语义，增加局部 `ai-provider-sync-button` 样式并改为青绿色同步色，不影响编辑、删除和新增模型按钮。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 渠道列表模型列标签布局和同步模型按钮颜色；测试有影响，更新插件自有 `TC001` 测试数据和 POM 断言，覆盖紧凑字号、单个可换行模型行、短模型同一行多个标签和同步按钮独立颜色。`i18n` 无影响，不新增或修改运行时文案、菜单、按钮文案、语言包或 API 文档；缓存一致性无影响，不新增缓存、快照或失效路径；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；API 契约、后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改路由、DTO、服务、数据库、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`plugin.md`、`frontend-ui.md`、`testing.md`，并评估 `i18n`、缓存一致性、数据权限、开发工具跨平台均无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`make i18n.check` 通过，仍仅有仓库既有模块级 `$t()` warning，未新增 `plugin:linapro-ai-core` warning；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --trace off --output ../../temp/20260604-fb25-tc001-results-4 --timeout 120000` 通过，3 个渠道管理子用例全部通过；截图验证 `temp/20260604082526-tc001-provider-list-multi-protocol-model.png` 显示 `mimo OpenAI` 与 `mimo Anthropic` 在同一行展示，`同步模型` 按钮为青绿色；`openspec validate add-ai-text-capability-center --strict`、`git diff --check` 与 `git -C apps/lina-plugins diff --check` 均通过。
  - 审查记录：已按 `lina-review` 从父仓库状态、插件子仓库状态、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮审查范围限定为渠道列表模型列布局、同步模型按钮局部样式、`TC001` 测试数据/POM 断言和 `FB-25` 任务记录。插件根目录无本地 `AGENTS.md`。E2E 质量审查触发原因是用户可观察页面布局和操作按钮视觉变化；覆盖依据为 `TC001` 的字号/间距/同行标签/按钮颜色断言和截图证据。审查未发现阻塞问题；剩余风险为工作树仍包含与本轮无关的既有 `FB-23`/`FB-24` 后端、API、i18n、E2E 和未跟踪 Go 文件改动，以及 `FB-26` 未完成任务，本轮未回退或审查为新增问题。
- [x] **FB-26**: 渠道请求必须去除模型名末尾 `[...]` 工具后缀
  - 问题分析：根因是 OpenAI-compatible 和 Anthropic-compatible adapter 构造渠道请求 payload 时直接使用平台保存的 `binding.ModelName`，因此 `mimo-v2.5-pro[1m]` 这类为 Codex、Claude Code 等 coding agent 工具准备的平台侧后缀会原样传给渠道，导致渠道按不存在的模型名处理请求。
  - 修复记录：新增 `providerRequestModelName()`，仅在真正构造 provider protocol payload 时去除模型名末尾一个或多个非空 `[...]` 工具后缀；OpenAI 和 Anthropic 文本生成请求改用清洗后的模型名。平台侧模型身份、页面展示、档位绑定、响应投影和调用日志仍保留原始模型名，不修改数据库存储或同步模型结果。
  - 影响分析：后端 Go 有影响，范围为 `linapro-ai-core` 插件 provider adapter 出站请求和自包含单元测试；API 契约无影响，不修改路由、HTTP 方法、DTO、权限标签或响应字段；数据权限无影响，不新增数据库读写、租户/组织可见性或管理权限边界；缓存一致性无影响，不新增缓存、快照或失效路径；`i18n` 无影响，不新增用户可见文案、错误消息、API 文档源文本或语言包；SQL/DAO 无影响，不修改迁移、seed、DAO、DO 或 Entity；开发工具跨平台无影响，不新增脚本、命令或 CI 入口；运行期 DI 无影响，未新增运行期依赖，继续复用既有 `serviceImpl` 和注入的共享 `httpClient`；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`backend-go.md`、`testing.md`，并评估 API、数据权限、缓存一致性、`i18n`、SQL/DAO、开发工具跨平台均无契约影响；使用 `lina-feedback`、`goframe-v2` 和 `lina-review`。
  - 验证记录：`GOWORK=off go test ./backend/internal/service/ai -run 'TestProviderRequestModelNameStripsPlatformSuffix|TestProviderAdaptersStripPlatformModelSuffix' -count=1` 通过，覆盖 helper 边界和 OpenAI/Anthropic 出站 payload；`GOWORK=off go test ./backend/internal/service/ai -count=1` 通过；`GOWORK=off go test ./backend/... -count=1` 通过；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check` 与 `git -C apps/lina-plugins diff --check` 均通过。
  - 审查记录：已按 `lina-review` 从父仓库状态、插件子仓库状态、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮审查范围限定为 `linapro-ai-core` provider adapter 出站模型名清洗、helper 边界测试、OpenAI/Anthropic fake server payload 断言和 `FB-26` 规范/任务记录。插件根目录无本地 `AGENTS.md`。E2E 质量审查未触发，原因是本轮没有前端页面、路由、表单、表格、权限交互或端到端工作流变化；后端运行时 bugfix 已由单元测试覆盖原问题复现场景和修复后预期。审查未发现阻塞问题；剩余风险为工作树仍包含与本轮无关的既有 `FB-23`/`FB-25` 后端、API、前端、i18n、E2E 和未跟踪 Go 文件改动，本轮未回退或审查为新增问题。
- [x] **FB-27**: 渠道列表模型列需加宽、超高折叠展开，并加宽端点列
  - 问题分析：根因是渠道列表模型列和端点列仍使用较窄的 `minWidth: 300`，模型标签较多时会明显挤压模型内容和端点 URL；上一轮模型列改为流式排列后没有高度上限，模型数量多时会撑高整行并影响表格扫描效率。页面也缺少显式展开入口，用户无法在保持默认紧凑行高的同时查看全部模型。
  - 修复记录：渠道模型列和端点列 `minWidth` 均提升到 `420`；模型列新增局部 `ProviderModelsCell` 单元格组件，按协议排序后在单个流式容器中展示模型标签，默认在内容高度超过 `72px` 时折叠并显示渐隐遮罩和“展开”按钮，点击后显示全部模型并切换为“折叠”。按钮复用宿主公共 `pages.common.expand/collapse` 翻译键，避免新增插件运行时资源和版本升级依赖。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 渠道列表模型列宽度、端点列宽度、模型列折叠/展开交互和局部样式；测试有影响，更新插件自有 `TC001` 测试数据和 POM 断言，覆盖模型列/端点列宽度、默认折叠高度、展开后完整高度和公共翻译文案。`i18n` 有影响但不新增插件语言包，复用宿主公共键并通过 `make i18n.check` 验证；缓存一致性无影响，不新增缓存、快照或失效路径；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；API 契约、后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改路由、DTO、服务、数据库、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估缓存一致性、数据权限、开发工具跨平台均无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`make i18n.check` 通过，仍仅有仓库既有模块级 `$t()` warning；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --trace off --output ../../temp/20260604-fb27-tc001-results-5 --timeout 120000` 通过，3 个渠道管理子用例全部通过，`.last-run.json` 为 `passed`；截图验证 `temp/20260604085144-tc001-provider-list-layout.png` 显示模型列默认折叠并展示“展开”按钮，`temp/20260604085150-tc001-provider-list-multi-protocol-model.png` 显示展开后全部模型可见；`openspec validate add-ai-text-capability-center --strict`、`git diff --check` 与 `git -C apps/lina-plugins diff --check` 均通过。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，从父仓库状态、插件子仓库状态、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮审查范围限定为渠道列表模型列/端点列宽度、模型列折叠展开局部组件和样式、`TC001` 测试数据/POM 断言以及 `FB-27` 任务记录。插件根目录无本地 `AGENTS.md`。E2E 质量审查触发原因是用户可观察表格布局和展开/折叠交互变化；覆盖依据为 `TC001` 中模型列/端点列宽度、默认折叠高度、展开后完整高度、公共翻译文案和截图证据。审查未发现阻塞问题；剩余风险为工作树仍包含与本轮无关的既有 `FB-23` 至 `FB-28` 后端、API、前端、i18n、E2E 和未跟踪 Go 文件改动，本轮未回退或审查为新增问题。
- [x] **FB-28**: 同步模型按钮颜色需从青绿色调整为常见橙黄色
  - 问题分析：根因是 `FB-25` 为同步模型按钮引入的局部样式使用青绿色 `hsl(188deg ...)`，虽然能和新增模型按钮区分，但颜色偏冷且不符合用户希望使用常见按钮颜色的反馈；测试侧也只断言颜色不同，未约束到用户确认的橙黄色方案。
  - 修复记录：保留 `ghost-button` 和操作列布局不变，仅将 `ai-provider-sync-button` 普通态改为常见琥珀/橙黄色 `#d97706`，hover 态改为更深的 `#b45309`，背景和边框使用同色低透明度；插件自有 `SmartCenterPage` POM 将同步按钮颜色断言收紧到浏览器计算色 `rgb(217, 119, 6)`，避免回退为青绿色。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 渠道列表同步模型按钮局部样式；测试有影响，更新插件自有 POM 并复用 `TC001` 覆盖按钮实际计算色。`i18n` 无影响，不新增或修改运行时文案、菜单、按钮文案、语言包或 API 文档；缓存一致性无影响，不新增缓存、快照或失效路径；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；API 契约、后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改路由、DTO、服务、数据库、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`，并评估缓存一致性、数据权限、`i18n`、开发工具跨平台均无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check` 与 `git -C apps/lina-plugins diff --check` 通过。首次运行 `pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --grep 'TC-1a' --trace off --output ../../temp/20260604-fb28-tc001a-results --timeout 120000` 时命中测试进程读取到的旧折叠按钮断言，复查当前 POM 已与页面“折叠/Collapse”文案一致；重跑 `pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --grep 'TC-1a' --trace off --output ../../temp/20260604-fb28-tc001a-results-2 --timeout 120000` 通过。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md` 和 `testing.md`，从父仓库状态、插件子仓库状态、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮审查范围限定为同步按钮局部色值、`SmartCenterPage` 颜色断言和 `FB-28` 任务记录。插件根目录无本地 `AGENTS.md`。E2E 质量审查触发原因是用户可观察按钮视觉变化和插件 POM 断言更新；覆盖依据为 `TC001` 中操作列布局、同步按钮计算色和定向 `TC-1a` 通过证据。审查未发现阻塞问题；剩余风险为工作树仍包含与本轮无关的既有 `FB-23` 至 `FB-27` 后端、API、前端、i18n、E2E 和未跟踪 Go 文件改动，本轮未回退或审查为新增问题。
- [x] **FB-29**: 同步模型按钮颜色需改为通用蓝色
  - 问题分析：根因是 `FB-28` 将同步模型按钮调整为橙黄色后，虽然使用了常见警示/强调色系，但用户继续反馈希望改回更通用的后台按钮蓝色；同步按钮仍使用局部色值和 POM 精确断言，因此需要同步替换样式与测试期望。
  - 修复记录：保留 `ghost-button`、操作列布局和低透明背景处理不变，将 `ai-provider-sync-button` 普通态改为 Ant Design 常见主蓝 `#1677ff`，hover 态改为 `#0958d9`；插件自有 `SmartCenterPage` POM 将同步按钮计算色断言更新为 `rgb(22, 119, 255)`，并移除同步按钮文字色必须不同于新增模型按钮的断言，因为通用蓝会接近默认 primary ghost 按钮色。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 渠道列表同步模型按钮局部样式；测试有影响，更新插件自有 POM 并复用 `TC001` 覆盖按钮实际计算色。`i18n` 无影响，不新增或修改运行时文案、菜单、按钮文案、语言包或 API 文档；缓存一致性无影响，不新增缓存、快照或失效路径；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；API 契约、后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改路由、DTO、服务、数据库、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`，并评估缓存一致性、数据权限、`i18n`、开发工具跨平台均无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check` 与 `git -C apps/lina-plugins diff --check` 通过；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --grep 'TC-1a' --trace off --output ../../temp/20260604-fb29-tc001a-results --timeout 120000` 通过。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md` 和 `testing.md`，从父仓库状态、插件子仓库状态、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮审查范围限定为同步按钮蓝色局部样式、`SmartCenterPage` 蓝色计算色断言和 `FB-29` 任务记录。插件根目录无本地 `AGENTS.md`。E2E 质量审查触发原因是用户可观察按钮视觉变化和插件 POM 断言更新；覆盖依据为 `TC001` 中操作列布局、同步按钮计算色和定向 `TC-1a` 通过证据。审查未发现阻塞问题；剩余风险为工作树仍包含与本轮无关的既有 `FB-23` 至 `FB-28` 后端、API、前端、i18n、E2E 和未跟踪 Go 文件改动，本轮未回退或审查为新增问题。
- [x] **FB-30**: 同步模型按钮不能与新增模型按钮同色
  - 问题分析：根因是 `FB-29` 使用 Ant Design 常见主蓝 `#1677ff`，该颜色与默认 primary ghost 按钮视觉接近，导致同步模型按钮和新增模型按钮看起来一样，无法形成行级操作区分。
  - 修复记录：同步模型按钮仍保留通用蓝色方向，但普通态改为更深一档的常见蓝 `#1d4ed8`，hover 态改为 `#1e40af`，背景和边框使用同色低透明度；插件自有 `SmartCenterPage` POM 恢复同步按钮文字色必须不同于新增模型按钮的断言，并将同步按钮计算色收紧为 `rgb(29, 78, 216)`。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 渠道列表同步模型按钮局部样式；测试有影响，更新插件自有 POM 并复用 `TC001` 覆盖按钮实际计算色和与新增模型按钮颜色不同。`i18n` 无影响，不新增或修改运行时文案、菜单、按钮文案、语言包或 API 文档；缓存一致性无影响，不新增缓存、快照或失效路径；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；API 契约、后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改路由、DTO、服务、数据库、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`，并评估缓存一致性、数据权限、`i18n`、开发工具跨平台均无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check` 与 `git -C apps/lina-plugins diff --check` 通过；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --grep 'TC-1a' --trace off --output ../../temp/20260604-fb30-tc001a-results --timeout 120000` 通过。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md` 和 `testing.md`，从父仓库状态、插件子仓库状态、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮审查范围限定为同步按钮深蓝局部样式、`SmartCenterPage` 颜色差异/计算色断言和 `FB-30` 任务记录。插件根目录无本地 `AGENTS.md`。E2E 质量审查触发原因是用户可观察按钮视觉变化和插件 POM 断言更新；覆盖依据为 `TC001` 中操作列布局、同步按钮与新增模型按钮不同色、同步按钮计算色和定向 `TC-1a` 通过证据。审查未发现阻塞问题；剩余风险为工作树仍包含与本轮无关的既有 `FB-23` 至 `FB-31` 后端、API、前端、i18n、E2E 和未跟踪 Go 文件改动，本轮未回退或审查为新增问题。
- [x] **FB-31**: 模型列折叠按钮需居中并增加默认展示高度
  - 问题分析：根因是模型列折叠按钮跟随 `ai-provider-model-list` 的 `items-start` 左对齐布局，按钮自身没有居中约束；默认折叠高度固定为 `72px`，模型数量较多时默认可见模型偏少，需要在保持表格可扫描的前提下展示更多模型。
  - 修复记录：模型列折叠高度提升到 `104px`，让默认状态展示更多模型标签；`ai-provider-model-list` 改为占满模型列宽度，折叠按钮增加 `self-center` 并补充 `inline-flex` 居中样式，使按钮在表格列内容区域水平居中。插件自有 `SmartCenterPage` POM 更新折叠高度断言，并新增按钮中心点与模型列列表中心点对齐断言。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 渠道列表模型列折叠高度和折叠按钮布局；测试有影响，更新插件自有 POM 并复用 `TC001` 覆盖默认折叠高度和按钮居中。`i18n` 无影响，不新增或修改运行时文案、菜单、按钮文案、语言包或 API 文档；缓存一致性无影响，不新增缓存、快照或失效路径；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；API 契约、后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改路由、DTO、服务、数据库、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估缓存一致性、数据权限、开发工具跨平台均无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`make i18n.check` 通过，仍仅有仓库既有模块级 `$t()` warning；`openspec validate add-ai-text-capability-center --strict` 通过；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --grep 'TC-1a' --trace off --output ../../temp/20260604-fb31-tc001a-results --timeout 120000` 通过，`.last-run.json` 为 `passed`。截图验证 `temp/20260604090521-tc001-provider-list-layout.png` 显示默认折叠状态展示更多模型且“展开”按钮居中，`temp/20260604090527-tc001-provider-list-multi-protocol-model.png` 显示展开后全部模型可见。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，从父仓库状态、插件子仓库状态、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮审查范围限定为模型列折叠高度、折叠按钮居中样式、`SmartCenterPage` 高度/居中断言和 `FB-31` 任务记录。插件根目录无本地 `AGENTS.md`。E2E 质量审查触发原因是用户可观察表格布局和折叠交互变化；覆盖依据为 `TC001` 中默认折叠高度、按钮中心点对齐、展开后完整高度和截图证据。审查未发现阻塞问题；剩余风险为工作树仍包含与本轮无关的既有 `FB-23` 至 `FB-30` 后端、API、前端、i18n、E2E 和未跟踪 Go 文件改动，本轮未回退或审查为新增问题。
- [x] **FB-32**: 渠道列表模型列去除折叠和展开功能
  - 问题分析：根因是上一轮为模型列引入了局部折叠组件、最大高度、渐隐遮罩和展开/折叠按钮；用户当前反馈要求去掉折叠和展开功能，因此模型标签应在模型列内直接完整流式换行展示，不再隐藏超高内容或提供展开入口。
  - 修复记录：已移除 `ProviderModelsCell` 组件、折叠高度常量、测量与 toggle 状态逻辑；`modelsCell()` 直接按协议排序后把全部模型渲染到单个 `flex-wrap` 行容器中，保持小一号字体、紧凑间距和模型列/端点列加宽。已删除 `provider-management.vue` 中折叠内容容器、渐隐遮罩和展开按钮样式。插件自有 `SmartCenterPage` POM 改为断言 `.ai-provider-model-toggle` 和 `.ai-provider-model-list-content` 不存在、模型列表 `max-height: none` 且 `overflow: visible`，并确认模型标签没有隐藏。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 渠道列表模型列渲染方式；测试有影响，更新插件自有 POM 并复用 `TC001` 覆盖无折叠按钮、无折叠容器和全量模型可见。`i18n` 无影响，删除展开/折叠文案使用点，不新增或修改运行时文案、菜单、按钮文案、语言包或 API 文档；缓存一致性无影响，不新增缓存、快照或失效路径；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；API 契约、后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改路由、DTO、服务、数据库、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估缓存一致性、数据权限、开发工具跨平台均无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`make i18n.check` 通过，仍仅有仓库既有模块级 `$t()` warning，未新增 `linapro-ai-core` warning；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check` 与 `git -C apps/lina-plugins diff --check` 均通过；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --grep 'TC-1a' --trace off --output ../../temp/20260604-fb32-tc001a-results --timeout 120000` 通过，`.last-run.json` 为 `passed`。截图验证 `temp/20260604091401-tc001-provider-list-layout.png` 和 `temp/20260604091407-tc001-provider-list-multi-protocol-model.png` 显示模型标签直接全量流式展示，未出现“展开/折叠”按钮；静态检索确认前端源码不再包含 `ProviderModelsCell`、折叠高度常量、`model-list-content`、`model-toggle`、`is-collapsed` 或 `is-expanded` 实现。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮审查范围限定为渠道列表模型列去除折叠/展开交互、相关样式删除、`SmartCenterPage` 无折叠断言和 `FB-32` 任务记录。插件根目录无本地 `AGENTS.md`。E2E 质量审查触发原因是用户可观察表格布局和交互删除；覆盖依据为 `TC001` 中无折叠按钮、无折叠内容容器、列表无最大高度、内容可见和截图证据。审查未发现阻塞问题；剩余风险为工作树仍包含与本轮无关的既有 `FB-23` 至 `FB-31` 后端、API、前端、i18n、E2E 和未跟踪 Go 文件改动，本轮未回退或审查为新增问题。
- [x] **FB-33**: 档位“测试”和“测试配置”的 `AI` 请求必须进入调用日志
  - 问题分析：根因是框架正式文本调用 `GenerateText` 会在成功、失败和绑定解析失败分支写入 `plugin_linapro_ai_invocation`，但档位管理列表“测试”和抽屉“测试配置”共同调用的 `TestTier` 直接执行 `callProvider`，只回写档位 `last_test_*` 摘要，没有写入调用日志，因此用户无法在调用日志中查看测试请求的渠道、模型、状态、用量和脱敏错误结果。
  - 修复记录：新增内部用途常量 `linapro-ai-core.tier.test`；将调用日志写入拆为通用的 `writeInvocationRecord`，保留正式文本调用的 `writeInvocation` 入口不变；`TestTier` 在 provider 调用结束后写入一条脱敏调用日志，记录实际 `capabilityType`、`capabilityMethod`、档位、渠道、模型、协议、`thinkingEffort`、成功/失败状态、用量、耗时和脱敏错误摘要，不写入完整 prompt、response、请求体、响应体或密钥。草稿测试仍不持久化档位绑定，已保存测试仍更新 `last_test_*` 摘要。
  - 影响分析：后端 Go 有影响，范围为 `linapro-ai-core` 插件 service 的档位测试和调用日志内部写入路径；测试有影响，更新插件 service 单测覆盖草稿测试、已保存测试和 provider 超时失败均进入调用日志。数据权限有影响，新增的是平台配置控制面的测试调用日志写入，仍在 `ensurePlatform` 平台上下文校验后执行，调用日志查询继续使用既有 `ai:invocation:list` 权限和数据库侧筛选，不新增租户/组织可见性边界；缓存一致性无影响，不新增缓存、快照或失效路径；`i18n` 无影响，不新增用户可见文案、错误消息、API 文档源文本或语言包；API 契约无影响，不修改路由、DTO、权限标签或响应字段；SQL/DAO 无影响，不修改迁移、seed、DAO、DO 或 Entity；开发工具跨平台无影响，不新增脚本、命令或 CI 入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`data-permission.md`、`plugin.md`、`backend-go.md`、`testing.md`、`i18n.md`，并评估 API、SQL/DAO、缓存一致性和开发工具跨平台无契约影响；使用 `lina-feedback`、`goframe-v2` 和 `lina-review`。
  - 验证记录：`GOWORK=off go test ./backend/internal/service/ai -run 'TestTierDraftTestDoesNotPersistBinding|TestTierTestAppliesProviderCallTimeout' -count=1` 通过；`GOWORK=off go test ./backend/... -count=1` 通过；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check -- openspec/changes/add-ai-text-capability-center/tasks.md` 与 `git -C apps/lina-plugins diff --check -- linapro-ai-core/backend/internal/service/ai/ai.go linapro-ai-core/backend/internal/service/ai/ai_invocation.go linapro-ai-core/backend/internal/service/ai/ai_tier.go linapro-ai-core/backend/internal/service/ai/ai_service_test.go` 通过。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`data-permission.md`、`plugin.md`、`backend-go.md`、`testing.md` 和 `i18n.md`，并使用 `goframe-v2`。审查范围限定为 `linapro-ai-core` 后端 service 的调用日志写入拆分、`TestTier` 成功/失败日志写入、相关单测断言和 `FB-33` 任务记录；插件根目录无本地 `AGENTS.md`。E2E 质量审查未触发，原因是本轮没有修改前端页面、路由、表单、表格、权限交互或 E2E 资产；后端运行时行为已由单元测试覆盖原问题复现场景和修复后预期。审查未发现阻塞问题；剩余风险为工作树仍包含与本轮无关的 `FB-34`、`FB-35` 前端和 POM 改动，本轮未回退或审查为新增问题。
- [x] **FB-34**: 新增渠道时不应默认填入 Anthropic 接入地址
  - 问题分析：根因是新增渠道抽屉前端同时在 `applyCreateDefaults()` 和渠道表单 schema 中写死了 `https://api.anthropic.com/v1` 作为实际字段值，因此点击“新增渠道”时即使用户没有配置 Anthropic，`Anthropic 接入地址`输入框也会被主动填入该地址。该行为不是后端、数据库 seed 或渠道详情接口注入导致。
  - 修复记录：移除新增渠道流程中的 `anthropicBaseUrl` 实际默认值，并删除表单 schema 的 `defaultValue`；保留输入框 placeholder 作为提示，不再把 Anthropic 地址写入表单值或提交 payload。编辑渠道时仍按已保存端点回填，避免影响已有配置维护。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 渠道新增抽屉的默认表单值；测试有影响，更新插件自有 `SmartCenterPage` 断言 Anthropic 接入地址默认值为空。`i18n` 有影响评估但不新增资源，仍复用既有 `Anthropic 接入地址` 和 placeholder 文案；缓存一致性无影响，不新增缓存、快照或失效路径；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；API 契约、后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改路由、DTO、服务、数据库、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估缓存一致性、数据权限、开发工具跨平台均无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`make i18n.check` 通过，仍仅有仓库既有模块级 `$t()` warning；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check` 与 `git -C apps/lina-plugins diff --check` 均通过；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --grep 'TC-1a' --trace off --output ../../temp/20260604-fb34-fb35-tc001a-results-3 --timeout 150000` 通过，`SmartCenterPage.assertCreateProviderDrawerChineseTranslations()` 已断言新增渠道抽屉的 Anthropic 接入地址默认值为空。
  - 审查记录：已按 `lina-review` 读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，从父仓库状态、插件子仓库状态、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮审查范围限定为 `FB-34/FB-35` 的前端表单、插件 POM 断言和任务记录。插件根目录无本地 `AGENTS.md`。E2E 质量审查触发原因是用户可观察表单默认值和模型协议字段变化；覆盖依据为 `TC001` 中新增渠道默认值、旧“端点 / 协议”消失、协议下拉不含 URL 和截图证据。审查未发现阻塞问题；剩余风险为工作树同时包含非本轮范围的 `FB-33` 后端与测试改动，本轮未回退或审查为新增问题。
- [x] **FB-35**: 新增和编辑模型表单的“端点 / 协议”字段改为只展示“协议”并隐藏端点信息
  - 问题分析：根因是模型抽屉复用内部 `endpointIds` 字段承载端点选择，表单 label 直接拼接为“端点 / 协议”，下拉选项也拼接了 `协议 / baseUrl`，导致用户在新增或编辑模型时既看到协议又看到端点地址，不符合“只选择协议”的交互语义。
  - 修复记录：模型表单 label 改为复用既有 `model.fields.protocol` 文案；端点选项在 UI 层按协议去重，只展示 `OpenAI`、`Anthropic`、`OpenAI Compatible`、`Anthropic Compatible`、`Voyage` 等协议名，不再展示 `baseUrl`。内部仍保留 `endpointIds` 作为提交字段，将所选协议映射到渠道端点 ID，保持现有 `modelAdd`、`modelUpdate`、模型能力、档位绑定和调用链路不变；编辑模型时优先保留当前模型或能力声明使用的端点 ID，避免保存时意外切换同协议端点。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 模型新增/编辑抽屉字段 label、协议选项展示和协议到端点 ID 的前端映射；测试有影响，更新插件自有 `SmartCenterPage` 断言旧“端点 / 协议”文案不存在、协议下拉不包含 URL 或 `/v1` 端点片段，并继续覆盖同一模型名按 OpenAI 与 Anthropic 协议创建两条模型记录。`i18n` 有影响评估但不新增资源，改为复用既有 `plugin.linapro-ai-core.model.fields.protocol`；缓存一致性无影响，不新增缓存、快照或失效路径；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；API 契约、后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改路由、DTO、服务、数据库、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估缓存一致性、数据权限、开发工具跨平台均无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`make i18n.check` 通过，仍仅有仓库既有模块级 `$t()` warning；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check` 与 `git -C apps/lina-plugins diff --check` 均通过；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --grep 'TC-1a' --trace off --output ../../temp/20260604-fb34-fb35-tc001a-results-3 --timeout 150000` 通过。截图验证 `temp/20260604094804-tc001-create-model-drawer-default.png` 显示字段名已为“协议”，未展示端点信息。
  - 审查记录：已按 `lina-review` 读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，审查确认模型表单仅改变前端展示与端点 ID 映射方式，未改变后端 API 契约、权限、数据权限、缓存、SQL/DAO 或运行期 DI；E2E 覆盖协议字段、无 URL 选项和多协议模型创建路径。审查未发现阻塞问题；剩余风险同 `FB-34`，工作树包含非本轮范围的 `FB-33` 后端与测试改动。
- [x] **FB-36**: 插件 AI Core schema SQL seed 和索引定义需合并简化
  - 问题分析：根因是 `plugin_linapro_ai_tier` 的 seed 分成 `text/generate` 单独插入和其他能力方法批量插入两段，`plugin_linapro_ai_method_default_param` 又单独维护一份能力方法列表，导致新增能力方法时需要在多个 seed 块同步更新；同时 `plugin_linapro_ai_tier` 和 `plugin_linapro_ai_method_default_param` 是非软删除表，唯一性可以直接作为建表约束表达，没必要再单独维护唯一索引语句；`idx_plugin_linapro_ai_invocation_multimodal_assets` 的名称包含 assets，但实际键列只服务能力方法和创建时间筛选排序，命名不准确。
  - 修复记录：将 `plugin_linapro_ai_tier` 和 `plugin_linapro_ai_method_default_param` 的业务唯一性内联为 `CREATE TABLE IF NOT EXISTS` 中的命名 `UNIQUE` 约束，保留所有软删除表的 partial unique index 语句不变；将调用日志方法筛选索引重命名为 `idx_plugin_linapro_ai_invocation_method_created`，键列保持 `capability_type, capability_method, created_at DESC`，继续覆盖调用日志按能力方法筛选并倒序分页的路径；将 method defaults 和 tier seed 收敛为一个 PostgreSQL CTE 块，通过 `capability_methods` 统一维护能力方法、默认参数和通用档位说明，通过 `tier_catalog` 统一维护三档顺序与 `text/generate` 的差异化说明，使用 `ON CONFLICT DO NOTHING` 保持 seed 幂等。
  - 影响分析：SQL/数据库有影响，范围仅为插件安装 schema 的约束表达、seed 组织和索引命名，不改变表字段、时间字段、软删除字段、DAO/DO/Entity 结构或运行时查询语义，因此未运行 `make dao`；高频调用日志方法筛选索引仍保留并与 `ListInvocations` 中 `capabilityType/capabilityMethod + createdAt DESC` 查询路径匹配，不引入 `N+1` 或无约束全表扫描。插件边界有影响，文件位于 `apps/lina-plugins/linapro-ai-core/manifest/sql/`，插件根目录无本地 `AGENTS.md`，继续遵守顶层插件 SQL 资源归属。`i18n` 无资源影响，seed 中已有英文源文案值保持不变，未新增菜单、按钮、错误消息、API 文档源文本或语言包；缓存一致性无影响，不新增缓存、快照或失效路径；数据权限无影响，不新增数据读写接口、租户/组织可见性或权限边界；API 契约、后端 Go、前端 UI 和开发工具跨平台无影响，未修改路由、DTO、服务代码、页面、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`database.md`、`testing.md`、`i18n.md`，并评估缓存一致性、数据权限、开发工具跨平台均无契约影响；使用 `lina-feedback` 和 `lina-review`。
  - 验证记录：`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check -- openspec/changes/add-ai-text-capability-center/tasks.md` 与 `git -C apps/lina-plugins diff --check -- linapro-ai-core/manifest/sql/001-linapro-ai-core-schema.sql` 通过；SQL 静态检索未发现 `ON DUPLICATE`、显式 seed 自增 `id`、非 `IF NOT EXISTS` 的裸 `CREATE TABLE`/`CREATE INDEX`、旧索引名 `idx_plugin_linapro_ai_invocation_multimodal_assets` 或已删除的独立唯一索引名残留；静态检索确认 `ON CONFLICT ("capability_type", "capability_method")` 和 `ON CONFLICT ("capability_type", "capability_method", "code")` 分别由真实业务唯一约束支撑。未运行 PostgreSQL 临时执行校验，原因是当前环境没有可用 `psql` 客户端；本轮以 OpenSpec、diff check 和 SQL 静态审查作为治理验证证据。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`database.md`、`testing.md` 和 `i18n.md`，从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮审查范围限定为 `FB-36` 任务记录和 `linapro-ai-core` 插件 schema SQL。插件根目录无本地 `AGENTS.md`。E2E 质量审查未触发，原因是本轮没有前端页面、路由、表单、表格、权限交互或端到端工作流变化；Go 单元测试未触发，原因是未修改 Go 运行时代码和 DAO 结构。审查未发现阻塞问题；剩余风险为未做真实 PostgreSQL 执行校验，以及工作树仍包含非本轮范围的既有 `FB-33` 至 `FB-35` 后端、前端和 POM 改动，本轮未回退或审查为新增问题。
- [x] **FB-37**: 调用日志页面补齐来源插件列、时间和能力方法筛选以及日志清理
  - 问题分析：根因是调用日志列表后端和 DTO 已具备 `sourcePluginId`、`capabilityType`、`capabilityMethod`、`startedAt`、`endedAt` 等筛选边界，但前端表格未展示来源插件字段，页面未提供创建时间范围筛选，清理日志也缺少独立 API、按钮权限、确认交互和自动化覆盖；能力方法筛选本身由既有 `capabilityKey` 控件承载，本轮需要确保它在新增时间范围归一化后仍正确拆分并发送到列表请求。
  - 修复记录：在调用日志表格新增来源插件列，保留能力方法列并在查询归一化中显式拆分 `capabilityKey` 为 `capabilityType`/`capabilityMethod`；新增创建时间 `RangePicker`，前端统一转换为 Unix 毫秒 `startedAt`/`endedAt` 传给列表和清理接口；新增 `DELETE /api/ai/invocations/clean`、`CleanReq`/`CleanRes`、生成控制器、service 清理方法和 `ai:invocation:clear` 按钮权限，未传时间范围时全量清理，传入任一边界时按创建时间范围清理并返回实际删除数量；补充插件 `plugin.yaml`、中英文菜单、运行时清理确认/成功文案和中文 `apidoc` 翻译；更新插件自有 `TC003`、POM 和 API helper 覆盖来源列、能力方法筛选、时间筛选、清理全量确认取消、范围清理和旧日志保留。
  - 影响分析：API 契约有影响，新增 `DELETE /ai/invocations/clean` 和独立权限标签，时间字段为 Unix 毫秒请求参数；后端 Go 有影响，范围为 `linapro-ai-core` 插件 invocation controller/service 和单测，清理使用数据库侧范围删除，不进行循环逐行删除；前端 UI 有影响，范围为调用日志筛选栏、表格列和清理按钮；`i18n` 有影响，更新启用 `i18n` 的插件菜单、运行时文案和中文 `apidoc` 资源，英文 `apidoc` 按规则保持空占位惯例；测试有影响，新增 service 单测断言范围/全量清理，并更新插件自有 E2E 与截图证据。数据权限有影响，调用日志属于平台配置控制面数据，清理操作继续先执行 `ensurePlatform` 并由宿主权限中间件校验 `ai:invocation:clear`，不新增租户/组织数据权限边界；缓存一致性无影响，不新增缓存、快照或失效路径；SQL/DAO 无结构影响，调用日志表无 `deleted_at`，日志清理为明确硬删除边界，既有 `source_plugin_id, created_at` 和 `capability_type, capability_method, created_at` 索引覆盖本轮筛选路径，未运行 `make dao`；开发工具跨平台无新增入口，仅使用既有 `make ctrl` 生成 controller；运行期 DI 无影响，未新增运行期依赖或 service 构造参数；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`data-permission.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`database.md`、`cache-consistency.md`、`frontend-ui.md`、`testing.md`、`i18n.md`、`dev-tooling.md`，并使用 `lina-feedback`、`lina-review`、`goframe-v2`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：`GOWORK=off go test ./backend/internal/service/ai -run 'TestCleanInvocationsDeletesAllOrTimeRange' -count=1 -v` 通过；`GOWORK=off go test ./backend/... -count=1` 通过；`cd apps/lina-core && go test ./internal/cmd -count=1` 通过；`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm --dir hack/tests test:validate` 通过；`make i18n.check` 通过，仍仅有仓库既有模块级 `$t()` warning，未新增 `linapro-ai-core` 违规；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check` 与 `git -C apps/lina-plugins diff --check` 通过；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --trace off --output ../../temp/20260604-fb37-tc003-results-final-2 --timeout 150000` 通过。截图验证 `temp/20260604120509-tc003-invocation-list-protocol-label.png`、`temp/20260604120509-tc003-invocation-detail-protocol-label.png`、`temp/20260604120510-tc003-invocation-filters-source-time.png` 和 `temp/20260604120511-tc003-invocation-range-clean.png` 显示来源插件列、能力方法、时间筛选、清理按钮、成功提示和清理后的空表格状态正常，无浮层遮挡、翻译 key、明显截断或重叠。新增清理单测最初暴露全量清理会污染共享测试库，本轮已改为事务回滚验证，重新运行定向和整包后端测试通过。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、全部命中规则文件和插件本地规范入口，从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪生成文件、OpenSpec 状态和插件本地 `AGENTS.md` 缺失判断展开反馈级审查；本轮审查范围限定为 `FB-37` 的调用日志列表列/筛选、清理 API/controller/service/权限/i18n、清理单测、`TC003`/POM/helper 和任务记录。E2E 质量审查触发原因是用户可观察表格、筛选栏、权限按钮、确认弹窗和端到端清理流程变化；覆盖依据为 `TC003` 中来源插件列可见、能力/来源/时间请求参数、清理全量确认取消、范围清理 DELETE 请求、目标日志删除、旧日志保留、英文关键文案和截图证据。审查未发现阻塞问题；剩余风险为当前工作树仍包含与本轮无关或相邻反馈的 `FB-33` 至 `FB-40` 后端、前端、`i18n`、SQL 和未跟踪 Go 生成文件改动，本轮未回退这些并行改动。
- [x] **FB-38**: 调用日志中的调用协议展示为统一大驼峰形式
  - 问题分析：根因是智能中心渠道、模型列表已有 `protocolLabel()` 用于把数据库协议值 `openai`、`anthropic` 等格式化为 `OpenAI`、`Anthropic`，但调用日志详情抽屉直接输出 `record.protocol`，表格也缺少可见的协议展示列，导致数据库中保存的原始协议值会原样暴露到调用日志页面。
  - 修复记录：将 `protocolLabel()` 导出为统一前端展示工具，并改为大小写无关地识别已知协议；调用日志表格新增“协议”列并放在“能力方法”后，使用 `protocolLabel()` 格式化展示；调用日志详情抽屉的协议字段改为复用同一格式化函数。插件自有 `TC003` 测试数据显式写入大写 `ANTHROPIC`，断言列表和详情均显示 `Anthropic` 且不显示原始大小写不统一值，并补充列表和详情截图证据。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 调用日志表格协议列和详情抽屉协议字段；OpenSpec 规范有影响，已在调用日志查询场景补充协议展示名验收要求；测试有影响，更新插件自有 `TC003` 和测试数据 helper 覆盖原始大小写不统一协议值到页面展示名的转换。`i18n` 有影响评估但不新增资源，协议展示名为渠道/协议品牌名，表格列标题复用既有 `model.fields.protocol` 翻译键；缓存一致性无影响，不新增缓存、快照或失效路径；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；API 契约、后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改路由、DTO、服务、数据库、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估缓存一致性、数据权限、开发工具跨平台均无契约影响；使用 `lina-feedback`、`vben`、`lina-e2e` 和 `lina-review`。
  - 验证记录：`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check -- openspec/changes/add-ai-text-capability-center/specs/linapro-ai-core-plugin/spec.md openspec/changes/add-ai-text-capability-center/tasks.md` 与 `git -C apps/lina-plugins diff --check -- linapro-ai-core/frontend/pages/ai-data.ts linapro-ai-core/frontend/pages/invocation-detail-drawer.vue linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts linapro-ai-core/hack/tests/support/ai-core-api.ts` 通过；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --trace off --output ../../temp/20260604-fb38-tc003-results-4 --timeout 150000` 通过。截图验证 `temp/20260604113533-tc003-invocation-list-protocol-label.png` 和 `temp/20260604113533-tc003-invocation-detail-protocol-label.png` 均显示 `Anthropic`，未显示原始 `anthropic` 或 `ANTHROPIC`。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，从父仓库状态、`apps/lina-plugins` 子仓库状态、子仓库 diff、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮审查范围限定为调用日志协议展示规范、`protocolLabel()` 大小写无关格式化、调用日志表格协议列、详情抽屉协议字段、`TC003` 断言和测试数据 helper。插件根目录无本地 `AGENTS.md`。E2E 质量审查触发原因是用户可观察表格和详情抽屉展示变化；覆盖依据为 `TC003` 中数据库写入 `ANTHROPIC`、列表/详情显示 `Anthropic`、不显示原始大小写值、脱敏断言和截图证据。审查未发现阻塞问题；剩余风险为工作树仍包含非本轮范围的既有 `FB-33` 至 `FB-37`、`FB-39` 后端、API、前端、i18n、SQL 和未跟踪 Go 文件改动，本轮未回退或审查为新增问题。
- [x] **FB-39**: SQL seed 不应为能力方法预设非空 AI 请求默认参数
  - 问题分析：根因是 `plugin_linapro_ai_method_default_param` 的 seed 直接写入了 `{"maxOutputTokens":1024}`、`{"count":1,"size":"1024x1024"}`、`{"durationMs":5000}` 等逐方法请求参数，看起来像平台运行时强制默认策略；但当前文本调用路径并不统一从 `default_params_json` 合并运行时参数，`maxOutputTokens` 实际优先使用请求值，其次使用模型能力声明，最后才交给渠道默认行为。因此非空 seed 更像管理侧模板，容易让用户误解为框架默认参数或隐藏限额。
  - 修复记录：保留 `default_params_json` 字段和表级默认值 `'{}'` 作为用户可配置入口，但从 `capability_methods` seed 列表和 `plugin_linapro_ai_method_default_param` 插入列中移除逐方法默认参数，新增安装时由表字段默认写入 `{}`；档位默认参数编辑器的中英文占位示例同步改为 `{}`，插件版本提升到 `v0.1.17` 以触发现有插件资源刷新；插件自有 `TC002` 不再把 `{"maxOutputTokens":1024}` 当作回退默认值，保留用户显式保存默认参数的 E2E 覆盖。
  - 影响分析：SQL/数据库有影响，范围为插件安装 schema 的 Seed DML，不改变表字段、约束、索引、软删除、时间字段、DAO/DO/Entity 或运行时查询路径，因此不运行 `make dao`；Seed 数据仍为系统初始化数据，Mock 数据无影响，幂等性仍通过 `ON CONFLICT DO NOTHING` 和真实唯一约束支撑。插件边界有影响，文件位于 `apps/lina-plugins/linapro-ai-core/`，插件根目录无本地 `AGENTS.md`，继续遵守顶层插件资源归属。`i18n` 有影响，更新启用 `i18n` 的 `linapro-ai-core` 插件 `en-US`/`zh-CN` 运行时占位文案；测试有影响，更新插件自有 `TC002` 并定向运行 `TC-2b`。缓存一致性无新增业务缓存或失效路径，插件版本升级仅触发现有运行时资源刷新；数据权限无影响，不新增数据读写接口、租户/组织可见性或权限边界；API 契约、后端 Go、前端逻辑和开发工具跨平台无影响，未修改路由、DTO、service、页面逻辑、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`database.md`、`testing.md`、`i18n.md`，并评估缓存一致性、数据权限、开发工具跨平台均无契约影响；使用 `lina-feedback` 和 `lina-e2e`。
  - 验证记录：`openspec validate add-ai-text-capability-center --strict` 通过；`pnpm -C hack/tests test:validate` 通过；`make i18n.check` 通过，仍仅有仓库既有模块级 `$t()` warning，未新增 `linapro-ai-core` 违规；`git diff --check -- openspec/changes/add-ai-text-capability-center/tasks.md` 与 `git -C apps/lina-plugins diff --check -- linapro-ai-core/manifest/sql/001-linapro-ai-core-schema.sql linapro-ai-core/manifest/i18n/zh-CN/plugin.json linapro-ai-core/manifest/i18n/en-US/plugin.json linapro-ai-core/plugin.yaml linapro-ai-core/hack/tests/e2e/TC002-smart-center-tier-management.ts` 通过；SQL 静态检索未发现 `ON DUPLICATE`、显式 seed 自增 `id`、非 `IF NOT EXISTS` 的裸 `CREATE TABLE`/`CREATE INDEX`、旧独立唯一索引名、旧调用日志索引名或旧非空默认参数 seed 残留；`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC002-smart-center-tier-management.ts --grep 'TC-2b' --trace off --output ../../temp/20260604-fb39-tc002b-results --timeout 150000` 通过。未运行 PostgreSQL 临时执行校验，原因是当前环境没有可用 `psql` 客户端。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`database.md`、`testing.md` 和 `i18n.md`，并使用 `lina-e2e`；从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查。本轮审查范围限定为 `FB-39` 的 schema seed 默认参数、插件运行时占位文案、插件版本、`TC002` 断言和任务记录；插件根目录无本地 `AGENTS.md`。E2E 质量审查触发原因是本轮更新插件自有 E2E 测试资产并改变默认参数编辑器占位文案；覆盖依据为 `TC002` 中显式用户参数保存、恢复 `{}` 回退、截图证据和定向 `TC-2b` 通过。审查未发现阻塞问题；剩余风险为当前环境没有 `psql`，未做真实 PostgreSQL 执行校验，以及工作树仍包含非本轮范围的既有 `FB-33` 至 `FB-38` 后端、API、前端、i18n、SQL 和未跟踪 Go 文件改动，本轮未回退或审查为新增问题。
- [x] **FB-40**: 智能中心英文菜单名称需更地道并修正调用日志英文标题
  - 问题分析：根因是 `linapro-ai-core` 英文源清单和 `en-US` 运行时资源仍使用 `Smart Center`、`Tier Management`、`Invocation Logs` 这组直译或偏技术化标题；其中用户反馈的 `Invoncation Logs` 属于拼写错误指向，当前仓库实际已有拼写为 `Invocation Logs`，但菜单语义仍不够贴近 AI 请求审计场景。源码插件资源通过 `plugin.yaml` 和 Go `embed` 进入运行时，单改前端页面标题不足以刷新已安装插件菜单和语言包。
  - 修复记录：将英文顶级入口统一改为 `AI Hub`，档位页面和菜单改为 `AI Tiers`，调用日志菜单、页面标题和相关英文确认文案改为 `Request Logs`；同步更新 `plugin.yaml` 源文案、`manifest/i18n/en-US/menu.json`、`plugin.json`、`error.json`、插件页面 `pluginPageMeta` 和相关 E2E 断言；插件版本提升到 `v0.1.18`，用于触发现有源码插件运行时资源升级。调用日志 E2E 增加英文侧边栏和页面标题断言，并修复日期范围 helper 对 Ant Design 只读输入的依赖，使日志筛选和范围清理验证继续稳定。
  - 影响分析：前端 UI 和 `i18n` 有影响，范围为启用 `i18n` 的 `linapro-ai-core` 插件英文菜单、页面标题、英文清理确认文案和插件显示名；测试有影响，更新插件自有 `TC002`、`TC003` 与 `SmartCenterPage`，覆盖 `AI Hub`、`AI Tiers`、`Request Logs` 英文显示以及调用日志日期范围筛选。插件边界有影响，文件位于 `apps/lina-plugins/linapro-ai-core/`，插件根目录无本地 `AGENTS.md`，继续遵守顶层插件资源归属；缓存一致性无新增业务缓存或失效路径，版本升级仅触发现有插件资源刷新；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；API 契约、后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改路由、DTO、service、数据库、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估缓存一致性、数据权限、开发工具跨平台均无契约影响；使用 `lina-feedback`、`vben`、`karpathy-guidelines` 和 `lina-e2e`。
  - 验证记录：`node -e` 解析 `linapro-ai-core` 英文 `menu.json`、`plugin.json`、`error.json` 通过；`pnpm -C apps/lina-vben/apps/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`make i18n.check` 通过，仍仅有仓库既有模块级 `$t()` warning，未新增 `linapro-ai-core` 违规；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check -- openspec/changes/add-ai-text-capability-center/tasks.md` 与 `git -C apps/lina-plugins diff --check -- linapro-ai-core/plugin.yaml linapro-ai-core/manifest/i18n/en-US/menu.json linapro-ai-core/manifest/i18n/en-US/plugin.json linapro-ai-core/manifest/i18n/en-US/error.json linapro-ai-core/frontend/pages/tier-management.vue linapro-ai-core/frontend/pages/invocation-logs.vue linapro-ai-core/hack/tests/e2e/TC002-smart-center-tier-management.ts linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts linapro-ai-core/hack/tests/pages/SmartCenterPage.ts` 通过；旧英文菜单标题静态检索仅剩 E2E 中对旧标题消失的负向断言。首次定向 E2E 因运行中的旧 `temp/bin/lina` 二进制尚未加载新嵌入资源而失败，服务重启并完成源码插件升级后，`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC002-smart-center-tier-management.ts apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --grep 'TC-2a|TC-3a' --trace off --output ../../temp/20260604-fb40-e2e-results-2 --timeout 150000` 中 `TC-2a` 通过，随后 `TC003` 因日期范围 helper 仍依赖只读输入失败；修复 helper 后，`pnpm --dir hack/tests exec playwright test apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --grep 'TC-3a' --trace off --output ../../temp/20260604-fb40-tc003-results-2 --timeout 150000` 通过。截图证据显示侧边栏、面包屑、标签页和页面标题均为 `AI Hub`、`AI Tiers`、`Request Logs`。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-e2e`；从父仓库状态、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查。本轮审查范围限定为 `FB-40` 的英文插件清单、`en-US` 运行时资源、插件页面 `pluginPageMeta`、`TC002`/`TC003` 英文文案断言、`SmartCenterPage` 调用日志日期范围 helper 和任务记录；插件根目录无本地 `AGENTS.md`。E2E 质量审查触发原因是用户可观察菜单、页面标题和启用 `i18n` 的英文翻译变化；覆盖依据为 `TC002` 断言 `AI Tiers`、`TC003` 断言 `AI Hub`/`AI Tiers`/`Request Logs`、旧标题消失、调用日志筛选/清理路径和截图证据。审查未发现阻塞问题；剩余风险为工作树仍包含非本轮范围的既有 `FB-33` 至 `FB-39` 后端、API、前端、`i18n`、SQL 和未跟踪 Go 文件改动，本轮未回退或审查为新增问题。
- [x] **FB-41**: 智能中心渠道菜单改为渠道管理并拆分模型管理独立菜单
  - 问题分析：根因是上一轮渠道页面高度和间距优化触碰了共享 `VXE` 搜索区样式，导致全局搜索框视觉被连带影响；同时模型管理仍嵌在渠道页面 `Tabs` 中，菜单语义仍是“渠道”，不符合当前要将渠道入口改为渠道管理、并把模型管理拆成独立菜单的反馈。
  - 修复记录：已还原共享 `VXE` 搜索框样式，本轮 `apps/lina-vben/packages/effects/plugins/src/vxe-table/use-vxe-grid.vue` 与 `style.css` 无差异；`provider-management.vue` 移除 `Tabs` 和内嵌模型表格，仅保留渠道列表；新增 `model-management.vue` 作为 `/ai/models` 独立动态页面；`plugin.yaml` 中渠道菜单改为 `Providers`，中文资源显示“渠道管理”，新增 `Models` 菜单并排序在渠道管理之后、档位管理之前，插件版本提升到 `v0.1.20` 触发源码插件资源刷新；同步维护 `en-US`/`zh-CN` 菜单资源和插件自有 `TC001`/`SmartCenterPage` 断言。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 渠道页面、模型管理页面、菜单排序和插件 E2E/POM；`i18n` 有影响，已更新启用 `i18n` 的插件中英文菜单资源；插件边界有影响，文件位于 `apps/lina-plugins/linapro-ai-core/`，插件根目录无本地 `AGENTS.md`，继续遵守顶层插件资源归属；测试有影响，新增 `TC-1d` 覆盖“渠道管理/模型管理/档位管理”顺序、旧称菜单消失、渠道页无 Tab、搜索区默认结构和模型页可见。缓存一致性无新增业务缓存或失效路径，插件版本升级仅触发现有插件资源刷新；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；API 契约、后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改路由、DTO、service、数据库、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估缓存一致性、数据权限、开发工具跨平台均无契约影响；使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。
  - 验证记录：`pnpm -C hack/tests test:validate` 通过；`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck` 通过；`make i18n.check` 通过，仍仅有仓库既有模块级 `$t()` warning，未新增 `linapro-ai-core` 资源覆盖问题；`openspec validate add-ai-text-capability-center --strict` 通过；`git diff --check -- openspec/changes/add-ai-text-capability-center/tasks.md apps/lina-vben/packages/effects/plugins/src/vxe-table/use-vxe-grid.vue apps/lina-vben/packages/effects/plugins/src/vxe-table/style.css` 通过；`git -C apps/lina-plugins diff --check -- linapro-ai-core/plugin.yaml linapro-ai-core/frontend/pages/provider-management.vue linapro-ai-core/frontend/pages/model-management.vue linapro-ai-core/manifest/i18n/zh-CN/menu.json linapro-ai-core/manifest/i18n/en-US/menu.json linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts linapro-ai-core/hack/tests/pages/SmartCenterPage.ts` 通过；`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --config=playwright.config.ts -g "TC-1d"` 通过。截图验证 `temp/20260605/20260605014320-tc001-provider-management-search-default.png` 和 `temp/20260605/20260605014321-tc001-provider-model-split-menus.png` 显示渠道页无 Tab、模型页独立、菜单排序正确且搜索区为默认结构。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-e2e`；从父仓库状态、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查。本轮审查范围限定为共享 `VXE` 搜索样式还原检查、渠道/模型菜单拆分、渠道页去 Tab、模型独立页、插件菜单 i18n、`TC001`/POM 断言和 `FB-41` 任务记录。插件根目录无本地 `AGENTS.md`。E2E 质量审查触发原因是用户可观察菜单、页面布局、搜索区视觉和启用 `i18n` 的中文菜单变化；覆盖依据为 `TC-1d` 中菜单顺序、旧文案消失、渠道页无 Tab、模型页可见、默认搜索区结构和截图证据。审查未发现阻塞问题；剩余风险为当前工作树仍包含与本轮无关或相邻反馈的 `FB-33` 至 `FB-40` 后端、API、前端、`i18n`、SQL 和未跟踪 Go 文件改动，本轮未回退这些并行改动。
- [x] **FB-42**: 智能中心旧称术语统一为渠道
  - 问题分析：根因是前一轮主要完成菜单和页面布局调整，但插件清单、运行时语言包、错误资源、API 文档源文本、SQL 描述性注释、E2E 测试数据和 OpenSpec 记录中仍残留历史旧称或英文展示旧称，导致智能中心同一能力在 UI、文档和测试证据中术语不一致。
  - 修复记录：已将启用 `i18n` 的 `linapro-ai-core` 插件中英文运行时资源、菜单资源、错误资源、中文 `apidoc` 翻译、插件清单描述、Go API 文档源文本、业务错误 fallback、SQL 描述性注释、E2E 可见测试数据和 OpenSpec 描述统一为“渠道/Provider”；插件版本提升到 `v0.1.22` 触发源码插件资源刷新。内部技术标识保持不变，包括 `providerId`、`/ai/providers`、`ai:provider:*`、`Provider` 类型名、provider factory 和插件能力 provider 概念，避免扩大 API、数据库和宿主能力契约变更。
  - 影响分析：`i18n` 有影响，范围为启用 `i18n` 的插件 `en-US`/`zh-CN` 运行时资源、菜单、错误资源和中文 `apidoc`；前端 UI 有影响，页面可见文案、表格标题、按钮和测试数据展示统一为“渠道/Provider”；API 契约仅文档源文本有影响，未修改 HTTP 方法、路径、DTO 字段、权限标签或响应结构；后端 Go 有影响，限于 API 文档标签和业务错误英文 fallback 字符串，无新增运行期依赖或 DI 来源变化；SQL 有影响，限于描述性注释和 `COMMENT` 文本，不改变表、列、索引、约束、Seed 数据、软删除或时间字段语义，不运行 `make dao`；测试有影响，更新插件自有 `TC001`、`TC002`、`TC004`、`TC005`、POM 和 helper 中的可见测试数据与断言；缓存一致性无新增业务缓存或失效路径，插件版本升级仅触发现有资源刷新；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；开发工具跨平台无影响，不修改脚本、CI 或代码生成入口。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`、`api-contract.md`、`backend-go.md`、`database.md`、`cache-consistency.md`、`data-permission.md`，并使用 `lina-feedback`、`vben`、`lina-e2e`、`goframe-v2` 和 `lina-review`。
  - 验证记录：旧中文词静态检索在 `linapro-ai-core`、本变更及相关 AI OpenSpec 活跃变更、宿主 AI 错误资源和宿主 AI 能力包中无命中；展示型英文旧称检索 `rg -n "AI provider|Provider ID|Provider display|Provider operation|provider-specific|Provider returned|Provider Name|Provider protocol|Provider website|Provider remark|Provider-owned|provider operation|provider endpoint|provider model" apps/lina-plugins/linapro-ai-core/backend/api apps/lina-plugins/linapro-ai-core/backend/internal/service/ai/ai_code.go apps/lina-plugins/linapro-ai-core/manifest/sql/001-linapro-ai-core-schema.sql apps/lina-plugins/linapro-ai-core/manifest/i18n apps/lina-plugins/linapro-ai-core/plugin.yaml` 无命中；宿主和插件中英文 JSON 资源解析通过；父仓库 scoped `git diff --check` 覆盖三个 AI OpenSpec 变更、宿主 AI 错误资源与宿主 AI 错误码文件并通过；插件 scoped `git -C apps/lina-plugins diff --check` 覆盖 `linapro-ai-core` 清单、语言包、SQL 描述、API 文档源、错误 fallback、E2E/POM/helper 并通过；`openspec validate add-ai-text-capability-center --strict`、`openspec validate extend-ai-multimodal-capabilities --strict`、`openspec validate refactor-ai-capability-namespace --strict` 均通过；`pnpm -C hack/tests test:validate` 通过；`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck` 通过；`GOWORK=off go test ./backend/api/... ./backend/internal/service/ai -count=1` 通过；`go test ./pkg/plugin/capability/ai/aicommon ./pkg/plugin/capability/ai/aitext -count=1` 通过；`make i18n.check` 通过，仍仅有仓库既有模块级 `$t()` warning，未新增 `linapro-ai-core` 资源覆盖问题；`make dev` 重启后后端 `http://127.0.0.1:9120/`、前端 `http://127.0.0.1:5666/` ready；`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --config=playwright.config.ts -g "TC-1a|TC-1d"` 通过。截图验证 `temp/20260605/20260605023647-tc001-provider-list-layout.png`、`temp/20260605/20260605023710-tc001-provider-management-search-default.png` 和 `temp/20260605/20260605023711-tc001-provider-model-split-menus.png` 显示渠道列表、渠道管理、模型管理和模型页“渠道”列正常，无旧称、未翻译 key、明显截断或重叠。
  - 审查记录：已按 `lina-review` 从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `linapro-ai-core` 渠道术语统一、宿主 AI 错误文案、相关 AI OpenSpec 活跃变更文案、插件中英文资源、中文 `apidoc`、API 文档源文本、错误 fallback、SQL 描述性注释、E2E/POM/helper 和 `FB-42` 任务记录。插件根目录无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`、`api-contract.md`、`backend-go.md`、`database.md`、`cache-consistency.md` 和 `data-permission.md`，并使用 `lina-feedback`、`vben`、`lina-e2e`、`goframe-v2` 和 `lina-review`。E2E 质量审查触发原因是用户可观察菜单、页面标题、表格标题、按钮和测试数据展示变化；覆盖依据为 `TC-1a` 与 `TC-1d` 断言渠道页、模型页、旧称消失和截图证据。API/backend 审查确认仅修改文档标签和错误 fallback，不改变 HTTP 方法、路径、DTO 字段、权限标签、响应结构、运行期依赖或数据库访问路径；SQL 审查确认仅修改描述性注释和 `COMMENT` 文本，不改变表结构、索引、Seed DML、软删除和时间字段，因此无需 `make dao`；缓存一致性、数据权限和开发工具跨平台均无契约影响。审查未发现阻塞问题；剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-43**: 调用日志删除按钮、清理弹窗和日期时间筛选组件需对齐系统监控日志页
  - 问题分析：根因是智能中心调用日志页面仍使用独立的 `Modal.confirm` 删除确认，并根据搜索表单当前时间范围隐式决定清理范围；删除按钮也缺少系统监控操作日志、登录日志所使用的危险主按钮样式。搜索区日期时间范围组件额外设置固定宽度、禁用清空和压小输入字号，导致视觉与系统监控日志页的日期范围筛选不一致。
  - 修复记录：调用日志删除入口改为 `danger + type="primary"`；删除流程改为与系统监控日志页一致的普通 `Modal`，包含警告提示、删除所有调用日志复选框、日期时间范围选择区和危险主确认按钮，提示区、全量选项和范围选择区均保持 `16px` 分隔。默认未勾选“删除所有调用日志”时要求选择完整日期时间范围；勾选后禁用范围选择器，并调用既有 `DELETE /api/ai/invocations/clean` 且不携带 `startedAt`/`endedAt`。搜索表单日期时间范围组件移除固定宽度和禁用清空，改为随表单列宽铺满，保留 `YYYY-MM-DD HH:mm:ss` 与 Unix 毫秒值契约。同步补齐插件 `en-US`/`zh-CN` 运行时文案并将插件版本提升到 `v0.1.23` 以刷新源码插件资源。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 调用日志页面删除按钮、删除弹窗、日期时间筛选组件和插件自有 `TC003`/POM；`i18n` 有影响，已更新启用 `i18n` 的插件 `en-US`/`zh-CN` 运行时资源，未修改宿主语言包或 API 文档源文本；插件边界有影响，文件位于 `apps/lina-plugins/linapro-ai-core/`，插件根目录无本地 `AGENTS.md`，继续遵守顶层插件资源归属；数据权限边界无新增后端变化，继续复用既有平台上下文和 `ai:invocation:clear` 清理权限，前端未绕过后端校验；缓存一致性无新增业务缓存、快照或失效路径，插件版本升级仅触发现有源码插件资源刷新；API 契约无变更，未修改 HTTP 方法、路径、DTO、权限标签或响应结构；后端 Go、SQL/DAO 和开发工具跨平台无影响，未修改服务、数据库、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：`make dev plugins=1` 已重启前后端并加载 `linapro-ai-core v0.1.23`；`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb43-tc003-results-3 --timeout 150000` 通过；截图验证 `temp/20260605/20260605031234-tc003-invocation-delete-dialog.png` 显示弹窗文案翻译正确、无原始 `i18n` key，警告提示、全量选项和日期时间范围区间隔清晰；`temp/20260605/20260605031241-tc003-invocation-filters-source-time.png` 显示日期时间范围输入完整可见且未裁切。治理验证通过：`openspec validate add-ai-text-capability-center --strict`、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests test:validate`、`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json`、`make i18n.check`、`git diff --check`、`git -C apps/lina-plugins diff --check`；`make i18n.check` 仍仅报告仓库既有模块级 `$t()` warning，未新增 `linapro-ai-core` 资源覆盖问题。
  - 审查记录：已按 `lina-review` 从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-43` 的调用日志删除按钮、清理弹窗、日期时间筛选组件、插件运行时 `i18n`、插件版本、`TC003`/POM 断言和任务记录。插件根目录无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`data-permission.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。E2E 质量审查触发原因是用户可观察按钮样式、弹窗内容、异常提示、日期时间控件和端到端清理流程变化；覆盖依据为 `TC003` 中按钮危险主样式、弹窗英文翻译、无原始 `i18n` key、`16px` 间距、未选范围校验、删除全部不带 `startedAt/endedAt`、范围删除只删除当前日期时间段日志、旧日志保留和截图证据。审查未发现阻塞问题；剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-44**: 调用日志创建时间筛选组件需改为与登录日志一致的日期选择
  - 问题分析：根因是 `linapro-ai-core` 调用日志创建时间筛选仍沿用日期时间范围配置，使用 `showTime`、Unix 毫秒 `valueFormat` 和双列日期时间输入语义；去掉日期时间后若直接复用登录日志原有单列宽度，两个 `YYYY-MM-DD` 日期会在 `152px` 控件内裁切，不满足 UI 文本可读要求，也无法形成操作日志、登录日志和调用日志三处一致的日期范围体验。
  - 修复记录：调用日志创建时间筛选改为日期范围选择，`valueFormat` 使用 `YYYY-MM-DD`，前端在提交列表查询和清理范围时把日期字符串转换为当天起止 Unix 毫秒，保持后端列表与清理接口契约不变；调用日志、系统监控登录日志和系统监控操作日志的日期范围筛选统一为跨两列表单项、`w-full` 输入宽度和 `80px` 标签宽度，确保两端日期完整显示且三处宽度一致。插件自有 `TC003` 增加日期格式、无时间面板、控件宽度和完整日期可见断言，并修正固定操作列详情按钮按目标数据行定位。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 调用日志筛选表单、`linapro-monitor-loginlog`/`linapro-monitor-operlog` 日期范围筛选宽度和插件自有 `TC003`/POM；测试有影响，更新插件 E2E 断言并重跑 `TC003`。`i18n` 无影响，不新增或修改运行时文案、菜单、按钮、表格、提示、API 文档源文本或语言包；API 契约无变更，仍向调用日志列表和清理接口发送既有 `startedAt`/`endedAt` 毫秒参数；后端 Go、SQL/DAO、缓存一致性、数据权限和开发工具跨平台均无影响，未修改服务、数据库、脚本、权限或可见性边界；插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。
  - 验证记录：实际 DOM 测量显示系统监控登录日志、操作日志和智能中心调用日志日期范围控件均为 `labelWidth=80`、`pickerWidth=402`；`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb44-tc003-results-6 --timeout 150000` 通过，截图 `temp/20260605/20260605034435-tc003-invocation-filters-source-time.png` 显示创建时间为日期范围且完整显示 `2026-06-05` 到 `2026-06-06`，无时间面板和未翻译 key。治理验证通过：`openspec validate add-ai-text-capability-center --strict`、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests test:validate`、`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json`、`make i18n.check`、`git diff --check`、`git -C apps/lina-plugins diff --check`；`make i18n.check` 仍仅报告仓库既有模块级 `$t()` warning，未新增 `linapro-ai-core` 或监控日志插件资源覆盖问题。
  - 审查记录：已按 `lina-review` 从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-44` 的调用日志创建时间日期范围筛选、日期转毫秒适配、系统监控登录/操作日志日期范围宽度同步、`TC003`/POM 断言和任务记录。`linapro-ai-core`、`linapro-monitor-loginlog`、`linapro-monitor-operlog` 插件根目录均无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`data-permission.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。E2E 质量审查触发原因是用户可观察筛选表单、日期组件、列表请求参数和端到端调用日志流程变化；覆盖依据为 `TC003` 中日期格式、无时间面板、三处日期控件宽度 DOM 测量、筛选后请求 `startedAt`/`endedAt` 参数、详情打开目标行、范围清理和截图证据。审查未发现阻塞问题；剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-45**: 模型管理编辑模型时可以修改能力方法
  - 问题分析：根因是模型编辑抽屉在编辑态禁用了 `capabilityKey` 字段，用户无法在模型管理页修改能力方法；同时后端 `PUT /ai/models/{id}/capabilities` 的既有语义只插入或更新提交项，不会删除旧能力声明，即使前端放开字段也会留下旧的 `capabilityType + capabilityMethod` 记录，导致模型列表和档位候选仍按旧能力方法命中。
  - 修复记录：模型编辑抽屉取消 `capabilityKey` 编辑态禁用；保存编辑模型时先更新模型身份，再按当前能力方法和新能力方法构造替换后的能力声明列表，保留其他显式能力声明并移除被替换的旧方法声明。后端 `UpsertModelCapabilities` 改为按提交 payload 替换当前模型的显式能力声明，拒绝空 payload、重复能力键和空能力键；被替换掉的旧能力若仍被任一档位绑定引用，则返回 `CodeModelInUse` 并保持原声明不变；替换成功后继续按既有路径失效档位解析缓存。API 文档源和启用 `i18n` 的插件中文 `apidoc` 同步改为“保存并替换显式能力声明”语义。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 模型编辑抽屉能力方法字段和保存工作流；后端 Go 与 API 文档有影响，范围为模型能力保存服务、controller 注释和 `UpsertCapabilitiesReq` 元数据，不新增 HTTP 路由、方法、DTO 字段或权限标签；`i18n` 有影响，已更新启用 `i18n` 的插件中文 `apidoc` 翻译，未新增运行时用户文案；测试有影响，新增 service 单测并更新插件自有 `TC001`/POM 覆盖编辑抽屉能力方法可改、旧能力列表移除、新能力列表可查。数据权限边界仍为平台管理 API 的 `ensurePlatform` 和既有 `ai:provider:update` 权限，不开放租户侧数据操作；缓存一致性沿用模型能力写入成功后的档位缓存失效，不新增缓存类型；SQL/DAO 无影响，不修改表结构、索引、Seed、软删除字段、时间字段或生成代码；开发工具跨平台无影响，不修改脚本、CI 或命令入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。
  - 验证记录：`GOWORK=off go test ./backend/internal/service/ai -count=1` 通过；`GOWORK=off go test ./backend/api/model/v1 ./backend/internal/controller/model -count=1` 通过；`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck` 通过；`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json` 通过；`python -m json.tool apps/lina-plugins/linapro-ai-core/manifest/i18n/zh-CN/apidoc/plugin-api-main.json >/dev/null` 通过；`make i18n.check` 通过，仍仅报告仓库既有 48 个模块级 `$t()` warning，未新增 `linapro-ai-core` 资源覆盖问题；`git -C apps/lina-plugins diff --check -- linapro-ai-core/frontend/pages/model-drawer.vue linapro-ai-core/backend/internal/service/ai/ai_multimodal_metadata.go linapro-ai-core/backend/internal/service/ai/ai.go linapro-ai-core/backend/api/model/v1/model_capabilities.go linapro-ai-core/backend/internal/controller/model/model_v1_upsert_capabilities.go linapro-ai-core/backend/internal/service/ai/ai_service_test.go linapro-ai-core/hack/tests/pages/SmartCenterPage.ts linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts linapro-ai-core/manifest/i18n/zh-CN/apidoc/plugin-api-main.json` 通过。已尝试运行 `E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts --config=playwright.config.ts --grep "TC-1a" --trace off --timeout 150000 --reporter=list`，但用例在进入本次编辑能力方法步骤前失败于前置“多协议同名模型删除后不应再显示 `mimo`”断言；失败原因是当前运行中的 `9120` 后端进程未加载此前已通过单测覆盖的同名模型聚合删除 Go 实现，本轮未按会话约束擅自重启服务。
  - 审查记录：已按 `lina-review` 从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-45` 的模型编辑能力方法、模型能力替换保存语义、API 文档源、中文 `apidoc`、service 单测、`TC001`/POM 断言和任务记录。插件根目录无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`data-permission.md`、`cache-consistency.md`、`database.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-feedback`、`lina-review`、`goframe-v2`、`vben` 和 `lina-e2e`。E2E 质量审查触发原因是用户可观察模型编辑抽屉和接口联动变化；覆盖依据为 `TC001` 新增编辑抽屉能力方法可选、两个 `PUT` 请求、列表旧能力移除和新能力显示断言，但当前运行中的 `9120` 后端未加载前置同名模型聚合删除实现，导致定向 E2E 在进入本轮目标步骤前失败。API/backend 审查确认 HTTP 方法、路径、DTO 字段和权限标签不变，仅更新 `PUT /ai/models/{id}/capabilities` 文档语义和 service 替换实现；数据权限仍为平台上下文与既有权限；缓存仍为事务成功后失效档位解析缓存；SQL/DAO、开发工具跨平台和运行期 DI 均无新增影响。审查未发现阻塞问题；剩余风险为目标浏览器 E2E 需要在重启并加载当前 Go 代码后补跑，且当前工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-46**: 调用日志创建时间宽度需保持与登录日志一致且标题与能力方法对齐
  - 问题分析：根因是 `FB-44` 为了让智能中心调用日志创建时间日期范围与登录日志日期范围控件同宽，将创建时间筛选项设置为 `80px` 标签宽度；但调用日志同一筛选区中的“能力方法”因英文标题较长使用 `112px` 标签宽度，导致两者标题右边界没有上下对齐。若直接把创建时间标签改成 `112px` 且继续 `w-full`，日期选择器本体会被压窄，无法继续和登录日志日期选择器保持同宽。
  - 修复记录：调用日志创建时间筛选项的标签宽度改为与能力方法一致的 `112px`，并按登录日志日期筛选 `80px` 标签宽度与调用日志 `112px` 标签宽度之间的 `32px` 差值，对创建时间 `RangePicker` 设置宽度补偿，保持日期选择器本体可见宽度与登录日志一致；插件自有 `TC003` POM 断言同步改为验证创建时间标签与能力方法标签左右边界对齐、创建时间标签宽度为 `112px`、日期选择器宽度保持在登录日志同宽区间、日期输入不裁切且无时间面板。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 调用日志筛选表单创建时间标签宽度、日期选择器宽度和插件自有 `TC003`/POM 布局断言；OpenSpec 规范有影响，已补充调用日志创建时间组件宽度和标题对齐验收要求；测试有影响，更新插件 E2E 断言并需要重跑 `TC003`。`i18n` 无影响，不新增或修改运行时文案、菜单、按钮、表格、提示、API 文档源文本或语言包；API 契约无变更，仍向调用日志列表和清理接口发送既有 `startedAt`/`endedAt` 毫秒参数；后端 Go、SQL/DAO、缓存一致性、数据权限和开发工具跨平台均无影响，未修改服务、数据库、脚本、权限或可见性边界；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。
  - 验证记录：DOM 测量显示系统监控登录日志日期选择器 `pickerWidth=402`，智能中心调用日志创建时间日期选择器 `pickerWidth=402`，调用日志“能力方法”和“创建时间”标签均为 `labelWidth=112` 且左右边界一致；第一次实现仅改标签宽度后 `TC003` 暴露日期选择器被压缩到 `370px`，改用宽度补偿后又暴露内部 `.overflow-hidden` 会裁剪右侧边框和图标，本轮已修复为创建时间表单项局部解除裁剪并通过截图验证。`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb46-tc003-results-4 --timeout 150000` 通过；截图 `temp/20260605/20260605054503-tc003-invocation-filters-source-time.png` 显示创建时间日期范围宽度、右侧图标、边框和“Source Plugin”标题均正常，无裁剪、未翻译 key 或明显重叠。治理验证通过：`openspec validate add-ai-text-capability-center --strict`、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests test:validate`、`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json`、`make i18n.check`、`git diff --check`、`git -C apps/lina-plugins diff --check`；`make i18n.check` 仍仅报告仓库既有 48 个模块级 `$t()` warning，未新增 `linapro-ai-core` 资源覆盖问题。
  - 审查记录：已按 `lina-review` 从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-46` 的调用日志创建时间标签宽度、日期选择器局部宽度补偿、内部裁剪解除、`TC003`/POM 布局断言、调用日志页面清理入口增量规范和任务记录。插件根目录无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`data-permission.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。E2E 质量审查触发原因是用户可观察筛选表单布局和日期组件宽度变化；覆盖依据为 `TC003` 中创建时间日期格式、无时间面板、创建时间与能力方法标签边界对齐、日期选择器宽度、内部不裁剪、筛选请求 `startedAt`/`endedAt` 参数、范围清理路径和截图证据。API、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无新增契约影响；审查未发现阻塞问题，剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-47**: 删除模型管理中的能力方法声明、筛选、编辑和档位候选限制
  - 问题分析：根因是模型管理把 `plugin_linapro_ai_model_capability` 这类显式能力元数据当成模型列表、筛选、表单和档位候选/校验的硬事实来源；但渠道模型能力无法可靠自动识别，导致管理员必须维护一份经常不完整的能力方法声明，档位选择也被过度限制。更合理的边界是模型管理只维护模型身份，能力方法继续作为档位、调用日志、host service 和显式高级元数据的身份维度，由管理员选择模型后通过保存、测试和运行时结构化错误判断是否适配。
  - 修复记录：后端模型列表、创建、更新、同步和渠道模型候选不再创建、读取、筛选或返回模型能力声明；档位主绑定校验改为只校验渠道、模型、端点真实存在、已启用且归属一致，不再要求模型能力声明匹配 `capabilityType + capabilityMethod`；默认 `thinkingEffort` 保存只校验平台枚举，不依赖模型声明；provider 调用不再从模型能力元数据回退 `maxOutputTokens`。前端模型管理移除能力方法、token 上限、thinking 支持字段、筛选和编辑逻辑；档位抽屉的模型选择只按渠道和启用状态加载候选；E2E helper/POM/TC001/TC002 同步改为验证模型身份管理和档位候选不依赖能力声明。验证过程中发现档位抽屉缺失既有“默认参数 JSON”编辑器，已复用 `JsonHighlightEditor` 恢复方法默认参数读取、校验和保存，保持该能力归属于档位/能力方法配置而不是模型管理。
  - 影响分析：API 契约有影响，范围为模型列表、创建、更新、渠道模型候选和档位绑定投影，不新增路由或权限标签；后端 Go 有影响，范围为 `linapro-ai-core` service、adapter 和对应单测；前端 UI 有影响，范围为模型管理字段/筛选、档位候选加载和档位默认参数编辑器恢复；`i18n` 有影响，已移除旧模型 UI 字段资源并更新端点引用错误/API 文档相关文案；测试有影响，更新插件自有 E2E、POM 和 helper。缓存一致性无新增缓存类型，档位缓存仍在绑定/方法默认参数等写入成功后按既有路径失效；数据权限边界不变，仍为平台配置控制面和既有权限；SQL/DAO 无结构变更，旧模型能力表保留为显式高级能力元数据，不再服务模型管理主路径；开发工具跨平台无影响，未修改脚本、CI 或长期工具；运行期 DI 无新增依赖；插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。
  - 验证记录：`GOWORK=off go test ./backend/internal/service/ai -count=1` 通过；`GOWORK=off go test ./backend/... -count=1` 通过；`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck` 通过；`pnpm -C hack/tests test:validate` 通过；`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json` 通过。重启当前项目 dev 服务后，`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC001-smart-center-provider-management.ts ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC002-smart-center-tier-management.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb47-ai-core-results-current --timeout 150000` 中 `TC001` 6 个子用例通过，`TC002` 除默认参数编辑器布局外的 5 个子用例通过；恢复编辑器并补齐布局后，`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC002-smart-center-tier-management.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb47-tc002-current-final --timeout 150000` 通过，6 个子用例全部通过。`openspec validate add-ai-text-capability-center --strict`、`openspec validate extend-ai-multimodal-capabilities --strict`、`git diff --check` 和 `git -C apps/lina-plugins diff --check` 均通过。静态扫描 `rg -n "editModelCapabilityFromModelManagement|filterModelsByProviderAndCapability|assertProviderUnclassifiedModel|supportedEfforts|supportsThinking|maxInputTokens|maxOutputTokens" apps/lina-plugins/linapro-ai-core/hack/tests apps/lina-plugins/linapro-ai-core/frontend/pages` 仅剩档位测试、方法默认参数和多模态默认参数允许命中。
  - 审查记录：已按 `lina-review` 重新从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-47` 的模型管理 identity-only 改造、档位绑定校验放宽、provider 调用 token 回退删减、档位默认参数编辑器恢复、插件自有 E2E/POM/helper 和任务记录。`linapro-ai-core` 插件根目录无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`database.md`、`cache-consistency.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`i18n.md` 和 `dev-tooling.md`，并使用 `lina-feedback`、`openspec-apply-change`、`goframe-v2`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。审查确认模型管理页面、模型表单、模型列表 API、渠道模型候选和档位绑定校验均不再依赖模型能力声明；`/ai/models/{id}/capabilities` 仅作为高级模型能力元数据接口保留，且接口文档明确模型管理、档位候选和档位绑定校验不依赖这些记录。E2E 质量审查触发原因是用户可观察模型管理字段、筛选和档位候选行为变化；覆盖依据为 `TC001` 模型管理隐藏能力控件和仅按渠道筛选、`TC002` 默认 `thinkingEffort=max` 无声明保存和禁用档位保留绑定。缓存、数据权限、SQL/DAO、开发工具跨平台和运行期 DI 均无新增契约影响；本轮复核验证 `GOWORK=off go test ./backend/internal/service/ai -count=1`、`GOWORK=off go test ./backend/... -count=1`、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests test:validate`、`openspec validate add-ai-text-capability-center --strict`、`openspec validate extend-ai-multimodal-capabilities --strict`、`git diff --check` 和 `git -C apps/lina-plugins diff --check` 均通过。审查未发现阻塞问题；剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-48**: 调用日志创建时间日期选择器不应因宽度补偿侵占来源插件筛选项
  - 问题分析：根因是 `FB-46` 为了在创建时间标签改为 `112px` 后继续得到与登录日志日期输入框相同的可见宽度，对调用日志创建时间 `RangePicker` 增加了 `calc(100% + 32px)` 宽度补偿并解除父级裁剪。实际 DOM 测量显示该补偿使输入框右边缘从自身表单容器右侧继续外扩到 `787px`，与“来源插件”标题形成 `-23px` 负间距，导致筛选区视觉上输入框过宽。
  - 修复记录：调用日志创建时间 `RangePicker` 恢复为与登录日志一致的标准 `w-full` 日期范围输入样式，移除 `ai-invocation-created-at-picker` 扩宽类和页面 scoped 宽度补偿/解除裁剪样式；保留创建时间与能力方法一致的 `112px` 标签宽度，继续保证两个标题左右边界上下对齐。增量规范同步收紧为日期选择器必须复用系统监控日志的双列表单项宽度和输入样式，且不得通过额外宽度补偿侵占相邻筛选项区域。插件自有 `SmartCenterPage` POM 改为断言创建时间输入框与自身控件容器等宽、日期文本完整、无时间面板，并与“来源插件”标题保持正向间距。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 调用日志筛选表单创建时间日期范围输入宽度和局部样式；OpenSpec 规范有影响，已更新调用日志页面清理入口场景的日期组件布局验收；测试有影响，更新插件自有 POM 并重跑 `TC003` 覆盖原问题复现场景。`i18n` 无影响，不新增或修改运行时文案、菜单、按钮、表格、提示、API 文档源文本或语言包；API 契约、后端 Go、SQL/DAO、缓存一致性、数据权限和开发工具跨平台均无影响，未修改路由、DTO、服务、数据库、缓存、权限、可见性边界、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。
  - 验证记录：修复前 DOM 测量显示登录日志日期选择器 `pickerWidth=402`，调用日志创建时间 `pickerWidth=402`，但调用日志输入框右边缘 `787px` 超出自身控件容器右侧 `756px`，与“来源插件”标题间距为 `-23px`。修复后 DOM 测量显示登录日志和调用日志日期筛选表单项均为 `fieldWidth=492`；调用日志创建时间标签仍为 `112px` 且与能力方法标签左右边界一致，输入框收敛为 `pickerWidth=370`、`pickerRight=755`，与自身控件容器右侧 `756px` 对齐，和“来源插件”标题保持 `9px` 间距。`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb48-tc003-results --timeout 150000` 通过；截图 `temp/20260605/20260605055717-tc003-invocation-filters-source-time.png` 显示日期完整、来源插件筛选项无重叠或截断。治理验证通过：`openspec validate add-ai-text-capability-center --strict`、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests test:validate`、`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json`、`make i18n.check`、`git diff --check`、`git -C apps/lina-plugins diff --check`；`make i18n.check` 仍仅报告仓库既有 48 个模块级 `$t()` warning，未新增 `linapro-ai-core` 资源覆盖问题。
  - 审查记录：已按 `lina-review` 重新从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-48` 的调用日志创建时间日期范围输入宽度收敛、页面局部扩宽样式移除、插件 POM 布局断言、调用日志页面清理入口增量规范和任务记录。插件根目录无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`data-permission.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。E2E 质量审查触发原因是用户可观察筛选表单布局和日期组件宽度变化；覆盖依据为 `TC003` 中创建时间日期格式、无时间面板、创建时间与能力方法标签边界对齐、输入框与自身控件容器等宽、来源插件正向间距、筛选请求和清理路径以及截图证据。API、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无新增契约影响；审查未发现阻塞问题，剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-49**: 恢复系统监控登录日志和操作日志日期筛选原有宽度
  - 问题分析：根因是 `FB-44` 为了让调用日志创建时间日期范围与监控日志日期范围看起来同宽，错误地把 `formItemClass: 'md:col-span-2 lg:col-span-2'` 和 `class: 'w-full'` 扩宽配置加入 `linapro-monitor-loginlog` 与 `linapro-monitor-operlog` 的查询表单，导致原本正常的监控日志日期筛选从单列表单项变成跨两列表单项。这是调用日志布局需求向无关监控日志插件扩散造成的回归。
  - 修复记录：已移除登录日志 `loginTime` 和操作日志 `operTime` 日期范围筛选上的跨两列表单项和 `w-full` 扩宽配置，恢复到原有单列表单项宽度；保留监控日志页面此前按需求去掉多选框的改动。增量规范同步调整为调用日志页面不得通过修改系统监控操作日志、登录日志原有筛选宽度来实现对齐。
  - 影响分析：前端 UI 有影响，范围为 `linapro-monitor-loginlog` 与 `linapro-monitor-operlog` 查询表单日期范围筛选宽度；OpenSpec 规范有影响，已收紧调用日志页面布局要求，明确不得修改监控日志原有宽度；测试有影响，需要用截图和 DOM 测量覆盖登录日志、操作日志和调用日志筛选区宽度。`i18n` 无影响，不新增或修改运行时文案、菜单、按钮、表格、提示、API 文档源文本或语言包；API 契约、后端 Go、SQL/DAO、缓存一致性、数据权限和开发工具跨平台均无影响，未修改路由、DTO、服务、数据库、缓存、权限、可见性边界、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；`linapro-monitor-loginlog`、`linapro-monitor-operlog` 和 `linapro-ai-core` 插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。
  - 验证记录：DOM 测量显示登录日志恢复为 `fieldWidth=242`、`pickerWidth=152`，截图 `temp/20260605/20260605061838-fb49-loginlog-filter-restored.png`；操作日志恢复为 `fieldWidth=242`、`pickerWidth=152`，截图 `temp/20260605/20260605061838-fb49-operlog-filter-restored.png`；调用日志保持 `fieldWidth=492`、`pickerWidth=370`，与“来源插件”标题间距 `9px`，截图 `temp/20260605/20260605061838-fb49-invocation-filter-check.png`。`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-monitor-loginlog/hack/tests/e2e/TC006-loginlog-delete.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb49-loginlog-results --timeout 150000` 通过，3 个子用例全部通过；`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-monitor-operlog/hack/tests/e2e/TC005-operlog-delete.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb49-operlog-results --timeout 150000` 通过，3 个子用例全部通过；`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb49-invocation-results --timeout 150000` 通过。治理验证通过：`openspec validate add-ai-text-capability-center --strict`、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests test:validate`、`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json`、`make i18n.check`；`make i18n.check` 仍仅报告仓库既有 48 个模块级 `$t()` warning，未新增资源覆盖问题。
  - 审查记录：已按 `lina-review` 重新从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-49` 的系统监控登录日志和操作日志日期筛选宽度恢复、调用日志布局回归确认、调用日志页面清理入口增量规范和任务记录。`linapro-ai-core`、`linapro-monitor-loginlog`、`linapro-monitor-operlog` 插件根目录均无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`data-permission.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-feedback`、`vben`、`lina-e2e` 和 `lina-review`。E2E 质量审查触发原因是用户可观察筛选表单布局和日期组件宽度回归修复；覆盖依据为登录日志、操作日志、调用日志三页 DOM 测量、截图证据，以及 `TC006`、`TC005`、`TC003` 端到端回归通过。API、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无新增契约影响；审查未发现阻塞问题，剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-50**: 调用日志创建时间日期选择器宽度改为 `242px`
  - 问题分析：根因是 `FB-48` 和 `FB-49` 仅把调用日志创建时间日期选择器从错误扩宽状态收敛到表单内容区 `w-full`，实际可见宽度仍约为 `370px`；用户当前明确要求智能中心调用日志页面中的日期组件宽度改为 `242px`，因此需要对调用日志创建时间 `RangePicker` 本体设置明确宽度，并同步更新布局断言。
  - 修复记录：调用日志创建时间 `RangePicker` 保留跨两列表单项和 `112px` 标签宽度，日期选择器本体改为 `style: { width: "242px" }`；插件自有 POM 宽度断言从 `360-380px` 改为 `241-243px`，继续断言日期文本完整、无时间面板、右侧来源插件筛选项不重叠。增量规范同步写明页面筛选区创建时间日期选择器可见宽度必须为 `242px`。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 调用日志筛选表单创建时间日期范围选择器宽度；OpenSpec 规范有影响，已更新调用日志页面清理入口场景的日期组件宽度验收；测试有影响，更新插件自有 POM 并需要重跑 `TC003` 覆盖原问题复现场景。`i18n` 无影响，不新增或修改运行时文案、菜单、按钮、表格、提示、API 文档源文本或语言包；API 契约、后端 Go、SQL/DAO、缓存一致性、数据权限和开发工具跨平台均无影响，未修改路由、DTO、服务、数据库、缓存、权限、可见性边界、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；`linapro-ai-core` 插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。
  - 验证记录：`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb50-invocation-results --timeout 150000` 通过；POM 断言已验证调用日志创建时间日期选择器 `pickerWidth` 在 `241-243px` 区间、创建时间标签宽度仍为 `112px`、日期文本完整、无时间面板、与“来源插件”保持正向间距。截图 `temp/20260605/20260605062854-tc003-invocation-filters-source-time.png` 显示创建时间日期框为 `242px` 宽度且无裁切或重叠。治理验证通过：`openspec validate add-ai-text-capability-center --strict`、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests test:validate`、`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json`、`make i18n.check`、`git diff --check`、`git -C apps/lina-plugins diff --check`；`make i18n.check` 仍仅报告仓库既有 48 个模块级 `$t()` warning，未新增资源覆盖问题。
  - 审查记录：已按 `lina-review` 重新从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-50` 的调用日志创建时间日期选择器 `242px` 宽度配置、插件 POM 宽度断言、调用日志页面清理入口增量规范和任务记录。`linapro-ai-core` 插件根目录无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`data-permission.md`、`cache-consistency.md`、`dev-tooling.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。E2E 质量审查触发原因是用户可观察筛选表单布局和日期组件宽度变化；覆盖依据为 `TC003` 中日期范围输入、`242px` DOM 宽度断言、筛选请求 `startedAt`/`endedAt` 参数、清理路径和截图证据。API、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无新增契约影响；审查未发现阻塞问题，剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-51**: 调用日志页面能力方法文案改为调用方法且方法下拉框宽度与日期选择器对齐
  - 问题分析：根因是调用日志页面继续复用通用能力方法查询 schema 和 `capability.method` 运行时文案，导致筛选区、表格列和详情抽屉仍显示“能力方法/Capability Method”；同时“调用方法”下拉框处于单列表单项内，`112px` 标签加 `242px` 控件超过单列可用宽度，被表单 `flex` 布局压缩到约 `128px`，无法与下方 `242px` 创建时间日期选择器对齐。
  - 修复记录：为调用日志页新增专用运行时文案键 `invocation.fields.method`，中英文分别为“调用方法”和 `Invocation Method`；调用日志筛选区、表格列标题和详情抽屉统一使用该键，保留其它页面继续使用通用“能力方法/Capability Method”。`buildCapabilityQuerySchema` 增加 `formItemClass` 扩展点，调用日志页将调用方法筛选项与创建时间一样设置为跨两列表单项，两个控件共享 `242px` 宽度和 `112px` 标签宽度；筛选项顺序调整为调用方法、用途、档位、创建时间、状态、来源插件，使调用方法标题与创建时间标题上下对齐，状态与用途列对齐，来源插件与档位列对齐。插件版本提升到 `v0.1.25` 并已执行源码插件运行时升级，确保嵌入前端资源和运行时 `i18n` 快照刷新。插件自有 `TC003` 和 `SmartCenterPage` POM 同步更新，断言页面不再显示“能力方法/Capability Method”，并验证调用方法下拉框与创建时间日期选择器可见宽度一致。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 调用日志筛选表单、调用日志表格列标题和详情抽屉字段标签；`i18n` 有影响，已更新启用多语言的插件运行时语言包并通过运行时接口确认 `en-US` 返回 `Invocation Method`；OpenSpec 规范和任务记录有影响，已补充调用日志页面方法下拉框与日期选择器宽度一致的验收；测试有影响，更新插件自有 E2E/POM 并重跑 `TC003`。API 契约、后端 Go、SQL/DAO、缓存一致性、数据权限和开发工具跨平台均无新增契约影响，未修改路由、DTO、服务、数据库、缓存、权限、可见性边界、脚本或 CI 入口；运行期 DI 无影响，未新增运行期依赖；插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。
  - 验证记录：重建开发服务后，先将源码插件运行时从 `v0.1.23` 升级到 `v0.1.24` 以加载新增 `i18n` 键，再因本轮布局修正将插件提升并升级到 `v0.1.25`；运行时插件状态为 `effectiveVersion=v0.1.25`、`discoveredVersion=v0.1.25`、`runtimeState=normal`，`GET /api/v1/i18n/runtime/messages?lang=en-US` 返回 `plugin.linapro-ai-core.invocation.fields.method=Invocation Method`。`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb51-invocation-results-5 --timeout 150000` 通过；截图 `temp/20260605/20260605072612-tc003-invocation-filters-source-time.png` 显示调用方法和创建时间控件同为 `242px`，无 raw `i18n` key、无重叠或明显截断。治理验证通过：`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json`、`pnpm -C hack/tests test:validate`、`make i18n.check`、`openspec validate add-ai-text-capability-center --strict`、`git diff --check` 和 `git -C apps/lina-plugins diff --check`；`make i18n.check` 仍仅报告仓库既有 48 个模块级 `$t()` warning，未新增 `linapro-ai-core` 资源覆盖问题。
  - 审查记录：已按 `lina-review` 从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、OpenSpec 状态、验证证据和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-51` 的调用日志专用“调用方法”文案、插件运行时语言包、调用方法/创建时间控件宽度和布局、插件版本、`TC003`/POM 断言、调用日志页面清理入口增量规范和任务记录。`linapro-ai-core` 插件根目录无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。E2E 质量审查触发原因是用户可观察文案和筛选表单布局变化；覆盖依据为 `TC003` 中实际英文文案出现、旧文案消失、调用方法与创建时间控件等宽、筛选请求、清理路径和截图证据。API、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无新增契约影响；审查未发现阻塞问题，剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-52**: 英文领域标识和展示源应保持 `Provider`/`provider`，中文展示继续使用“渠道/渠道管理”
  - 问题分析：根因是 `FB-42` 将中文产品命名“渠道”扩散到了英文展示源和文档源，形成“渠道/英文旧称”的双语绑定；但当前 AI 能力契约、权限、路由、DTO、数据库表名和插件能力接缝都以 `provider` 为稳定领域标识。更合理的分层是英文源内容和技术语义统一使用 `Provider`/`provider`，中文运行时翻译继续使用“渠道/渠道管理”。“供应商管理”不作为本轮中文名，原因是它更像公司主体管理，弱化了接入通路、协议端点、模型绑定和运行治理的含义。
  - 修复记录：已将 `linapro-ai-core` 英文菜单、运行时资源、插件清单源内容、API 文档源文本、业务错误 fallback、SQL 描述性注释、E2E 可见测试数据和相关 AI OpenSpec 英文描述从英文旧称收敛回 `Provider/provider`；中文 `zh-CN` 运行时资源、菜单、错误资源和中文 `apidoc` 继续使用“渠道/渠道管理”。插件版本保持提升后的 `v0.1.24`，用于触发现有源码插件资源刷新。
  - 影响分析：`i18n` 有影响，范围为启用 `i18n` 的 `linapro-ai-core` 英文运行时资源、菜单、错误资源、插件清单源内容和中文 `apidoc` 中的英文错误标识描述，中文展示继续保留“渠道”；前端 UI 有影响，英文页面标题、按钮、表格列、抽屉标题和测试数据展示恢复 `Provider`，中文页面保持“渠道”；API 契约仅文档源文本有影响，未修改 HTTP 方法、路径、DTO 字段名、权限标签或响应结构；后端 Go 有影响，限于 API 文档标签、注释和业务错误英文 fallback，无新增运行期依赖或 DI 来源变化；SQL 有影响，限于描述性注释和 `COMMENT` 文本，不改变表、列、索引、约束、Seed、软删除或时间字段语义，不运行 `make dao`；测试有影响，更新插件自有 `TC001` 至 `TC005`、POM 和 helper 中的英文可见测试数据与断言。缓存一致性无新增业务缓存或失效路径，插件版本升级仅触发现有资源刷新；数据权限无影响，不新增数据读取、写入、租户/组织可见性或权限边界；开发工具跨平台无影响，不修改脚本、CI 或代码生成入口；插件目录本地 `AGENTS.md` 不存在，已按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`architecture.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`database.md`、`cache-consistency.md`、`data-permission.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并使用 `lina-feedback`、`lina-review`、`goframe-v2`、`vben` 和 `lina-e2e`。
  - 验证记录：英文旧称残留静态检索使用不含旧词字面量的模式覆盖 `linapro-ai-core`、宿主 AI 能力包和相关 AI OpenSpec 变更，结果无命中；英文复数误用风险检索无命中；插件中英文 JSON、错误资源和中文 `apidoc` JSON 解析通过。`openspec validate add-ai-text-capability-center --strict`、`openspec validate extend-ai-multimodal-capabilities --strict`、`openspec validate refactor-ai-capability-namespace --strict` 通过；`go test ./pkg/plugin/capability/ai/aicommon ./pkg/plugin/capability/ai/aitext -count=1` 通过；`GOWORK=off go test ./backend/api/... ./backend/internal/service/ai -count=1` 通过；`pnpm -C hack/tests test:validate` 通过；`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json` 通过；`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck` 通过；`make i18n.check` 通过，仍仅报告仓库既有 48 个模块级 `$t()` warning，未新增 `linapro-ai-core` 资源覆盖问题；`git diff --check` 与 `git -C apps/lina-plugins diff --check` 通过；`TC001-smart-center-provider-management.ts` 与 `TC002-smart-center-tier-management.ts` 共 12 个子用例通过；`TC004-smart-center-provider-endpoints.ts` 通过。补跑 `TC003-ai-invocation-logs.ts` 时失败于既有未完成 `FB-51` 的“调用方法/Invocation Method”文案断言，本轮未将该失败归因于 `FB-52`，并保留 `FB-51` 继续跟踪。
  - 审查记录：已按 `lina-review` 从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-52` 的 Provider 英文标识修复、插件 i18n/manifest/API 文档源/错误 fallback/SQL 注释/E2E 文案和相关 AI OpenSpec 文档。审查确认未改变 API 路由、DTO 字段、权限、数据库结构、查询路径、缓存失效、数据权限或运行期 DI；E2E 质量审查触发原因是用户可观察中英文文案变化，覆盖依据为 `TC001`、`TC002`、`TC004` 通过以及 `TC003` 失败原因已由未完成 `FB-51` 单独跟踪。审查未发现 `FB-52` 阻塞问题；剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-53**: 调用日志搜索区需改为均衡响应式布局
  - 问题分析：根因是调用日志筛选表单继续使用 `lg:grid-cols-4`，同时调用方法和创建时间筛选项还保留固定 `242px` 宽度与创建时间跨列布局，导致常规桌面宽度下第一列出现明显空洞，右侧状态、来源插件等字段被压缩，整体不像一个均衡的搜索区。
  - 修复记录：调用日志页面搜索表单改为 `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`，桌面常规宽度优先形成两行三列；表单默认标签宽度收敛为 `80px`，调用方法保留必要的 `112px` 标签宽度和不换行约束，创建时间恢复标准日期范围选择器并移除固定 `242px` 宽度和跨列配置，来源插件使用 `96px` 标签宽度以避免标题挤压；同步清理不再使用的 `formItemClass` 跨列扩展点。插件自有 POM 同步改为断言调用方法、用途、档位、创建时间、状态、来源插件的两行三列位置关系、控件最小可读宽度、日期文本完整和无重叠。增量规范已同步改为要求均衡响应式网格布局，不再要求调用方法或创建时间固定 `242px`。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 调用日志筛选表单布局、标签宽度和控件宽度；OpenSpec 规范和任务记录有影响；测试有影响，更新插件自有 POM 并重跑 `TC003` 覆盖布局回归。`i18n` 无资源影响，不新增或修改运行时文案、菜单、按钮、表格、提示、API 文档源文本或语言包；API 契约、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无影响，未修改路由、DTO、服务、数据库、缓存、权限、可见性边界、脚本、CI 入口或运行期依赖；插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估架构、数据权限、缓存一致性和开发工具跨平台无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb53-invocation-results-current-2 --timeout 150000` 通过；截图 `temp/20260605/20260605080525-tc003-invocation-search-labels.png` 和 `temp/20260605/20260605080533-tc003-invocation-filters-source-time.png` 显示搜索区为两行三列，控件宽度均衡，创建时间日期文本完整，来源插件和状态筛选项无重叠或明显截断。治理验证通过：`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json`、`pnpm -C hack/tests test:validate`、`openspec validate add-ai-text-capability-center --strict`、`git diff --check` 和 `git -C apps/lina-plugins diff --check`；静态检索确认不再存在 `formItemClass`、`invocationQueryControlWidth` 或 `ai-invocation-created-at` 布局补偿残留。
  - 审查记录：已按 `lina-review` 从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-53` 的调用日志搜索表单三列响应式布局、标签宽度收敛、固定宽度和跨列配置移除、插件 POM 布局断言、调用日志页面清理入口增量规范和任务记录。`linapro-ai-core` 插件根目录无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。E2E 质量审查触发原因是用户可观察筛选表单布局变化；覆盖依据为 `TC003` 中两行三列 DOM 位置断言、控件最小宽度断言、日期范围文本完整断言、无时间面板断言、筛选请求和清理路径以及截图证据。API、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无新增契约影响；审查未发现阻塞问题，剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-54**: 调用日志搜索区同列上下标题文字需对齐
  - 问题分析：根因是 `FB-53` 只保证了调用日志搜索区两行三列的位置均衡，但同一列上下两行使用了不同标签宽度：第一列“调用方法”为 `112px`，“创建时间”为 `80px`；第三列“档位”为默认 `80px`，“来源插件”为 `96px`。在 `Ant Design Vue` 表单标签右对齐模式下，标签宽度不同会导致可见标题文字右边界上下错位。
  - 修复记录：第一列统一使用 `112px` 标签宽度，第三列统一使用 `96px` 标签宽度；为避免第一列标签变宽后压缩创建时间日期文本，将桌面三列从等宽 `lg:grid-cols-3` 调整为 `lg:grid-cols-[1.2fr_1fr_1fr]`，保持两行三列布局的同时给日期范围输入留出足够空间。插件自有 POM 增加可见文字 `Range` 测量，断言“调用方法/创建时间”“用途/状态”“档位/来源插件”的标题文字右边界和控件起点上下对齐，并继续断言日期文本完整、无时间面板和无重叠。增量规范同步补充同列上下筛选标题文字按统一右边界对齐的验收。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 调用日志筛选表单标签宽度、桌面列比例和布局断言；OpenSpec 规范和任务记录有影响；测试有影响，更新插件自有 POM 并重跑 `TC003` 覆盖标题对齐、日期可读和筛选路径。`i18n` 无资源影响，不新增或修改运行时文案、菜单、按钮、表格、提示、API 文档源文本或语言包；API 契约、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无影响，未修改路由、DTO、服务、数据库、缓存、权限、可见性边界、脚本、CI 入口或运行期依赖；插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估架构、数据权限、缓存一致性和开发工具跨平台无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：首次仅统一第一列标签宽度时，`TC003` 暴露创建时间输入被压缩，日期输入 `scrollWidth=82` 大于 `clientWidth+4=69`；最终改为第一列略宽的三列比例后，`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb54-invocation-results-2 --timeout 150000` 通过；截图 `temp/20260605/20260605082810-tc003-invocation-search-labels.png` 和 `temp/20260605/20260605082819-tc003-invocation-filters-source-time.png` 显示搜索标题上下文字对齐、日期文本完整、状态和来源插件筛选项无重叠或明显截断。治理验证通过：`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json`、`pnpm -C hack/tests test:validate`、`openspec validate add-ai-text-capability-center --strict`、`git diff --check` 和 `git -C apps/lina-plugins diff --check`。
  - 审查记录：已按 `lina-review` 从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-54` 的调用日志搜索表单同列标题文字右边界对齐、第一列比例列调整、插件 POM 可见文字测量断言、调用日志页面清理入口增量规范和任务记录。`linapro-ai-core` 插件根目录无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。E2E 质量审查触发原因是用户可观察搜索标题布局变化；覆盖依据为 `TC003` 中同列标题文字右边界断言、控件起点断言、日期文本完整断言、无时间面板断言、筛选请求和清理路径以及截图证据。API、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无新增契约影响；审查未发现阻塞问题，剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-55**: 调用日志搜索区桌面布局调整为首行四项次行两项
  - 问题分析：根因是 `FB-54` 为保证两行三列同列标题对齐，将第一列调成 `1.2fr`，导致“创建时间”日期范围在第二行第一列获得了比“调用方法”更宽的视觉区域；用户期望搜索区在桌面常规宽度下改为首行四项、次行两项，让“创建时间”回到“调用方法”正下方并与其控件等宽，同时缩窄日期范围输入。
  - 修复记录：调用日志搜索 schema 调整为“调用方法、用途、档位、状态 / 创建时间、来源插件”的 4+2 顺序；桌面网格改为四列比例布局，第一列使用 `minmax(360px,1.65fr)` 保证“调用方法/创建时间”控件同宽且日期文本完整，第二至四列分别使用较窄比例列；“用途/来源插件”统一 `96px` 标签宽度，“档位/状态”显式收窄为 `64px` 标签宽度，避免首行右侧条件被标签挤压。插件自有 POM 同步断言首行四项、次行两项、第一列和第二列上下标题右边界对齐、创建时间与调用方法控件等宽、日期文本完整和无重叠。增量规范同步改为 4+2 布局验收。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 调用日志筛选表单字段顺序、桌面网格列宽、标签宽度和布局断言；OpenSpec 规范和任务记录有影响；测试有影响，更新插件自有 POM 并重跑 `TC003` 覆盖布局回归。`i18n` 无资源影响，不新增或修改运行时文案、菜单、按钮、表格、提示、API 文档源文本或语言包；API 契约、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无影响，未修改路由、DTO、服务、数据库、缓存、权限、可见性边界、脚本、CI 入口或运行期依赖；插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估架构、数据权限、缓存一致性和开发工具跨平台无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：前两次 `TC003` 分别暴露四等列导致第一列控件仅约 `120px`、日期文本裁切，以及首列放宽后第三/第四列控件仅约 `57px`；最终通过显式四列最小宽度和短标签宽度收敛后，`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb55-invocation-results-4 --timeout 150000` 通过；截图 `temp/20260605/20260605084459-tc003-invocation-search-labels.png` 和 `temp/20260605/20260605084508-tc003-invocation-filters-source-time.png` 显示首行四项、次行两项，“创建时间”与“调用方法”同宽，日期文本完整，控件无重叠。治理验证通过：`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json`、`pnpm -C hack/tests test:validate`、`openspec validate add-ai-text-capability-center --strict`、`git diff --check` 和 `git -C apps/lina-plugins diff --check`。
  - 审查记录：已按 `lina-review` 重新从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-55` 的调用日志搜索区 4+2 桌面布局、标签宽度收敛、插件 POM 布局断言、调用日志页面清理入口增量规范和任务记录。`linapro-ai-core` 插件根目录无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。E2E 质量审查触发原因是用户可观察搜索表单布局变化；覆盖依据为 `TC003` 中首行四项、次行两项、同列标题右边界、创建时间与调用方法控件等宽、日期文本完整、筛选请求和清理路径断言以及截图证据。API、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无新增契约影响；审查未发现阻塞问题，剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-56**: 调用日志搜索区创建时间与调用方法控件宽度固定为 `242px`
  - 问题分析：根因是 `FB-55` 的四列网格最小轨道宽度为 `368px + 228px + 204px + 204px`，再叠加默认 `8px` 列间距后超过 1280px 视口下搜索栏实际可用的 `992px`，导致右侧“状态”控件越出搜索栏右边界；若直接压缩第一列，`Ant Design Vue` 表单内部弹性布局又会把“调用方法/创建时间”控件从目标 `242px` 压到约 `238px`。同时“来源插件”英文标题不能低于 `96px` 标签宽度，否则会换行并破坏上下标题对齐。
  - 修复记录：调用日志页继续保持首行“调用方法、用途、档位、状态”、第二行“创建时间、来源插件”的 4+2 布局；“调用方法”和“创建时间”控件继续显式设置为 `242px`，两者标签宽度统一为 `112px`；“用途/来源插件”标签宽度保持 `96px` 以避免英文换行，“档位/状态”标签宽度收敛到 `48px`，把空间让给右侧控件；桌面四列改为 `minmax(368px,1.45fr) / minmax(228px,1fr) / minmax(190px,0.95fr) / minmax(190px,0.95fr)`，并在 `xl` 下将列间距收敛为 `4px`，保证四列总宽不越出搜索栏。插件自有 POM 同步增强断言，除 `242px` 等宽外，还验证所有筛选控件右边界不超过搜索栏容器和视口。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 调用日志筛选表单标签宽度、桌面四列轨道宽度、列间距和布局断言；OpenSpec 规范和任务记录有影响；测试有影响，更新插件自有 POM 并重跑 `TC003` 覆盖布局回归和溢出复现场景。`i18n` 无资源影响，不新增或修改运行时文案、菜单、按钮、表格、提示、API 文档源文本或语言包；API 契约、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无影响，未修改路由、DTO、服务、数据库、缓存、权限、可见性边界、脚本、CI 入口或运行期依赖；插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估架构、数据权限、缓存一致性和开发工具跨平台无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：修复前 DOM 测量显示搜索栏可用宽度为 `992px`，`Status` 控件右边界为 `1291px`，超过搜索栏右边界 `1256px`；直接压缩第一列后又暴露“调用方法/创建时间”控件宽度被压到约 `238px`。最终实现后 DOM 测量显示 `columnGap=4px`，调用方法和创建时间控件均为 `242px`，用途/来源插件控件均为 `122px`，档位/状态控件均为 `134px`，状态控件右边界为 `1255px`，未超过搜索栏右边界 `1256px`，且来源插件标题保持单行。`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb56-invocation-results-4 --timeout 150000` 通过；截图 `temp/20260605/20260605091315-tc003-invocation-search-labels.png` 和 `temp/20260605/20260605091324-tc003-invocation-filters-source-time.png` 显示搜索区无未翻译 key、无换行、无重叠或明显截断。治理验证通过：`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json`、`pnpm -C hack/tests test:validate`、`openspec validate add-ai-text-capability-center --strict`、`git diff --check -- openspec/changes/add-ai-text-capability-center/specs/linapro-ai-core-plugin/spec.md openspec/changes/add-ai-text-capability-center/tasks.md` 和 `git -C apps/lina-plugins diff --check -- linapro-ai-core/frontend/pages/ai-data.ts linapro-ai-core/frontend/pages/invocation-logs.vue linapro-ai-core/hack/tests/pages/SmartCenterPage.ts`。
  - 审查记录：已按 `lina-review` 从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-56` 的调用日志搜索区 `242px` 控件宽度、四列最小轨道和列间距调整、插件 POM 溢出断言、调用日志页面清理入口增量规范和任务记录。`linapro-ai-core` 插件根目录无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。E2E 质量审查触发原因是用户可观察搜索表单布局和控件宽度变化；覆盖依据为 `TC003` 中首行四项、次行两项、调用方法与创建时间 `242px` 宽度断言、右侧控件不越界断言、筛选请求和清理路径断言以及截图证据。API、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无新增契约影响；审查未发现阻塞问题，剩余风险为工作树仍包含其他活跃反馈和无关变更，本轮未回退或审查为新增问题。
- [x] **FB-57**: 调用日志搜索区调用方法和创建时间左侧需要保留英文安全间距
  - 问题分析：根因是英文 `Invocation Method` 文案实际宽度约 `122px`，而第一列标签盒宽度为 `112px`。在 `Ant Design Vue` 表单标签右对齐模式下，文字会向左溢出到标签盒外，导致英文环境下标题文字左侧超过搜索栏左边界；仅增大标签宽度会破坏 `242px` 控件宽度和四列布局，直接改其它列宽又不符合用户要求。
  - 修复记录：为调用日志“调用方法”和“创建时间”两个第一列筛选项增加专用 `formItemClass`，在调用日志页局部对该类设置 `16px` 左侧内边距；第一列网格最小宽度从 `368px` 调整到 `384px`，列间距从 `4px` 收敛为 `0`，用于容纳新增内边距并保持首列控件仍为 `242px`。其它搜索条件 schema 宽度不变：用途/来源插件仍使用 `96px` 标签宽度，档位/状态仍使用 `48px` 标签宽度，控件宽度继续由现有四列布局决定。插件自有 POM 增加第一列左侧安全间距断言，并为用途、来源插件、档位和状态增加最大宽度断言，防止本轮误把其它搜索条件框改宽。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 调用日志筛选表单第一列左侧内边距、桌面第一列最小宽度、列间距和布局断言；OpenSpec 规范和任务记录有影响；测试有影响，更新插件自有 POM 并重跑 `TC003` 覆盖英文标题左侧不越界和其它筛选框宽度不变。`i18n` 无资源影响，不新增或修改运行时文案、菜单、按钮、表格、提示、API 文档源文本或语言包；API 契约、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无影响，未修改路由、DTO、服务、数据库、缓存、权限、可见性边界、脚本、CI 入口或运行期依赖；插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估架构、数据权限、缓存一致性和开发工具跨平台无契约影响；使用 `lina-feedback`、`vben`、`frontend-design` 和 `lina-e2e`。
  - 验证记录：修复前 DOM 测量显示搜索栏左边界为 `264px`，`Invocation Method` 可见文字左边界为 `253.64px`，已越出搜索栏左边界。修复后 DOM 测量显示“调用方法/创建时间”标签盒左边界均为 `280px`，`Invocation Method` 可见文字左边界为 `269.64px`，相对搜索栏左边界保留约 `5.6px` 安全间距；调用方法和创建时间控件均保持 `242px`，用途/来源插件控件均保持 `122px`，档位/状态控件均保持 `132px`，状态控件右边界为 `1255px`，未超过搜索栏右边界 `1256px`。第一次 `TC003` 运行失败于旧断言仍要求创建时间与来源插件间距必须大于 `6px`，实际为 `5px` 正向间距且无重叠；已将该断言收敛为保持正向间距。最终 `E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb57-invocation-results-2 --timeout 150000` 通过；截图 `temp/20260605/20260605092557-tc003-invocation-search-labels.png` 和 `temp/20260605/20260605092606-tc003-invocation-filters-source-time.png` 显示第一列英文标题未越界、无未翻译 key、无换行、无重叠或明显截断。治理验证通过：`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json`、`pnpm -C hack/tests test:validate`、`openspec validate add-ai-text-capability-center --strict`、`git diff --check -- openspec/changes/add-ai-text-capability-center/specs/linapro-ai-core-plugin/spec.md openspec/changes/add-ai-text-capability-center/tasks.md` 和 `git -C apps/lina-plugins diff --check -- linapro-ai-core/frontend/pages/ai-data.ts linapro-ai-core/frontend/pages/invocation-logs.vue linapro-ai-core/hack/tests/pages/SmartCenterPage.ts`。
  - 审查记录：已按 `lina-review` 从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-57` 的调用日志搜索区第一列英文安全间距、第一列最小轨道和列间距调整、插件 POM 左边界及其它控件宽度断言、调用日志页面清理入口增量规范和任务记录。`linapro-ai-core` 插件根目录无本地 `AGENTS.md`。已重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，并使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。E2E 质量审查触发原因是用户可观察搜索表单布局变化；覆盖依据为 `TC003` 中英文标题左边界断言、调用方法与创建时间 `242px` 宽度断言、其它搜索条件控件最大宽度断言、筛选请求和清理路径断言以及截图证据。API、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无新增契约影响；审查未发现阻塞问题。
- [x] **FB-58**: 调用日志搜索区各输入域之间的水平间隔需保持一致
  - 问题分析：根因是 `FB-57` 为第一列英文标题安全边距将调用日志搜索区列间距从 `4px` 收敛为 `0`，再依赖第一列 `16px` 内边距和各列剩余轨道宽度形成视觉间隔；不同列的标签宽度和剩余宽度不同，导致顶部搜索区各筛选字段组之间的水平间隔看起来不一致。
  - 修复记录：调用日志搜索表单保留首行四项、次行两项布局，以及“调用方法/创建时间” `242px` 控件宽度；桌面网格改为显式 `xl:gap-x-2` 统一字段组间隔，并将四列最小轨道收敛为 `380px / 220px / 184px / 184px`，用列宽中的空余量换取统一列间距，避免右侧字段溢出。插件自有 `SmartCenterPage` POM 增加四个横向字段组间距测量，断言第一行三个间隔和第二行间隔均不小于 `7px`，且与平均间隔偏差不超过 `2px`；同时按最终轨道宽度更新非主控件的最小可读宽度断言。
  - 影响分析：前端 UI 有影响，范围为 `linapro-ai-core` 调用日志筛选表单桌面列间距、列轨道宽度和布局断言；OpenSpec 规范和任务记录有影响；测试有影响，更新插件自有 POM 并重跑 `TC003` 覆盖字段组间隔一致、控件宽度、筛选请求和清理流程。`i18n` 无资源影响，不新增或修改运行时文案、菜单、按钮、表格、提示、API 文档源文本或语言包；API 契约、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无影响，未修改路由、DTO、服务、数据库、缓存、权限、可见性边界、脚本、CI 入口或运行期依赖；插件目录本地 `AGENTS.md` 不存在，按顶层插件规则执行。已读取规则：`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md`、`i18n.md`，并评估架构、数据权限、缓存一致性和开发工具跨平台无契约影响；使用 `lina-feedback`、`vben`、`frontend-design`、`lina-e2e` 和 `lina-review`。
  - 验证记录：首次改为 `370px / 222px / 188px / 188px` 加 `gap-x-2` 时，`TC003` 暴露第一列轨道过窄，调用方法控件被压缩为 `232px`，未满足既有 `242px` 宽度契约；最终调整为 `380px / 220px / 184px / 184px` 后，`E2E_BASE_URL=http://127.0.0.1:5666 E2E_BACKEND_BASE_URL=http://127.0.0.1:9120 E2E_WORKSPACE_BASE_PATH=/admin pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-ai-core/hack/tests/e2e/TC003-ai-invocation-logs.ts --config=playwright.config.ts --trace off --output ../../temp/20260605-fb58-invocation-results-2 --timeout 150000` 通过。截图验证 `temp/20260605/20260605094800-tc003-invocation-search-labels.png` 和 `temp/20260605/20260605094809-tc003-invocation-filters-source-time.png` 显示搜索区无未翻译 key、无换行、无重叠或明显截断。治理验证通过：`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests exec tsc --noEmit --project tsconfig.json`、`pnpm -C hack/tests test:validate`、`openspec validate add-ai-text-capability-center --strict`、`git diff --check -- openspec/changes/add-ai-text-capability-center/specs/linapro-ai-core-plugin/spec.md openspec/changes/add-ai-text-capability-center/tasks.md` 和 `git -C apps/lina-plugins diff --check -- linapro-ai-core/frontend/pages/invocation-logs.vue linapro-ai-core/hack/tests/pages/SmartCenterPage.ts`。
  - 审查记录：已按 `lina-review` 重新读取 `AGENTS.md`、`.agents/rules/openspec.md`、`documentation.md`、`plugin.md`、`frontend-ui.md`、`testing.md` 和 `i18n.md`，从父仓库状态、未跟踪文件、`apps/lina-plugins` 子仓库状态、子仓库 diff、未跟踪文件、OpenSpec 状态和插件本地规范入口展开反馈级审查；本轮范围限定为 `FB-58` 的调用日志搜索区字段组统一间距、列轨道宽度调整、插件 POM 间距/宽度断言、调用日志页面清理入口增量规范和任务记录。`linapro-ai-core` 插件根目录无本地 `AGENTS.md`。E2E 质量审查触发原因是用户可观察搜索表单布局变化；覆盖依据为 `TC003` 中字段组水平间距一致断言、调用方法与创建时间 `242px` 宽度断言、筛选请求和清理路径断言以及截图证据。API、后端 Go、SQL/DAO、缓存一致性、数据权限、开发工具跨平台和运行期 DI 均无新增契约影响；审查未发现阻塞问题。
