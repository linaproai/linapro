## 1. 规则与基线确认

- [x] 1.1 实现前重新读取命中的规则文件：`openspec.md`、`architecture.md`、`plugin.md`、`backend-go.md`、`cache-consistency.md`、`testing.md`、`i18n.md`，若实现阶段触及脚本或生成入口还必须读取`dev-tooling.md`。
- [x] 1.2 复查 `apps/lina-core/internal/service/plugin` 和 `apps/lina-core/pkg/plugin` 的生产代码、测试代码、协议样板和小文件行数基线，记录本变更只优化真实风险和可维护样板，不按行数做无差别删除。
- [x] 1.3 静态检索 `GetDesiredManifest`、`ScanManifests`、`ParseRuntimeWasmArtifact`、WASM host service 包级变量、`New()` fallback、runtime setter wiring 和 host service protocol alias/client/stub/dispatcher 同步点，形成实现清单。

执行记录：已读取`AGENTS.md`、`openspec.md`、`architecture.md`、`plugin.md`、`backend-go.md`、`cache-consistency.md`、`testing.md`、`documentation.md`、`i18n.md`和`data-permission.md`，并使用`openspec-apply-change`、`goframe-v2`、`lina-review`技能入口。当前不触及`apps/lina-plugins/<plugin-id>/`文件，无插件本地`AGENTS.md`读取要求；暂不触及脚本、`linactl`或生成入口，因此`dev-tooling.md`将在实际命中时再读取。基线：`apps/lina-core/internal/service/plugin`为`229`个 Go 文件，生产`36680`行、测试`25941`行；`apps/lina-core/pkg/plugin`为`210`个 Go 文件，生产`23778`行、测试`6724`行；两个目录生产小文件少于`50`行的共有`69`个、`1993`行，主要集中在`pkg/plugin/pluginbridge/protocol`、`pkg/plugin/capability/contract`、`internal/service/plugin/internal/runtime`和`pkg/plugin/capability/guest`。实现清单确认：`ListRuntimeStates`在 registry 循环中调用`GetDesiredManifest`，而该方法每次都会`ScanManifests`；WASM `cacheHostService`默认`kvcache.New()`、`storageConfigSvc`默认`config.New()`，runtime 构造中存在`session.NewDBStore()`fallback；runtime/catalog/integration/lifecycle 均存在 setter wiring 和部分 nil-safe 降级；host service 协议同步点分散在 capability map、payload codec、`protocol`alias、guest client、非 WASI stub 和 WASM dispatcher。影响判断：本组为基线和治理记录，不改变运行时行为；无`i18n`资源、数据权限、SQL、HTTP API、前端或缓存实现变更。

## 2. Runtime Manifest 批量读取

- [x] 2.1 为 catalog 增加 manifest 批量读取、manifest snapshot 或等价 map 构建能力，保证一次查询中可复用已扫描 manifest 结果。
- [x] 2.2 修改 `ListRuntimeStates` 和相关 runtime state projection 路径，避免在 registry 循环中逐项调用会触发完整扫描的 `GetDesiredManifest`。
- [x] 2.3 补充单元测试或静态断言，证明 runtime state 列表不会随 registry 行数重复执行完整 `ScanManifests`，动态 artifact 在单次列表调用中不会被重复解析。
- [x] 2.4 检查其他列表、启动投影或高频路径是否存在同类逐项 manifest scan，发现同范围问题时一并收敛，超出范围问题另行记录。

执行记录：已在`catalog.ManifestReader`新增`ScanManifestsByID()`，该方法仅在单次调用内将`ScanManifests()`结果构建为`pluginID -> manifest`内存 map，不引入跨请求缓存、失效或集群状态；`GetDesiredManifest()`继续保持单项入口语义并复用该 map 构建逻辑。`ListRuntimeStates()`改为循环前执行一次`ScanManifestsByID()`，循环内只做 map 查找；扫描失败时按旧行为降级为空 map 并继续返回 registry 投影，避免改变列表容错语义。后台全量`ReconcileRuntimePlugins()`和`RefreshInstalledRuntimePluginReleases()`主节点路径同样改为每轮只构建一次 manifest map，并通过私有`lookupDesiredManifest()`传递给每个 registry；单插件管理触发路径仍保留原单项读取形态。新增`TestListRuntimeStatesScansDynamicManifestsOnce`，通过`Catalog.SetArtifactParser`注入计数 parser，验证两个 registry、两个动态 artifact 的 runtime state 列表调用中每个 artifact 只解析一次。静态复查确认`plugin_list.go`的管理列表已有`management.WithManifestSnapshot`和依赖快照缓存；剩余`GetDesiredManifest`调用主要位于单插件生命周期、显式 upgrade、uninstall repair、按请求 enablement fallback。`BootstrapAutoEnable`配置项循环涉及实际安装/启用副作用和依赖校验，不属于本组只读列表、启动投影或后台全量收敛的低风险收敛范围，后续如需优化应单独设计上下文 manifest snapshot 贯穿 lifecycle。验证：`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/runtime -count=1`通过。影响判断：无 HTTP API、SQL、前端、用户可见文案或数据权限变更；`i18n`无影响；未新增长期缓存，缓存一致性影响为无新增失效域；未触及脚本、`linactl`或生成入口，开发工具跨平台无影响；未触及`apps/lina-plugins/<plugin-id>/`，无插件本地`AGENTS.md`读取要求。

## 3. WASM Host Service 与 Runtime DI

- [x] 3.1 移除 WASM `cache`、`storage/config` 等 host service 生产包级默认 `New()` fallback，改为 nil 初始状态和显式配置校验。
- [x] 3.2 检查 `lock`、`notify`、`config`、`manifest`、`AI`、`org`、`tenant` 等 WASM host service 配置入口，确保新增或已有 dispatcher 都被 `ConfigureWasmHostServices` 和测试覆盖。
- [x] 3.3 调整 runtime `sessionStore`、topology、menu sync、hook dispatch、JWT config、upload size、user context、permission filter、cache notifier 和 dependency validator 的 wiring 校验，区分必需依赖和可选降级能力。
- [x] 3.4 更新测试 fixture，所有 WASM host service 测试显式注入 fake、memory 或共享测试服务，不依赖生产包级默认实例。
- [x] 3.5 在任务记录中追溯新增或修改依赖的 owner、创建位置、传递路径和共享实例策略，明确缓存一致性影响。

执行记录：已移除`wasm`生产包级`cacheHostService = kvcache.New()`和`storageConfigSvc = config.New()`默认实例，改为 nil 初始状态；`cache`、`storage`dispatcher 及 storage 卸载清理路径在缺失配置时返回显式 internal error 或 Go error，不再隐式创建独立服务图。`ConfigureWasmHostServices()`已覆盖`cache`、`lock`、`notify`、`storage`、`AI`、`org`、`tenant`、`config`、`host-config`和`manifest`配置入口，并新增 root 级缺失依赖测试；root `plugin`包测试 fixture 也显式模拟 HTTP startup 配置，避免直接构造`plugin.Service`的测试依赖生产默认实例。`runtime.New()`已移除`session.NewDBStore()`fallback；root `plugin.New()`继续要求调用方传入启动期`session.Store`，并在所有 setter 完成后调用`ValidateRequiredDependencies()`校验`catalogSvc`、`lifecycleSvc`、`reconcilerLockSvc`、`topology`、`menuMgr`、`hookDispatcher`、`jwtConfig`、`uploadSize`、`userCtx`、`sessionStore`、`menuFilter`、`cacheChangeNotifier`和`dependencyValidator`。测试 fixture 通过`internal/testutil.NewServices()`、root `newTestServiceWithTopology()`和同包 WASM helper 显式注入 fake、memory 或共享测试服务。

DI 来源检查：`kvcache.Service`的 owner 为宿主启动期或测试 fixture 创建的统一 cache 服务，生产传递路径为 HTTP startup `kvCacheSvc` -> `plugin.ConfigureWasmHostServices()` -> `wasm.ConfigureCacheHostService()`，测试传递路径为 root/testutil fixture 显式创建后注入；不再存在生产包级 fallback。`storage`配置 reader 的 owner 为启动期`configsvc.Service`，生产传递路径为 HTTP startup `configSvc` -> `plugin.ConfigureWasmHostServices()` -> `wasm.ConfigureStorageHostService()`，测试同样显式注入；卸载清理复用同一配置 reader。`session.Store`的 owner 为启动期 session store，生产传递路径为 HTTP startup `sessionStore` -> `plugin.New()` -> `runtimeSvc.SetSessionStore()`，测试 fixture 显式使用`session.NewDBStore()`；`runtime.New()`不再自建 fallback。`lock`、`notify`、`config`、`host-config`、`manifest`、`AI`、`org`、`tenant`host service 的 owner 分别为启动期 host lock、notify、config factory、host config、manifest factory 和 capability directory，均经`ConfigureWasmHostServices()`进入`wasm`配置入口，缺失时 fail fast。缓存一致性影响：本组只删除孤立默认实例并强制复用启动期共享实例或共享后端，没有新增缓存数据、失效域或跨实例同步机制；集群一致性边界得到收敛。数据权限影响：未新增业务数据读写、列表、下载、租户或组织可见性路径，host service 授权语义保持不变。`i18n`影响：未新增用户可见文案、语言包或 API 文档源文本。开发工具跨平台影响：未触及脚本、`linactl`、生成入口或 CI。测试策略：新增和更新单元测试覆盖缺失配置 fail-fast、显式注入和 runtime wiring 校验；本组为内部 DI/缓存一致性治理，无用户可观察 UI 行为，未触发 E2E 质量审查。

验证：`cd apps/lina-core && go test ./internal/service/plugin/internal/wasm ./internal/service/plugin/internal/runtime ./internal/service/plugin -count=1`通过；`cd apps/lina-core && go test ./internal/cmd -count=1`通过；`cd apps/lina-core && go test ./internal/cmd/internal/httpstartup -count=1`通过；`git diff --check`通过；`openspec validate optimize-plugin-runtime-complexity --strict`通过。静态检索`rg -n "var .* = .*New\\(|= kvcache\\.New\\(|= config\\.New\\(|session\\.NewDBStore\\(" apps/lina-core/internal/service/plugin/internal/wasm apps/lina-core/internal/service/plugin/internal/runtime apps/lina-core/internal/service/plugin/plugin.go apps/lina-core/internal/service/plugin/plugin_test.go apps/lina-core/internal/service/plugin/internal/testutil`确认生产`wasm`、`runtime`和`plugin.go`不再保留相关默认实例，剩余命中均为测试或 testutil 显式 fixture。

## 4. Pluginbridge 协议样板治理

- [x] 4.1 建立 host service service/method/capability/resource/payload/guest/dispatcher 的单一描述源或集中 Go 描述表，覆盖现有已发布 host service。
- [x] 4.2 新增协议覆盖测试，校验描述源中的每个 service/method 都有对应 public protocol alias、payload codec、guest client 或明确豁免、非 WASI stub 和 host dispatcher。
- [x] 4.3 若采用生成代码，提供跨平台生成入口并为生成文件添加 `Code generated` 标记；若第一阶段不生成，记录后续迁移策略并确保覆盖测试已阻断遗漏。
- [x] 4.4 保持 dynamic plugin bridge wire 行为不变，补充或复用 payload round trip 测试覆盖 runtime、storage、data、cache、lock、config、notify、manifest、org、tenant 和 AI 代表性协议。
- [x] 4.5 清理确定重复的 protocol alias 小文件、重复注释或转发样板；不得为了减少文件数破坏 `pluginbridge` 的公开协议出口和 internal 子组件边界。

执行记录：已新增`hostservice_descriptor.go`作为第一阶段集中 Go 描述表，覆盖`runtime`、`cron`、`storage`、`network`、`data`、`cache`、`lock`、`notify`、`config`、`hostConfig`、`manifest`、`AI`、`org`和`tenant`等已发布 host service，以及`secret`、`event`、`queue`预留 capability 声明。`hostServiceMethodCapabilityMap`、`allCapabilities`、默认 method 和资源类型集合均改为从描述表派生，删除原先多处手写 map，减少 service/method/capability/resource 重复同步点。新增`hostservice_descriptor_test.go`，通过 AST 校验每个已发布 descriptor method 都有`protocol`公开 method 常量、请求/响应 payload 类型和 marshal/unmarshal alias、guest client 使用点、WASM host dispatcher 使用点，并按`!wasip1`build tag 校验非 WASI guest factory stub、data stub 与 capability 派生来源。`AI`、`org`和`tenant`不采用独立 factory stub，而是通过`guest_transport.go`统一委托`pluginbridge/guest`非 WASI stub；已扩展`TestSharedCapabilityServicesUseBridgeTransport`覆盖`AI`、`org`和`tenant`在普通 Go 构建中返回`ErrHostCallsUnavailable`。本阶段未引入生成代码、生成文件、`linactl`、`make`目标或脚本，因此无`dev-tooling`跨平台入口变更；后续若把 descriptor 迁移为生成 codec、alias、client/stub 或 dispatcher，必须先读取`dev-tooling.md`并提供跨平台生成入口与`Code generated`标记。payload round trip 覆盖情况：复用已有 runtime envelope/value、storage、data、cache、lock、config、notify、manifest、org/tenant capability payload 测试，并新增`hostservice_ai_codec_test.go`覆盖`AI` text generation 代表性 JSON request。小文件清理判断：`pkg/plugin/pluginbridge/protocol`短文件均为公开协议 facade alias，保留其一对一出口边界；本阶段不为减少文件数合并这些文件，实际清理集中在删除重复 capability/resource/default method map。影响判断：wire service/method 字符串、payload 字段、guest helper 语义和 host dispatcher 行为保持不变；无 HTTP API、SQL、前端、用户可见文案或数据权限变更；`i18n`无影响；不新增缓存、失效域或跨实例状态，缓存一致性无新增风险；未触及`apps/lina-plugins/<plugin-id>/`。

验证：`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/internal/hostservice -count=1`通过；`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/internal/hostservice ./pkg/plugin/pluginbridge/protocol ./pkg/plugin/capability/guest ./pkg/plugin/capability/data ./internal/service/plugin/internal/wasm -count=1`通过；`git diff --check`通过；`openspec validate optimize-plugin-runtime-complexity --strict`通过。

## 5. 组织收敛、验证与审查

- [x] 5.1 清理 `internal/service/plugin/internal/wasm/hostfn.go` 等文件中的重复注释，并合并同一组件下少于 50 行且只有同类 alias/转发职责的小文件。
- [x] 5.2 评估 `pluginhost` 生命周期 handler 内部存储是否可改为 hook key/map；若收益明确，保持公开 typed API 不变并补充回归测试；若收益不足，记录暂不修改理由。
- [x] 5.3 运行后端 Go 编译门禁，至少覆盖 `cd apps/lina-core && go test ./internal/service/plugin/... ./pkg/plugin/... -count=1`；若耗时或环境阻断，运行更窄但覆盖变更包的替代命令并记录剩余风险。
- [x] 5.4 运行 `openspec validate optimize-plugin-runtime-complexity --strict`、`git diff --check` 和必要的静态检索，确认无协议行为、DI 来源、缓存一致性、`i18n` 或数据权限遗漏。
- [x] 5.5 完成每组任务后调用 `lina-review` 执行任务级审查，最终审查必须列出已读取规则文件、验证命令、缓存一致性判断、数据权限无影响判断、`i18n` 无影响或实际影响判断。

执行记录：已清理`internal/service/plugin/internal/wasm/hostfn.go`和`hostctx.go`中同一声明前的重复注释；复查`internal/service/plugin/internal/wasm`生产 Go 文件，最小生产文件为`hostfn_log.go`的`53`行，未发现少于`50`行且只有同类 alias/转发职责的生产小文件，因此不做文件合并。已评估`pkg/plugin/pluginhost`生命周期 handler 内部存储：当前`sourcePlugin`使用强类型字段保存 install、upgrade、disable、uninstall、tenant 和 install-mode handler，公开 typed API、getter 和 lifecycle adapter 均直接依赖这些类型；改为`map[LifecycleHook]any`不能减少公开方法和 adapter 分支，反而会引入类型断言并降低编译期约束，因此本阶段保留 typed 字段。验证：`cd apps/lina-core && go test ./internal/service/plugin/internal/wasm -count=1`通过；`cd apps/lina-core && go test ./pkg/plugin/pluginhost -count=1`通过；`cd apps/lina-core && go test -p 1 ./internal/service/plugin/... ./pkg/plugin/... -count=1`通过；`openspec validate optimize-plugin-runtime-complexity --strict`通过；`git diff --check`通过。复核中曾执行默认并发包门禁`cd apps/lina-core && go test ./internal/service/plugin/... ./pkg/plugin/... -count=1`，暴露既有测试共享目录竞态：`internal/service/plugin`会在共享`apps/lina-plugins`下创建并清理临时插件目录，而`pkg/plugin/capability`治理测试会并发扫描同一目录，导致偶发`plugin does not exist`或扫描到已被清理的`zz_generated_wasm_dispatcher.go`路径；单独复跑`go test ./internal/service/plugin -run TestBootstrapAutoEnableHonorsPerEntryMockDataOptIn -count=1 -v`和`go test ./pkg/plugin/capability -run TestRepositoryPluginCapabilityBoundaries -count=1 -v`均通过，因此本变更最终以后端变更范围串行包门禁作为可复现验证结论。静态检索确认生产`wasm`、`runtime`和`plugin.go`不再保留`kvcache.New()`、`config.New()`或`session.NewDBStore()`默认实例，剩余命中均在测试或 testutil；`hostServiceMethodCapabilityMap`、`allCapabilities`和 host service resource map 的手写 map 重复源已清空。影响判断：本组仅清理注释、记录结构评估并执行验证；未改变 HTTP API、SQL、前端、用户可见文案、数据权限路径、缓存数据或跨实例状态；`i18n`无影响；开发工具跨平台无影响；未触及`apps/lina-plugins/<plugin-id>/`。

最终审查：已按`lina-review`完成全部变更审查，审查范围来自`git status --short`、`git ls-files --others --exclude-standard`和 OpenSpec 活跃变更上下文，覆盖`34`个已跟踪或未跟踪文件；已列出读取规则文件、验证命令、缓存一致性判断、数据权限无影响判断和`i18n`无影响判断。审查结论为严重`0`、警告`0`，未发现阻塞问题。
