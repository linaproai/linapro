## 1. 清单模型与版本约束

- [ ] 1.1 扩展插件 manifest 类型，新增 `dependencies.framework.version` 与 `dependencies.plugins[]` 结构，并同步动态插件 artifact manifest 序列化/反序列化
- [ ] 1.2 实现依赖字段规范化与校验，覆盖默认值、插件 ID、版本范围、自依赖、重复依赖、未知安装策略和无效结构
- [ ] 1.3 确定 LinaPro 框架版本权威来源，并实现框架版本读取与语义化版本范围匹配
- [ ] 1.4 更新源码插件和动态插件示例 `plugin.yaml` 注释，至少为一个官方插件样例添加依赖声明
- [ ] 1.5 补充 manifest 单元测试，覆盖无依赖清单、有效依赖清单、无效依赖清单和动态插件产物依赖解析

## 2. 依赖解析器

- [ ] 2.1 新增内部依赖解析组件，输入目标插件、manifest 集合、registry/release 状态和框架版本，输出检查结论、阻断原因、自动安装计划、依赖链和反向依赖
- [ ] 2.2 实现依赖图构建、确定性拓扑排序、自依赖检测和循环依赖检测
- [ ] 2.3 实现硬依赖、软依赖、`install:auto` 与 `install:manual` 的状态判定
- [ ] 2.4 实现反向硬依赖查询，优先读取已安装发布快照，无法确认安全时保守阻断
- [ ] 2.5 为依赖解析器添加单元测试，覆盖框架版本不满足、缺失依赖、版本不满足、软依赖、自动安装计划、手动依赖阻断、循环依赖和反向依赖保护

## 3. 生命周期接入

- [ ] 3.1 将依赖检查接入显式插件安装路径，在任何安装副作用前生成并执行自动安装计划
- [ ] 3.2 实现自动依赖安装结果记录与结构化业务错误，错误包含目标插件、依赖插件、版本范围、循环链、已安装依赖和失败原因
- [ ] 3.3 将反向依赖保护接入插件卸载路径，存在下游硬依赖时阻断卸载
- [ ] 3.4 将依赖解析接入 `BootstrapAutoEnable`，确保启动自动启用按拓扑顺序安装自动依赖且不隐式启用依赖插件
- [ ] 3.5 将依赖校验接入源码插件升级与动态插件安装/刷新路径，确保新版本依赖不满足时不切换有效发布
- [ ] 3.6 确认依赖生命周期变化在单机模式刷新本地投影，在集群模式复用插件 runtime revision/event、enabled snapshot、frontend bundle 和 i18n bundle 失效机制

## 4. API、前端与 i18n

- [ ] 4.1 设计并实现插件依赖检查 API 或扩展现有插件详情/安装接口，返回框架版本检查、依赖状态、自动安装计划、手动处理项、软依赖提示和卸载阻断项
- [ ] 4.2 更新插件安装响应和错误响应 DTO，使用 `bizerr` 定义稳定错误码和 messageKey，避免自由文本作为调用端合同
- [ ] 4.3 更新插件管理页面，展示依赖摘要、安装确认中的自动安装计划、阻断原因和卸载下游依赖提示
- [ ] 4.4 同步维护前端运行时语言包、宿主 `manifest/i18n`、插件相关 apidoc i18n JSON 和必要的动态插件清单文案
- [ ] 4.5 确认前端只展示服务端依赖检查结果，不在前端重复实现依赖图或版本约束决策

## 5. 测试与回归验证

- [ ] 5.1 运行插件 catalog、依赖解析器、生命周期、启动自动启用、源码插件升级和动态插件运行时相关 Go 单元测试
- [ ] 5.2 增加或更新插件管理 E2E 测试，覆盖安装前依赖计划展示、依赖阻断展示和卸载反向依赖阻断
- [ ] 5.3 在集群/Redis 可用环境下运行插件 runtime revision、enabled snapshot、frontend/i18n bundle 失效相关回归测试
- [ ] 5.4 运行插件 full 模式验证，覆盖官方源码插件 manifest 解析、后端单测和插件自有 E2E
- [ ] 5.5 运行 `openspec validate plugin-dependency-management --strict`
- [ ] 5.6 运行 `git diff --check -- openspec/changes/plugin-dependency-management apps/lina-core apps/lina-vben apps/lina-plugins`
- [ ] 5.7 记录 i18n、缓存一致性、数据权限、RESTful API、开发工具脚本和跨平台影响评估
- [ ] 5.8 完成实现后调用 `lina-review`，重点审查依赖图语义、自动安装副作用边界、缓存一致性、i18n 覆盖、数据权限无泄露和测试覆盖

## Feedback

- [x] **FB-1**: 新增跨平台 `make tidy` 指令，扫描仓库 Go 模块并逐个执行 `go mod tidy`
