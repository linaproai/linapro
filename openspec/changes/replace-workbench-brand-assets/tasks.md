## 1. 品牌资产落位

- [x] 1.1 将用户提供的 `linapro-mark.png` 拷贝到默认管理工作台可构建交付的静态资源目录
- [x] 1.2 使用用户提供的 `favicon.ico` 替换默认管理工作台当前 favicon

## 2. 默认引用更新

- [x] 2.1 更新默认偏好配置，使管理工作台壳层和认证页使用本地 `linapro-mark.png`
- [x] 2.2 确认前端入口继续通过现有 `/favicon.ico` 路径加载 favicon
- [x] 2.3 更新宿主公共前端配置初始化数据、服务兜底值与接口示例，使默认 Logo 与深色 Logo 均指向本地 `linapro-mark.png`
- [x] 2.4 调整工作台壳层与认证页 Logo 渲染方式，使用图标 Logo 加应用名文本组合

## 3. 验证与审查

- [x] 3.1 运行前端类型检查或构建验证，确认资源引用可被正常处理
- [x] 3.2 执行本次变更审查，确认 OpenSpec 文档、实现与验证结果一致

## Feedback

- [x] **FB-1**: 使用 Playwright 验证并修复登录页与管理后台左上角仍未显示新横向 Logo 的问题
- [x] **FB-2**: 使用纯图标 `linapro-mark.png` 替换横向 Logo，并恢复登录页与工作台 Logo 右侧应用名文本
- [x] **FB-3**: 系统参数新增用户默认头像并默认指向 `/avatar.webp`
