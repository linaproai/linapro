# 设计：还原系统参数对 plugin.* 的管理面收敛

## 决策

撤回「管理面隐藏/锁定 plugin.*」方案。系统参数页按 `sys_config` 可见行全量展示（在既有租户回退与分页规则下），插件键与宿主键同等出现在列表中。

## 已移除

- List/Export 过滤
- plugin 创建/更新/导入/删除/详情拒绝
- 密钥投影掩码与 isBuiltin 强制投影
- 相关错误码与 i18n
