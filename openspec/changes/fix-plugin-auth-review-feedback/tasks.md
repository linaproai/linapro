## Feedback

- [x] **FB-1**: `/auth/providers` 匿名接口泄露 backend redirect 默认值和 redirect rules JSON
- [x] **FB-2**: `/auth/providers` provider 列表路径按 provider 数量读取插件设置，存在 N+1 数据库访问
- [x] **FB-3**: `pluginsettings.upsertValue` 使用 count-then-insert/update 且手写时间字段，存在并发唯一键竞争并绕过框架时间策略
- [x] **FB-4**: OAuth handoff、登录 provider 按钮和 OIDC 插件设置页存在硬编码可见文案，未接入 i18n
- [x] **FB-5**: 已安装但未启用的认证插件仍会出现在匿名 `/auth/providers` 登录入口列表
- [x] **FB-6**: `pluginsettings` 清空使用软删除路径，遗留 soft-deleted 行后 upsert 不再恢复可见性，导致清空后再写入对 GetString/List 不可见
- [x] **FB-7**: 宿主新增 `/auth/providers` 接口与 `error.auth.external.*` `bizerr.Code`，但 `manifest/i18n` 中缺少对应 apidoc 与 runtime 翻译条目
- [x] **FB-8**: 活跃 OpenSpec 变更 `fix-plugin-auth-review-feedback` 缺 `specs/` 增量规范，无法通过 `openspec validate --strict`，归档审计链路缺口

## Feedback Execution Record

- 规则加载：已读取 `AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/backend-go.md`、`.agents/rules/api-contract.md`、`.agents/rules/database.md`、`.agents/rules/frontend-ui.md`、`.agents/rules/testing.md`、`.agents/rules/i18n.md`、`.agents/rules/data-permission.md`、`.agents/rules/plugin.md` 和 `.agents/rules/cache-consistency.md`。`openspec` CLI 当前环境不可用，后续严格校验会记录为不可用并使用文件检查、编译和测试作为替代验证。
- 根因确认：`/auth/providers` 是匿名接口，但 DTO、service projection 和 frontend type 直接携带 `BackendRedirectEnabled`、`BackendRedirectDefault`、`BackendRedirectRules`；OIDC provider 的 `LoginEntry(ctx)` 每次读取插件设置，导致高频登录页随 provider 数量放大 `sys_config` 查询；`pluginsettings.upsertValue` 先 `Count()` 再 `Insert/Update`，并手写 `CreatedAt/UpdatedAt`；前端新增 OAuth 页面和 OIDC 设置页存在中英文硬编码。
- FB-5 根因确认：`auth.ListProviders` 调用 `pluginSvc.IsEnabled` 作为 provider 过滤器，该方法面向业务入口可见性并带运行时入口语义；认证 provider 能力已有专用 `pluginSvc.IsProviderEnabled`，其语义是平台级 framework capability provider 是否启用。匿名登录页应使用后者，避免仅安装未启用的 source plugin 暴露登录按钮。
- 追加根因确认：OIDC 插件设置页曾维护私有 `enabled` 开关，这会与宿主插件启用状态形成两个权威来源。该开关已取消，登录入口显示和 OAuth 发起/回调统一读取宿主 provider enablement 状态。
- 影响分析：`i18n` 有影响，宿主认证语言包和 OIDC 插件页面文案已接入运行时多语言，Google 插件页面标题改为 i18n key；缓存一致性有影响但不新增缓存，`IsProviderEnabled` 复用插件 runtime enabled snapshot 与既有 freshness 机制；数据权限按公开资源例外处理，匿名接口只暴露 button metadata；开发工具跨平台无影响；测试策略为后端单元测试 + JSON/静态检索治理验证。
- 验证记录：新增/更新单元测试覆盖公开 DTO 字段收窄、auth provider disabled 过滤、`auth.ListProviders` 使用 provider enablement 语义、`pluginsettings` upsert 冲突更新列不覆盖 `is_builtin`、OIDC 插件 controller `isProviderEnabled` 在 nil controller / nil pluginState / 平台启用 / 平台禁用 / 不退化到 `IsEnabled` 五个分支的行为；`openspec` CLI 当前不可用，使用文档结构检查作为替代治理验证。
- 缓存陈旧窗口与恢复路径：OIDC 控制器 `isProviderEnabled` 调用宿主 `PluginState.IsProviderEnabled`，该方法读 platform plugin enabled snapshot。snapshot 由宿主插件生命周期服务在启用/禁用、安装/卸载、升级提交时统一刷新，最大可接受陈旧窗口随宿主 snapshot freshness 配置而定，未在本变更中收紧。故障降级策略采用 fail-closed：snapshot 缺失、读取失败或 controller 未注入 PluginState 时 `isProviderEnabled` 返回 false，登录入口与 OAuth 回调均拒绝；admin 在插件管理重新启用后 snapshot 自动刷新并恢复登录可用，无需重启进程。
- OAuth error code 收敛：Google/Discord 插件 OAuth controller 已新增本地 `oauth_code.go`，插件自身产生的 OAuth 错误（provider 禁用、settings 不可用、authorize URL 构建失败、回调缺参数、state 校验失败、code exchange 失败、userinfo 失败、邮箱未验证、空登录结果）统一通过 `bizerr.MustDefineWithKey` 定义。`writeError` 4xx body 返回 `errorCode/messageKey/message/fallback` 投影，`redirectWithCode` 统一使用 `bizerr.Code.RuntimeCode()` 生成 handoff query code；外部 provider 回传的 `error` 参数和宿主 `LoginByExternal` 返回的 `AUTH_*` runtime code 保持上游稳定契约，不在插件侧重写。
- 审查回归补充：上一次 `lina-review` 报告 3 个严重 + 6 个警告。本轮已修复：补全 controller `New` 和 `isProviderEnabled` 的 godoc 注释；新增 5 个 controller `isProviderEnabled` 单元测试用例；调整 `StartLogin` / `HandleCallback` 先做 `isProviderEnabled` 再读 settings；`plugin.go` 把未使用的 `ctx` 参数改为 `_ context.Context` 删除 `_ = ctx`；oauth service `resp.Body.Close()` 关闭错误改走 `closeResponseBody` helper 并通过 `lina-core/pkg/logger` 记录；OIDC 插件 SPA landing fallback 默认值更新为 `/dashboard/analytics`；OAuth controller 本地 raw error code 已收敛为 `bizerr.Code`。`openspec validate --strict` 因 CLI 不可用仍作为待补验证记录。
- FB-6 根因确认：`pluginsettings.deleteByFullKey` 走 GoFrame 默认 `Delete()` 触发 `deleted_at` 软删除。`sys_config` 唯一索引 `(tenant_id, key)` 在 PG 层不过滤 `deleted_at`，因此后续 `upsertValue` 仍然落到软删除残留行上，`OnDuplicate` 列表又只更新 `name/value`，结果 `deleted_at` 保留为非空，对 `GetString`/`List` 默认查询不可见。修复采用「物理删除 + upsert 恢复 deleted_at」双保险：`deleteByFullKey` 改为 `Unscoped().Delete()` 物理移除；`upsertValue` 在 `OnDuplicate` 列表中加入 `DeletedAt`，使遗留软删除残留行在下一次写入时同时被恢复为可见状态。
- FB-6 验证记录：新增 `pluginsettings_test.go` 静态合同 `TestDeleteByFullKeyUsesUnscoped`；扩展 `TestUpsertValueConflictUpdateColumns` 把 `DeletedAt` 列入 OnDuplicate 必含列；新增 `pluginsettings_integration_test.go` 中 `TestSetStringRecoversAfterClearAndRewrite`、`TestUpsertValueRecoversSoftDeletedRow`、`TestSetStringClearRemovesRowPhysically` 三个 DB 集成回归用例。
- FB-7 根因确认：宿主新增 `/auth/providers` 公开 DTO 与 `CodeAuthExternalIdentityInvalid` / `CodeAuthExternalUserNotProvisioned` 两个 `bizerr.Code` 时未同步运行时翻译资源。`manifest/i18n/zh-CN/apidoc/core-api-auth.json` 缺 `ListProvidersReq/Res/ProviderEntity` 翻译节点；`manifest/i18n/en-US/apidoc/core-api-auth.json` 整个文件缺失，破坏宿主 apidoc 占位文件惯例；`manifest/i18n/zh-CN/error.json` 与 `manifest/i18n/en-US/error.json` 缺 `error.auth.external.identityInvalid` 与 `error.auth.external.userNotProvisioned` 翻译条目。
- FB-7 验证记录：补全 `zh-CN/apidoc/core-api-auth.json` 三个 schema 翻译节点；新建 `en-US/apidoc/core-api-auth.json` 空对象占位文件；在 `zh-CN/error.json` 与 `en-US/error.json` 的 `error.auth` 子树下补 `external.identityInvalid` 与 `external.userNotProvisioned`。`python -m json.tool` 验证 4 个 JSON 文件均为合法语法。
- FB-8 根因确认：活跃 OpenSpec 变更 `fix-plugin-auth-review-feedback` 此前仅包含 `proposal.md` 与 `tasks.md`，没有 `specs/` 增量规范，无法通过 `openspec validate --strict`，也不符合 OpenSpec 提案产物结构。本次反馈覆盖了多条 capability 行为变化（公开 provider 投影、插件设置 upsert 语义、OIDC 插件 provider 启停单源、host i18n 治理），必须在 `specs/` 中固化为正式契约。
- FB-8 验证记录：新增 `design.md` 记录 D1–D7 决策；新增 `specs/plugin-settings/spec.md`、`specs/auth-provider/spec.md`、`specs/oidc-plugins/spec.md`、`specs/host-i18n/spec.md` 四个增量规范，覆盖原子 upsert / 软删除恢复 / 物理清空、公开按钮投影 / IsProviderEnabled 过滤 / 静态 LoginEntry、provider 启停单源 / fail-closed / 本地 bizerr 错误 / 上游错误透传 / handoff lowerCamelCase 映射、apidoc 与 runtime 错误本地化覆盖。后续 `openspec validate fix-plugin-auth-review-feedback --strict` 在具备 CLI 的环境运行。
